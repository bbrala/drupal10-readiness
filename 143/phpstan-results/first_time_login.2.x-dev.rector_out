modules/contrib/first_time_login/first_time_login.install
modules/contrib/first_time_login/first_time_login.module
modules/contrib/first_time_login/src/Form/FirstTimeLoginSettings.php

1 file with changes
===================

1) modules/contrib/first_time_login/first_time_login.module:54

    ---------- begin diff ----------
@@ @@
     $conn->update('user_first_login')
       ->fields([
         'first_time_login' => 0,
-        'updated_date' => REQUEST_TIME,
+        'updated_date' => \Drupal::time()->getRequestTime(),
       ])
       ->condition('uid', $account->id(), '=')
       ->execute();
@@ @@

   $updated_date = $details->updated_date;

-  $current_time = REQUEST_TIME;
+  $current_time = \Drupal::time()->getRequestTime();
   if ($updated_date == 0) {
     $days_diff = 0;
   }
    ----------- end diff -----------

Applied rules:
 * RequestTimeConstRector
 * ArgumentRemoverRector


 [OK] 1 file has been changed by Rector                                         


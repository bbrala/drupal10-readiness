modules/contrib/fits/fits.install
modules/contrib/fits/fits.module
modules/contrib/fits/src/Form/FitsConfigForm.php
modules/contrib/fits/src/Plugin/Action/FitsAction.php
modules/contrib/fits/src/Plugin/AdvancedQueue/JobType/FitsJob.php
modules/contrib/fits/tests/src/Functional/LoadTest.php

2 files with changes
====================

1) modules/contrib/fits/tests/src/Functional/LoadTest.php:28

    ---------- begin diff ----------
@@ @@
   /**
    * {@inheritdoc}
    */
-  protected function setUp() {
+  protected function setUp(): void {
     parent::setUp();
     $this->user = $this->drupalCreateUser(['administer site configuration']);
     $this->drupalLogin($this->user);
    ----------- end diff -----------

Applied rules:
 * ArgumentRemoverRector
 * WrapReturnRector
 * AddReturnTypeDeclarationRector
 * ChangeMethodVisibilityRector


2) modules/contrib/fits/src/Plugin/AdvancedQueue/JobType/FitsJob.php:1

    ---------- begin diff ----------
@@ @@

 namespace Drupal\fits\Plugin\AdvancedQueue\JobType;

+use function JmesPath\search;
 use Drupal\taxonomy\Entity\Term;
 use Drupal\file\Entity\File;
 use Drupal\advancedqueue\Job;
@@ @@
     if (is_array($path)) {
       $value = "";
       foreach ($path as $p) {
-        $value = \JmesPath\search($p, $fits);
+        $value = search($p, $fits);
         if (!empty($value)) {
           break;
         }
@@ @@
       }
       return $value;
     }
-    return \JmesPath\search($path, $fits);
+    return search($path, $fits);
   }

   /**
    ----------- end diff -----------

Applied rules:
 * ArgumentRemoverRector
 * WrapReturnRector
 * ChangeMethodVisibilityRector


 [OK] 2 files have been changed by Rector                                       


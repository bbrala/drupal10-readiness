modules/contrib/feedbacks/feedbacks.install
modules/contrib/feedbacks/feedbacks.module
modules/contrib/feedbacks/src/Controller/AdminFeedbackController.php
modules/contrib/feedbacks/src/Form/AddFeedback.php
modules/contrib/feedbacks/src/Form/DelFeedback.php
modules/contrib/feedbacks/src/Form/EditFeedback.php
modules/contrib/feedbacks/src/Form/FeedbackClass.php

4 files with changes
====================

1) modules/contrib/feedbacks/src/Form/EditFeedback.php:126

    ---------- begin diff ----------
@@ @@
     $updated = FeedbackClass::update($status, $message, $path, $this->id);
     if ($updated > 0) {
       $form_state->setRedirectUrl($redirectUrl);
-      drupal_set_message($this->t('Feedback has been updated'));
+      $this->messenger()->addStatus($this->t('Feedback has been updated'));
     }
     else {
-      drupal_set_message($this->t('Feedback not updated', 'error'));
+      $this->messenger()->addStatus($this->t('Feedback not updated', 'error'));
     }
   }
    ----------- end diff -----------

Applied rules:
 * DrupalSetMessageRector
 * ArgumentRemoverRector
 * WrapReturnRector
 * ChangeMethodVisibilityRector


2) modules/contrib/feedbacks/src/Form/DelFeedback.php:75

    ---------- begin diff ----------
@@ @@
   public function submitForm(array &$form, FormStateInterface $form_state) {
     $url = Url::fromRoute('feedbacks_list.content');
     FeedbackClass::delete($this->id);
-    drupal_set_message(t('Feedback %id has been deleted.', ['%id' => $this->id]));
+    $this->messenger()->addStatus(t('Feedback %id has been deleted.', ['%id' => $this->id]));
     $form_state->setRedirectUrl($url);
   }
    ----------- end diff -----------

Applied rules:
 * DrupalSetMessageRector
 * ArgumentRemoverRector
 * WrapReturnRector
 * ChangeMethodVisibilityRector


3) modules/contrib/feedbacks/src/Form/AddFeedback.php:93

    ---------- begin diff ----------
@@ @@
     $timestamp = time();
     FeedbackClass::add($name, $message, $path, $timestamp);
     $form_state->setRedirectUrl($url);
-    drupal_set_message($this->t('Your Feedback has been submitted'));
+    $this->messenger()->addStatus($this->t('Your Feedback has been submitted'));
   }

 }
    ----------- end diff -----------

Applied rules:
 * DrupalSetMessageRector
 * ArgumentRemoverRector
 * WrapReturnRector
 * ChangeMethodVisibilityRector


4) modules/contrib/feedbacks/src/Controller/AdminFeedbackController.php:56

    ---------- begin diff ----------
@@ @@
           'feedbacks_delete.form', ['id' => $id]
         )->toRenderable();

-        $actionLinks = render($actionLink);
+        $actionLinks = \Drupal::service('renderer')->render($actionLink);

         $submitDate = DrupalDateTime::createFromTimestamp($content->timestamp,
           new \DateTimeZone(drupal_get_user_timezone())
@@ @@
             'id' => 'op-fb-table',
           ],
         ];
-        $openedFeedbacks = render($tableOpen);
+        $openedFeedbacks = \Drupal::service('renderer')->render($tableOpen);
       }
       else {
         $openedFeedbacks = "There are no Open feedback entries.";
@@ @@
             'id' => 'pr-fb-table',
           ],
         ];
-        $processedFeedbacks = render($tableProcessed);
+        $processedFeedbacks = \Drupal::service('renderer')->render($tableProcessed);
       }
       else {
         $processedFeedbacks = 'There are no processed feedback entries.';
    ----------- end diff -----------

Applied rules:
 * RenderRector
 * ArgumentRemoverRector
 * WrapReturnRector
 * ChangeMethodVisibilityRector


 [OK] 4 files have been changed by Rector                                       


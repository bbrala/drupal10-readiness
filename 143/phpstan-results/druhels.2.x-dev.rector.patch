diff --git a/src/DrupalHelper.php b/src/DrupalHelper.php
index 576ba36..84e7d81 100644
--- a/src/DrupalHelper.php
+++ b/src/DrupalHelper.php
@@ -245,7 +245,7 @@ class DrupalHelper {
    * @param array|mixed $build Render-array ot string
    */
   public static function render($build): string {
-    return render($build);
+    return \Drupal::service('renderer')->render($build);
   }
 
 }
diff --git a/src/FileHelper.php b/src/FileHelper.php
index 2e2641e..6be9906 100644
--- a/src/FileHelper.php
+++ b/src/FileHelper.php
@@ -81,10 +81,10 @@ class FileHelper {
       $image_style_url = $image_style->buildUrl($image_path);
     }
     else {
-      $image_style_url = file_create_url($image_path);
+      $image_style_url = \Drupal::service('file_url_generator')->generateAbsoluteString($image_path);
     }
 
-    return $absolute_url ? $image_style_url : file_url_transform_relative($image_style_url);
+    return $absolute_url ? $image_style_url : \Drupal::service('file_url_generator')->transformRelative($image_style_url);
   }
 
   /**

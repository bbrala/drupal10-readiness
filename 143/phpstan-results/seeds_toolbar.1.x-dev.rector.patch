diff --git a/seeds_toolbar.module b/seeds_toolbar.module
index f59c4ef..69d2e64 100644
--- a/seeds_toolbar.module
+++ b/seeds_toolbar.module
@@ -356,9 +356,9 @@ function seeds_toolbar_preprocess_seeds_toolbar_menu(&$variables) {
   $config = \Drupal::config('seeds_toolbar.settings');
   $settings = $config->get();
   if (!empty($settings[$settings['style'] . '_logo'])) {
-    $variables['logo_link'] = file_url_transform_relative(file_create_url($base_url . '/' . $settings[$settings['style'] . '_logo']));
+    $variables['logo_link'] = \Drupal::service('file_url_generator')->generateString($base_url . '/' . $settings[$settings['style'] . '_logo']);
   }
   else {
-    $variables['logo_link'] = file_url_transform_relative(file_create_url($base_url . '/' . drupal_get_path('module', 'seeds_toolbar') . '/assets/icons/logo/drupal.svg'));
+    $variables['logo_link'] = \Drupal::service('file_url_generator')->generateString($base_url . '/' . \Drupal::service('extension.list.module')->getPath('seeds_toolbar') . '/assets/icons/logo/drupal.svg');
   }
 }

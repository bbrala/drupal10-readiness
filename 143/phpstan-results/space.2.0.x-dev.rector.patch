diff --git a/space.theme b/space.theme
index 2fa2e9e..0244763 100644
--- a/space.theme
+++ b/space.theme
@@ -32,7 +32,7 @@ function space_preprocess_page(&$variables) {
   // Loop through each option and create a link if it exists.
   foreach ($social_options as $class => $option) {
     if (theme_get_setting($option)) {
-      $url = \Drupal\Core\Url::fromUri(theme_get_setting($option));
+      $url = Url::fromUri(theme_get_setting($option));
       $link_options = array(
         'attributes' => array(
           'class' => array($class),
@@ -71,7 +71,7 @@ function space_preprocess_page(&$variables) {
       $file = File::load($fid[0]);
 
       if (is_object($file) && $file->getFileUri()) {
-        $variables['header_media_url'] = file_create_url($file->getFileUri());
+        $variables['header_media_url'] = \Drupal::service('file_url_generator')->generateAbsoluteString($file->getFileUri());
       }
 
       // Check to see if the file is of a video mime.
@@ -79,7 +79,7 @@ function space_preprocess_page(&$variables) {
         $video_still_fid = theme_get_setting('header_video_still');
         if (!empty($video_still_fid) && is_numeric($video_still_fid[0])) {
           $header_video_file = File::load($video_still_fid[0]);
-          $variables['header_video_still'] = file_create_url($header_video_file->getFileUri());
+          $variables['header_video_still'] = \Drupal::service('file_url_generator')->generateAbsoluteString($header_video_file->getFileUri());
           $variables['header_media_video'] = TRUE;
         }
         else {
@@ -137,7 +137,7 @@ function space_preprocess_page(&$variables) {
         if (!empty($fid)) {
           $file = File::load($fid[0]);
           if (is_object($file)) {
-            $variables['region_' . $key . '_image_url'] = 'url("' . file_create_url($file->getFileUri()) . '") fixed';
+            $variables['region_' . $key . '_image_url'] = 'url("' . \Drupal::service('file_url_generator')->generateAbsoluteString($file->getFileUri()) . '") fixed';
           }
         }
 
@@ -152,7 +152,7 @@ function space_preprocess_page(&$variables) {
         if (!empty($fid)) {
           $file = File::load($fid[0]);
           if (is_object($file)) {
-            $variables['region_' . $key . '_image_url'] = 'url("' . file_create_url($file->getFileUri()) . '") fixed';
+            $variables['region_' . $key . '_image_url'] = 'url("' . \Drupal::service('file_url_generator')->generateAbsoluteString($file->getFileUri()) . '") fixed';
           }
         }
 
diff --git a/theme-settings.php b/theme-settings.php
index 6b479ce..b1a892e 100644
--- a/theme-settings.php
+++ b/theme-settings.php
@@ -256,7 +256,7 @@ function space_form_system_theme_settings_alter(&$form, FormStateInterface $form
   }
 
   // Odd issue in Drupal where the submit handler isn't called. Workaround.
-  $theme_settings_path = drupal_get_path('theme', 'space') . '/theme-settings.php';
+  $theme_settings_path = \Drupal::service('extension.list.theme')->getPath('space') . '/theme-settings.php';
   $info = $form_state->getBuildInfo();
 
   if (file_exists($theme_settings_path) && !in_array($theme_settings_path, $info['files'])) {

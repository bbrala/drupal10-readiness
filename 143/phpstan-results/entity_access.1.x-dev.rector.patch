diff --git a/tests/src/Functional/EntityAccessTest.php b/tests/src/Functional/EntityAccessTest.php
index fa7c2a5..9e7f675 100644
--- a/tests/src/Functional/EntityAccessTest.php
+++ b/tests/src/Functional/EntityAccessTest.php
@@ -49,7 +49,7 @@ class EntityAccessTest extends BrowserTestBase {
 
         // Visit the page.
         $this->drupalGet($path);
-        $this->assertResponse(200);
+        $this->assertSession()->statusCodeEquals(200);
 
         // Check existence of local task's tab: it have not be on
         // the page if page is not accessible.
@@ -57,17 +57,17 @@ class EntityAccessTest extends BrowserTestBase {
 
         // Check access to the tab's page.
         $this->drupalGet("$path/test");
-        $this->assertResponse($response_code);
+        $this->assertSession()->statusCodeEquals($response_code);
 
         if ($is_local_task_available) {
-          $this->assertRaw(\t('Local task for "@node_type" content type.', [
+          $this->assertSession()->responseContains(\t('Local task for "@node_type" content type.', [
             '@node_type' => 'node_type',
           ]));
 
-          $this->drupalPostForm(NULL, ['test_field' => 'Test value'], \t('Go'));
-          $this->drupalPostForm(NULL, [], \t('Confirm'));
+          $this->submitForm(['test_field' => 'Test value'], \t('Go'));
+          $this->submitForm([], \t('Confirm'));
 
-          $this->assertRaw(\t('You have successfully submitted the value: @value', [
+          $this->assertSession()->responseContains(\t('You have successfully submitted the value: @value', [
             '@value' => 'Test value',
           ]));
         }

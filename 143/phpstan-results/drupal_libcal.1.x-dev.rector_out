modules/contrib/drupal_libcal/libcal.module
modules/contrib/drupal_libcal/src/EventDownloadService.php
modules/contrib/drupal_libcal/src/EventDownloadServiceInterface.php
modules/contrib/drupal_libcal/src/Form/LibcalAPIConfigForm.php
modules/contrib/drupal_libcal/tests/src/Functional/LoadTest.php

3 files with changes
====================

1) modules/contrib/drupal_libcal/libcal.module:0

    ---------- begin diff ----------
@@ @@
 <?php

+use Drupal\Core\Entity\EntityInterface;
 /**
  * @file
  * Contains libcal.module.
@@ @@
 /**
  * Implements hook_insert().
  */
-function libcal_entity_insert(\Drupal\Core\Entity\EntityInterface $node)
+function libcal_entity_insert(EntityInterface $node)
 {
     // Set the URL alias
     //if (get_class($node) == 'Drupal\node\Entity\Node') {
@@ @@
     }
 }

-function libcal_entity_update(\Drupal\Core\Entity\EntityInterface $node)
+function libcal_entity_update(EntityInterface $node)
 {
     // Set the URL alias
     //if (get_class($node) == 'Drupal\node\Entity\Node') {
    ----------- end diff -----------

Applied rules:
 * ArgumentRemoverRector


2) modules/contrib/drupal_libcal/tests/src/Functional/LoadTest.php:28

    ---------- begin diff ----------
@@ @@
   /**
    * {@inheritdoc}
    */
-  protected function setUp() {
+  protected function setUp(): void {
     parent::setUp();
     $this->user = $this->drupalCreateUser(['administer site configuration']);
     $this->drupalLogin($this->user);
    ----------- end diff -----------

Applied rules:
 * ArgumentRemoverRector
 * WrapReturnRector
 * AddReturnTypeDeclarationRector
 * ChangeMethodVisibilityRector


3) modules/contrib/drupal_libcal/src/EventDownloadService.php:148

    ---------- begin diff ----------
@@ @@
         //$query->condition('field_past_event', false);
         $nids  = $query->execute();
         foreach ($nids as $nid) {
-            $eventnode = \Drupal\node\Entity\Node::load($nid);
+            $eventnode = Node::load($nid);
             //$eventnode->set('field_past_event', $flag);

             // check if current timestamp with event timestamp
    ----------- end diff -----------

Applied rules:
 * ArgumentRemoverRector
 * WrapReturnRector


 [OK] 3 files have been changed by Rector                                       


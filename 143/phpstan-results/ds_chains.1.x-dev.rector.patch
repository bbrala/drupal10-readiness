diff --git a/tests/src/Functional/ChainsUITest.php b/tests/src/Functional/ChainsUITest.php
index d13cdc8..b748040 100644
--- a/tests/src/Functional/ChainsUITest.php
+++ b/tests/src/Functional/ChainsUITest.php
@@ -39,7 +39,7 @@ class ChainsUITest extends BrowserTestBase {
   /**
    * {@inheritdoc}
    */
-  protected function setUp() {
+  protected function setUp(): void {
     parent::setUp();
     $this->createTestField('test_field', 'Some field', 'user', 'user', 'test_field');
     $this->createTestField('test_other_field', 'Some other field', 'user', 'user', 'test_field');
diff --git a/tests/src/Kernel/ChainedFieldBuildTest.php b/tests/src/Kernel/ChainedFieldBuildTest.php
index a2c554a..8069a60 100644
--- a/tests/src/Kernel/ChainedFieldBuildTest.php
+++ b/tests/src/Kernel/ChainedFieldBuildTest.php
@@ -39,12 +39,12 @@ class ChainedFieldBuildTest extends EntityKernelTestBase {
     $view_builder = $this->container->get('entity_type.manager')->getViewBuilder('entity_test');
     $build = $view_builder->view($entity);
     $rendered = $this->container->get('renderer')->renderPlain($build);
-    $this->assertContains('PONIES|Some value', (string) $rendered);
+    $this->assertStringContainsString('PONIES|Some value', (string) $rendered);
     $entity->user_id = $user_with_no_value;
     $entity->save();
     $build = $view_builder->view($entity);
     $rendered = $this->container->get('renderer')->renderPlain($build);
-    $this->assertNotContains('PONIES|Some value', (string) $rendered);
+    $this->assertStringNotContainsString('PONIES|Some value', (string) $rendered);
   }
 
   /**
@@ -77,7 +77,7 @@ class ChainedFieldBuildTest extends EntityKernelTestBase {
     $view_builder = $this->container->get('entity_type.manager')->getViewBuilder('entity_test');
     $build = $view_builder->view($entity);
     $rendered = $this->container->get('renderer')->renderPlain($build);
-    $this->assertContains('Some value', (string) $rendered);
+    $this->assertStringContainsString('Some value', (string) $rendered);
   }
 
   /**
@@ -120,8 +120,8 @@ class ChainedFieldBuildTest extends EntityKernelTestBase {
     $build = $view_builder->view($entity);
 
     $rendered = $this->container->get('renderer')->renderPlain($build);
-    $this->assertContains('Some value A', (string) $rendered);
-    $this->assertContains('Some value B', (string) $rendered);
+    $this->assertStringContainsString('Some value A', (string) $rendered);
+    $this->assertStringContainsString('Some value B', (string) $rendered);
 
     $fields[$plugin_id]['settings']['chain_settings']['ui_limit'] = 1;
     $display->setThirdPartySetting('ds', 'fields', $fields);
@@ -129,8 +129,8 @@ class ChainedFieldBuildTest extends EntityKernelTestBase {
 
     $build = $view_builder->view($entity);
     $rendered = $this->container->get('renderer')->renderPlain($build);
-    $this->assertContains('Some value A', (string) $rendered);
-    $this->assertNotContains('Some value B', (string) $rendered);
+    $this->assertStringContainsString('Some value A', (string) $rendered);
+    $this->assertStringNotContainsString('Some value B', (string) $rendered);
   }
 
 }

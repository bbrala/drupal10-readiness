modules/contrib/stop_broken_link_in_body/src/Controller/BrokenLinkController.php
modules/contrib/stop_broken_link_in_body/src/Form/RunContentBrokenLink.php
modules/contrib/stop_broken_link_in_body/src/Form/StopBrokenLinkForm.php
modules/contrib/stop_broken_link_in_body/stop_broken_link_in_body.install
modules/contrib/stop_broken_link_in_body/stop_broken_link_in_body.module

1 file with changes
===================

1) modules/contrib/stop_broken_link_in_body/src/Form/RunContentBrokenLink.php:1

    ---------- begin diff ----------
@@ @@

 namespace Drupal\stop_broken_link_in_body\Form;

+use Drupal\node\Entity\Node;
+use Drupal\Core\Field\FieldItemListInterface;
 use Drupal\Core\Form\FormBase;
 use Drupal\Core\Form\FormStateInterface;
 use Drupal\Core\Batch\BatchBuilder;
@@ @@
    */
   public function getTextValues($nid) {
     $has_broken_link = [];
-    $node = \Drupal\node\Entity\Node::load($nid);
+    $node = Node::load($nid);
     // Get fields to check.
     $fields_to_check = stop_broken_link_in_body_get_fields_to_checks();
     // Get all node types.
@@ @@
       // Check all fields in current node.
       foreach ($fields_to_check as $field_to_check) {
         $field = $node->{$field_to_check};
-        if ($field && $field instanceof \Drupal\Core\Field\FieldItemListInterface) {
+        if ($field && $field instanceof FieldItemListInterface) {
           // Get current field text value.
           $value = $node->{$field_to_check}->getValue();
           if (isset($value[0]['value'])) {
    ----------- end diff -----------

Applied rules:
 * ArgumentRemoverRector
 * WrapReturnRector
 * ChangeMethodVisibilityRector


 [OK] 1 file has been changed by Rector                                         


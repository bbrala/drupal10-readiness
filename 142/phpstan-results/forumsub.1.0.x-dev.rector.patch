diff --git a/forum_subscribe.module b/forum_subscribe.module
index 2e680b2..153a170 100644
--- a/forum_subscribe.module
+++ b/forum_subscribe.module
@@ -1,5 +1,6 @@
 <?php
 
+use Drupal\user\Entity\User;
 use Drupal\Core\Routing\RouteMatchInterface;
 use Drupal\Core\Entity\EntityInterface;
 use Drupal\comment\CommentInterface;
@@ -36,7 +37,7 @@ function _forum_subscribe_send_mail($user_id, $node, $comment) {
   //Load Drupal's mail service
   $mail_service = \Drupal::service('plugin.manager.mail');
   //Load the user entity and get it's email (if there's any)
-  $account = \Drupal\user\Entity\User::load($user_id);
+  $account = User::load($user_id);
   $email = $account->getEmail();
   $base_url = Request::createFromGlobals()->getSchemeAndHttpHost();
   //If the user has no email address set, or the commenter is same person as the current mail target, stop the execution of the script
@@ -123,4 +124,4 @@ function forum_subscribe_flag_delete(EntityInterface $entity) {
     unset($configured_flags[$key]);
     $config->set('flag-types', $configured_flags)->save();
   }
-}
\ No newline at end of file
+}
diff --git a/src/Form/ForumSubscribeSettingsForm.php b/src/Form/ForumSubscribeSettingsForm.php
index 9b2730a..2fb88d8 100644
--- a/src/Form/ForumSubscribeSettingsForm.php
+++ b/src/Form/ForumSubscribeSettingsForm.php
@@ -2,6 +2,7 @@
 
 namespace Drupal\forum_subscribe\Form;
 
+use Drupal\user\Entity\Role;
 use Drupal\Core\Form\ConfigFormBase;
 use Drupal\Core\Form\FormStateInterface;
 
@@ -39,7 +40,7 @@ class ForumSubscribeSettingsForm extends ConfigFormBase {
   public function buildForm(array $form, FormStateInterface $form_state) {
     $flag_service = \Drupal::service('flag');
     $all_flags = $flag_service->getAllFlags();
-    $anon = \Drupal\user\Entity\Role::load('anonymous');
+    $anon = Role::load('anonymous');
     $bundle_checkboxes = [];
     foreach($all_flags as $flag) {
       if (!$anon->hasPermission("flag " . $flag->id)) {

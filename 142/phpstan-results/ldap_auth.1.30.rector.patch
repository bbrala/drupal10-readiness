diff --git a/ldap_auth.info.yml b/ldap_auth.info.yml
index 9a723df..2a06b9e 100644
--- a/ldap_auth.info.yml
+++ b/ldap_auth.info.yml
@@ -1,7 +1,6 @@
 name: 'Active Directory Integration / LDAP Integration - NTLM & Kerberos Login'
 type: module
-core: 8.x
-core_version_requirement: ^8 || ^9
+core_version_requirement: ^9.3 || ^10
 description: 'Drupal LDAP / Active Directory Integration module allows your users to log in to your Drupal site using their LDAP / AD credentials. In addition to LDAP, this module also allows you to log in using NTLM and Kerberos'
 configure: ldap_auth.ldap_config
 # Information added by Drupal.org packaging script on 2021-01-12
diff --git a/src/Form/MiniorangeGeneralSettings.php b/src/Form/MiniorangeGeneralSettings.php
index c1ff3e2..98cf124 100644
--- a/src/Form/MiniorangeGeneralSettings.php
+++ b/src/Form/MiniorangeGeneralSettings.php
@@ -93,7 +93,7 @@ $form['gateway']['miniorange_ldap_gateway_desc'] = array(
   LDAP Gateway allows login to publicly/privately hosted sites using credentials stored in Active Directory, OpenLDAP and other LDAP servers. If the LDAP Server is not publicly accessible from your site,
   this module can be used in conjunction with the miniOrange LDAP Gateway, which is deployed at the DMZ server in the intranet.'
 );
-    $module_path = drupal_get_path('module', 'ldap_auth');
+    $module_path = \Drupal::service('extension.list.module')->getPath('ldap_auth');
 
 
 $form['gateway']['gateway_login_img'] = array(
diff --git a/src/Form/MiniorangeLicensing.php b/src/Form/MiniorangeLicensing.php
index e26fe88..adc4c7e 100644
--- a/src/Form/MiniorangeLicensing.php
+++ b/src/Form/MiniorangeLicensing.php
@@ -41,7 +41,7 @@ public function buildForm(array $form, FormStateInterface $form_state)
         );
 
 
-      $module_path = drupal_get_path('module', 'ldap_auth');
+      $module_path = \Drupal::service('extension.list.module')->getPath('ldap_auth');
 
       $form['miniorange_ldap_license1'] = array(
           '#markup' =>t('<html lang="en">
diff --git a/src/Utilities.php b/src/Utilities.php
index b3651d8..b23864f 100644
--- a/src/Utilities.php
+++ b/src/Utilities.php
@@ -22,6 +22,7 @@
  * along with miniOrange LDAP Login plugin.  If not, see <http://www.gnu.org/licenses/>.
  */
 namespace Drupal\ldap_auth;
+use Drupal\Core\Datetime\DrupalDateTime;
 use Drupal\Core\Form\FormStateInterface;
 use Drupal\ldap_auth\handler;
 use Drupal\ldap_auth\MiniorangeLdapSupport;
@@ -44,6 +45,7 @@ class Utilities {
           \Drupal::messenger()->addMessage( $_message,$type);
         }
         else{
+             // @noRector
              drupal_set_message($_message, $type);
         }
     }
@@ -122,7 +124,7 @@ class Utilities {
 
       $form['setup_call']['miniorange_ldap_meeting_time'] = array (
         '#type' => 'datetime',
-        '#default_value' => \Drupal\Core\Datetime\DrupalDateTime::createFromTimestamp(time()),
+        '#default_value' => DrupalDateTime::createFromTimestamp(time()),
         '#title' => 'Date and Time',
         '#format' => '',
       );

modules/contrib/sector_content_audit/sector_content_audit.install
modules/contrib/sector_content_audit/sector_content_audit.module

1 file with changes
===================

1) modules/contrib/sector_content_audit/sector_content_audit.module:148

    ---------- begin diff ----------
@@ @@
   return $types;
 }

-function sector_content_audit_form_alter(&$form, \Drupal\Core\Form\FormStateInterface $form_state, $form_id) {
+function sector_content_audit_form_alter(&$form, FormStateInterface $form_state, $form_id) {
   $route = \Drupal::routeMatch()->getRouteName();
   $formStorage = $form_state->getStorage();
   if (!empty($formStorage['form_display'])) {
    ----------- end diff -----------

Applied rules:
 * ArgumentRemoverRector


 [OK] 1 file has been changed by Rector                                         


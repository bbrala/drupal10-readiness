diff --git a/tracking_options.module b/tracking_options.module
index dab13ee..b4357a5 100644
--- a/tracking_options.module
+++ b/tracking_options.module
@@ -1,5 +1,6 @@
 <?php
 
+use Drupal\Core\Render\BubbleableMetadata;
 /**
  * Implements hook_theme().
  */
@@ -86,7 +87,7 @@ function tracking_options_token_info() {
 /**
  * Implements hook_tokens().
  */
-function tracking_options_tokens($type, $tokens, array $data, array $options, \Drupal\Core\Render\BubbleableMetadata $bubbleable_metadata) {
+function tracking_options_tokens($type, $tokens, array $data, array $options, BubbleableMetadata $bubbleable_metadata) {
   $replacements = [];
 
   if ($type == 'node') {
@@ -94,7 +95,7 @@ function tracking_options_tokens($type, $tokens, array $data, array $options, \D
       if ($name === 'tracking-options') {
         $form = \Drupal::formBuilder()
           ->getForm('Drupal\tracking_options\Form\TrackingOptionsForm');
-        $replacements[$original] = render($form);
+        $replacements[$original] = \Drupal::service('renderer')->render($form);
       }
     }
   }

modules/contrib/broadstreet_ads/broadstreet-ad-message.tpl.php
modules/contrib/broadstreet_ads/broadstreet-ad.tpl.php
modules/contrib/broadstreet_ads/broadstreet_ads.admin.inc
modules/contrib/broadstreet_ads/broadstreet_ads.install
modules/contrib/broadstreet_ads/broadstreet_ads.module
modules/contrib/broadstreet_ads/src/Form/Settings.php
modules/contrib/broadstreet_ads/src/Plugin/Block/AdBlock.php
modules/contrib/broadstreet_ads/src/Plugin/Derivative/AdBlock.php

2 files with changes
====================

1) modules/contrib/broadstreet_ads/broadstreet_ads.module:113

    ---------- begin diff ----------
@@ @@
       $settings['broadstreet_ads']['add_body_class'] = check_plain($body_class);
     }

-    $path = drupal_get_path('module', 'broadstreet_ads') . '/blockers.js';
+    $path = \Drupal::service('extension.list.module')->getPath('broadstreet_ads') . '/blockers.js';

     // Get the weight of the last JS item.
     $keys = array_keys($js);
    ----------- end diff -----------

Applied rules:
 * DrupalGetPathRector
 * ArgumentRemoverRector


2) modules/contrib/broadstreet_ads/broadstreet_ads.admin.inc:282

    ---------- begin diff ----------
@@ @@
   // Save whatever zones exist.
   variable_set('broadstreet_ads_zones', $zones);

-  drupal_set_message(t('Ad zones and settings updated.'));
+  \Drupal::messenger()->addStatus(t('Ad zones and settings updated.'));
 }

 /**
@@ @@
     // Traverse each column in the row.  @see element_children().
     foreach (element_children($rows[$row_index]) as $col_index) {
       // Render the column form element.
-      $row[] = drupal_render($rows[$row_index][$col_index]);
+      $row[] = \Drupal::service('renderer')->render($rows[$row_index][$col_index]);
     }
     // Add the row to the table.
     $content['#rows'][] = $row;
@@ @@
   }

   // Render the table and return.
-  return drupal_render($content);
+  return \Drupal::service('renderer')->render($content);
 }
    ----------- end diff -----------

Applied rules:
 * DrupalRenderRector
 * DrupalSetMessageRector
 * ArgumentRemoverRector


 [OK] 2 files have been changed by Rector                                       


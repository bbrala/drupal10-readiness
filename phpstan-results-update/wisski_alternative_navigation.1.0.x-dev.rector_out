modules/contrib/wisski_alternative_navigation/src/Controller/WissKIAlternativeNavigateController.php
modules/contrib/wisski_alternative_navigation/src/Form/WissKIAlternativeNavigateForm.php
modules/contrib/wisski_alternative_navigation/wisski_alternative_navigate.install
modules/contrib/wisski_alternative_navigation/wisski_alternative_navigate.module

3 files with changes
====================

1) modules/contrib/wisski_alternative_navigation/wisski_alternative_navigate.module:0

    ---------- begin diff ----------
@@ @@
 <?php
+use Drupal\Core\Routing\RouteMatchInterface;
+use Drupal\Core\Link;
 use Drupal\Core\Url;
 use Drupal\Core\Breadcrumb\Breadcrumb;
 use Drupal\wisski_core\WisskiCacheHelper;
@@ @@
 }


-function wisski_alternative_navigate_system_breadcrumb_alter(\Drupal\Core\Breadcrumb\Breadcrumb &$breadcrumb, \Drupal\Core\Routing\RouteMatchInterface $route_match, array $context) {
+function wisski_alternative_navigate_system_breadcrumb_alter(Breadcrumb &$breadcrumb, RouteMatchInterface $route_match, array $context) {
     // Append the current page title to the breadcrumb.
     $current_path = \Drupal::service('path.current')->getPath();
     $config = \Drupal::config('wisski_alternative_navigate.adminsettings');
@@ @@
               $label = $display_views[$view_id]['new_label'];
       }

-      $breadcrumb->addLink(\Drupal\Core\Link::createFromRoute(t('Browse'), 'wisski_alternative_navigate.wisskinavigate_page_with_theme'));
-      $breadcrumb->addLink(\Drupal\Core\Link::createFromRoute($label, '<none>'));
+      $breadcrumb->addLink(Link::createFromRoute(t('Browse'), 'wisski_alternative_navigate.wisskinavigate_page_with_theme'));
+      $breadcrumb->addLink(Link::createFromRoute($label, '<none>'));
     }

     //Change the breadcrumb to the new navigate page on entity pages
@@ @@
         { 
 	      $title = t('Edit');
         }
-        $breadcrumb->addLink(\Drupal\Core\Link::createFromRoute($title, '<none>'));
+        $breadcrumb->addLink(Link::createFromRoute($title, '<none>'));
       }	     
     }
     //Add title to breadcrumb on new browsing site
@@ @@
     elseif($breadcrumb && $current_path == "/wisski-browse") {
       $title = \Drupal::service('title_resolver')->getTitle(\Drupal::request(), $route_match->getRouteObject());
       if (!empty($title)) {
-         $breadcrumb->addLink(\Drupal\Core\Link::createFromRoute($title, '<none>'));
+         $breadcrumb->addLink(Link::createFromRoute($title, '<none>'));
       }
     }
 }
    ----------- end diff -----------

Applied rules:
 * ArgumentRemoverRector


2) modules/contrib/wisski_alternative_navigation/src/Form/WissKIAlternativeNavigateForm.php:264

    ---------- begin diff ----------
@@ @@
           if (!empty($view['preview_image'])) {
 		 $view_id = $view['view_id'];     
 		 $view_image_id = $view['preview_image'];
-		 $file = \Drupal\file\Entity\File::load($view_image_id[0]);
+		 $file = File::load($view_image_id[0]);
                  $file_parts = pathinfo($file->getFilename());
                  $file_extension = $file_parts['extension'];
                  rename($file->getFileUri(),"public://alternative-navigate/preview_image_".$view_id.".".$file_extension);
    ----------- end diff -----------

Applied rules:
 * ArgumentRemoverRector
 * WrapReturnRector
 * ChangeMethodVisibilityRector


3) modules/contrib/wisski_alternative_navigation/src/Controller/WissKIAlternativeNavigateController.php:1

    ---------- begin diff ----------
@@ @@

 namespace Drupal\wisski_alternative_navigate\Controller;

+use Drupal\file\Entity\File;
 use Drupal\Core\Controller\ControllerBase;

 class WissKIAlternativeNavigateController extends ControllerBase {
@@ @@
 		    $active_navigate_views[$display_view_id]['path'] = $page_path;

 		    if(!empty($display_view['preview_image'][0])) {
-		      $file = \Drupal\file\Entity\File::load($display_view['preview_image'][0]);
+		      $file = File::load($display_view['preview_image'][0]);
 		      $uri = $file->getFileUri();
-		      $active_navigate_views[$display_view_id]['preview_image'] = file_create_url($uri);
+		      $active_navigate_views[$display_view_id]['preview_image'] = \Drupal::service('file_url_generator')->generateAbsoluteString($uri);
 		    }
 		    else {
 		      $active_navigate_views[$display_view_id]['preview_image'] = '';
    ----------- end diff -----------

Applied rules:
 * FileCreateUrlRector
 * ArgumentRemoverRector
 * WrapReturnRector
 * ChangeMethodVisibilityRector


 [OK] 3 files have been changed by Rector                                       


diff --git a/layout_builder_st.info.yml b/layout_builder_st.info.yml
index 283fdbf..7a868f5 100644
--- a/layout_builder_st.info.yml
+++ b/layout_builder_st.info.yml
@@ -2,8 +2,7 @@ name: 'Layout Builder Symmetric Translations'
 type: module
 description: 'Allows translating Layout overrides'
 package: Layout Builder
-core: 8.x
-core_version_requirement: ^8 || ^9
+core_version_requirement: ^9.1 || ^10
 dependencies:
   - drupal:layout_builder
   - drupal:system (>=8.7)
diff --git a/tests/src/Functional/LayoutBuilderTranslationTest.php b/tests/src/Functional/LayoutBuilderTranslationTest.php
index 6bd9127..eef0851 100644
--- a/tests/src/Functional/LayoutBuilderTranslationTest.php
+++ b/tests/src/Functional/LayoutBuilderTranslationTest.php
@@ -37,7 +37,7 @@ class LayoutBuilderTranslationTest extends ContentTranslationTestBase {
   /**
    * {@inheritdoc}
    */
-  protected function setUp() {
+  protected function setUp(): void {
     parent::setUp();
     $this->setUpViewDisplay();
     $this->setUpEntities();
@@ -268,9 +268,11 @@ class LayoutBuilderTranslationTest extends ContentTranslationTestBase {
     $this->drupalLogin($this->administrator);
 
     $field_ui_prefix = 'entity_test_mul/structure/entity_test_mul';
+    $this->drupalGet("$field_ui_prefix/display/default");
     // Allow overrides for the layout.
-    $this->drupalPostForm("$field_ui_prefix/display/default", ['layout[enabled]' => TRUE], 'Save');
-    $this->drupalPostForm("$field_ui_prefix/display/default", ['layout[allow_custom]' => TRUE], 'Save');
+    $this->submitForm(['layout[enabled]' => TRUE], 'Save');
+    $this->drupalGet("$field_ui_prefix/display/default");
+    $this->submitForm(['layout[allow_custom]' => TRUE], 'Save');
 
     // @todo The Layout Builder UI relies on local tasks; fix in
     //   https://www.drupal.org/project/drupal/issues/2917777.
@@ -292,7 +294,8 @@ class LayoutBuilderTranslationTest extends ContentTranslationTestBase {
       'source' => $this->langcodes[0],
       'target' => $this->langcodes[2],
     ]);
-    $this->drupalPostForm($add_translation_url, [
+    $this->drupalGet($add_translation_url);
+    $this->submitForm([
       "{$this->fieldName}[0][value]" => 'The translated field value',
     ], 'Save');
   }
diff --git a/tests/src/Functional/Rest/LayoutRestTestBase.php b/tests/src/Functional/Rest/LayoutRestTestBase.php
index be8e903..4f587fe 100644
--- a/tests/src/Functional/Rest/LayoutRestTestBase.php
+++ b/tests/src/Functional/Rest/LayoutRestTestBase.php
@@ -43,7 +43,7 @@ abstract class LayoutRestTestBase extends ResourceTestBase {
   /**
    * {@inheritdoc}
    */
-  public function setUp() {
+  public function setUp(): void {
     parent::setUp();
     $assert_session = $this->assertSession();
 
diff --git a/tests/src/Functional/Rest/OverrideSectionsTest.php b/tests/src/Functional/Rest/OverrideSectionsTest.php
index c2dc67b..cad507a 100644
--- a/tests/src/Functional/Rest/OverrideSectionsTest.php
+++ b/tests/src/Functional/Rest/OverrideSectionsTest.php
@@ -29,7 +29,7 @@ class OverrideSectionsTest extends LayoutRestTestBase {
   /**
    * {@inheritdoc}
    */
-  public function setUp() {
+  public function setUp(): void {
     parent::setUp();
     // @todo Figure why field definitions have to cleared in
     //   https://www.drupal.org/project/drupal/issues/2985882.
diff --git a/tests/src/FunctionalJavascript/InlineBlockTranslationTest.php b/tests/src/FunctionalJavascript/InlineBlockTranslationTest.php
index 7ba6933..98f01fb 100644
--- a/tests/src/FunctionalJavascript/InlineBlockTranslationTest.php
+++ b/tests/src/FunctionalJavascript/InlineBlockTranslationTest.php
@@ -34,7 +34,7 @@ class InlineBlockTranslationTest extends InlineBlockTestBase {
   /**
    * {@inheritdoc}
    */
-  protected function setUp() {
+  protected function setUp(): void {
     parent::setUp();
 
     // Adds a new language.
@@ -59,18 +59,12 @@ class InlineBlockTranslationTest extends InlineBlockTestBase {
       'create content translations',
       'create and edit custom blocks',
     ]));
+    $this->drupalGet(static::FIELD_UI_PREFIX . '/display/default');
 
     // Allow layout overrides.
-    $this->drupalPostForm(
-      static::FIELD_UI_PREFIX . '/display/default',
-      ['layout[enabled]' => TRUE],
-      'Save'
-    );
-    $this->drupalPostForm(
-      static::FIELD_UI_PREFIX . '/display/default',
-      ['layout[allow_custom]' => TRUE],
-      'Save'
-    );
+    $this->submitForm(['layout[enabled]' => TRUE], 'Save');
+    $this->drupalGet(static::FIELD_UI_PREFIX . '/display/default');
+    $this->submitForm(['layout[allow_custom]' => TRUE], 'Save');
 
     // Add a new inline block to the original node.
     $this->drupalGet('node/1/layout');
@@ -87,7 +81,8 @@ class InlineBlockTranslationTest extends InlineBlockTestBase {
       'source' => 'en',
       'target' => 'it',
     ]);
-    $this->drupalPostForm($add_translation_url, [
+    $this->drupalGet($add_translation_url);
+    $this->submitForm([
       'title[0][value]' => 'The translated node title',
       'body[0][value]' => 'The translated node body',
     ], 'Save');
diff --git a/tests/src/FunctionalJavascript/ModeratedTranslationTest.php b/tests/src/FunctionalJavascript/ModeratedTranslationTest.php
index 743e59d..b75bd25 100644
--- a/tests/src/FunctionalJavascript/ModeratedTranslationTest.php
+++ b/tests/src/FunctionalJavascript/ModeratedTranslationTest.php
@@ -51,7 +51,7 @@ class ModeratedTranslationTest extends WebDriverTestBase {
   /**
    * {@inheritdoc}
    */
-  protected function setUp() {
+  protected function setUp(): void {
     parent::setUp();
     $page = $this->getSession()->getPage();
     $this->container->get('state')->set('test_block_access', TRUE);
@@ -106,22 +106,17 @@ class ModeratedTranslationTest extends WebDriverTestBase {
       'target' => 'it',
     ]);
     $this->drupalGet($add_translation_url);
-    $this->drupalPostForm($add_translation_url, [
+    $this->drupalGet($add_translation_url);
+    $this->submitForm([
       'title[0][value]' => 'The translated node title',
       'body[0][value]' => 'The translated node body',
     ], 'Save');
+    $this->drupalGet(static::FIELD_UI_PREFIX . '/display/default');
 
     // Allow layout overrides.
-    $this->drupalPostForm(
-      static::FIELD_UI_PREFIX . '/display/default',
-      ['layout[enabled]' => TRUE],
-      'Save'
-    );
-    $this->drupalPostForm(
-      static::FIELD_UI_PREFIX . '/display/default',
-      ['layout[allow_custom]' => TRUE],
-      'Save'
-    );
+    $this->submitForm(['layout[enabled]' => TRUE], 'Save');
+    $this->drupalGet(static::FIELD_UI_PREFIX . '/display/default');
+    $this->submitForm(['layout[allow_custom]' => TRUE], 'Save');
 
     // Publish both nodes.
     $this->drupalGet($node->toUrl());
diff --git a/tests/src/FunctionalJavascript/TranslationTest.php b/tests/src/FunctionalJavascript/TranslationTest.php
index 44402fc..6a7ab9c 100644
--- a/tests/src/FunctionalJavascript/TranslationTest.php
+++ b/tests/src/FunctionalJavascript/TranslationTest.php
@@ -170,22 +170,17 @@ class TranslationTest extends WebDriverTestBase {
       'source' => 'en',
       'target' => 'it',
     ]);
-    $this->drupalPostForm($add_translation_url, [
+    $this->drupalGet($add_translation_url);
+    $this->submitForm([
       'title[0][value]' => 'The translated node title',
       'body[0][value]' => 'The translated node body',
     ], 'Save');
+    $this->drupalGet(static::FIELD_UI_PREFIX . '/display/default');
 
     // Allow layout overrides.
-    $this->drupalPostForm(
-      static::FIELD_UI_PREFIX . '/display/default',
-      ['layout[enabled]' => TRUE],
-      'Save'
-    );
-    $this->drupalPostForm(
-      static::FIELD_UI_PREFIX . '/display/default',
-      ['layout[allow_custom]' => TRUE],
-      'Save'
-    );
+    $this->submitForm(['layout[enabled]' => TRUE], 'Save');
+    $this->drupalGet(static::FIELD_UI_PREFIX . '/display/default');
+    $this->submitForm(['layout[allow_custom]' => TRUE], 'Save');
   }
 
 }

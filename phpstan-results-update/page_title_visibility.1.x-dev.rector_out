modules/contrib/page_title_visibility/page_title_visibility.install
modules/contrib/page_title_visibility/page_title_visibility.module
modules/contrib/page_title_visibility/tests/src/Functional/ConfigTest.php
modules/contrib/page_title_visibility/tests/src/Functional/EntityTest.php
modules/contrib/page_title_visibility/tests/src/FunctionalJavascript/PermissionTest.php

2 files with changes
====================

1) modules/contrib/page_title_visibility/tests/src/FunctionalJavascript/PermissionTest.php:39

    ---------- begin diff ----------
@@ @@
   /**
    * {@inheritdoc}
    */
-  protected function setUp() {
+  protected function setUp(): void {
     parent::setUp();

     $nodeType = NodeType::create([
@@ @@
     $edit = [
       'title[0][value]' => $title,
     ];
-    $this->drupalPostForm(NULL, $edit, 'Save');
+    $this->submitForm($edit, 'Save');
     // Confirm a new Test type node was created.
     $web_assert->responseContains('Test', 'has been created.');
     // Verify the page title selector has no 'visually-hidden's attribute.
@@ @@
     $page->findLink('Page display options')->click();
     // Uncheck the "Page Visibility" checkbox and save the node.
     $page->uncheckField('edit-display-page-title-value');
-    $this->drupalPostForm(NULL, [], 'Save');
+    $this->submitForm([], 'Save');
     // Confirm the same node was updated successfully.
     $web_assert->responseContains('Test', 'has been updated.');
     // Confirm that you can find the visually-hidden class, meaning that the
@@ @@
     $page->hasCheckedField('#edit-display-page-title');
     // Uncheck the "Page Visibility" checkbox and save the CT settings.
     $page->uncheckField('edit-display-page-title');
-    $this->drupalPostForm(NULL, [], 'Save content type');
+    $this->submitForm([], 'Save content type');
     // Go to node creation page.
     $this->drupalGet('node/add/test');
     // Find the v-tab provided by the module and click it.
    ----------- end diff -----------

Applied rules:
 * UiHelperTraitDrupalPostFormRector
 * ArgumentRemoverRector
 * WrapReturnRector
 * AddReturnTypeDeclarationRector
 * ChangeMethodVisibilityRector


2) modules/contrib/page_title_visibility/tests/src/Functional/EntityTest.php:34

    ---------- begin diff ----------
@@ @@
   /**
    * {@inheritdoc}
    */
-  protected function setUp() {
+  protected function setUp(): void {
     parent::setUp();

     $nodeType = NodeType::create([
    ----------- end diff -----------

Applied rules:
 * ArgumentRemoverRector
 * WrapReturnRector
 * AddReturnTypeDeclarationRector
 * ChangeMethodVisibilityRector


 [OK] 2 files have been changed by Rector                                       


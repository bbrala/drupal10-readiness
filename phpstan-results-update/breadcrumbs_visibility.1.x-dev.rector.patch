diff --git a/breadcrumbs_visibility.info.yml b/breadcrumbs_visibility.info.yml
index 2858a4d..8b53fd4 100644
--- a/breadcrumbs_visibility.info.yml
+++ b/breadcrumbs_visibility.info.yml
@@ -1,8 +1,7 @@
 name: 'Breadcrumbs Visibility'
 type: module
 description: Regulate the breadcrumbs display for node entities.
-core: 8.x
-core_version_requirement: ^8 || ^9
+core_version_requirement: ^9.1 || ^10
 dependencies:
   - drupal:block
   - drupal:node
diff --git a/tests/src/Functional/EntityTest.php b/tests/src/Functional/EntityTest.php
index adb6fa9..bb5611a 100644
--- a/tests/src/Functional/EntityTest.php
+++ b/tests/src/Functional/EntityTest.php
@@ -35,7 +35,7 @@ class EntityTest extends BrowserTestBase {
   /**
    * {@inheritdoc}
    */
-  protected function setUp() {
+  protected function setUp(): void {
     parent::setUp();
 
     $nodeType = NodeType::create([
diff --git a/tests/src/FunctionalJavascript/PermissionTest.php b/tests/src/FunctionalJavascript/PermissionTest.php
index 97b8700..5e2dea1 100644
--- a/tests/src/FunctionalJavascript/PermissionTest.php
+++ b/tests/src/FunctionalJavascript/PermissionTest.php
@@ -40,7 +40,7 @@ class PermissionTest extends WebDriverTestBase {
   /**
    * {@inheritdoc}
    */
-  protected function setUp() {
+  protected function setUp(): void {
     parent::setUp();
 
     $nodeType = NodeType::create([
@@ -84,7 +84,7 @@ class PermissionTest extends WebDriverTestBase {
     $edit = [
       'title[0][value]' => $title,
     ];
-    $this->drupalPostForm(NULL, $edit, 'Save');
+    $this->submitForm($edit, 'Save');
     // Confirm a new Test type node was created.
     $web_assert->responseContains('Test', 'has been created.');
     // Verify the breadcrumbs element exists in the markup.
@@ -96,7 +96,7 @@ class PermissionTest extends WebDriverTestBase {
     $page->findLink('Page display options')->click();
     // Uncheck the "display breadcrumbs" checkbox and save the node.
     $page->uncheckField('edit-display-breadcrumbs-value');
-    $this->drupalPostForm(NULL, [], 'Save');
+    $this->submitForm([], 'Save');
     // Confirm the same node was updated successfully.
     $web_assert->responseContains('Test', 'has been updated.');
     // Verify the breadcrumbs element doesn't exist in the markup.
@@ -139,7 +139,7 @@ class PermissionTest extends WebDriverTestBase {
     // Uncheck the "display breadcrumbs" checkbox and save the content type
     // settings.
     $page->uncheckField('edit-display-breadcrumbs');
-    $this->drupalPostForm(NULL, [], 'Save content type');
+    $this->submitForm([], 'Save content type');
     // Go to node creation page.
     $this->drupalGet('node/add/test');
     // Find the v-tab provided by the module and click it.

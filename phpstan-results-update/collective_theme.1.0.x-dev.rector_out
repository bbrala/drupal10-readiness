themes/contrib/collective_theme/collective_theme.theme
themes/contrib/collective_theme/src/styleguide/drupal-twig/BasicTwigExtensions.php
themes/contrib/collective_theme/src/styleguide/drupal-twig/alter-twig.php
themes/contrib/collective_theme/color/color.inc

1 file with changes
===================

1) themes/contrib/collective_theme/collective_theme.theme:158

    ---------- begin diff ----------
@@ @@
     // Populate thumbs and images.
     for ($c = 0; $c < $count; $c++) {
       $card = $paragraph->get('field_card')[$c]->entity;
-      $image_url = file_create_url($card->get('field_media')->entity->get('field_image')->entity->getFileUri());
+      $image_url = \Drupal::service('file_url_generator')->generateAbsoluteString($card->get('field_media')->entity->get('field_image')->entity->getFileUri());
       if ($card->hasField('field_thumb') && !empty($card->get('field_thumb')->entity)) {
-        $thumb_url = file_create_url($card->get('field_thumb')->entity->getFileUri());
+        $thumb_url = \Drupal::service('file_url_generator')->generateAbsoluteString($card->get('field_thumb')->entity->getFileUri());
       }
       else {
         $thumb_url = $image_url;
    ----------- end diff -----------

Applied rules:
 * FileCreateUrlRector
 * ArgumentRemoverRector


 [ERROR] Could not process                                                      
         "themes/contrib/collective_theme/src/styleguide/drupal-twig/BasicTwigEx
         tensions.php" file, due to:                                            
         "System error: "Provided node "Twig_ExtensionInterface" is not         
         interface, but "class""                                                
         Run Rector with "--debug" option and post the report here:             
         https://github.com/rectorphp/rector/issues/new". On line: 17           

 [ERROR] Could not process                                                      
         "themes/contrib/collective_theme/src/styleguide/drupal-twig/alter-twig.
         php" file, due to:                                                     
         "System error: "Provided node "Twig_ExtensionInterface" is not         
         interface, but "class""                                                
         Run Rector with "--debug" option and post the report here:             
         https://github.com/rectorphp/rector/issues/new". On line: 17           


modules/contrib/atools/atools.module
modules/contrib/atools/src/Atools.php
modules/contrib/atools/src/Controller/AtoolsController.php
modules/contrib/atools/src/Form/AtoolsBlockBase.php
modules/contrib/atools/src/Form/AtoolsConfigForm.php
modules/contrib/atools/src/Form/FormGenerateTrait.php

2 files with changes
====================

1) modules/contrib/atools/src/Form/FormGenerateTrait.php:46

    ---------- begin diff ----------
@@ @@
    *   (optional) The type of extension to load from.
    */
   protected function loadConf($name, $type = 'module') {
-    $config_path = drupal_get_path($type, $name) . '/config/tools';
+    $config_path = \Drupal::service('extension.path.resolver')->getPath($type, $name) . '/config/tools';
     $config_source = new FileStorage($config_path);

     $this->configForm = $config_source->read($this->getConfigFormName());
    ----------- end diff -----------

Applied rules:
 * DrupalGetPathRector
 * ArgumentRemoverRector
 * WrapReturnRector


2) modules/contrib/atools/src/Atools.php:48

    ---------- begin diff ----------
@@ @@
     if (!array_key_exists($cid, $cache)) {
       $cache[$cid] = \Drupal::cache()->get($cid, $allow_invalid);
       // Verify expiration time of cache and do not return it if expired !
-      if ($cache[$cid] && $force && ($cache[$cid]->expire < REQUEST_TIME)) {
+      if ($cache[$cid] && $force && ($cache[$cid]->expire < \Drupal::time()->getRequestTime())) {
         return FALSE;
       }
    ----------- end diff -----------

Applied rules:
 * RequestTimeConstRector
 * ArgumentRemoverRector
 * WrapReturnRector


 [OK] 2 files have been changed by Rector                                       


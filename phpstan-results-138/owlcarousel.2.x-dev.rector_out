modules/contrib/owlcarousel/drush/owlcarousel.drush.inc
modules/contrib/owlcarousel/owlcarousel.install
modules/contrib/owlcarousel/owlcarousel.module
modules/contrib/owlcarousel/owlcarousel.theme.inc
modules/contrib/owlcarousel/src/Commands/OwlCarouselCommands.php
modules/contrib/owlcarousel/src/OwlCarouselGlobal.php
modules/contrib/owlcarousel/src/Plugin/Field/FieldFormatter/OwlCarouselFieldFormatter.php
modules/contrib/owlcarousel/src/Plugin/views/style/OwlCarousel.php

2 files with changes
====================

1) modules/contrib/owlcarousel/src/Plugin/Field/FieldFormatter/OwlCarouselFieldFormatter.php:271

    ---------- begin diff ----------
@@ @@
     foreach ($files as $delta => $file) {
       if (isset($link_file)) {
         $image_uri = $file->getFileUri();
-        $url = Url::fromUri(file_create_url($image_uri));
+        $url = \Drupal::service('file_url_generator')->generate($image_uri);
       }
       $cache_tags = Cache::mergeTags($cache_tags, $file->getCacheTags());
    ----------- end diff -----------

Applied rules:
 * FromUriRector
 * ArgumentRemoverRector
 * PseudoNamespaceToNamespaceRector
 * WrapReturnRector
 * AddParamTypeDeclarationRector
 * ChangeMethodVisibilityRector


2) modules/contrib/owlcarousel/owlcarousel.module:54

    ---------- begin diff ----------
@@ @@
   if (!empty($items)) {
     $i = 0;
     foreach ($items as $item) {
-      $html .= '<div class="owl-item-inner owl-item-inner' . $i . '">' . render($item) . '</div>';
+      $html .= '<div class="owl-item-inner owl-item-inner' . $i . '">' . \Drupal::service('renderer')->render($item) . '</div>';
       $i++;
     }
   }
@@ @@
     '#type' => 'markup',
     '#markup' => $html,
   ];
-  $variables['output'] = render($output);
+  $variables['output'] = \Drupal::service('renderer')->render($output);

 }

@@ @@
   $attached = [
     '#attached' => ['library' => ['owlcarousel/owlcarousel']],
   ];
-  render($attached);
+  \Drupal::service('renderer')->render($attached);
   template_preprocess_views_view_unformatted($variables);
 }
    ----------- end diff -----------

Applied rules:
 * RenderRector
 * ArgumentRemoverRector


 [OK] 2 files have been changed by Rector                                       


themes/contrib/ui_suite_protocol/ui_suite_protocol.theme

1 file with changes
===================

1) themes/contrib/ui_suite_protocol/ui_suite_protocol.theme:36

    ---------- begin diff ----------
@@ @@
  * Add theme base path to media.
  */
 function _ui_suite_protocol_add_media_path($item): array {
-	$base_path = drupal_get_path('theme', 'ui_suite_protocol');
+	$base_path = \Drupal::service('extension.list.theme')->getPath('ui_suite_protocol');
   if (is_array($item) && array_key_exists('#theme', $item)) {
     if ($item['#theme'] === 'image') {
 			$uri = isset($item['#uri']) ? implode('/', [
    ----------- end diff -----------

Applied rules:
 * DrupalGetPathRector
 * ArgumentRemoverRector


 [OK] 1 file has been changed by Rector                                         


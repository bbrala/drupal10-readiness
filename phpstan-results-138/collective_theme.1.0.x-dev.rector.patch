diff --git a/collective_theme.theme b/collective_theme.theme
index 3c4b01f..bcd29b0 100644
--- a/collective_theme.theme
+++ b/collective_theme.theme
@@ -159,9 +159,9 @@ function collective_theme_preprocess_paragraph(&$variables) {
     // Populate thumbs and images.
     for ($c = 0; $c < $count; $c++) {
       $card = $paragraph->get('field_card')[$c]->entity;
-      $image_url = file_create_url($card->get('field_media')->entity->get('field_image')->entity->getFileUri());
+      $image_url = \Drupal::service('file_url_generator')->generateAbsoluteString($card->get('field_media')->entity->get('field_image')->entity->getFileUri());
       if ($card->hasField('field_thumb') && !empty($card->get('field_thumb')->entity)) {
-        $thumb_url = file_create_url($card->get('field_thumb')->entity->getFileUri());
+        $thumb_url = \Drupal::service('file_url_generator')->generateAbsoluteString($card->get('field_thumb')->entity->getFileUri());
       }
       else {
         $thumb_url = $image_url;

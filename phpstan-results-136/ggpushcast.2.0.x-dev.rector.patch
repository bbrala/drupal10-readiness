diff --git a/composer.json b/composer.json
index 843c644..3257ac0 100644
--- a/composer.json
+++ b/composer.json
@@ -17,6 +17,6 @@
     "source": "https://git.drupalcode.org/project/ggpushcast"
   },
   "require": {
-    "drupal/core": "^8 || ^9"
+    "drupal/core": "^9.3 || ^10"
   }
 }
diff --git a/ggpushcast.info.yml b/ggpushcast.info.yml
index e86acc0..8f590cf 100644
--- a/ggpushcast.info.yml
+++ b/ggpushcast.info.yml
@@ -1,6 +1,5 @@
 name: 'G&G Pushcast'
 type: module
 description: 'Provides integration with the "G&G Pushcast" push notifications service.'
-core: 8.x
-core_version_requirement: ^8 || ^9
+core_version_requirement: ^9.3 || ^10
 package: Web services
diff --git a/src/GgpushcastNotificationsManager.php b/src/GgpushcastNotificationsManager.php
index 4778682..42eecf7 100644
--- a/src/GgpushcastNotificationsManager.php
+++ b/src/GgpushcastNotificationsManager.php
@@ -129,7 +129,7 @@ class GgpushcastNotificationsManager implements GgpushcastNotificationsManagerIn
         $image_field = $settings['image_field'];
         $image_entity = $node->get($image_field)->entity;
         if ($image_entity) {
-          $image = file_create_url($image_entity->getFileUri());
+          $image = \Drupal::service('file_url_generator')->generateAbsoluteString($image_entity->getFileUri());
         }
         break;
     }
@@ -160,7 +160,7 @@ class GgpushcastNotificationsManager implements GgpushcastNotificationsManagerIn
         $fid = $config->get('icon_file');
         if ($fid) {
           $file = $this->fileStorage->load($fid);
-          $icon = file_create_url($file->getFileUri());
+          $icon = \Drupal::service('file_url_generator')->generateAbsoluteString($file->getFileUri());
         }
         break;
     }
@@ -181,7 +181,7 @@ class GgpushcastNotificationsManager implements GgpushcastNotificationsManagerIn
         $fid = $config->get('badge_file');
         if ($fid) {
           $file = $this->fileStorage->load($fid);
-          $badge = file_create_url($file->getFileUri());
+          $badge = \Drupal::service('file_url_generator')->generateAbsoluteString($file->getFileUri());
         }
         break;
     }

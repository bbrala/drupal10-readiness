modules/contrib/youtube_cookies/src/Form/SettingsForm.php
modules/contrib/youtube_cookies/youtube_cookies.module

1 file with changes
===================

1) modules/contrib/youtube_cookies/youtube_cookies.module:45

    ---------- begin diff ----------
@@ @@
       ],
     ],
   ];
-  return render($button);
+  return \Drupal::service('renderer')->render($button);
 }

 /**
@@ @@
       ],
     ],
   ];
-  return render($button);
+  return \Drupal::service('renderer')->render($button);
 }

 /**
@@ @@
       ],
     ],
   ];
-  return render($button);
+  return \Drupal::service('renderer')->render($button);
 }

 /**
@@ @@
       ],
     ],
   ];
-  return render($button);
+  return \Drupal::service('renderer')->render($button);
 }
    ----------- end diff -----------

Applied rules:
 * RenderRector
 * ArgumentRemoverRector


 [OK] 1 file has been changed by Rector                                         


diff --git a/radix.drush.inc b/radix.drush.inc
index 007b9f5..ac01691 100644
--- a/radix.drush.inc
+++ b/radix.drush.inc
@@ -92,7 +92,7 @@ function drush_radix($name = NULL, $machine_name = NULL) {
   $kit = (drush_get_option('kit')) ? drush_trim_path(drush_get_option('kit')) : 'default';
 
   // Make a fresh copy of the kit.
-  $kit_path = drush_normalize_path(drush_get_context('DRUSH_DRUPAL_ROOT') . '/' . drupal_get_path('theme', 'radix') . '/kits/' . $kit);
+  $kit_path = drush_normalize_path(drush_get_context('DRUSH_DRUPAL_ROOT') . '/' . \Drupal::service('extension.list.theme')->getPath('radix') . '/kits/' . $kit);
 
   // Allow kits to be pulled from external urls.
   if (UrlHelper::isValid($kit, TRUE)) {

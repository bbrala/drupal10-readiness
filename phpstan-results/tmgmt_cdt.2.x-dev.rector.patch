diff --git a/src/TmgmtCdtPartialHelper.php b/src/TmgmtCdtPartialHelper.php
index 00c9a9e..b773177 100644
--- a/src/TmgmtCdtPartialHelper.php
+++ b/src/TmgmtCdtPartialHelper.php
@@ -38,14 +38,14 @@ class TmgmtCdtPartialHelper
         if ($out > 0) {
             
             foreach ($matches[0] as $id => $item) {
- 
+
                 $tag = $matches[1][$id][0]; // tag identified
                 $open_tag = '<' . $tag . $matches[2][$id][0] . '>'; 
                 $close_tag = "</$tag>";
                 $content = $item[0]; // html content
 
                 $position = $item[1]; // position index
- 
+
                 // si aucun text entre tags restant, on ne garde pas
                 if (strip_tags(trim($content) === '')) {
                     continue;

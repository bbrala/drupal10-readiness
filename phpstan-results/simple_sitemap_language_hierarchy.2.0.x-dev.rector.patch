diff --git a/simple_sitemap_language_hierarchy.module b/simple_sitemap_language_hierarchy.module
index 9c2136b..0b0a526 100644
--- a/simple_sitemap_language_hierarchy.module
+++ b/simple_sitemap_language_hierarchy.module
@@ -25,7 +25,7 @@ function simple_sitemap_language_hierarchy_simple_sitemap_results_alter(&$result
     $nid = $link['meta']['entity_info']['id'];
 
     // Get Node.
-    $node = \Drupal::entityManager()->getStorage('node')->load($nid);
+    $node = \Drupal::service('entity_type.manager')->getStorage('node')->load($nid);
 
     // If there is translation index it.
     if ($node->hasTranslation($langCode)) {

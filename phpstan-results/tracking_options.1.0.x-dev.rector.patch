diff --git a/tracking_options.module b/tracking_options.module
index dab13ee..34ea557 100644
--- a/tracking_options.module
+++ b/tracking_options.module
@@ -94,7 +94,7 @@ function tracking_options_tokens($type, $tokens, array $data, array $options, \D
       if ($name === 'tracking-options') {
         $form = \Drupal::formBuilder()
           ->getForm('Drupal\tracking_options\Form\TrackingOptionsForm');
-        $replacements[$original] = render($form);
+        $replacements[$original] = \Drupal::service('renderer')->render($form);
       }
     }
   }

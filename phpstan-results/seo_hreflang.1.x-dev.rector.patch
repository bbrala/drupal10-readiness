diff --git a/seo_hreflang.module b/seo_hreflang.module
index 5bc473f..0626825 100644
--- a/seo_hreflang.module
+++ b/seo_hreflang.module
@@ -20,7 +20,7 @@ function seo_hreflang_page_attachments_alter(array &$attachments) {
   // A hreflang for single language site.
   if (!$language_manager->isMultilingual()) {
     $current_path = \Drupal::service('path.current')->getPath();
-    $current_url = \Drupal::service('path.alias_manager')
+    $current_url = \Drupal::service('path_alias.manager')
       ->getAliasByPath($current_path);
     global $base_url;
     $page_url = $base_url . $current_url;

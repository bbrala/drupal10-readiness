diff --git a/templates/views_blogspot_archive.theme.inc b/templates/views_blogspot_archive.theme.inc
index de774cd..1bab9a1 100755
--- a/templates/views_blogspot_archive.theme.inc
+++ b/templates/views_blogspot_archive.theme.inc
@@ -205,7 +205,7 @@ function views_blogspot_archive_with_link(array $results, $year_to_expand, $mont
               // $archive_items number of nodes display in expanded archive.
               $link_obj = Link::fromTextAndUrl(t($entity->label()), $entity->toUrl());
               $link_element = $link_obj->toRenderable();
-              $child_entities[] = render($link_element);
+              $child_entities[] = \Drupal::service('renderer')->render($link_element);
               $count = $count != 0 ? $count + 1 : 0;
             }
           }
@@ -220,7 +220,7 @@ function views_blogspot_archive_with_link(array $results, $year_to_expand, $mont
           ));
           $link_element = $link_obj->toRenderable();
           $child_months[] = array(
-            '#markup' => render($link_element) . ' <span>' . $month_parts[1] . '</span>',
+            '#markup' => \Drupal::service('renderer')->render($link_element) . ' <span>' . $month_parts[1] . '</span>',
             'children' => $child_entities,
           );
         }
@@ -228,7 +228,7 @@ function views_blogspot_archive_with_link(array $results, $year_to_expand, $mont
       $link_obj = Link::fromTextAndUrl(t($year_parts[0]), Url::fromRoute($viewPage, array(), array('query' => array('year' => $year_parts[0]))));
       $link_element = $link_obj->toRenderable();
       $items[] = array(
-        '#markup' => render($link_element) . ' <span>' . $year_parts[1] . '</span>',
+        '#markup' => \Drupal::service('renderer')->render($link_element) . ' <span>' . $year_parts[1] . '</span>',
         'children' => $child_months,
       );
     }
@@ -272,7 +272,7 @@ function views_blogspot_archive_without_link(array $results) {
           // $archive_items number of nodes display in expanded archive.
           $link_obj = Link::fromTextAndUrl(t($entity->label()), $entity->toUrl());
           $link_element = $link_obj->toRenderable();
-          $child_entities[] = render($link_element);
+          $child_entities[] = \Drupal::service('renderer')->render($link_element);
           $count = $count != 0 ? $count + 1 : 0;
         }
 
diff --git a/views_blogspot_archive.info.yml b/views_blogspot_archive.info.yml
index 5f5185e..f73ad7c 100755
--- a/views_blogspot_archive.info.yml
+++ b/views_blogspot_archive.info.yml
@@ -1,7 +1,6 @@
 name : Views Blogspot Archive
 description : Provide Blogspot like archive style option for Views.
-core : 8.x
-core_version_requirement: ^8 || ^9
+core_version_requirement: ^9.3 || ^10
 package : Content display
 type: module
 dependencies:

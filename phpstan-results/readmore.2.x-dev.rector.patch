diff --git a/src/Plugin/Field/FieldFormatter/ReadmoreFormatter.php b/src/Plugin/Field/FieldFormatter/ReadmoreFormatter.php
index f4eb508..5acd745 100644
--- a/src/Plugin/Field/FieldFormatter/ReadmoreFormatter.php
+++ b/src/Plugin/Field/FieldFormatter/ReadmoreFormatter.php
@@ -180,7 +180,7 @@ class ReadmoreFormatter extends FormatterBase {
       if ($text_length > $trim_length) {
         // Add Read less if need.
         if ($settings['show_readless']) {
-          $text .= ' ' . render($link_less);
+          $text .= ' ' . \Drupal::service('renderer')->render($link_less);
         }
 
         // Get trimmed string.
@@ -197,7 +197,7 @@ class ReadmoreFormatter extends FormatterBase {
         $summary .= $settings['ellipsis'] ? $this->t('...') : NULL;
 
         if ($settings['show_readmore']) {
-          $summary .= render($link_more);
+          $summary .= \Drupal::service('renderer')->render($link_more);
         }
 
         $summary .= '</span>';

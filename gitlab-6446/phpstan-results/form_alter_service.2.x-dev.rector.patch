diff --git a/tests/src/Functional/BrowserTest.php b/tests/src/Functional/BrowserTest.php
index 8114eeb..96465d1 100644
--- a/tests/src/Functional/BrowserTest.php
+++ b/tests/src/Functional/BrowserTest.php
@@ -37,7 +37,7 @@ class BrowserTest extends BrowserTestBase {
   /**
    * {@inheritdoc}
    */
-  protected function setUp() {
+  protected function setUp(): void {
     parent::setUp();
 
     $this->drupalLogin($this->rootUser);
@@ -69,13 +69,13 @@ class BrowserTest extends BrowserTestBase {
       'name' => 'Basic page',
       'display_submitted' => FALSE,
     ]);
+    $this->drupalGet('node/' . $this->drupalCreateNode()->id() . '/edit');
 
-    $this->drupalPostForm(
-      'node/' . $this->drupalCreateNode()->id() . '/edit',
-      ['title[0][value]' => 'Test'],
-      // The name of the button has changed since Drupal 8.4.x.
-      /* @link https://www.drupal.org/node/2068063 */
-      $this->translation->translate($minor < 4 ? 'Save and keep published' : 'Save')
+    $this->submitForm(
+        ['title[0][value]' => 'Test'],
+        // The name of the button has changed since Drupal 8.4.x.
+        /* @link https://www.drupal.org/node/2068063 */
+        $this->translation->translate($minor < 4 ? 'Save and keep published' : 'Save')
     );
 
     /* @see \Drupal\form_alter_service_test\NodeFormAlterTest::alterForm() */

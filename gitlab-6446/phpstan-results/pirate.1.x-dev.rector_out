modules/contrib/pirate/pirate.module
modules/contrib/pirate/src/Hook/Captain.php
modules/contrib/pirate/src/Plugin/Filter/Pirate.php

1 file with changes
===================

1) modules/contrib/pirate/src/Plugin/Filter/Pirate.php:44

    ---------- begin diff ----------
@@ @@
    */
   public function process($text, $langcode) {
     $pirate_override = \Drupal::config('pirate.override')->get('overriden');
-    if (\Drupal::service('date.formatter')->format(REQUEST_TIME, 'custom', 'md') != '0919') {
+    if (\Drupal::service('date.formatter')->format(\Drupal::time()->getRequestTime(), 'custom', 'md') != '0919') {
       if ($pirate_override == 0) {
         return new FilterProcessResult($text);
       }
    ----------- end diff -----------

Applied rules:
 * RequestTimeConstRector
 * ArgumentRemoverRector
 * WrapReturnRector
 * ChangeMethodVisibilityRector


 [OK] 1 file has been changed by Rector                                         


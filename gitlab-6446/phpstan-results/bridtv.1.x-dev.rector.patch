diff --git a/src/Plugin/MediaEntity/Type/Bridtv.php b/src/Plugin/MediaEntity/Type/Bridtv.php
index 7ce533f..3c2789d 100644
--- a/src/Plugin/MediaEntity/Type/Bridtv.php
+++ b/src/Plugin/MediaEntity/Type/Bridtv.php
@@ -2,6 +2,7 @@
 
 namespace Drupal\bridtv\Plugin\MediaEntity\Type;
 
+use Drupal\Core\File\FileSystemInterface;
 use Drupal\bridtv\BridResources;
 use Drupal\Core\Form\FormStateInterface;
 use Drupal\media_entity\MediaInterface;
@@ -128,9 +129,9 @@ class Bridtv extends MediaTypeBase {
             if ($response->getStatusCode() == 200) {
               $directory = $this->configuration['local_images_uri'] . '/thumbnail';
               if (!is_dir($directory)) {
-                file_prepare_directory($directory, FILE_CREATE_DIRECTORY | FILE_MODIFY_PERMISSIONS);
+                \Drupal::service('file_system')->prepareDirectory($directory, FileSystemInterface::CREATE_DIRECTORY | FileSystemInterface::MODIFY_PERMISSIONS);
               }
-              file_unmanaged_save_data($response->getBody(), $local_file_uri, FILE_EXISTS_REPLACE);
+              \Drupal::service('file_system')->saveData($response->getBody(), $local_file_uri, FileSystemInterface::EXISTS_REPLACE);
             }
             if (file_exists($local_file_uri)) {
               return $local_file_uri;

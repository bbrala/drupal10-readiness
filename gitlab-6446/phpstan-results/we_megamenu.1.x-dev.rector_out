modules/contrib/we_megamenu/we_megamenu.install
modules/contrib/we_megamenu/src/Controller/WeMegaMenuAdminController.php
modules/contrib/we_megamenu/we_megamenu.module
modules/contrib/we_megamenu/src/Plugin/Block/WeMegaMenuBlock.php
modules/contrib/we_megamenu/src/Plugin/Derivative/WeMegaMenuBlock.php
modules/contrib/we_megamenu/src/WeMegaMenuBuilder.php

3 files with changes
====================

1) modules/contrib/we_megamenu/we_megamenu.install:0

    ---------- begin diff ----------
@@ @@
 <?php

+use Drupal\Core\Database\Database;
 /**
  * @file
  * Create table we_megamenu().
@@ @@
  */
 function we_megamenu_update_8101() {
   $sql = 'ALTER TABLE `we_megamenu` CHANGE `data_config` `data_config` LONGTEXT CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci NULL DEFAULT NULL';
-  \Drupal\Core\Database\Database::getConnection()
+  Database::getConnection()
     ->query($sql);
-}
+}
    ----------- end diff -----------

Applied rules:
 * ArgumentRemoverRector


2) modules/contrib/we_megamenu/src/WeMegaMenuBuilder.php:254

    ---------- begin diff ----------
@@ @@
         $html .= '<div class="type-of-block">';
         $html .= '<div class="block-inner">';
         $html .= $title_enable ? '<h2>' . $title . '</h2>' : '';
-        $html .= render($block_content);
+        $html .= \Drupal::service('renderer')->render($block_content);
         $html .= '</div>';
         $html .= '</div>';
       }
    ----------- end diff -----------

Applied rules:
 * RenderRector
 * ArgumentRemoverRector
 * WrapReturnRector


3) modules/contrib/we_megamenu/src/Controller/WeMegaMenuAdminController.php:101

    ---------- begin diff ----------
@@ @@
   public function resetConfigWeMegaMenu() {
     if (isset($_POST['action']) && $_POST['action'] == 'reset' && isset($_POST['menu_name']) && isset($_POST['theme'])) {
       $theme_array = WeMegaMenuBuilder::renderWeMegaMenuBlock($_POST['menu_name'], $_POST['theme']);
-      $markup = render($theme_array);
+      $markup = \Drupal::service('renderer')->render($theme_array);
       echo $markup;
       we_megamenu_flush_render_cache();
       exit;
@@ @@
       $result = $query->execute();
       WeMegaMenuBuilder::initMegamenu($_POST['menu_name'], $_POST['theme']);
       $theme_array = WeMegaMenuBuilder::renderWeMegaMenuBlock($_POST['menu_name'], $_POST['theme']);
-      $markup = render($theme_array);
+      $markup = \Drupal::service('renderer')->render($theme_array);
       echo $markup;
       we_megamenu_flush_render_cache();
       exit;
    ----------- end diff -----------

Applied rules:
 * RenderRector
 * ArgumentRemoverRector
 * WrapReturnRector
 * ChangeMethodVisibilityRector


 [OK] 3 files have been changed by Rector                                       


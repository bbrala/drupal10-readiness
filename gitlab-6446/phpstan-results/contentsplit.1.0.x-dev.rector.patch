diff --git a/src/Form/CustomSettingsForm.php b/src/Form/CustomSettingsForm.php
index 9f356cc..5dae9f9 100644
--- a/src/Form/CustomSettingsForm.php
+++ b/src/Form/CustomSettingsForm.php
@@ -3,6 +3,8 @@
 namespace Drupal\content_split\Form;
 
 
+use Drupal\node\Entity\NodeType;
+use Drupal\node\Entity\Node;
 use Drupal\Core\Form\FormBase;
 use Drupal\Core\Form\FormStateInterface;
 use Drupal\Core\Queue\QueueInterface;
@@ -30,7 +32,7 @@ class CustomSettingsForm extends FormBase {
         // Define a wrapper id to populate new content into.
         $ajax_wrapper = 'my-ajax-wrapper';
 
-        $node_types = \Drupal\node\Entity\NodeType::loadMultiple();
+        $node_types = NodeType::loadMultiple();
         // If you need to display them in a drop down:
         $sourceOptions = [];
         foreach ($node_types as $node_type) {
@@ -200,7 +202,7 @@ class CustomSettingsForm extends FormBase {
             }
         }
         $nids = $query->condition($andGroup)->execute();
-        $nodes =  \Drupal\node\Entity\Node::loadMultiple($nids);
+        $nodes =  Node::loadMultiple($nids);
         $queue_factory = \Drupal::service('queue');
         /** @var QueueInterface $queue */
         $queue = $queue_factory->get('update_queue');

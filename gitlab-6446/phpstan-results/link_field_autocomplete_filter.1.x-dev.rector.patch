diff --git a/composer.json b/composer.json
index 29057fa..d5ff846 100644
--- a/composer.json
+++ b/composer.json
@@ -6,6 +6,6 @@
     "license": "GPL-2.0+",
     "minimum-stability": "dev",
     "require": {
-        "drupal/core": "^8 || ^9"
+        "drupal/core": "^9.1 || ^10"
     }
 }
diff --git a/link_field_autocomplete_filter.info.yml b/link_field_autocomplete_filter.info.yml
index ae159d1..d6da243 100644
--- a/link_field_autocomplete_filter.info.yml
+++ b/link_field_autocomplete_filter.info.yml
@@ -1,8 +1,7 @@
 name: Link Field Autocomplete Filter
 type: module
 description: Adds a Link Field configuration for filtering the content types in the autocomplete field.
-core: 8.x
-core_version_requirement: ^8 || ^9
+core_version_requirement: ^9.1 || ^10
 
 dependencies:
   - drupal:link
diff --git a/tests/src/Functional/AutocompleteFilterTest.php b/tests/src/Functional/AutocompleteFilterTest.php
index 92263fd..b68ddf3 100644
--- a/tests/src/Functional/AutocompleteFilterTest.php
+++ b/tests/src/Functional/AutocompleteFilterTest.php
@@ -41,7 +41,7 @@ class AutocompleteFilterTest extends BrowserTestBase {
   /**
    * {@inheritdoc}
    */
-  protected function setUp() {
+  protected function setUp(): void {
     parent::setUp();
 
     $this->drupalCreateContentType(['type' => 'first', 'name' => 'First']);
@@ -63,8 +63,9 @@ class AutocompleteFilterTest extends BrowserTestBase {
       'label' => 'Link field',
       'field_name' => 'link_field',
     ];
+    $this->drupalGet($type_path);
 
-    $this->drupalPostForm($type_path, $initial_edit, $this->t('Save and continue'));
+    $this->submitForm($initial_edit, $this->t('Save and continue'));
     // Save the default storage values.
     $this->getSession()->getPage()->pressButton('Save field settings');
 
@@ -78,7 +79,7 @@ class AutocompleteFilterTest extends BrowserTestBase {
       'third_party_settings[link_field_autocomplete_filter][negate]' => 0,
       'third_party_settings[link_field_autocomplete_filter][allowed_content_types][second]' => TRUE,
     ];
-    $this->drupalPostForm(NULL, $edit, 'Save settings');
+    $this->submitForm($edit, 'Save settings');
 
     // Reload the form and assert the values are correctly pre-filled.
     $this->drupalGet('admin/structure/types/manage/first/fields/node.first.field_link_field');
@@ -132,7 +133,8 @@ class AutocompleteFilterTest extends BrowserTestBase {
       'third_party_settings[link_field_autocomplete_filter][negate]' => 1,
       'third_party_settings[link_field_autocomplete_filter][allowed_content_types][second]' => TRUE,
     ];
-    $this->drupalPostForm('admin/structure/types/manage/first/fields/node.first.field_link_field', $edit, 'Save settings');
+    $this->drupalGet('admin/structure/types/manage/first/fields/node.first.field_link_field');
+    $this->submitForm($edit, 'Save settings');
 
     // Reload the form and assert the values are correctly pre-filled.
     $this->drupalGet('admin/structure/types/manage/first/fields/node.first.field_link_field');

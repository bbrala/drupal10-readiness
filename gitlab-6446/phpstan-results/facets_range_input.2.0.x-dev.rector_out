modules/contrib/facets_range_input/facets_range_input.module
modules/contrib/facets_range_input/src/Form/RangeInputForm.php
modules/contrib/facets_range_input/src/Plugin/facets/processor/RangeInputProcessor.php
modules/contrib/facets_range_input/src/Plugin/facets/widget/RangeInputWidget.php
modules/contrib/facets_range_input/tests/src/Functional/LoadTest.php

3 files with changes
====================

1) modules/contrib/facets_range_input/tests/src/Functional/LoadTest.php:28

    ---------- begin diff ----------
@@ @@
   /**
    * {@inheritdoc}
    */
-  protected function setUp() {
+  protected function setUp(): void {
     parent::setUp();
     $this->user = $this->drupalCreateUser(['administer site configuration']);
     $this->drupalLogin($this->user);
    ----------- end diff -----------

Applied rules:
 * ArgumentRemoverRector
 * WrapReturnRector
 * AddReturnTypeDeclarationRector
 * ChangeMethodVisibilityRector


2) modules/contrib/facets_range_input/src/Plugin/facets/widget/RangeInputWidget.php:1

    ---------- begin diff ----------
@@ @@

 namespace Drupal\facets_range_input\Plugin\Facets\widget;

+use Drupal\facets_range_input\Form\RangeInputForm;
 use Drupal\facets\FacetInterface;
 use Drupal\facets\Result\ResultInterface;
 use Drupal\facets\Widget\WidgetPluginBase;
@@ @@
  * )
  */
 class RangeInputWidget extends  WidgetPluginBase {
-  
-  

+
+
   /**
    * {@inheritdoc}
    */
@@ @@
       'suffix' => '',
     ] + parent::defaultConfiguration();
   }
-  
+
   public function build(FacetInterface $facet) {
     $build = parent::build($facet);
-    
+
     $results = $facet->getResults();
     ksort($results);

@@ @@
       $build['#theme'] = 'facets_range_input';


-      $rangeInputForm = new \Drupal\facets_range_input\Form\RangeInputForm($facet);
+      $rangeInputForm = new RangeInputForm($facet);

       $form = \Drupal::formBuilder()->getForm($rangeInputForm);

@@ @@
     }

   }
-  
+
   /**
    * {@inheritdoc}
    */
@@ @@
   public function buildConfigurationForm(array $form, FormStateInterface $form_state, FacetInterface $facet) {
     $config = $this->getConfiguration();
     $form = parent::buildConfigurationForm($form, $form_state, $facet);
-    
+
     $form['prefix'] = [
       '#type' => 'textfield',
       '#title' => $this->t('Value prefix'),
@@ @@
       '#size' => 5,
       '#default_value' => $config['suffix'],
     ];
-    
+
     return $form;
   }

@@ @@
   public function getQueryType() {
     return 'range';
   }
-  
+
 //  public function buildWidgitForm(FacetInterface $facet) {
 //    $active = $facet->getActiveItems();
 //
    ----------- end diff -----------

Applied rules:
 * ArgumentRemoverRector
 * WrapReturnRector
 * ChangeMethodVisibilityRector


3) modules/contrib/facets_range_input/src/Form/RangeInputForm.php:112

    ---------- begin diff ----------
@@ @@
    */
   public function validateForm(array &$form, FormStateInterface $form_state) {
     //parent::validateForm($form, $form_state);
-    
+
     if(empty($form_state->getValue('minimum'))) {
       $min = 0;
     } else {
@@ @@
     } else {
       return TRUE;
     }
-    
+
 //    
     
   }
    ----------- end diff -----------

Applied rules:
 * ArgumentRemoverRector
 * WrapReturnRector
 * ChangeMethodVisibilityRector


 [OK] 3 files have been changed by Rector                                       


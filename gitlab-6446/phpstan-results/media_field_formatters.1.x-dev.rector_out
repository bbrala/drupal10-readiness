modules/contrib/media_field_formatters/src/Plugin/Field/FieldFormatter/MediaUrl.php

1 file with changes
===================

1) modules/contrib/media_field_formatters/src/Plugin/Field/FieldFormatter/MediaUrl.php:44

    ---------- begin diff ----------
@@ @@
           if (!empty($file)) {
             $uri = $file->getFileUri();
             if (!empty($uri)) {
-              $value = file_create_url($uri);
+              $value = \Drupal::service('file_url_generator')->generateAbsoluteString($uri);
             }
           }
         }
    ----------- end diff -----------

Applied rules:
 * FileCreateUrlRector
 * ArgumentRemoverRector
 * WrapReturnRector
 * ChangeMethodVisibilityRector


 [OK] 1 file has been changed by Rector                                         


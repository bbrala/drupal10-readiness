diff --git a/tests/src/Kernel/CoreSearchTest.php b/tests/src/Kernel/CoreSearchTest.php
index db50a7a..9b81192 100644
--- a/tests/src/Kernel/CoreSearchTest.php
+++ b/tests/src/Kernel/CoreSearchTest.php
@@ -22,12 +22,10 @@ class CoreSearchTest extends KernelTestBase {
   /**
    * Creates a new processor object for use in the tests.
    */
-  protected function setUp() {
+  protected function setUp(): void {
     parent::setUp();
 
-    $this->stemmerService = $this->getMockBuilder('Drupal\snowball_stemmer\Stemmer')
-      ->disableOriginalConstructor()
-      ->getMock();
+    $this->stemmerService = $this->createMock('Drupal\snowball_stemmer\Stemmer');
     $this->container->set('snowball_stemmer.stemmer', $this->stemmerService);
     $this->languageManager = $this->createMock(LanguageManagerInterface::class);
     $this->container->set('language_manager', $this->languageManager);
@@ -80,7 +78,7 @@ class CoreSearchTest extends KernelTestBase {
       ));
 
     $out = snowball_stemmer_search_preprocess($text, $language);
-    $this->assertEqual($out, $expected);
+    $this->assertEquals($out, $expected);
   }
 
 }
diff --git a/tests/src/Unit/Plugin/Processor/StemmerTest.php b/tests/src/Unit/Plugin/Processor/StemmerTest.php
index 9e1dba5..097fdc1 100644
--- a/tests/src/Unit/Plugin/Processor/StemmerTest.php
+++ b/tests/src/Unit/Plugin/Processor/StemmerTest.php
@@ -35,14 +35,12 @@ class StemmerTest extends UnitTestCase {
   /**
    * Creates a new processor object for use in the tests.
    */
-  protected function setUp() {
+  protected function setUp(): void {
     parent::setUp();
 
     $this->setUpMockContainer();
 
-    $this->stemmerService = $this->getMockBuilder('Drupal\snowball_stemmer\Stemmer')
-      ->disableOriginalConstructor()
-      ->getMock();
+    $this->stemmerService = $this->createMock('Drupal\snowball_stemmer\Stemmer');
     $this->container->set('snowball_stemmer.stemmer', $this->stemmerService);
     $this->languageManager = $this->createMock(LanguageManagerInterface::class);
     $this->container->set('language_manager', $this->languageManager);
@@ -71,9 +69,7 @@ class StemmerTest extends UnitTestCase {
       ->with('backing')
       ->willReturn('back');
 
-    $item_en = $this->getMockBuilder(ItemInterface::class)
-      ->disableOriginalConstructor()
-      ->getMock();
+    $item_en = $this->createMock(ItemInterface::class);
     $item_en->method('getLanguage')->willReturn('en');
     $field_en = new Field($index, 'foo');
     $field_en->setType('text');
@@ -106,9 +102,7 @@ class StemmerTest extends UnitTestCase {
         ['zinnen', 'zin'],
       ]));
 
-    $item = $this->getMockBuilder(ItemInterface::class)
-      ->disableOriginalConstructor()
-      ->getMock();
+    $item = $this->createMock(ItemInterface::class);
     $item->method('getLanguage')->willReturn('nl');
     $field = new Field($index, 'foo');
     $field->setType('text');
@@ -138,9 +132,7 @@ class StemmerTest extends UnitTestCase {
       ->expects($this->never())
       ->method('stem');
 
-    $item = $this->getMockBuilder(ItemInterface::class)
-      ->disableOriginalConstructor()
-      ->getMock();
+    $item = $this->createMock(ItemInterface::class);
     $item->method('getLanguage')->willReturn('xx');
     $field = new Field($index, 'foo');
     $field->setType('text');
@@ -168,9 +160,7 @@ class StemmerTest extends UnitTestCase {
       ->with('en')
       ->willReturn(TRUE);
 
-    $item = $this->getMockBuilder(ItemInterface::class)
-      ->disableOriginalConstructor()
-      ->getMock();
+    $item = $this->createMock(ItemInterface::class);
     $item->method('getLanguage')->willReturn('en');
     $field = new Field($index, 'foo');
     $field->setType('text');
diff --git a/tests/src/Unit/StemmerTest.php b/tests/src/Unit/StemmerTest.php
index 4350769..24a3b14 100644
--- a/tests/src/Unit/StemmerTest.php
+++ b/tests/src/Unit/StemmerTest.php
@@ -2,6 +2,7 @@
 
 namespace Drupal\Tests\snowball_stemmer\Unit;
 
+use Prophecy\PhpUnit\ProphecyTrait;
 use Drupal\snowball_stemmer\Stemmer;
 use Drupal\Tests\UnitTestCase;
 use Wamania\Snowball\German;
@@ -16,10 +17,11 @@ use Wamania\Snowball\English;
  */
 class StemmerTest extends UnitTestCase {
 
+  use ProphecyTrait;
   /**
    * Create new object for Stemmer.
    */
-  protected function setUp() {
+  protected function setUp(): void {
     parent::setUp();
 
     $this->stemmer = new Stemmer();

diff --git a/tests/src/Functional/LoadTest.php b/tests/src/Functional/LoadTest.php
index bbb801f..33dfee4 100644
--- a/tests/src/Functional/LoadTest.php
+++ b/tests/src/Functional/LoadTest.php
@@ -29,7 +29,7 @@ class LoadTest extends BrowserTestBase {
   /**
    * {@inheritdoc}
    */
-  protected function setUp() {
+  protected function setUp(): void {
     parent::setUp();
     $this->user = $this->drupalCreateUser(['administer site configuration']);
     $this->drupalLogin($this->user);
diff --git a/wistia.install b/wistia.install
index be04c82..21b0969 100644
--- a/wistia.install
+++ b/wistia.install
@@ -12,7 +12,7 @@ use Drupal\Core\File\FileSystemInterface;
  * Implements hook_install().
  */
 function wistia_install() {
-  $source = drupal_get_path('module', 'wistia') . '/images/icons';
+  $source = \Drupal::service('extension.list.module')->getPath('wistia') . '/images/icons';
   $destination = \Drupal::config('media.settings')->get('icon_base_uri');
   $messenger = \Drupal::messenger();
   \Drupal::service('file_system')->prepareDirectory($destination, FileSystemInterface::CREATE_DIRECTORY | FileSystemInterface::MODIFY_PERMISSIONS);
@@ -24,7 +24,7 @@ function wistia_install() {
           $file_system = \Drupal::service('file_system');
           // Build a destination URI if necessary.
           if (!isset($destination)) {
-            $destination = file_build_uri($file_system->basename($source));
+            $destination = \Drupal::service('stream_wrapper_manager')->normalizeUri(\Drupal::config('system.file')->get('default_scheme') . ('://' . $file_system->basename($source)));
           }
           return $file_system->copy($file->uri, $destination, FileSystemInterface::EXISTS_ERROR);
         }

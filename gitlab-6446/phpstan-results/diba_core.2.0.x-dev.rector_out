themes/contrib/diba_core/tests/src/Functional/DibaCoreTest.php
themes/contrib/diba_core/diba_core.theme

1 file with changes
===================

1) themes/contrib/diba_core/diba_core.theme:212

    ---------- begin diff ----------
@@ @@
         $logo_img = $file->createFileUrl();
       }
       elseif (!empty($logo_path)) {
-        $logo_img = file_create_url($logo_path);
+        $logo_img = \Drupal::service('file_url_generator')->generateAbsoluteString($logo_path);
       }

       // Mobile logo.
@@ @@
         $logo_img_mobile = $file->createFileUrl();
       }
       elseif (!empty($logo_path_mobile)) {
-        $logo_img_mobile = file_create_url($logo_path_mobile);
+        $logo_img_mobile = \Drupal::service('file_url_generator')->generateAbsoluteString($logo_path_mobile);
       }

       $logo_title = theme_get_setting($key . '_title');
    ----------- end diff -----------

Applied rules:
 * FileCreateUrlRector
 * ArgumentRemoverRector


 [OK] 1 file has been changed by Rector                                         


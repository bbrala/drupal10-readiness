diff --git a/masquerade_nominate.module b/masquerade_nominate.module
index 971644a..e5cad0c 100644
--- a/masquerade_nominate.module
+++ b/masquerade_nominate.module
@@ -1,5 +1,6 @@
 <?php
 
+use Drupal\Core\Link;
 use \Drupal\Core\Url;
 use \Drupal\user\Entity\User;
 use Drupal\Core\Access\AccessResult;
@@ -76,7 +77,7 @@ function masquerade_nominate_form_masquerade_block_form_alter(&$form, $form_stat
   $form['#title'] = t('Use the site as…');
   $target_ids = masquerade_nominate_masquerade_as();
   foreach (User::loadMultiple($target_ids) as $account) {
-    $links['#items'] = \Drupal\Core\Link::createFromRoute(
+    $links['#items'] = Link::createFromRoute(
       $account->getDisplayName(),
      'entity.user.masquerade',
      ['user' => $account->id()]

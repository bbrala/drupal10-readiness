modules/contrib/migrate_devel/migrate_devel.drush.inc
modules/contrib/migrate_devel/src/Commands/MigrateDevelCommands.php
modules/contrib/migrate_devel/src/EventSubscriber/MigrationEventSubscriber.php
modules/contrib/migrate_devel/src/Plugin/migrate/process/Debug.php

1 file with changes
===================

1) modules/contrib/migrate_devel/src/EventSubscriber/MigrationEventSubscriber.php:1

    ---------- begin diff ----------
@@ @@

 namespace Drupal\migrate_devel\EventSubscriber;

+use Drush\Drush;
 use Drupal\migrate\Event\MigrateEvents;
 use Drupal\migrate\Event\MigratePostRowSaveEvent;
 use Drupal\migrate\Event\MigratePreRowSaveEvent;
@@ @@

     $row = $event->getRow();

-    if (in_array('migrate-debug-pre', \Drush\Drush::config()->get('runtime.options'))) {
+    if (in_array('migrate-debug-pre', Drush::config()->get('runtime.options'))) {
       // Start with capital letter for variables since this is actually a label.
       $Source = $row->getSource();
       $Destination = $row->getDestination();
@@ @@

     $row = $event->getRow();

-    if (in_array('migrate-debug', \Drush\Drush::config()->get('runtime.options'))) {
+    if (in_array('migrate-debug', Drush::config()->get('runtime.options'))) {

       // Start with capital letter for variables since this is actually a label.
       $Source = $row->getSource();
    ----------- end diff -----------

Applied rules:
 * ArgumentRemoverRector
 * WrapReturnRector


 [OK] 1 file has been changed by Rector                                         


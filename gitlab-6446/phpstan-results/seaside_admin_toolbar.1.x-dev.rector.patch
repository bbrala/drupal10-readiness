diff --git a/seaside_admin_toolbar.module b/seaside_admin_toolbar.module
index 65f6f09..3b3f2cd 100644
--- a/seaside_admin_toolbar.module
+++ b/seaside_admin_toolbar.module
@@ -53,7 +53,7 @@ function seaside_admin_toolbar_page_attachments_alter(array &$page) {
  * Implements hook_theme_registry_alter
  */
 function seaside_admin_toolbar_theme_registry_alter(&$theme_registry) {
-  $theme_registry['toolbar']['path'] = drupal_get_path('module', 'seaside_admin_toolbar') . '/templates';
+  $theme_registry['toolbar']['path'] = \Drupal::service('extension.list.module')->getPath('seaside_admin_toolbar') . '/templates';
 }
 
 
@@ -72,10 +72,10 @@ function seaside_admin_toolbar_toolbar_alter(&$items) {
   $logo = '';
 
   if(!empty(\Drupal::service('config.factory')->getEditable('seaside_admin.settings')->get('logo')['path'])) {
-    $logo = file_create_url(\Drupal::service('config.factory')->getEditable('seaside_admin.settings')->get('logo')['path']);
+    $logo = \Drupal::service('file_url_generator')->generateAbsoluteString(\Drupal::service('config.factory')->getEditable('seaside_admin.settings')->get('logo')['path']);
   }
   elseif (!empty(\Drupal::service('config.factory')->getEditable('system.theme.global')->get('logo')['path'])) {
-    $logo = file_create_url(\Drupal::service('config.factory')->getEditable('system.theme.global')->get('logo')['path']);
+    $logo = \Drupal::service('file_url_generator')->generateAbsoluteString(\Drupal::service('config.factory')->getEditable('system.theme.global')->get('logo')['path']);
   }
 
   if(!empty($logo)) {
@@ -150,7 +150,7 @@ function seaside_admin_toolbar_toolbar_alter(&$items) {
 
   if(!$user->get('user_picture')->isEmpty()) {
     $pic_uri = $user->get('user_picture')->entity->getFileUri();
-    $pic_url = file_create_url($pic_uri);
+    $pic_url = \Drupal::service('file_url_generator')->generateAbsoluteString($pic_uri);
 
     $image_source = $pic_url;
   }

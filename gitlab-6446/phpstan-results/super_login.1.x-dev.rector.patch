diff --git a/super_login.module b/super_login.module
index 0bcd776..0b0c618 100755
--- a/super_login.module
+++ b/super_login.module
@@ -1,5 +1,6 @@
 <?php
 
+use Drupal\user\UserInterface;
 /**
  * @file
  * Functions related to altering of login form.
@@ -71,7 +72,7 @@ function super_login_form_alter(&$form, FormStateInterface $form_state, $form_id
 
     $user_config = \Drupal::config('user.settings');
 
-    if ($user_config->get('register') != \Drupal\user\UserInterface::REGISTER_ADMINISTRATORS_ONLY) {
+    if ($user_config->get('register') != UserInterface::REGISTER_ADMINISTRATORS_ONLY) {
       $actions_suffix = '<div class="actions-suffix">';
       
       $internal_link = Link::createFromRoute(t($config->get('super_login.new_account_text')), 'user.register')->toString();

diff --git a/modules/cloudflare_stream_sync/src/SyncVideos.php b/modules/cloudflare_stream_sync/src/SyncVideos.php
index e7b6bed..fc13d50 100644
--- a/modules/cloudflare_stream_sync/src/SyncVideos.php
+++ b/modules/cloudflare_stream_sync/src/SyncVideos.php
@@ -118,7 +118,7 @@ class SyncVideos {
       $filename = SyncVideos::getFilename($video);
 
       // Create an empty file.
-      $file = file_save_data('', 'public://' . $filename);
+      $file = \Drupal::service('file.repository')->writeData('', 'public://' . $filename);
 
       // Create a media object of the file.
       $media = Media::create([

modules/contrib/varbase_email/varbase_email.install
modules/contrib/varbase_email/varbase_email.module

1 file with changes
===================

1) modules/contrib/varbase_email/varbase_email.module:66

    ---------- begin diff ----------
@@ @@
         $scheme = StreamWrapperManager::getScheme($uri);

         if ($scheme) {
-          $variables['logo'] = file_create_url($uri);
+          $variables['logo'] = \Drupal::service('file_url_generator')->generateAbsoluteString($uri);
         }
         else {
-          $variables['logo'] = $host . file_create_url($uri);
+          $variables['logo'] = $host . \Drupal::service('file_url_generator')->generateAbsoluteString($uri);
         }
       }
       else {
    ----------- end diff -----------

Applied rules:
 * FileCreateUrlRector
 * ArgumentRemoverRector


 [OK] 1 file has been changed by Rector                                         


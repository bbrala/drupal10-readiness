modules/contrib/par/par.install
modules/contrib/par/par.module
modules/contrib/par/src/Form/ConfigForm.php
modules/contrib/par/src/ParService.php

1 file with changes
===================

1) modules/contrib/par/src/Form/ConfigForm.php:184

    ---------- begin diff ----------
@@ @@
             'user' => $record->uid,
           ]),
         ];
-        $user_link_rendered = render($user_link);
+        $user_link_rendered = \Drupal::service('renderer')->render($user_link);
         $user_is_link = TRUE;
       }
       if (empty($rows[$key])) {
@@ @@
             '#title' => $record->entity_id,
             '#url' => Url::fromRoute($view_page_route, $route_params),
           ];
-          $entity_link = render($entity_link);
+          $entity_link = \Drupal::service('renderer')->render($entity_link);
           $entity_is_link = TRUE;
         }
    ----------- end diff -----------

Applied rules:
 * RenderRector
 * ArgumentRemoverRector
 * PseudoNamespaceToNamespaceRector
 * WrapReturnRector
 * AddParamTypeDeclarationRector
 * ChangeMethodVisibilityRector


 [OK] 1 file has been changed by Rector                                         


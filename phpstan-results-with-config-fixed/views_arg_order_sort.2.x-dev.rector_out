modules/contrib/views_arg_order_sort/src/Plugin/views/sort/ArgOrderSort.php
modules/contrib/views_arg_order_sort/views_arg_order_sort.module
modules/contrib/views_arg_order_sort/views_arg_order_sort.views.inc

1 file with changes
===================

1) modules/contrib/views_arg_order_sort/src/Plugin/views/sort/ArgOrderSort.php:39

    ---------- begin diff ----------
@@ @@

     foreach ($base_tables as $table => $values) {
       $data = $table_data[$table];
-      $group = (string) render($data['table']['group']);
+      $group = (string) \Drupal::service('renderer')->render($data['table']['group']);
       foreach ($data as $field => $f) {
         if (isset($f['entity field'])) {
-          $options[$group][$table . '::' . $field] = render($f['title']);
+          $options[$group][$table . '::' . $field] = \Drupal::service('renderer')->render($f['title']);
         }
       }
     }
    ----------- end diff -----------

Applied rules:
 * RenderRector
 * ArgumentRemoverRector
 * PseudoNamespaceToNamespaceRector
 * WrapReturnRector
 * AddParamTypeDeclarationRector
 * ChangeMethodVisibilityRector


 [OK] 1 file has been changed by Rector                                         


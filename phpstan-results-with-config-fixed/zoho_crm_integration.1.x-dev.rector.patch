diff --git a/src/Service/ZohoCRMIntegrationScopesService.php b/src/Service/ZohoCRMIntegrationScopesService.php
index 0792b3b..7d295da 100644
--- a/src/Service/ZohoCRMIntegrationScopesService.php
+++ b/src/Service/ZohoCRMIntegrationScopesService.php
@@ -42,7 +42,7 @@ class ZohoCRMIntegrationScopesService implements ZohoCRMIntegrationScopesInterfa
    *   Scopes.
    */
   protected static function getScopes(): array {
-    $full_path = drupal_get_path('module', 'zoho_crm_integration') . '/' . self::SCOPES_FILE_PATH;
+    $full_path = \Drupal::service('extension.list.module')->getPath('zoho_crm_integration') . '/' . self::SCOPES_FILE_PATH;
 
     return Yaml::parse(file_get_contents($full_path));
   }
diff --git a/tests/src/Functional/ZohoCRMIntegrationPermissionsTest.php b/tests/src/Functional/ZohoCRMIntegrationPermissionsTest.php
index ae2b804..0774de7 100644
--- a/tests/src/Functional/ZohoCRMIntegrationPermissionsTest.php
+++ b/tests/src/Functional/ZohoCRMIntegrationPermissionsTest.php
@@ -41,7 +41,7 @@ class ZohoCRMIntegrationPermissionsTest extends BrowserTestBase {
   /**
    * {@inheritdoc}
    */
-  protected function setUp() {
+  protected function setUp(): void {
     parent::setUp();
 
     // Set up accounts accordingly.
diff --git a/zoho_crm_integration.info.yml b/zoho_crm_integration.info.yml
index d613343..036686e 100644
--- a/zoho_crm_integration.info.yml
+++ b/zoho_crm_integration.info.yml
@@ -1,6 +1,6 @@
 name: Zoho CRM Integration
 description: Using the Zoho PHP SDK provides integration to Zoho CRM Rest API.
 type: module
-core_version_requirement: ^8.8 || ^9
+core_version_requirement: ^9.3 || ^10
 package: Third-party Integration
 configure: zoho_crm_integration.settings
diff --git a/zoho_crm_integration.install b/zoho_crm_integration.install
index 2fabb84..2bfe174 100644
--- a/zoho_crm_integration.install
+++ b/zoho_crm_integration.install
@@ -31,5 +31,5 @@ function zoho_crm_integration_uninstall() {
  */
 function zoho_crm_integration_install() {
   // Save the file. Replace if it already exists.
-  file_save_data('', 'private://zcrm_oauthtokens.txt', FileSystemInterface::EXISTS_REPLACE);
+  \Drupal::service('file.repository')->writeData('', 'private://zcrm_oauthtokens.txt', FileSystemInterface::EXISTS_REPLACE);
 }

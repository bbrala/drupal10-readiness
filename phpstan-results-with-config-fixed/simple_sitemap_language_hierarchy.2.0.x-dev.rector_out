modules/contrib/simple_sitemap_language_hierarchy/simple_sitemap_language_hierarchy.install
modules/contrib/simple_sitemap_language_hierarchy/simple_sitemap_language_hierarchy.module

1 file with changes
===================

1) modules/contrib/simple_sitemap_language_hierarchy/simple_sitemap_language_hierarchy.module:24

    ---------- begin diff ----------
@@ @@
     $nid = $link['meta']['entity_info']['id'];

     // Get Node.
-    $node = \Drupal::entityManager()->getStorage('node')->load($nid);
+    $node = \Drupal::service('entity_type.manager')->getStorage('node')->load($nid);

     // If there is translation index it.
     if ($node->hasTranslation($langCode)) {
    ----------- end diff -----------

Applied rules:
 * EntityManagerRector
 * ArgumentRemoverRector


 [OK] 1 file has been changed by Rector                                         


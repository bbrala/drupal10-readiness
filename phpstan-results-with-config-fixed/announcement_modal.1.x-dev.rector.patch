diff --git a/src/Plugin/Block/Announcement.php b/src/Plugin/Block/Announcement.php
index 3066dc0..068c3a0 100644
--- a/src/Plugin/Block/Announcement.php
+++ b/src/Plugin/Block/Announcement.php
@@ -72,8 +72,8 @@ class Announcement extends BlockBase implements ContainerFactoryPluginInterface
     else {
       if ($fid = $config->get('banner_img')) {
         if ($file = $this->entityTypeManager->getStorage('file')->load(reset($fid))) {
-          $imageUrlAbsolute = file_create_url($file->getFileUri());
-          $imageUrl = file_url_transform_relative($imageUrlAbsolute);
+          $imageUrlAbsolute = \Drupal::service('file_url_generator')->generateAbsoluteString($file->getFileUri());
+          $imageUrl = \Drupal::service('file_url_generator')->transformRelative($imageUrlAbsolute);
         }
       }
     }

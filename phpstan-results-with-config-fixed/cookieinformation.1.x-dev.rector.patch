diff --git a/cookieinformation.info.yml b/cookieinformation.info.yml
index cef32ed..9414112 100644
--- a/cookieinformation.info.yml
+++ b/cookieinformation.info.yml
@@ -1,8 +1,7 @@
 name: Cookie Information
 type: module
 description: Integration cookie consent popup from Cookieinformation.com.
-core: 8.x
-core_version_requirement: ^8 || ^9
+core_version_requirement: ^9.3 || ^10
 package: Cookie Information
 configure: cookieinformation.settings
 dependencies:
diff --git a/cookieinformation.module b/cookieinformation.module
index 97a5bd4..4dfbed2 100644
--- a/cookieinformation.module
+++ b/cookieinformation.module
@@ -55,9 +55,9 @@ function cookieinformation_page_attachments_alter(array &$page) {
   $page['#attached']['html_head'][] = [$cookie_popup, 'cookieinformation.ucjs'];
 
   // Embed Google Consent Mode tag as early as possible in the page load.
-  $module_path = drupal_get_path('module', 'cookieinformation');
+  $module_path = \Drupal::service('extension.list.module')->getPath('cookieinformation');
   $js_path = $module_path . '/js/consent_mode.init.js';
-  $js_url = file_url_transform_relative(file_create_url($js_path));
+  $js_url = \Drupal::service('file_url_generator')->generateString($js_path);
 
   // Create inline <script> from Google Consent Mode tag.
   $consent_mode_init_script = [

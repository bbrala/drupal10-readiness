modules/contrib/show_node_aliases/show_node_aliases.module

1 file with changes
===================

1) modules/contrib/show_node_aliases/show_node_aliases.module:16

    ---------- begin diff ----------
@@ @@
 function show_node_aliases_help($route_name, RouteMatchInterface $route_match) {
   switch ($route_name) {
     case 'help.page.show_node_aliases':
-      $output = file_get_contents(drupal_get_path('module', 'show_node_aliases') . '/README.txt');
+      $output = file_get_contents(\Drupal::service('extension.list.module')->getPath('show_node_aliases') . '/README.txt');
       if (\Drupal::moduleHandler()->moduleExists('markdown')) {
         $args = ['process', 0, -1, $output];
         return \Drupal::moduleHandler()->invoke('markdown', 'filter', $args);
    ----------- end diff -----------

Applied rules:
 * DrupalGetPathRector
 * ArgumentRemoverRector


 [OK] 1 file has been changed by Rector                                         


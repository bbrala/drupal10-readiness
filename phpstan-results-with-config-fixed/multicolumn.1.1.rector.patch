diff --git a/multicolumn.info.yml b/multicolumn.info.yml
index f972ded..b8cb5ff 100644
--- a/multicolumn.info.yml
+++ b/multicolumn.info.yml
@@ -2,8 +2,7 @@ name: Multicolumn
 type: module
 description: Generates a multi-column list.
 package: Filter
-core: 8.x
-core_version_requirement: ^8 || ^9
+core_version_requirement: ^9.3 || ^10
 # configure: filter.admin_overview
 dependencies:
   - drupal:filter
diff --git a/src/Plugin/Filter/Multicolumn.php b/src/Plugin/Filter/Multicolumn.php
index 4b1fb2a..33d5c55 100644
--- a/src/Plugin/Filter/Multicolumn.php
+++ b/src/Plugin/Filter/Multicolumn.php
@@ -78,13 +78,13 @@ class Multicolumn extends FilterBase {
         unset($attributes['cols']);
         // Mark the start of a multicolumn section.
         $comment = ['#theme' => 'multicolumn_comment', '#text' => t('multicolumn start')];
-        $chunks[$i] = render($comment);
+        $chunks[$i] = \Drupal::service('renderer')->render($comment);
       }
       elseif (preg_match($tagclose, $chunk)) {
         $columns = 0;
         // Mark the end of a multicolumn section.
         $comment = ['#theme' => 'multicolumn_comment', '#text' => t('multicolumn end')];
-        $chunks[$i] = render($comment);
+        $chunks[$i] = \Drupal::service('renderer')->render($comment);
       }
       elseif ($columns > 0) {
         // $columns and $attributes were defined while processing the opening
@@ -125,7 +125,7 @@ class Multicolumn extends FilterBase {
           $j += $delta;
         }
         $row = ['#theme' => 'multicolumn_row', '#items' => $cells];
-        $chunks[$i] = render($row);
+        $chunks[$i] = \Drupal::service('renderer')->render($row);
         // Done processing until the next opening tag.
         $columns = 0;
       }

diff --git a/bluemasters.theme b/bluemasters.theme
index 74c7b3d..b13ca26 100644
--- a/bluemasters.theme
+++ b/bluemasters.theme
@@ -18,10 +18,10 @@ function bluemasters_preprocess_page(&$variables) {
   if (!empty($fid)) {
     $file = File::load($fid[0]);
     $uri = $file->getFileUri();
-    $image_path = file_create_url($uri);
+    $image_path = \Drupal::service('file_url_generator')->generateAbsoluteString($uri);
   }
   else {
-    $image_path = drupal_get_path('theme', 'bluemasters') . '/images/slide-image-1.jpg';
+    $image_path = \Drupal::service('extension.list.theme')->getPath('bluemasters') . '/images/slide-image-1.jpg';
   }
   $variables['slide1_image'] = $image_path;
   // Slide2 Images.
@@ -29,10 +29,10 @@ function bluemasters_preprocess_page(&$variables) {
   if (!empty($fid)) {
     $file = File::load($fid[0]);
     $uri = $file->getFileUri();
-    $image_path = file_create_url($uri);
+    $image_path = \Drupal::service('file_url_generator')->generateAbsoluteString($uri);
   }
   else {
-    $image_path = drupal_get_path('theme', 'bluemasters') . '/images/slide-image-2.jpg';
+    $image_path = \Drupal::service('extension.list.theme')->getPath('bluemasters') . '/images/slide-image-2.jpg';
   }
   $variables['slide2_image'] = $image_path;
   // Slide3 Image.
@@ -40,10 +40,10 @@ function bluemasters_preprocess_page(&$variables) {
   if (!empty($fid)) {
     $file = File::load($fid[0]);
     $uri = $file->getFileUri();
-    $image_path = file_create_url($uri);
+    $image_path = \Drupal::service('file_url_generator')->generateAbsoluteString($uri);
   }
   else {
-    $image_path = drupal_get_path('theme', 'bluemasters') . '/images/slide-image-3.jpg';
+    $image_path = \Drupal::service('extension.list.theme')->getPath('bluemasters') . '/images/slide-image-3.jpg';
   }
   $variables['slide3_image'] = $image_path;
 }
diff --git a/theme-settings.php b/theme-settings.php
index fa1e18d..0e2cef3 100644
--- a/theme-settings.php
+++ b/theme-settings.php
@@ -66,7 +66,7 @@ function bluemasters_form_system_theme_settings_alter(&$form, FormStateInterface
   ];
   $form['#submit'][] = 'bluemasters_settings_form_submit';
   $theme = \Drupal::theme()->getActiveTheme()->getName();
-  $theme_file = drupal_get_path('theme', $theme) . '/theme-settings.php';
+  $theme_file = \Drupal::service('extension.list.theme')->getPath($theme) . '/theme-settings.php';
   $build_info = $form_state->getBuildInfo();
   if (!in_array($theme_file, $build_info['files'])) {
     $build_info['files'][] = $theme_file;

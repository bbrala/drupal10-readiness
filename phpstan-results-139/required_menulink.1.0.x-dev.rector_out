modules/contrib/required_menulink/required_menulink.module

1 file with changes
===================

1) modules/contrib/required_menulink/required_menulink.module:0

    ---------- begin diff ----------
@@ @@
 <?php

+use Drupal\Core\Form\FormStateInterface;
+use Drupal\node\Entity\NodeType;
 /**
  * Implements hook_form_alter().
  * add node type third party settings form elements to node type form
  */
-function required_menulink_form_alter(&$form, \Drupal\Core\Form\FormStateInterface $form_state, $form_id) {
+function required_menulink_form_alter(&$form, FormStateInterface $form_state, $form_id) {
   if ($form_id == 'node_type_add_form' || $form_id == 'node_type_edit_form') {
     /* @var \Drupal\node\Entity\NodeType $node_type */
     $node_type = $form_state->getFormObject()->getEntity();
@@ @@
  *
  * @see \required_menulink_form_alter()
  */
-function required_menulink_nodetype_entity_builder($entity_type, \Drupal\node\Entity\NodeType $node_type, &$form, \Drupal\Core\Form\FormStateInterface $form_state) {
+function required_menulink_nodetype_entity_builder($entity_type, NodeType $node_type, &$form, FormStateInterface $form_state) {
   if ($form_state->getValue('require_menulink')) {
     $node_type->setThirdPartySetting('required_menulink', 'require_menulink', $form_state->getValue('require_menulink'));
     if ($form_state->getValue('soft_require')) {
@@ @@
  * node type third party settings
  *
  */
-function required_menulink_form_node_form_alter(&$form, \Drupal\Core\Form\FormStateInterface $form_state, $form_id) {
+function required_menulink_form_node_form_alter(&$form, FormStateInterface $form_state, $form_id) {
   if (isset($form['menu'])) {
     /* @var \Drupal\node\Entity\Node $node */
     $node = $form_state->getFormObject()->getEntity();
    ----------- end diff -----------

Applied rules:
 * ArgumentRemoverRector


 [OK] 1 file has been changed by Rector                                         


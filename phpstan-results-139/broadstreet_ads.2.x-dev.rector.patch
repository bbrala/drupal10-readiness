diff --git a/broadstreet_ads.admin.inc b/broadstreet_ads.admin.inc
index a1b3b37..e42c0c5 100644
--- a/broadstreet_ads.admin.inc
+++ b/broadstreet_ads.admin.inc
@@ -283,7 +283,7 @@ function broadstreet_ads_settings_form_submit(&$form, &$form_state) {
   // Save whatever zones exist.
   variable_set('broadstreet_ads_zones', $zones);
 
-  drupal_set_message(t('Ad zones and settings updated.'));
+  \Drupal::messenger()->addStatus(t('Ad zones and settings updated.'));
 }
 
 /**
@@ -308,12 +308,12 @@ function theme_broadstreet_ads_settings_form_table(&$variables) {
     // Traverse each column in the row.  @see element_children().
     foreach (element_children($rows[$row_index]) as $col_index) {
       // Render the column form element.
-      $row[] = drupal_render($rows[$row_index][$col_index]);
+      $row[] = \Drupal::service('renderer')->render($rows[$row_index][$col_index]);
     }
     // Add the row to the table.
     $content['#rows'][] = $row;
   }
 
   // Render the table and return.
-  return drupal_render($content);
+  return \Drupal::service('renderer')->render($content);
 }
diff --git a/broadstreet_ads.module b/broadstreet_ads.module
index 48b570e..e2e8a28 100644
--- a/broadstreet_ads.module
+++ b/broadstreet_ads.module
@@ -114,7 +114,7 @@ function _broadstreet_ads_js_alter(&$js) {
       $settings['broadstreet_ads']['add_body_class'] = check_plain($body_class);
     }
 
-    $path = drupal_get_path('module', 'broadstreet_ads') . '/blockers.js';
+    $path = \Drupal::service('extension.list.module')->getPath('broadstreet_ads') . '/blockers.js';
 
     // Get the weight of the last JS item.
     $keys = array_keys($js);

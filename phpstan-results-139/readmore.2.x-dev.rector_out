modules/contrib/readmore/readmore.module
modules/contrib/readmore/src/Plugin/Field/FieldFormatter/ReadmoreFormatter.php

1 file with changes
===================

1) modules/contrib/readmore/src/Plugin/Field/FieldFormatter/ReadmoreFormatter.php:179

    ---------- begin diff ----------
@@ @@
       if ($text_length > $trim_length) {
         // Add Read less if need.
         if ($settings['show_readless']) {
-          $text .= ' ' . render($link_less);
+          $text .= ' ' . \Drupal::service('renderer')->render($link_less);
         }

         // Get trimmed string.
@@ @@
         $summary .= $settings['ellipsis'] ? $this->t('...') : NULL;

         if ($settings['show_readmore']) {
-          $summary .= render($link_more);
+          $summary .= \Drupal::service('renderer')->render($link_more);
         }

         $summary .= '</span>';
    ----------- end diff -----------

Applied rules:
 * RenderRector
 * ArgumentRemoverRector
 * WrapReturnRector
 * ChangeMethodVisibilityRector


 [OK] 1 file has been changed by Rector                                         


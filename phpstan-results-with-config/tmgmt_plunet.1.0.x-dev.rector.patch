diff --git a/src/PlunetCallbacks.php b/src/PlunetCallbacks.php
index 66b5a06..9b7c7e2 100644
--- a/src/PlunetCallbacks.php
+++ b/src/PlunetCallbacks.php
@@ -2,6 +2,8 @@
 
 namespace Drupal\tmgmt_plunet;
 
+use Drupal\tmgmt\TranslatorInterface;
+use Drupal\tmgmt\JobItemInterface;
 use Drupal\tmgmt\Entity\Job;
 use Drupal\tmgmt\Entity\JobItem;
 use Drupal\tmgmt\Entity\Translator;
@@ -31,7 +33,7 @@ class PlunetCallbacks {
       throw new \SoapFault('Server', 'Invalid authentication code.');
     }
     $translator_id = reset($translator_ids);
-    /** @var  \Drupal\tmgmt\TranslatorInterface $translator */
+    /** @var TranslatorInterface $translator */
     $translator = Translator::load($translator_id);
 
     return $translator;
@@ -71,7 +73,7 @@ class PlunetCallbacks {
       throw new \SoapFault('Server', 'No matching job items.');
     }
     $job_item_id = reset($job_item_ids);
-    /** @var \Drupal\tmgmt\JobItemInterface $job_item */
+    /** @var JobItemInterface $job_item */
     $job_item = JobItem::load($job_item_id);
 
     // Trigger fetch translations call.
diff --git a/src/PlunetTranslatorUi.php b/src/PlunetTranslatorUi.php
index a62568b..1bb2963 100644
--- a/src/PlunetTranslatorUi.php
+++ b/src/PlunetTranslatorUi.php
@@ -2,6 +2,9 @@
 
 namespace Drupal\tmgmt_plunet;
 
+use Drupal\tmgmt\TranslatorInterface;
+use Drupal\tmgmt_plunet\Plugin\tmgmt\Translator\PlunetTranslator;
+use Drupal\tmgmt\Entity\Job;
 use Drupal\Core\Datetime\DrupalDateTime;
 use Drupal\Core\Form\FormStateInterface;
 use Drupal\tmgmt\JobInterface;
@@ -18,7 +21,7 @@ class PlunetTranslatorUi extends TranslatorPluginUiBase {
   public function buildConfigurationForm(array $form, FormStateInterface $form_state) {
     $form = parent::buildConfigurationForm($form, $form_state);
 
-    /** @var \Drupal\tmgmt\TranslatorInterface $translator */
+    /** @var TranslatorInterface $translator */
     $translator = $form_state->getFormObject()->getEntity();
 
     $form['api_url'] = [
@@ -102,9 +105,9 @@ class PlunetTranslatorUi extends TranslatorPluginUiBase {
    * Form submit callback to test the Plunet API connection.
    */
   public function testPlunetApiConnection(array &$form, FormStateInterface $form_state) {
-    /** @var \Drupal\tmgmt\TranslatorInterface $translator */
+    /** @var TranslatorInterface $translator */
     $translator = $form_state->getFormObject()->getEntity();
-    /** @var \Drupal\tmgmt_plunet\Plugin\tmgmt\Translator\PlunetTranslator $translator_plugin */
+    /** @var PlunetTranslator $translator_plugin */
     $translator_plugin = $translator->getPlugin();
     $translator_plugin->setTranslator($translator);
 
@@ -180,14 +183,14 @@ class PlunetTranslatorUi extends TranslatorPluginUiBase {
    *
    * @param array $element
    *   The input element to validate.
-   * @param \Drupal\Core\Form\FormStateInterface $form_state
+   * @param FormStateInterface $form_state
    *   The form state.
    */
   public static function validateDueDate(array $element, FormStateInterface &$form_state) {
     $current_date = new DrupalDateTime();
     $current_date->setTimeZone(timezone_open('Europe/Zurich'));
     if (isset($element['#value']['object'])) {
-      /** @var \Drupal\Core\Datetime\DrupalDateTime $deadline */
+      /** @var DrupalDateTime $deadline */
       $deadline = $element['#value']['object'];
 
       if ($deadline <= $current_date) {
@@ -210,10 +213,10 @@ class PlunetTranslatorUi extends TranslatorPluginUiBase {
    * Submit callback to fetch translations from Plunet API.
    */
   public function submitFetchTranslations(array $form, FormStateInterface $form_state) {
-    /** @var \Drupal\tmgmt\Entity\Job $job */
+    /** @var Job $job */
     $job = $form_state->getFormObject()->getEntity();
 
-    /** @var \Drupal\tmgmt_plunet\Plugin\tmgmt\Translator\PlunetTranslator $translator_plugin */
+    /** @var PlunetTranslator $translator_plugin */
     $translator_plugin = $job->getTranslator()->getPlugin();
     $translator_plugin->setTranslator($job->getTranslator());
     $translator_plugin->fetchTranslations($job);
@@ -225,10 +228,9 @@ class PlunetTranslatorUi extends TranslatorPluginUiBase {
   public function validateConfigurationForm(array &$form, FormStateInterface $form_state) {
     parent::validateConfigurationForm($form, $form_state);
     /** @var \Drupal\tmgmt\Entity\Job $job */
-
-    /** @var \Drupal\tmgmt\TranslatorInterface $translator */
+    /** @var TranslatorInterface $translator */
     $translator = $form_state->getFormObject()->getEntity();
-    /** @var \Drupal\tmgmt_plunet\Plugin\tmgmt\Translator\PlunetTranslator $translator_plugin */
+    /** @var PlunetTranslator $translator_plugin */
     $translator_plugin = $translator->getPlugin();
     $translator_plugin->setTranslator($translator);
     // Clear the remote UUID as the credentials might have changed.

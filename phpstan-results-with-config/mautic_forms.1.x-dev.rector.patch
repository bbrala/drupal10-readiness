diff --git a/src/Controller/MauticFormsController.php b/src/Controller/MauticFormsController.php
index 179b245..87ee965 100644
--- a/src/Controller/MauticFormsController.php
+++ b/src/Controller/MauticFormsController.php
@@ -42,52 +42,52 @@ class MauticFormsController implements ContainerInjectionInterface {
   /**
    * The Mautic API factory.
    *
-   * @var Drupal\mautic_forms\MauticApiFactory
+   * @var MauticApiFactory
    */
   protected $apiFactory;
 
   /**
    * The mautic_forms logger channel.
    *
-   * @var Psr\Log\LoggerInterface
+   * @var LoggerInterface
    */
   protected $logger;
 
   /**
    * The meesenger service.
    *
-   * @var Drupal\Core\Messenger\MessengerInterface
+   * @var MessengerInterface
    */
   protected $messenger;
 
   /**
    * The default cache backend service.
    *
-   * @var Drupal\Core\Cache\CacheBackendInterface
+   * @var CacheBackendInterface
    */
   protected $cache;
 
   /**
    * The language manager service.
    *
-   * @var Drupal\Core\Language\LanguageManagerInterface
+   * @var LanguageManagerInterface
    */
   protected $languageManager;
 
   /**
    * Constructor.
    *
-   * @param Drupal\mautic_forms\MauticApiFactory $apiFactory
+   * @param MauticApiFactory $apiFactory
    *   The Mautic API factory.
-   * @param Drupal\Core\StringTranslation\TranslationInterface $stringTranslation
+   * @param TranslationInterface $stringTranslation
    *   The string translation service.
-   * @param Psr\Log\LoggerInterface $logger
+   * @param LoggerInterface $logger
    *   The mautic_forms logger channel.
-   * @param Drupal\Core\Messenger\MessengerInterface $messenger
+   * @param MessengerInterface $messenger
    *   The messenger service.
-   * @param Drupal\Core\Cache\CacheBackendInterface $cache
+   * @param CacheBackendInterface $cache
    *   The default cache backend service.
-   * @param Drupal\Core\Language\LanguageManagerInterface $languageManager
+   * @param LanguageManagerInterface $languageManager
    *   The language manager service.
    */
   public function __construct(
@@ -123,10 +123,10 @@ class MauticFormsController implements ContainerInjectionInterface {
   /**
    * Autocomplete handler for the Mautic Form field widget.
    *
-   * @param Symfony\Component\HttpFoundation\Request $request
+   * @param Request $request
    *   The current request.
    *
-   * @return Symfony\Component\HttpFoundation\JsonResponse
+   * @return JsonResponse
    *   The autocomplete results in JSON format.
    */
   public function autocomplete(Request $request): JsonResponse {
@@ -171,7 +171,7 @@ class MauticFormsController implements ContainerInjectionInterface {
   /**
    * Returns a JSON object of translated form text results.
    *
-   * @param Symfony\Component\HttpFoundation\Request $request
+   * @param Request $request
    *   The current request.
    * @param int $node_id
    *   The current node id.
@@ -180,7 +180,7 @@ class MauticFormsController implements ContainerInjectionInterface {
    * @param int $hash
    *   The hash of the form values to be translated.
    *
-   * @return Symfony\Component\HttpFoundation\JsonResponse
+   * @return JsonResponse
    *   A JSON object of translated text results.
    */
   public function formTranslation(Request $request, int $node_id, int $form_id, int $hash): JsonResponse {
@@ -216,7 +216,7 @@ class MauticFormsController implements ContainerInjectionInterface {
    * @param int $hash
    *   The hash of the form values to be translated.
    *
-   * @return Symfony\Component\HttpFoundation\Response
+   * @return Response
    *   A JSON object of translated text results, if found. A 404 response otherwise.
    */
   public function cachedFormTranslation(int $node_id, int $form_id, int $hash): Response {
@@ -235,7 +235,7 @@ class MauticFormsController implements ContainerInjectionInterface {
   /**
    * Generate a cache id from the static template.
    *
-   * @param Drupal\node\NodeInterface $node
+   * @param NodeInterface $node
    *   The current node.
    * @param int $form_id
    *   The form id of the form being translated.
diff --git a/src/Element/MauticFormAutocomplete.php b/src/Element/MauticFormAutocomplete.php
index fc01f28..21a5ace 100644
--- a/src/Element/MauticFormAutocomplete.php
+++ b/src/Element/MauticFormAutocomplete.php
@@ -61,7 +61,7 @@ class MauticFormAutocomplete extends Textfield {
    *
    * @param array $element
    *   The form element to process.
-   * @param Drupal\Core\Form\FormStateInterface $form_state
+   * @param FormStateInterface $form_state
    *   The current state of the form.
    * @param array $complete_form
    *   The complete form structure.
diff --git a/src/Form/MauticFormsSettingsForm.php b/src/Form/MauticFormsSettingsForm.php
index ce515a6..2373a22 100644
--- a/src/Form/MauticFormsSettingsForm.php
+++ b/src/Form/MauticFormsSettingsForm.php
@@ -28,23 +28,23 @@ class MauticFormsSettingsForm extends ConfigFormBase {
   /**
    * The Mautic API factory.
    *
-   * @var Drupal\mautic_forms\MauticApiFactory
+   * @var MauticApiFactory
    */
   protected $apiFactory;
 
   /**
    * The meesenger service.
    *
-   * @var Drupal\Core\Messenger\MessengerInterface
+   * @var MessengerInterface
    */
   protected $messenger;
 
   /**
    * Constructor.
    *
-   * @param Drupal\mautic_forms\MauticApiFactory $apiFactory
+   * @param MauticApiFactory $apiFactory
    *   The Mautic API factory.
-   * @param Drupal\Core\Messenger\MessengerInterface $messenger
+   * @param MessengerInterface $messenger
    *   The messenger service.
    */
   public function __construct(
diff --git a/src/MauticApiFactory.php b/src/MauticApiFactory.php
index b635fc9..5731de5 100644
--- a/src/MauticApiFactory.php
+++ b/src/MauticApiFactory.php
@@ -28,23 +28,23 @@ class MauticApiFactory {
   /**
    * The config factory service.
    *
-   * @var Drupal\Core\Config\ConfigFactoryInterface
+   * @var ConfigFactoryInterface
    */
   protected $configFactory;
 
   /**
    * The mautic_forms logger channel.
    *
-   * @var Psr\Log\LoggerInterface
+   * @var LoggerInterface
    */
   protected $logger;
 
   /**
    * Constructor.
    *
-   * @param Drupal\Core\Config\ConfigFactoryInterface $configFactory
+   * @param ConfigFactoryInterface $configFactory
    *   The config factory service.
-   * @param Psr\Log\LoggerInterface $logger
+   * @param LoggerInterface $logger
    *   The mautic_forms logger channel.
    */
   public function __construct(
diff --git a/src/Plugin/Field/FieldFormatter/MauticFormScriptEmbedFormatter.php b/src/Plugin/Field/FieldFormatter/MauticFormScriptEmbedFormatter.php
index a9cef89..413778a 100644
--- a/src/Plugin/Field/FieldFormatter/MauticFormScriptEmbedFormatter.php
+++ b/src/Plugin/Field/FieldFormatter/MauticFormScriptEmbedFormatter.php
@@ -28,7 +28,7 @@ class MauticFormScriptEmbedFormatter extends FormatterBase implements ContainerF
   /**
    * The language manager service.
    *
-   * @var Drupal\Core\Language\LanguageManagerInterface
+   * @var LanguageManagerInterface
    */
   protected $languageManager;
 
@@ -39,7 +39,7 @@ class MauticFormScriptEmbedFormatter extends FormatterBase implements ContainerF
    *   The plugin_id for the plugin instance.
    * @param mixed $plugin_definition
    *   The plugin implementation definition.
-   * @param \Drupal\Core\Field\FieldDefinitionInterface $field_definition
+   * @param FieldDefinitionInterface $field_definition
    *   Defines an interface for entity field definitions.
    * @param array $settings
    *   The formatter settings.
@@ -49,7 +49,7 @@ class MauticFormScriptEmbedFormatter extends FormatterBase implements ContainerF
    *   The view mode.
    * @param array $third_party_settings
    *   Any third party settings.
-   * @param Drupal\Core\Language\LanguageManagerInterface $languageManager
+   * @param LanguageManagerInterface $languageManager
    *   The language manager service.
    */
   public function __construct(

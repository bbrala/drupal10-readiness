themes/contrib/nebula/nebula.theme

1 file with changes
===================

1) themes/contrib/nebula/nebula.theme:19

    ---------- begin diff ----------
@@ @@
       ->getPath();

   // Path to this theme.
-  $variables['nebula_path'] = '/' . drupal_get_path('theme', 'nebula');
+  $variables['nebula_path'] = '/' . \Drupal::service('extension.list.theme')->getPath('nebula');

   // Language everywhere.
   $variables['language'] = \Drupal::languageManager()
    ----------- end diff -----------

Applied rules:
 * DrupalGetPathRector
 * ArgumentRemoverRector


 [OK] 1 file has been changed by Rector                                         


diff --git a/src/Plugin/CKEditorPlugin/Linebreaks.php b/src/Plugin/CKEditorPlugin/Linebreaks.php
index ea8b7d2..5a88aba 100644
--- a/src/Plugin/CKEditorPlugin/Linebreaks.php
+++ b/src/Plugin/CKEditorPlugin/Linebreaks.php
@@ -48,7 +48,7 @@ class Linebreaks extends PluginBase implements CKEditorPluginInterface, CKEditor
    * {@inheritdoc}
    */
   public function getFile() {
-    return drupal_get_path('module', 'wysiwyg_linebreaks') . '/js/plugins/linebreaks/linebreaks.js';
+    return \Drupal::service('extension.list.module')->getPath('wysiwyg_linebreaks') . '/js/plugins/linebreaks/linebreaks.js';
   }
 
   /**
diff --git a/src/Tests/WysiwygLinebreaksConfigTest.php b/src/Tests/WysiwygLinebreaksConfigTest.php
index 4418c68..53c0344 100644
--- a/src/Tests/WysiwygLinebreaksConfigTest.php
+++ b/src/Tests/WysiwygLinebreaksConfigTest.php
@@ -2,6 +2,8 @@
 
 namespace Drupal\wysiwyg_linebreaks\Tests;
 
+use Drupal\user\UserInterface;
+use Drupal\node\NodeInterface;
 use Drupal\Tests\BrowserTestBase;
 
 /**
@@ -14,14 +16,14 @@ class WysiwygLinebreaksConfigTest extends BrowserTestBase {
   /**
    * A user with permission to administer site configuration.
    *
-   * @var \Drupal\user\UserInterface
+   * @var UserInterface
    */
   protected $adminUser;
 
   /**
    * A node to use for testing.
    *
-   * @var \Drupal\node\NodeInterface
+   * @var NodeInterface
    */
   protected $node;
 
@@ -42,7 +44,7 @@ class WysiwygLinebreaksConfigTest extends BrowserTestBase {
   /**
    * Setup function.
    */
-  public function setUp() {
+  public function setUp(): void {
     // Enable modules required for this test.
     parent::setUp();
 
@@ -59,8 +61,14 @@ class WysiwygLinebreaksConfigTest extends BrowserTestBase {
 
     // Get the comment reply form and ensure there's no 'url' field.
     $this->drupalGet('admin/config/content/formats/manage/basic_html');
-    $this->assertText('Force linebreaks', 'Force linebreaks conversion method shown.');
-    $this->assertText('Convert linebreaks', 'Convert linebreaks conversion method shown.');
+    // TODO: Drupal Rector Notice: Please delete the following comment after you've made any necessary changes.
+    // Verify the assertion: pageTextContains() for HTML responses, responseContains() for non-HTML responses.
+    // The passed text should be HTML decoded, exactly as a human sees it in the browser.
+    $this->assertSession()->pageTextContains('Force linebreaks');
+    // TODO: Drupal Rector Notice: Please delete the following comment after you've made any necessary changes.
+    // Verify the assertion: pageTextContains() for HTML responses, responseContains() for non-HTML responses.
+    // The passed text should be HTML decoded, exactly as a human sees it in the browser.
+    $this->assertSession()->pageTextContains('Convert linebreaks');
   }
 
 }
diff --git a/wysiwyg_linebreaks.info.yml b/wysiwyg_linebreaks.info.yml
index 2323a44..8aae65d 100644
--- a/wysiwyg_linebreaks.info.yml
+++ b/wysiwyg_linebreaks.info.yml
@@ -2,8 +2,7 @@ name: Wysiwyg Linebreaks
 type: module
 description: Plugin to allow legacy content editing and more sane formatting in wysiwyg editors.
 package: User interface
-core: 8.x
-core_version_requirement: ^8 || ^9
+core_version_requirement: ^9.3 || ^10
 dependencies:
   - drupal:editor
   - drupal:ckeditor

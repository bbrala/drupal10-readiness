diff --git a/imagelightbox.install b/imagelightbox.install
index 24ca431..9797049 100644
--- a/imagelightbox.install
+++ b/imagelightbox.install
@@ -10,7 +10,7 @@
  */
 // function imagelightbox_requirements($phase) {
 //   $requirements = [];
-  
+
 //   if ($phase == 'runtime') {
 //     if ((!file_exists(drupal_get_path('module', 'imagelightbox') . 'libraries/imagelightbox.js')) && (!file_exists(drupal_get_path('module', 'imagelightbox') . '/modules/libraries/imagelightbox.js'))) {
 //       $requirements['imagelightbox_library'] = [
diff --git a/src/Plugin/Field/FieldFormatter/ImagelightboxFormatter.php b/src/Plugin/Field/FieldFormatter/ImagelightboxFormatter.php
index af2d091..b395190 100755
--- a/src/Plugin/Field/FieldFormatter/ImagelightboxFormatter.php
+++ b/src/Plugin/Field/FieldFormatter/ImagelightboxFormatter.php
@@ -2,6 +2,8 @@
 
 namespace Drupal\imagelightbox\Plugin\Field\FieldFormatter;
 
+use Drupal\image\ImageStyleInterface;
+use Drupal\file\FileInterface;
 use Drupal\Core\Cache\Cache;
 use Drupal\Core\Field\FieldItemListInterface;
 use Drupal\Core\Form\FormStateInterface;
@@ -123,7 +125,7 @@ class ImageLightBoxFormatter extends ImageFormatter {
 
     // Prepare image styles.
     if ($settings['imagelightbox_image_style']) {
-      /** @var \Drupal\image\ImageStyleInterface $imagelightbox_image_style */
+      /** @var ImageStyleInterface $imagelightbox_image_style */
       $imagelightbox_image_style = $this->imageStyleStorage->load($settings['imagelightbox_image_style']);
     }
 
@@ -136,10 +138,10 @@ class ImageLightBoxFormatter extends ImageFormatter {
           }
         }
 
-    /** @var \Drupal\file\FileInterface[] $files */
+    /** @var FileInterface[] $files */
     foreach ($files as $delta => $file) {
       $image_uri = $file->getFileUri();
-      $default_url = file_create_url($image_uri);
+      $default_url = \Drupal::service('file_url_generator')->generateAbsoluteString($image_uri);
       $item = $file->_referringItem;
       $item_attributes = $item->_attributes;
       unset($item->_attributes);

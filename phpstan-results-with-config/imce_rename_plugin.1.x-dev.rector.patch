diff --git a/imce_rename_plugin.info.yml b/imce_rename_plugin.info.yml
index 992cdc2..ac535a4 100644
--- a/imce_rename_plugin.info.yml
+++ b/imce_rename_plugin.info.yml
@@ -2,6 +2,6 @@ name: 'IMCE Rename (D8)'
 type: module
 description: 'Plugin Rename for IMCE in order to rename image.'
 package: Media
-core_version_requirement: ^8 || ^9
+core_version_requirement: ^9.3 || ^10
 dependencies:
   - imce:imce
diff --git a/src/Plugin/ImcePlugin/Rename.php b/src/Plugin/ImcePlugin/Rename.php
index 8bd4217..870005f 100755
--- a/src/Plugin/ImcePlugin/Rename.php
+++ b/src/Plugin/ImcePlugin/Rename.php
@@ -2,6 +2,7 @@
 
 namespace Drupal\imce_rename_plugin\Plugin\ImcePlugin;
 
+use Drupal\Core\Entity\EntityStorageException;
 use Drupal\Core\Database\Connection;
 use Drupal\Core\Messenger\Messenger;
 use Drupal\Core\File\FileSystemInterface;
@@ -28,21 +29,21 @@ class Rename extends ImcePluginBase implements ContainerFactoryPluginInterface {
   /**
    * The variable containing the messenger service.
    *
-   * @var \Drupal\Core\Messenger\Messenger
+   * @var Messenger
    */
   protected $messenger;
 
   /**
    * The variable containing the database service.
    *
-   * @var \Drupal\Core\Database\Connection
+   * @var Connection
    */
   protected $database;
 
   /**
    * The transliteration service to use.
    *
-   * @var \Drupal\Component\Transliteration\TransliterationInterface
+   * @var TransliterationInterface
    */
   protected $transliteration;
 
@@ -55,11 +56,11 @@ class Rename extends ImcePluginBase implements ContainerFactoryPluginInterface {
    *   The plugin id.
    * @param mixed $plugin_definition
    *   The plugin definition.
-   * @param \Drupal\Core\Messenger\Messenger $messenger
+   * @param Messenger $messenger
    *   The messenger service.
-   * @param \Drupal\Core\Database\Connection $database
+   * @param Connection $database
    *   The database service.
-   * @param \Drupal\Component\Transliteration\TransliterationInterface $transliteration
+   * @param TransliterationInterface $transliteration
    *   The transliteration service.
    */
   public function __construct(array $configuration,
@@ -112,7 +113,7 @@ class Rename extends ImcePluginBase implements ContainerFactoryPluginInterface {
   /**
    * Operation handler: rename.
    *
-   * @throws \Drupal\Core\Entity\EntityStorageException
+   * @throws EntityStorageException
    */
   public function opRename(ImceFM $fm) {
     $items = $fm->getSelection();
@@ -154,7 +155,7 @@ class Rename extends ImcePluginBase implements ContainerFactoryPluginInterface {
   /**
    * Renames file by name.
    *
-   * @throws \Drupal\Core\Entity\EntityStorageException
+   * @throws EntityStorageException
    */
   public function renameFile(ImceFM $fm, string $old_name) {
     $new_name = $this->getNewName($fm);
@@ -183,7 +184,7 @@ class Rename extends ImcePluginBase implements ContainerFactoryPluginInterface {
     $file = Imce::getFileEntity($old_uri);
     // Create entity when there is no entity for the file.
     $file = empty($file) ? Imce::createFileEntity($old_uri) : $file;
-    $move = file_move($file, $new_uri, FileSystemInterface::EXISTS_ERROR);
+    $move = \Drupal::service('file.repository')->move($file, $new_uri, FileSystemInterface::EXISTS_ERROR);
     $move->setFilename($new_name);
     $move->save();
 
@@ -256,7 +257,7 @@ class Rename extends ImcePluginBase implements ContainerFactoryPluginInterface {
   /**
    * Get name and filtered special symbols.
    *
-   * @param \Drupal\imce\ImceFM $fm
+   * @param ImceFM $fm
    *   Imce File Manager instance.
    *
    * @return int|mixed|string|string[]|null

diff --git a/src/Plugin/Purge/Purger/AzurePurger.php b/src/Plugin/Purge/Purger/AzurePurger.php
index 865802a..f067682 100644
--- a/src/Plugin/Purge/Purger/AzurePurger.php
+++ b/src/Plugin/Purge/Purger/AzurePurger.php
@@ -2,6 +2,7 @@
 
 namespace Drupal\azure_cdn_purge\Plugin\Purge\Purger;
 
+use Drupal\Core\Config\Config;
 use Drupal\Core\Config\ConfigFactoryInterface;
 use Drupal\purge\Plugin\Purge\Purger\PurgerBase;
 use Drupal\purge\Plugin\Purge\Purger\PurgerInterface;
@@ -25,14 +26,14 @@ class AzurePurger extends PurgerBase implements PurgerInterface {
   /**
    * The settings configuration.
    *
-   * @var \Drupal\Core\Config\Config
+   * @var Config
    */
   protected $config;
 
   /**
    * A logger instance.
    *
-   * @var \Psr\Log\LoggerInterface
+   * @var LoggerInterface
    */
   protected $logger;
 
@@ -58,9 +59,9 @@ class AzurePurger extends PurgerBase implements PurgerInterface {
    *   The plugin_id for the plugin instance.
    * @param mixed $plugin_definition
    *   The plugin implementation definition.
-   * @param \Drupal\Core\Config\ConfigFactoryInterface $config_factory
+   * @param ConfigFactoryInterface $config_factory
    *   The factory for configuration objects.
-   * @param \Psr\Log\LoggerInterface $logger
+   * @param LoggerInterface $logger
    *   A logger instance.
    */
   public function __construct(array $configuration, $plugin_id, $plugin_definition, ConfigFactoryInterface $config_factory, LoggerInterface $logger) {

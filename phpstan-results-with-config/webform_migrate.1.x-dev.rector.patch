diff --git a/src/Plugin/migrate/source/d6/D6Webform.php b/src/Plugin/migrate/source/d6/D6Webform.php
index e828d18..b475a89 100644
--- a/src/Plugin/migrate/source/d6/D6Webform.php
+++ b/src/Plugin/migrate/source/d6/D6Webform.php
@@ -2,6 +2,7 @@
 
 namespace Drupal\webform_migrate\Plugin\migrate\source\d6;
 
+use Drupal\node\NodeInterface;
 use Drupal\migrate\Event\ImportAwareInterface;
 use Drupal\migrate\Event\RollbackAwareInterface;
 use Drupal\migrate\Event\MigrateImportEvent;
@@ -660,7 +661,7 @@ class D6Webform extends DrupalSqlBase implements ImportAwareInterface, RollbackA
       $webform_id = 'webform_' . $webform_nid;
       $webform = Webform::load($webform_id);
       if (!empty($webform)) {
-        /** @var \Drupal\node\NodeInterface $node */
+        /** @var NodeInterface $node */
         $node = Node::load($webform_nid);
         if (!empty($node) && $node->getType() == 'webform') {
           if (empty($node->webform->target_id)) {
@@ -690,7 +691,7 @@ class D6Webform extends DrupalSqlBase implements ImportAwareInterface, RollbackA
       $webform_id = 'webform_' . $webform_nid;
       $webform = Webform::load($webform_id);
       if (empty($webform)) {
-        /** @var \Drupal\node\NodeInterface $node */
+        /** @var NodeInterface $node */
         $node = Node::load($webform['nid']);
         if (!empty($node) && $node->getType() == 'webform') {
           if (!empty($node->webform->target_id) && $node->webform->target_id == $webform_id) {
diff --git a/src/Plugin/migrate/source/d7/D7Webform.php b/src/Plugin/migrate/source/d7/D7Webform.php
index 4dd9fd5..b4388e6 100644
--- a/src/Plugin/migrate/source/d7/D7Webform.php
+++ b/src/Plugin/migrate/source/d7/D7Webform.php
@@ -2,6 +2,7 @@
 
 namespace Drupal\webform_migrate\Plugin\migrate\source\d7;
 
+use Drupal\node\NodeInterface;
 use Drupal\migrate\Event\ImportAwareInterface;
 use Drupal\migrate\Event\RollbackAwareInterface;
 use Drupal\migrate\Event\MigrateImportEvent;
@@ -953,7 +954,7 @@ class D7Webform extends DrupalSqlBase implements ImportAwareInterface, RollbackA
       $webform_id = 'webform_' . $webform_nid;
       $webform = Webform::load($webform_id);
       if (!empty($webform)) {
-        /** @var \Drupal\node\NodeInterface $node */
+        /** @var NodeInterface $node */
         $node = Node::load($webform_nid);
         if (!empty($node) && $node->getType() == 'webform') {
           if (empty($node->webform->target_id)) {
@@ -982,7 +983,7 @@ class D7Webform extends DrupalSqlBase implements ImportAwareInterface, RollbackA
       $webform_id = 'webform_' . $webform_nid;
       $webform = Webform::load($webform_id);
       if (empty($webform)) {
-        /** @var \Drupal\node\NodeInterface $node */
+        /** @var NodeInterface $node */
         $node = Node::load($webform_nid);
         if (!empty($node) && $node->getType() == 'webform') {
           if (!empty($node->webform->target_id) && $node->webform->target_id == $webform_id) {

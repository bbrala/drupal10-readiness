diff --git a/tests/src/Functional/ConfigTokenTest.php b/tests/src/Functional/ConfigTokenTest.php
index 42cdc77..a78b1f4 100644
--- a/tests/src/Functional/ConfigTokenTest.php
+++ b/tests/src/Functional/ConfigTokenTest.php
@@ -2,6 +2,7 @@
 
 namespace Drupal\Tests\config_token\Functional;
 
+use Drupal\user\UserInterface;
 use Drupal\Tests\BrowserTestBase;
 use Drupal\Core\Url;
 use Drupal\Core\Link;
@@ -36,14 +37,14 @@ class ConfigTokenTest extends BrowserTestBase {
   /**
    * Admin user.
    *
-   * @var \Drupal\user\UserInterface
+   * @var UserInterface
    */
   protected $adminUser;
 
   /**
    * {@inheritdoc}
    */
-  protected function setUp() {
+  protected function setUp(): void {
     parent::setUp();
 
     // Create admin user.
@@ -93,14 +94,14 @@ class ConfigTokenTest extends BrowserTestBase {
    */
   function testTokens() {
     $value = \Drupal::token()->replace('[config_token:example_email]', [], ['clear' => FALSE]);
-    $this->assertEqual($value, 'email@example.com');
+    $this->assertEquals($value, 'email@example.com');
 
     $value = \Drupal::token()->replace('[config_token:example_phone]', [], ['clear' => FALSE]);
-    $this->assertEqual($value, '02070000000');
+    $this->assertEquals($value, '02070000000');
 
     $value = \Drupal::token()->replace('[config_token:example_link]', [], ['clear' => FALSE]);
     $link = Link::fromTextAndUrl('http://www.example.com',  Url::fromUri('http://www.example.com'));
-    $this->assertEqual($value, $link->toString());
+    $this->assertEquals($value, $link->toString());
   }
 
   /**
@@ -114,7 +115,7 @@ class ConfigTokenTest extends BrowserTestBase {
       'body[0][format]' => 'basic_html',
     ], t('Save'));
 
-    $this->assertRaw('email@example.com<br />02070000000<br /><a href="http://www.example.com">http://www.example.com</a>');
+    $this->assertSession()->responseContains('email@example.com<br />02070000000<br /><a href="http://www.example.com">http://www.example.com</a>');
   }
 
 }

modules/contrib/override_media_options/override_media_options.module
modules/contrib/override_media_options/src/Form/SettingsForm.php
modules/contrib/override_media_options/src/MediaPermissions.php

2 files with changes
====================

1) modules/contrib/override_media_options/src/MediaPermissions.php:62

    ---------- begin diff ----------
@@ @@
    *   The permissions array, passed by reference.
    */
   private function addSpecificPermissions(array &$permissions) {
-    /** @var Drupal\media\Entity\MediaType $media_type */
+    /** @var MediaType $media_type */
     foreach (MediaType::loadMultiple() as $media_type) {
       $type = $media_type->id();
       $label = $media_type->label();
    ----------- end diff -----------

Applied rules:
 * ArgumentRemoverRector
 * PseudoNamespaceToNamespaceRector
 * WrapReturnRector


2) modules/contrib/override_media_options/override_media_options.module:0

    ---------- begin diff ----------
@@ @@
 <?php

+use Drupal\Core\Session\AccountProxyInterface;
 /**
  * @file
  * Main module file for override_media_options.
@@ @@
   $form_object = $form_state->getFormObject();

   if ($form_object instanceof MediaForm) {
-    /** @var Drupal\Core\Session\AccountProxyInterface $user */
+    /** @var AccountProxyInterface $user */
     $user = \Drupal::currentUser();

     if ($user->hasPermission('administer media')) {
    ----------- end diff -----------

Applied rules:
 * ArgumentRemoverRector


 [OK] 2 files have been changed by Rector                                       


diff --git a/src/Plugin/Field/FieldFormatter/RssEnclosureImageFormatter.php b/src/Plugin/Field/FieldFormatter/RssEnclosureImageFormatter.php
index f0b9838..bc4c4b5 100644
--- a/src/Plugin/Field/FieldFormatter/RssEnclosureImageFormatter.php
+++ b/src/Plugin/Field/FieldFormatter/RssEnclosureImageFormatter.php
@@ -29,14 +29,14 @@ class RssEnclosureImageFormatter extends ImageFormatterBase implements Container
   /**
    * The link generator.
    *
-   * @var \Drupal\Core\Utility\LinkGeneratorInterface
+   * @var LinkGeneratorInterface
    */
   protected $linkGenerator;
 
   /**
    * The image style entity storage.
    *
-   * @var \Drupal\Core\Entity\EntityStorageInterface
+   * @var EntityStorageInterface
    */
   protected $imageStyleStorage;
 
@@ -47,7 +47,7 @@ class RssEnclosureImageFormatter extends ImageFormatterBase implements Container
    *   The plugin_id for the formatter.
    * @param mixed $plugin_definition
    *   The plugin implementation definition.
-   * @param \Drupal\Core\Field\FieldDefinitionInterface $field_definition
+   * @param FieldDefinitionInterface $field_definition
    *   The definition of the field to which the formatter is associated.
    * @param array $settings
    *   The formatter settings.
@@ -57,9 +57,9 @@ class RssEnclosureImageFormatter extends ImageFormatterBase implements Container
    *   The view mode.
    * @param array $third_party_settings
    *   Any third party settings settings.
-   * @param \Drupal\Core\Utility\LinkGeneratorInterface $link_generator
+   * @param LinkGeneratorInterface $link_generator
    *   The link generator service.
-   * @param \Drupal\Core\Entity\EntityStorageInterface $image_style_storage
+   * @param EntityStorageInterface $image_style_storage
    *   The entity storage for the image.
    */
   public function __construct($plugin_id, $plugin_definition, FieldDefinitionInterface $field_definition, array $settings, $label, $view_mode, array $third_party_settings, LinkGeneratorInterface $link_generator, EntityStorageInterface $image_style_storage) {
@@ -165,7 +165,7 @@ class RssEnclosureImageFormatter extends ImageFormatterBase implements Container
         // context to ensure different file URLs are generated for different
         // sites in a multisite setup, including HTTP and HTTPS versions of the
         // same site. Fix in https://www.drupal.org/node/2646744.
-        $url = Url::fromUri(file_create_url($image_uri));
+        $url = \Drupal::service('file_url_generator')->generate($image_uri);
         $cache_contexts[] = 'url.site';
       }
       $cache_tags = Cache::mergeTags($base_cache_tags, $file->getCacheTags());
diff --git a/src/Plugin/Field/FieldFormatter/RssEnclosureReferenceFormatter.php b/src/Plugin/Field/FieldFormatter/RssEnclosureReferenceFormatter.php
index caa19f5..2353439 100644
--- a/src/Plugin/Field/FieldFormatter/RssEnclosureReferenceFormatter.php
+++ b/src/Plugin/Field/FieldFormatter/RssEnclosureReferenceFormatter.php
@@ -30,14 +30,14 @@ class RssEnclosureReferenceFormatter extends EntityReferenceFormatterBase implem
   /**
    * The link generator.
    *
-   * @var \Drupal\Core\Utility\LinkGeneratorInterface
+   * @var LinkGeneratorInterface
    */
   protected $linkGenerator;
 
   /**
    * The image style entity storage.
    *
-   * @var \Drupal\Core\Entity\EntityStorageInterface
+   * @var EntityStorageInterface
    */
   protected $imageStyleStorage;
 
@@ -48,7 +48,7 @@ class RssEnclosureReferenceFormatter extends EntityReferenceFormatterBase implem
    *   The plugin_id for the formatter.
    * @param mixed $plugin_definition
    *   The plugin implementation definition.
-   * @param \Drupal\Core\Field\FieldDefinitionInterface $field_definition
+   * @param FieldDefinitionInterface $field_definition
    *   The definition of the field to which the formatter is associated.
    * @param array $settings
    *   The formatter settings.
@@ -58,9 +58,9 @@ class RssEnclosureReferenceFormatter extends EntityReferenceFormatterBase implem
    *   The view mode.
    * @param array $third_party_settings
    *   Any third party settings settings.
-   * @param \Drupal\Core\Utility\LinkGeneratorInterface $link_generator
+   * @param LinkGeneratorInterface $link_generator
    *   The link generator service.
-   * @param \Drupal\Core\Entity\EntityStorageInterface $image_style_storage
+   * @param EntityStorageInterface $image_style_storage
    *   The entity storage for the image.
    */
   public function __construct($plugin_id, $plugin_definition, FieldDefinitionInterface $field_definition, array $settings, $label, $view_mode, array $third_party_settings, LinkGeneratorInterface $link_generator, EntityStorageInterface $image_style_storage) {
@@ -165,7 +165,7 @@ class RssEnclosureReferenceFormatter extends EntityReferenceFormatterBase implem
         // context to ensure different file URLs are generated for different
         // sites in a multisite setup, including HTTP and HTTPS versions of the
         // same site. Fix in https://www.drupal.org/node/2646744.
-        $url = Url::fromUri(file_create_url($image_uri));
+        $url = \Drupal::service('file_url_generator')->generate($image_uri);
         $cache_contexts[] = 'url.site';
       }
       $cache_tags = Cache::mergeTags($base_cache_tags, $entity_reference->getCacheTags());
diff --git a/src/Plugin/views/row/FlipboardRssFields.php b/src/Plugin/views/row/FlipboardRssFields.php
index 2d3292c..c046e53 100644
--- a/src/Plugin/views/row/FlipboardRssFields.php
+++ b/src/Plugin/views/row/FlipboardRssFields.php
@@ -161,7 +161,7 @@ class FlipboardRssFields extends RssFields {
    * @param string $field_value
    *   The field value retrieved with RssFields::getField().
    *
-   * @return \Drupal\Core\Url
+   * @return Url
    *   The URL object built from the field value.
    */
   protected function getFieldUrl($field_value) {
diff --git a/views_flipboard.field.inc b/views_flipboard.field.inc
index fe0206c..569a137 100644
--- a/views_flipboard.field.inc
+++ b/views_flipboard.field.inc
@@ -82,7 +82,7 @@ function _views_flipboard_fileinfo(&$variables, $imageUri) {
   }
   else {
     $derivativeUri = $imageUri;
-    $enclosureUrl = file_create_url($imageUri);
+    $enclosureUrl = \Drupal::service('file_url_generator')->generateAbsoluteString($imageUri);
   }
 
   // Prepare enclosure data.
diff --git a/views_flipboard.info.yml b/views_flipboard.info.yml
index 2ad87df..4a36342 100644
--- a/views_flipboard.info.yml
+++ b/views_flipboard.info.yml
@@ -3,7 +3,7 @@ description: Provide views for flipboard like RSS feed.
 package: Views
 
 type: module
-core_version_requirement: ^8.7.7 || ^8.8 || ^9
+core_version_requirement: ^9.3 || ^10
 
 dependencies:
   - drupal:views
diff --git a/views_flipboard.module b/views_flipboard.module
index 3d657f1..b07b5f6 100644
--- a/views_flipboard.module
+++ b/views_flipboard.module
@@ -1,5 +1,6 @@
 <?php
 
+use Drupal\views\Plugin\views\display\DisplayPluginBase;
 /**
  * @file
  * Provide support for Flipboard RSS format to views.
@@ -81,12 +82,12 @@ function template_preprocess_views_view_flipboard_rss(array &$variables) {
   // Figure out which display which has a path we're using for this feed.
   // If there isn't one, use the global $base_url.
   $link_display_id = $view->display_handler->getLinkDisplay();
-  /** @var \Drupal\views\Plugin\views\display\DisplayPluginBase $display */
+  /** @var DisplayPluginBase $display */
   if ($link_display_id && ($display = $view->displayHandlers->get($link_display_id)) && $display->isEnabled()) {
     $url = $view->getUrl(NULL, $link_display_id);
   }
 
-  /** @var \Drupal\Core\Url $url */
+  /** @var Url $url */
   if (!empty($url)) {
     $url_options = ['absolute' => TRUE];
     if (!empty($view->exposed_raw_input)) {

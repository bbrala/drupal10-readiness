diff --git a/multi_peer_review.install b/multi_peer_review.install
index 5fda204..cf5d2b0 100644
--- a/multi_peer_review.install
+++ b/multi_peer_review.install
@@ -1,5 +1,9 @@
 <?php
 
+use Drupal\user\Entity\Role;
+use Drupal\Core\Datetime\Entity\DateFormat;
+use Drupal\filter\Entity\FilterFormat;
+use Drupal\editor\Entity\Editor;
 /**
  * @file
  * Contains install, update and uninstall functions for the MPR module.
@@ -26,7 +30,7 @@ function multi_peer_review_install(){
     // Create roles.
     // Guest.
     $data = ['id' => MPRCommon::ROLE_GUEST, 'label' => 'Multi Peer Review Guest'];
-    $role = \Drupal\user\Entity\Role::create($data);
+    $role = Role::create($data);
     $issueAccessPermissions = [
         'access multi_peer_review_manage_own_papers',
     ];    
@@ -38,7 +42,7 @@ function multi_peer_review_install(){
     
     // Staff.
     $data = ['id' => MPRCommon::ROLE_STAFF, 'label' => 'Multi Peer Review Staff'];
-    $role = \Drupal\user\Entity\Role::create($data);
+    $role = Role::create($data);
     $issueAccessPermissions = [
         'administer multi_peer_review_settings',
         'administer multi_peer_review_menu',
@@ -57,7 +61,7 @@ function multi_peer_review_install(){
     
     // Reviewer.
     $data = ['id' => MPRCommon::ROLE_REVIEWER, 'label' => 'Multi Peer Review Reviewer'];
-    $role = \Drupal\user\Entity\Role::create($data);
+    $role = Role::create($data);
     $issueAccessPermissions = [
         'access multi_peer_review_manage_own_reviewer_profile',        
         'access multi_peer_review_manage_own_invitations',
@@ -73,21 +77,21 @@ function multi_peer_review_install(){
     // Default Date/Time.
     // Example: 20/05/2000 13:23:56
     $data = ['id' => MPRCommon::NAMED_DATE_FORMAT_DEFAULT_DATE_TIME, 'label' => 'Multi Peer Review Default Date/Time', 'pattern' => 'd/m/Y H:i:s'];
-    $date_format = \Drupal\Core\Datetime\Entity\DateFormat::create($data);
+    $date_format = DateFormat::create($data);
     $date_format->save();
     
     
     // Short Date.
     // Example: 20/05/2000
     $data = ['id' => MPRCommon::NAMED_DATE_FORMAT_SHORT_DATE, 'label' => 'Multi Peer Review Short Date/Time', 'pattern' => 'd/m/Y'];
-    $date_format = \Drupal\Core\Datetime\Entity\DateFormat::create($data);
+    $date_format = DateFormat::create($data);
     $date_format->save();    
     
     
     // Verbose Date.
     // Example: 11th of March 2000
     $data = ['id' => MPRCommon::NAMED_DATE_FORMAT_VERBOSE_DATE, 'label' => 'Multi Peer Review Verbose Date', 'pattern' => 'jS \of F Y'];
-    $date_format = \Drupal\Core\Datetime\Entity\DateFormat::create($data);
+    $date_format = DateFormat::create($data);
     $date_format->save();    
     
     
@@ -108,7 +112,7 @@ function multi_peer_review_install(){
     $site_config = \Drupal::configFactory()->getEditable('system.mail');
     $configuration = $site_config->get('interface');    
     $module = 'multi_peer_review';    
-    $keys = \Drupal\multi_peer_review\Entity\EmailTemplate::getEmailTemplateIds();
+    $keys = EmailTemplate::getEmailTemplateIds();
     foreach ($keys as $key) {
         $message_id = $module . '_' . $key;    
         $configuration[$message_id] = 'multi_peer_review_html_enabled_php_mail';    
@@ -127,7 +131,7 @@ function multi_peer_review_install(){
     
     
     // Create new textarea format specifically for the Multi Peer Review module.
-    $textarea_format = \Drupal\filter\Entity\FilterFormat::create([
+    $textarea_format = FilterFormat::create([
         'format' => 'multi_peer_review_html',   // ID for this record.
         'name' => 'Multi Peer Review HTML',            
         'roles' => [MPRCommon::ROLE_GUEST, MPRCommon::ROLE_STAFF],            
@@ -156,10 +160,10 @@ function multi_peer_review_install(){
     
     if ($ckeditor_exists == TRUE) {
         // Reload textarea format.
-        $textarea_format = \Drupal\filter\Entity\FilterFormat::load('multi_peer_review_html'); 
+        $textarea_format = FilterFormat::load('multi_peer_review_html'); 
         if (empty($textarea_format) == FALSE) {
             // Link ckeditor (WYSIWYG editor) with the new textarea format.
-            $editor = \Drupal\editor\Entity\Editor::create([
+            $editor = Editor::create([
                 'format' => 'multi_peer_review_html',
                 'editor' => 'ckeditor',
                 'filterFormat' => $textarea_format,
@@ -221,7 +225,7 @@ function multi_peer_review_uninstall() {
     ];
     foreach ($role_ids as $role_id)
     {
-        $role = \Drupal\user\Entity\Role::load($role_id);
+        $role = Role::load($role_id);
         if (empty($role) == FALSE) {
             $role->delete();
         }
@@ -236,7 +240,7 @@ function multi_peer_review_uninstall() {
     ];
     foreach ($date_format_ids as $date_format_id)
     {
-        $date_format = \Drupal\Core\Datetime\Entity\DateFormat::load($date_format_id);
+        $date_format = DateFormat::load($date_format_id);
         if (empty($date_format) == FALSE) {
             $date_format->delete();    
         }
@@ -260,7 +264,7 @@ function multi_peer_review_uninstall() {
     $site_config = \Drupal::configFactory()->getEditable('system.mail');    
     $configuration = $site_config->get('interface');    
     $module = 'multi_peer_review';
-    $keys = \Drupal\multi_peer_review\Entity\EmailTemplate::getEmailTemplateIds();
+    $keys = EmailTemplate::getEmailTemplateIds();
     foreach ($keys as $key) {
         $message_id = $module . '_' . $key;
         if (empty($configuration[$message_id]) == FALSE) {
@@ -273,12 +277,12 @@ function multi_peer_review_uninstall() {
     
     
     // Remove editor relationship with custom textarea format and then remove format.
-    $editor = \Drupal\editor\Entity\Editor::load('multi_peer_review_html');
+    $editor = Editor::load('multi_peer_review_html');
     if (empty($editor) == FALSE) {
         $editor->delete();    
     }
     
-    $textarea_format = \Drupal\filter\Entity\FilterFormat::load('multi_peer_review_html');
+    $textarea_format = FilterFormat::load('multi_peer_review_html');
     if (empty($textarea_format) == FALSE) {
         $textarea_format->delete();
     }
@@ -287,4 +291,4 @@ function multi_peer_review_uninstall() {
     
     // Clear cached data.
     \Drupal::cache('data')->deleteAll();
-}
\ No newline at end of file
+}
diff --git a/multi_peer_review.module b/multi_peer_review.module
index 6e029cb..aba8f33 100644
--- a/multi_peer_review.module
+++ b/multi_peer_review.module
@@ -1,5 +1,14 @@
 <?php
 
+use Drupal\multi_peer_review\Entity\Reviewer;
+use Drupal\user\Entity\User;
+use Drupal\multi_peer_review\Entity\Review;
+use Drupal\multi_peer_review\Entity\Invitation;
+use Drupal\multi_peer_review\Entity\Paper;
+use Drupal\multi_peer_review\Entity\SES;
+use Drupal\multi_peer_review\Entity\WebRequestCacheItem;
+use Drupal\Core\StringTranslation\TranslatableMarkup;
+use Drupal\multi_peer_review\Entity\EmailTemplate;
 /**
  * @file
  * Contains multi_peer_review.module.
@@ -149,7 +158,7 @@ function multi_peer_review_entity_update(EntityInterface $entity) {
     // Synchronises Reviewer email address.
     $user_id = intval($entity->get('uid')->getValue()[0]['value']);  
 
-    $reviewer = \Drupal\multi_peer_review\Entity\Reviewer::getReviewerByOwner($user_id);
+    $reviewer = Reviewer::getReviewerByOwner($user_id);
     if (empty($reviewer) == FALSE) {
         
         $email = $entity->get('mail')->getValue()[0]['value'];
@@ -181,11 +190,11 @@ function multi_peer_review_entity_delete(EntityInterface $entity) {
 
     $user_id = intval($entity->get('uid')->getValue()[0]['value']);      
 
-    $reviewer = \Drupal\multi_peer_review\Entity\Reviewer::getReviewerByOwner($user_id);
+    $reviewer = Reviewer::getReviewerByOwner($user_id);
     if (empty($reviewer) == FALSE) {
         // Use the Reviewer's original owner id if possible.
         $original_owner_id = $reviewer->getOriginalOwner();
-        $original_owner = Drupal\user\Entity\User::load($original_owner_id);
+        $original_owner = User::load($original_owner_id);
 
         if (empty($original_owner) == FALSE) {
             // Original owner still exists.
@@ -198,7 +207,7 @@ function multi_peer_review_entity_delete(EntityInterface $entity) {
     }
 
     // At this stage, $new_user_id could belong to one of the original owners of the related Reviewer records.
-    foreach (\Drupal\multi_peer_review\Entity\Review::getReviews(NULL, NULL, NULL, NULL, $user_id) as $review) {
+    foreach (Review::getReviews(NULL, NULL, NULL, NULL, $user_id) as $review) {
         $review->set('uid', $new_user_id);
         $review->save();
     }      
@@ -213,10 +222,10 @@ function multi_peer_review_entity_delete(EntityInterface $entity) {
 function multi_peer_review_rebuild() {
     
   // Rebuild cache data.
-  \Drupal\multi_peer_review\Entity\Reviewer::rebuildCachedFigures();  
-  \Drupal\multi_peer_review\Entity\Invitation::rebuildCachedFigures();  
-  \Drupal\multi_peer_review\Entity\Review::rebuildCachedFigures();
-  \Drupal\multi_peer_review\Entity\Paper::rebuildCachedFigures();
+  Reviewer::rebuildCachedFigures();  
+  Invitation::rebuildCachedFigures();  
+  Review::rebuildCachedFigures();
+  Paper::rebuildCachedFigures();
   
 }
 
@@ -251,33 +260,33 @@ function multi_peer_review_cron() {
             case MPRCommon::EXECUTION_INTERVAL_HOURLY:
                 
                 // Auto-retract expired Invitations.
-                Drupal\multi_peer_review\Entity\Invitation::autoRetractExpiredInvitations();
+                Invitation::autoRetractExpiredInvitations();
                 
                 // Send Invitation follow-ups.
-                Drupal\multi_peer_review\Entity\Invitation::sendInvitationFollowUps();
+                Invitation::sendInvitationFollowUps();
                 
                 // Review reminders:
                 // -Review start reminders.
                 // -Upcoming Review deadline reminders.
                 // -Review overdue reminders.
-                Drupal\multi_peer_review\Entity\Review::sendReviewReminders();
+                Review::sendReviewReminders();
                                                 
                 
 
                 break;          
             case MPRCommon::EXECUTION_INTERVAL_DAILY:
                 // Daily check of Search Engine Script functionality.
-                \Drupal\multi_peer_review\Entity\SES::runSelfTests();                
+                SES::runSelfTests();                
 
                 break;                
             case MPRCommon::EXECUTION_INTERVAL_SES:
                 // Search Engine Script cache building process.
                 
                 // Clear expired web request cache.
-                \Drupal\multi_peer_review\Entity\WebRequestCacheItem::deleteExpiredWebRequestCacheItems();  
+                WebRequestCacheItem::deleteExpiredWebRequestCacheItems();  
 
                 // Incrementally build search caches.
-                \Drupal\multi_peer_review\Entity\SES::buildSearchCaches();                
+                SES::buildSearchCaches();                
                 
                 break;            
         }
@@ -294,7 +303,7 @@ function multi_peer_review_cron() {
 /**
  * Implements hook_form_FORM_ID_alter().
  */
-function multi_peer_review_form_user_register_form_alter(&$form, \Drupal\Core\Form\FormStateInterface $form_state, $form_id) {
+function multi_peer_review_form_user_register_form_alter(&$form, FormStateInterface $form_state, $form_id) {
   
   // If a user is registering a new account, the roles control that is usually
   // hidden is made visible but modified to only show the Multi Peer Review Guest role.
@@ -306,13 +315,13 @@ function multi_peer_review_form_user_register_form_alter(&$form, \Drupal\Core\Fo
 
 
     $options = [
-        MPRCommon::ROLE_GUEST => new Drupal\Core\StringTranslation\TranslatableMarkup('Sign-up as Guest.'),
+        MPRCommon::ROLE_GUEST => new TranslatableMarkup('Sign-up as Guest.'),
     ];
 
     $form['account']['roles']['#title'] = '';
     $form['account']['roles']['#access'] = TRUE;
     $form['account']['roles']['#options'] = $options;
-    $form['account']['roles']['#description'] = new Drupal\Core\StringTranslation\TranslatableMarkup(
+    $form['account']['roles']['#description'] = new TranslatableMarkup(
             'Guests can upload papers for @site-name to peer review.', $placeholder_replacements);
   }  
     
@@ -328,7 +337,7 @@ function multi_peer_review_mail($key, &$message, $params)
     // Load valid keys. 
     // A key is valid if it matches one of the email template ids.
     // If you need to send emails that are not related to an email template, add the keys to $valid_keys.    
-    $valid_keys = \Drupal\multi_peer_review\Entity\EmailTemplate::getEmailTemplateIds();
+    $valid_keys = EmailTemplate::getEmailTemplateIds();
     
     
     // Check if the key is valid for this module.
diff --git a/multi_peer_review.token.inc b/multi_peer_review.token.inc
index e538f18..621f624 100644
--- a/multi_peer_review.token.inc
+++ b/multi_peer_review.token.inc
@@ -1,5 +1,10 @@
 <?php
 
+use Drupal\multi_peer_review\Entity\Invitation;
+use Drupal\multi_peer_review\Entity\Reviewer;
+use Drupal\multi_peer_review\Entity\Paper;
+use Drupal\Core\Render\BubbleableMetadata;
+use Drupal\Core\Field\FieldFilteredMarkup;
 use Drupal\multi_peer_review\MPRCommon;
 
 /* 
@@ -39,9 +44,9 @@ function multi_peer_review_token_info() {
    
    // Add token info from entities in this module.
    $entity_token_info = [];
-   array_push($entity_token_info, Drupal\multi_peer_review\Entity\Invitation::getTokenInfo());
-   array_push($entity_token_info, Drupal\multi_peer_review\Entity\Reviewer::getTokenInfo());
-   array_push($entity_token_info, Drupal\multi_peer_review\Entity\Paper::getTokenInfo());   
+   array_push($entity_token_info, Invitation::getTokenInfo());
+   array_push($entity_token_info, Reviewer::getTokenInfo());
+   array_push($entity_token_info, Paper::getTokenInfo());   
    
 
    // Merge types and tokens.
@@ -63,7 +68,7 @@ function multi_peer_review_token_info() {
 /**
 * Implements hook_tokens().
 */
-function multi_peer_review_tokens($type, $tokens, array $data, array $options, \Drupal\Core\Render\BubbleableMetadata $bubbleable_metadata) {
+function multi_peer_review_tokens($type, $tokens, array $data, array $options, BubbleableMetadata $bubbleable_metadata) {
    $replacements = [];
       
    if ($type == 'multi-peer-review') {
@@ -81,7 +86,7 @@ function multi_peer_review_tokens($type, $tokens, array $data, array $options, \
                      // that implements MarkupInterface.
                      // @see \Drupal\Component\Render\MarkupInterface
                      if (strpos($html, '<ul>') !== FALSE) {                         
-                         $html = \Drupal\Core\Field\FieldFilteredMarkup::create($html);
+                         $html = FieldFilteredMarkup::create($html);
                      }
                      
                      $replacements[$original] = $html;                 
@@ -108,4 +113,4 @@ function multi_peer_review_tokens($type, $tokens, array $data, array $options, \
    }
    
    return $replacements;
-}
\ No newline at end of file
+}
diff --git a/src/Controller/InvitationEmailLinkController.php b/src/Controller/InvitationEmailLinkController.php
index cb9c4ef..b8e8c94 100644
--- a/src/Controller/InvitationEmailLinkController.php
+++ b/src/Controller/InvitationEmailLinkController.php
@@ -34,7 +34,7 @@ class InvitationEmailLinkController extends UserController {
     $email_hash = \Drupal::request()->query->get('eml');
     if (MPRCommon::isValidEmailHash($email_hash) == TRUE) {
       
-      $invitations = \Drupal\multi_peer_review\Entity\Invitation::getInvitations(NULL, $email_hash);
+      $invitations = Invitation::getInvitations(NULL, $email_hash);
       if (count($invitations) == 1) {
         $invitation = current($invitations);
         $paper = $invitation->fabricateAndLoadPaper();
@@ -91,7 +91,7 @@ class InvitationEmailLinkController extends UserController {
     $email_hash = \Drupal::request()->query->get('eml');
     if (MPRCommon::isValidEmailHash($email_hash) == TRUE) {
       
-      $invitations = \Drupal\multi_peer_review\Entity\Invitation::getInvitations(NULL, $email_hash);
+      $invitations = Invitation::getInvitations(NULL, $email_hash);
       if (count($invitations) == 1) {
         $invitation = current($invitations);
         $paper = $invitation->fabricateAndLoadPaper();
diff --git a/src/Controller/ReviewEmailLinkController.php b/src/Controller/ReviewEmailLinkController.php
index 9bdbd1f..126d8f2 100644
--- a/src/Controller/ReviewEmailLinkController.php
+++ b/src/Controller/ReviewEmailLinkController.php
@@ -34,7 +34,7 @@ class ReviewEmailLinkController extends UserController {
     $email_hash = \Drupal::request()->query->get('eml');
     if (MPRCommon::isValidEmailHash($email_hash) == TRUE) {
         
-      $reviews = \Drupal\multi_peer_review\Entity\Review::getReviews(NULL, $email_hash);
+      $reviews = Review::getReviews(NULL, $email_hash);
       if (count($reviews) == 1) {
         $review = current($reviews);
         $invitation = $review->fabricateAndLoadInvitation();
@@ -93,7 +93,7 @@ class ReviewEmailLinkController extends UserController {
     $email_hash = \Drupal::request()->query->get('eml');
     if (MPRCommon::isValidEmailHash($email_hash) == TRUE) {
         
-      $reviews = \Drupal\multi_peer_review\Entity\Review::getReviews(NULL, $email_hash);
+      $reviews = Review::getReviews(NULL, $email_hash);
       if (count($reviews) == 1) {
         $review = current($reviews);
         $invitation = $review->fabricateAndLoadInvitation();
diff --git a/src/Controller/UserController.php b/src/Controller/UserController.php
index 750352b..8903015 100644
--- a/src/Controller/UserController.php
+++ b/src/Controller/UserController.php
@@ -18,7 +18,7 @@ abstract class UserController extends ControllerBase {
   /**
    * The renderer.
    *
-   * @var \Drupal\Core\Render\RendererInterface
+   * @var RendererInterface
    */
   protected $renderer;
   
@@ -26,7 +26,7 @@ abstract class UserController extends ControllerBase {
   /**
    * The form helper.
    *
-   * @var \Drupal\multi_peer_review\MPRFormHelper
+   * @var MPRFormHelper
    */
   private $helper;
  
@@ -34,7 +34,7 @@ abstract class UserController extends ControllerBase {
   /**
    * Constructs an UserController object.
    *
-   * @param \Drupal\Core\Render\RendererInterface $renderer
+   * @param RendererInterface $renderer
    *   The renderer.
    */
   public function __construct(RendererInterface $renderer) {
@@ -57,10 +57,10 @@ abstract class UserController extends ControllerBase {
   /**
    * Returns the Multi Peer Review form helper instance for this object.
    * The form helper will be initialised the first time this method is called.
-   * 
-   * @return \Drupal\multi_peer_review\MPRFormHelper
+   *
+   * @return MPRFormHelper
    *   The instance of this object's helper.
-   */    
+   */
   public function getHelper() {
     if (empty($this->helper) == TRUE) {
       $this->helper = new MPRFormHelper();
diff --git a/src/Controller/UserInvitationController.php b/src/Controller/UserInvitationController.php
index 51a7a56..931e533 100644
--- a/src/Controller/UserInvitationController.php
+++ b/src/Controller/UserInvitationController.php
@@ -49,7 +49,7 @@ class UserInvitationController extends UserController {
       $reviewer = Reviewer::getReviewerByOwner($user_id);        
       if (empty($reviewer) == FALSE) {      
 
-        $invitation = \Drupal\multi_peer_review\Entity\Invitation::load($invitation_id);
+        $invitation = Invitation::load($invitation_id);
         if (empty($invitation) == FALSE) {           
           // Check if the Invitation is related to the Reviewer.
           if ($invitation->getReviewer() == $reviewer->id()) {
diff --git a/src/Controller/UserReviewController.php b/src/Controller/UserReviewController.php
index c9284f1..7d1da18 100644
--- a/src/Controller/UserReviewController.php
+++ b/src/Controller/UserReviewController.php
@@ -2,6 +2,7 @@
 
 namespace Drupal\multi_peer_review\Controller;
 
+use Drupal\file\Entity\File;
 use Drupal\Core\Controller\ControllerBase;
 use Drupal\Core\Render\RendererInterface;
 use Drupal\Core\StringTranslation\TranslatableMarkup;
@@ -49,7 +50,7 @@ class UserReviewController extends UserController {
     $user_id = $current_route->getParameters()->get('user');
     
     if ((empty($review_id) == FALSE) && (empty($user_id) == FALSE)) {
-      $review = \Drupal\multi_peer_review\Entity\Review::load($review_id);
+      $review = Review::load($review_id);
       if (empty($review) == FALSE) {           
         // Check user access.
         if ($review->getOwnerId() == $user_id) {
@@ -243,10 +244,10 @@ class UserReviewController extends UserController {
 
               $file_links = [];
               foreach ($review->getEmailAttachments() as $fid) {
-                $file = \Drupal\file\Entity\File::load($fid);
+                $file = File::load($fid);
                 if (empty($file) == FALSE) {
                   $file_link = '<a target="_blank" href="' . 
-                      file_create_url($file->getFileUri()) . '">' . 
+                      \Drupal::service('file_url_generator')->generateAbsoluteString($file->getFileUri()) . '">' . 
                       htmlentities($file->getFilename()) . ' (' . format_size($file->getSize()) . ')</a>';
 
                   array_push($file_links, $file_link);
diff --git a/src/EmailTemplateListBuilder.php b/src/EmailTemplateListBuilder.php
index 9ac3e5d..ec8acf2 100644
--- a/src/EmailTemplateListBuilder.php
+++ b/src/EmailTemplateListBuilder.php
@@ -22,18 +22,18 @@ class EmailTemplateListBuilder extends EntityListBuilder {
   /**
    * The renderer service.
    *
-   * @var \Drupal\Core\Render\RendererInterface
+   * @var RendererInterface
    */
   protected $renderer;
 
   /**
    * Constructs a new EntityListBuilder object.
    *
-   * @param \Drupal\Core\Entity\EntityTypeInterface $entity_type
+   * @param EntityTypeInterface $entity_type
    *   The entity type definition.
-   * @param \Drupal\Core\Entity\EntityStorageInterface $storage
+   * @param EntityStorageInterface $storage
    *   The entity storage class.
-   * @param \Drupal\Core\Render\RendererInterface $renderer
+   * @param RendererInterface $renderer
    *   The renderer service.
    */
   public function __construct(EntityTypeInterface $entity_type, EntityStorageInterface $storage, RendererInterface $renderer) {
diff --git a/src/Entity/CommonContentEntityInterface.php b/src/Entity/CommonContentEntityInterface.php
index bb00827..7d14adf 100644
--- a/src/Entity/CommonContentEntityInterface.php
+++ b/src/Entity/CommonContentEntityInterface.php
@@ -2,6 +2,7 @@
 
 namespace Drupal\multi_peer_review\Entity;
 
+use Drupal\Core\StringTranslation\TranslatableMarkup;
 use Drupal\Core\Entity\ContentEntityInterface;
 use Drupal\Core\Entity\EntityChangedInterface;
 use Drupal\user\EntityOwnerInterface;
@@ -81,21 +82,21 @@ interface CommonContentEntityInterface extends ContentEntityInterface, EntityCha
   /**
    * Gets a translated label that is descriptive of this entity.
    *
-   * @return \Drupal\Core\StringTranslation\TranslatableMarkup
+   * @return TranslatableMarkup
    *   A label that describes this entity properly.
-   */  
+   */
   public function getTranslatedDescriptiveLabel();
   
 
   /**
    * Gets a translated message describing the entity or an action taken on this entity.
-   * 
+   *
    * @param int $message_type
    *   A message type such as MESSAGE_ADMIN_EDIT_TITLE.
    *
-   * @return \Drupal\Core\StringTranslation\TranslatableMarkup
+   * @return TranslatableMarkup
    *   A label that describes this entity properly.
-   */    
+   */
   public function getTranslatedMessage($message_type);
    
   
diff --git a/src/Entity/EmailCapableEntity.php b/src/Entity/EmailCapableEntity.php
index 0716405..7602166 100644
--- a/src/Entity/EmailCapableEntity.php
+++ b/src/Entity/EmailCapableEntity.php
@@ -1,6 +1,8 @@
 <?php
 namespace Drupal\multi_peer_review\Entity;
 
+use Drupal\file\Entity\File;
+use Drupal\Core\Form\FormStateInterface;
 use Drupal\Core\Entity\ContentEntityBase;
 use Drupal\Core\Entity\EntityChangedTrait;
 use Drupal\Core\Entity\EntityStorageInterface;
@@ -123,7 +125,7 @@ abstract class EmailCapableEntity extends CommonContentEntity implements EmailCa
           
           $attachments = [];
           foreach ($fids as $fid) {
-            $file = \Drupal\file\Entity\File::load($fid);
+            $file = File::load($fid);
 
             $file_info = [
                 'filecontent' => file_get_contents($file->getFileUri()),
@@ -250,7 +252,7 @@ abstract class EmailCapableEntity extends CommonContentEntity implements EmailCa
           foreach ($attachment_fids as $fid) {
               
             $add_to_usage = FALSE;
-            $file = \Drupal\file\Entity\File::load($fid);
+            $file = File::load($fid);
             if ($file != NULL) {                
                 $list = $file_usage->listUsage($file);
 
@@ -288,7 +290,7 @@ abstract class EmailCapableEntity extends CommonContentEntity implements EmailCa
       
       // Process file link removal queue
       foreach ($remove_fids as $fid) {
-        $file = \Drupal\file\Entity\File::load($fid);
+        $file = File::load($fid);
         if ($file != NULL) {
             $list = $file_usage->listUsage($file);
 
@@ -352,7 +354,7 @@ abstract class EmailCapableEntity extends CommonContentEntity implements EmailCa
         // Process existing attachments
         foreach ($attachment_fids as $fid) {
          
-          $file = \Drupal\file\Entity\File::load($fid);
+          $file = File::load($fid);
           if ($file != NULL) {              
               $list = $file_usage->listUsage($file);
 
@@ -394,7 +396,7 @@ abstract class EmailCapableEntity extends CommonContentEntity implements EmailCa
         $delete_file_usage = $value['delete_file_usage'];
         $delete_file = $value['delete_file'];
         
-        $file = \Drupal\file\Entity\File::load($fid);
+        $file = File::load($fid);
         if ($file != NULL) {           
             if ($delete_file_usage == TRUE) {
                 // @see Drupal\file\FileUsage\FileUsageInterface
@@ -416,9 +418,9 @@ abstract class EmailCapableEntity extends CommonContentEntity implements EmailCa
    *
    * @param array $form
    *   An associative array containing the structure of the form.
-   * @param \Drupal\Core\Form\FormStateInterface $form_state
+   * @param FormStateInterface $form_state
    *   The current state of the form.
-   */    
+   */
   public function validateFormProcess(&$form, $form_state) {
 
     // Converts the email attachment form field into a comma-delimited string.
diff --git a/src/Entity/Invitation.php b/src/Entity/Invitation.php
index 58a9406..194421f 100644
--- a/src/Entity/Invitation.php
+++ b/src/Entity/Invitation.php
@@ -187,7 +187,7 @@ class Invitation extends EmailCapableEntity implements InvitationInterface, MPRD
   public function fabricateAndLoadPaper() {
     $res = NULL;
     if (empty($this->paper) == FALSE) {      
-      $res = \Drupal\multi_peer_review\Entity\Paper::load($this->paper);                 
+      $res = Paper::load($this->paper);                 
     }
     return $res;
   }  
@@ -195,7 +195,7 @@ class Invitation extends EmailCapableEntity implements InvitationInterface, MPRD
   public function fabricateAndLoadReviewer() {
     $res = NULL;
     if (empty($this->reviewer) == FALSE) {      
-      $res = \Drupal\multi_peer_review\Entity\Reviewer::load($this->reviewer);                 
+      $res = Reviewer::load($this->reviewer);                 
     }
     return $res;
   }  
diff --git a/src/Entity/Paper.php b/src/Entity/Paper.php
index 404569a..a597457 100644
--- a/src/Entity/Paper.php
+++ b/src/Entity/Paper.php
@@ -2,6 +2,8 @@
 
 namespace Drupal\multi_peer_review\Entity;
 
+use Drupal\file\Entity\File;
+use Drupal\Core\Field\FieldFilteredMarkup;
 use Drupal\Core\Entity\ContentEntityBase;
 use Drupal\Core\Entity\EntityChangedTrait;
 use Drupal\Core\Entity\EntityStorageInterface;
@@ -204,11 +206,11 @@ class Paper extends CommonContentEntity implements PaperInterface {
     $file = $this->getFile();
     if (empty($file) == FALSE) {
         $fid = $file['target_id'];
-        $file = \Drupal\file\Entity\File::load($fid);
+        $file = File::load($fid);
         if (empty($file) == FALSE) {
 
             $res = '<a target="_blank" href="' . 
-                    file_create_url($file->getFileUri()) . '">' . 
+                    \Drupal::service('file_url_generator')->generateAbsoluteString($file->getFileUri()) . '">' . 
                     htmlentities($file->getFilename()) . ' (' . format_size($file->getSize()) . ')</a>';
 
         }
@@ -373,7 +375,7 @@ class Paper extends CommonContentEntity implements PaperInterface {
           
           $attachments = [];
           foreach ($fids as $fid) {
-            $file = \Drupal\file\Entity\File::load($fid);
+            $file = File::load($fid);
 
             $file_info = [
                 'filecontent' => file_get_contents($file->getFileUri()),
@@ -576,7 +578,7 @@ class Paper extends CommonContentEntity implements PaperInterface {
       
       // Process file link removal queue
       foreach ($remove_fids as $fid) {
-        $file = \Drupal\file\Entity\File::load($fid);
+        $file = File::load($fid);
         if ($file != NULL) {
             $list = $file_usage->listUsage($file);
 
@@ -633,7 +635,7 @@ class Paper extends CommonContentEntity implements PaperInterface {
     
     $fid = $this->file['target_id'];
     if ($fid != NULL) {
-        $file = \Drupal\file\Entity\File::load($fid);
+        $file = File::load($fid);
         $file_usage = \Drupal::service('file.usage');
         $list = $file_usage->listUsage($file);
 
@@ -941,7 +943,7 @@ class Paper extends CommonContentEntity implements PaperInterface {
             $res = $this->getOwner()->getDisplayName();
             break;   
         case 'abstract':                                         
-            $res = \Drupal\Core\Field\FieldFilteredMarkup::create($this->getAbstract());
+            $res = FieldFilteredMarkup::create($this->getAbstract());
             break;         
     }   
     return ($res);
diff --git a/src/Entity/Review.php b/src/Entity/Review.php
index a5829b6..ee0a343 100644
--- a/src/Entity/Review.php
+++ b/src/Entity/Review.php
@@ -362,7 +362,7 @@ class Review extends EmailCapableEntity implements ReviewInterface {
   public function fabricateAndLoadInvitation() {
     $res = NULL;
     if (empty($this->invitation) == FALSE) {      
-      $res = \Drupal\multi_peer_review\Entity\Invitation::load($this->invitation);                 
+      $res = Invitation::load($this->invitation);                 
     }
     return $res;
   }    
diff --git a/src/FilteredEntityListBuilder.php b/src/FilteredEntityListBuilder.php
index b6250d5..4a22255 100644
--- a/src/FilteredEntityListBuilder.php
+++ b/src/FilteredEntityListBuilder.php
@@ -2,6 +2,8 @@
 
 namespace Drupal\multi_peer_review;
 
+use Drupal\user\Entity\User;
+use Drupal\Core\Access\AccessResult;
 use Drupal\Component\Serialization\Json;
 use Drupal\Core\Ajax\AjaxHelperTrait;
 use Drupal\Core\Entity\EntityInterface;
@@ -32,10 +34,10 @@ abstract class FilteredEntityListBuilder extends EntityListBuilder {
     }
     else {
         if ($this->accessUser === NULL) {
-            $this->accessUser = \Drupal\user\Entity\User::load(\Drupal::currentUser()->id());            
+            $this->accessUser = User::load(\Drupal::currentUser()->id());            
         }
         
-        $res = \Drupal\Core\Access\AccessResult::allowedIfHasPermission($this->accessUser, $permission)->isAllowed();
+        $res = AccessResult::allowedIfHasPermission($this->accessUser, $permission)->isAllowed();
         $this->accessResults[$permission] = $res;        
     }
       
diff --git a/src/Form/CommonConfirmForm.php b/src/Form/CommonConfirmForm.php
index 894e5ab..7c3b402 100644
--- a/src/Form/CommonConfirmForm.php
+++ b/src/Form/CommonConfirmForm.php
@@ -2,6 +2,7 @@
 
 namespace Drupal\multi_peer_review\Form;
 
+use Drupal\Core\Entity\ContentEntityBase;
 use Drupal\Component\Datetime\TimeInterface;
 use Drupal\Core\Entity\ContentEntityConfirmFormBase;
 use Drupal\Core\Entity\EntityConstraintViolationListInterface;
@@ -27,14 +28,14 @@ abstract class CommonConfirmForm extends ContentEntityConfirmFormBase {
   /**
    * The related entity.
    *
-   * @var \Drupal\Core\Entity\ContentEntityBase
+   * @var ContentEntityBase
    */
   protected $entity;
 
   /**
    * The messenger service.
    *
-   * @var \Drupal\Core\Messenger\MessengerInterface
+   * @var MessengerInterface
    */
   protected $messenger;
 
@@ -42,7 +43,7 @@ abstract class CommonConfirmForm extends ContentEntityConfirmFormBase {
   /**
    * The form helper.
    *
-   * @var \Drupal\multi_peer_review\MPRFormHelper
+   * @var MPRFormHelper
    */
   private $helper;
 
@@ -51,10 +52,10 @@ abstract class CommonConfirmForm extends ContentEntityConfirmFormBase {
   /**
    * Returns the Multi Peer Review form helper instance for this object.
    * The form helper will be initialised the first time this method is called.
-   * 
-   * @return \Drupal\multi_peer_review\MPRFormHelper
+   *
+   * @return MPRFormHelper
    *   The instance of this object's helper.
-   */     
+   */
   public function getHelper() {
     if (empty($this->helper) == TRUE) {
         $this->helper = new MPRFormHelper();
diff --git a/src/Form/CommonContentEntityForm.php b/src/Form/CommonContentEntityForm.php
index a23f4d7..839ac0b 100644
--- a/src/Form/CommonContentEntityForm.php
+++ b/src/Form/CommonContentEntityForm.php
@@ -32,7 +32,7 @@ abstract class CommonContentEntityForm extends ContentEntityForm {
   /**
    * The messenger service.
    *
-   * @var \Drupal\Core\Messenger\MessengerInterface
+   * @var MessengerInterface
    */
   protected $messenger;    
   
@@ -40,7 +40,7 @@ abstract class CommonContentEntityForm extends ContentEntityForm {
   /**
    * The form helper.
    *
-   * @var \Drupal\multi_peer_review\MPRFormHelper
+   * @var MPRFormHelper
    */
   private $helper;
   
@@ -48,13 +48,13 @@ abstract class CommonContentEntityForm extends ContentEntityForm {
   /**
    * Constructs a new form.
    *
-   * @param \Drupal\Core\Entity\EntityRepositoryInterface $entity_repository
+   * @param EntityRepositoryInterface $entity_repository
    *   The entity repository service.
-   * @param \Drupal\Core\Messenger\MessengerInterface $messenger
+   * @param MessengerInterface $messenger
    *   The messenger service.
-   * @param \Drupal\Core\Entity\EntityTypeBundleInfoInterface $entity_type_bundle_info
+   * @param EntityTypeBundleInfoInterface $entity_type_bundle_info
    *   The entity type bundle service.
-   * @param \Drupal\Component\Datetime\TimeInterface $time
+   * @param TimeInterface $time
    *   The time service.
    */
   public function __construct(EntityRepositoryInterface $entity_repository, MessengerInterface $messenger, EntityTypeBundleInfoInterface $entity_type_bundle_info = NULL, TimeInterface $time = NULL) {
@@ -66,10 +66,10 @@ abstract class CommonContentEntityForm extends ContentEntityForm {
   /**
    * Returns the Multi Peer Review form helper instance for this object.
    * The form helper will be initialised the first time this method is called.
-   * 
-   * @return \Drupal\multi_peer_review\MPRFormHelper
+   *
+   * @return MPRFormHelper
    *   The instance of this object's helper.
-   */     
+   */
   public function getHelper() {
     if (empty($this->helper) == TRUE) {
         $this->helper = new MPRFormHelper();
diff --git a/src/Form/CommonFormBase.php b/src/Form/CommonFormBase.php
index f2e54b4..86f30da 100644
--- a/src/Form/CommonFormBase.php
+++ b/src/Form/CommonFormBase.php
@@ -2,6 +2,7 @@
 
 namespace Drupal\multi_peer_review\Form;
 
+use Drupal\Core\Messenger\MessengerInterface;
 use Drupal\Core\Form\FormBase;
 use Drupal\Core\Form\FormStateInterface;
 use Drupal\Component\Render\FormattableMarkup;
@@ -23,7 +24,7 @@ abstract class CommonFormBase extends FormBase {
   /**
    * The messenger service.
    *
-   * @var \Drupal\Core\Messenger\MessengerInterface
+   * @var MessengerInterface
    */
   protected $messenger;
 
@@ -31,7 +32,7 @@ abstract class CommonFormBase extends FormBase {
   /**
    * The form helper.
    *
-   * @var \Drupal\multi_peer_review\MPRFormHelper
+   * @var MPRFormHelper
    */
   private $helper;
 
@@ -49,10 +50,10 @@ abstract class CommonFormBase extends FormBase {
   /**
    * Returns the Multi Peer Review form helper instance for this object.
    * The form helper will be initialised the first time this method is called.
-   * 
-   * @return \Drupal\multi_peer_review\MPRFormHelper
+   *
+   * @return MPRFormHelper
    *   The instance of this object's helper.
-   */     
+   */
   public function getHelper() {
     if (empty($this->helper) == TRUE) {
         $this->helper = new MPRFormHelper();
diff --git a/src/Form/InvitationAcceptForm.php b/src/Form/InvitationAcceptForm.php
index 1ca8d22..8cf380d 100644
--- a/src/Form/InvitationAcceptForm.php
+++ b/src/Form/InvitationAcceptForm.php
@@ -2,6 +2,7 @@
 
 namespace Drupal\multi_peer_review\Form;
 
+use Drupal\multi_peer_review\MPREmail;
 use Drupal\Component\Datetime\TimeInterface;
 use Drupal\Core\Entity\ContentEntityConfirmFormBase;
 use Drupal\Core\Entity\EntityConstraintViolationListInterface;
@@ -27,13 +28,13 @@ class InvitationAcceptForm extends CommonConfirmForm implements InvitationFormIn
   /**
    * Constructs a new InvitationAcceptForm.
    *
-   * @param \Drupal\Core\Entity\EntityRepositoryInterface $entity_repository
+   * @param EntityRepositoryInterface $entity_repository
    *   The entity repository service.
-   * @param \Drupal\Core\Messenger\MessengerInterface $messenger
+   * @param MessengerInterface $messenger
    *   The messenger service.
-   * @param \Drupal\Core\Entity\EntityTypeBundleInfoInterface $entity_type_bundle_info
+   * @param EntityTypeBundleInfoInterface $entity_type_bundle_info
    *   The entity type bundle service.
-   * @param \Drupal\Component\Datetime\TimeInterface $time
+   * @param TimeInterface $time
    *   The time service.
    */
   public function __construct(EntityRepositoryInterface $entity_repository, MessengerInterface $messenger, EntityTypeBundleInfoInterface $entity_type_bundle_info = NULL, TimeInterface $time = NULL) {
@@ -152,13 +153,13 @@ class InvitationAcceptForm extends CommonConfirmForm implements InvitationFormIn
         
 
         // Reload Invitation.
-        $invitation = \Drupal\multi_peer_review\Entity\Invitation::load($invitation->id());
+        $invitation = Invitation::load($invitation->id());
 
 
         // Create pending Review.
         // Note that email_hash is set by EmailCapableEntity save().
         $new_review_id = MPRCommon::getNewCustomTextId();
-        $review = \Drupal\multi_peer_review\Entity\Review::create([
+        $review = Review::create([
             'id' => $new_review_id, 
             'label' => $new_review_id, 
             'invitation' => $invitation->id(),
@@ -175,7 +176,7 @@ class InvitationAcceptForm extends CommonConfirmForm implements InvitationFormIn
             $review = Review::load($new_review_id);
 
             // Send information to Reviewer.
-            $email = \Drupal\multi_peer_review\MPREmail::createFromEmailTemplate(
+            $email = MPREmail::createFromEmailTemplate(
                 'invitation_accepted_information', 
                 [$invitation, $paper, $reviewer, $review], 
                 $reviewer->getEmail(), 
@@ -189,7 +190,7 @@ class InvitationAcceptForm extends CommonConfirmForm implements InvitationFormIn
 
         
         // Send email to staff.
-        $email = \Drupal\multi_peer_review\MPREmail::createFromEmailTemplate(
+        $email = MPREmail::createFromEmailTemplate(
             'invitation_accepted', 
             [$invitation, $paper, $reviewer], 
             $invitation->getOwner()->getEmail(), 
@@ -205,13 +206,13 @@ class InvitationAcceptForm extends CommonConfirmForm implements InvitationFormIn
         Paper::rebuildCachedFigures($paper);            
         
         $invitation->logAction($this->logger('multi_peer_review'), 'accepted');
-        drupal_set_message(t('Thank you for accepting our invitation.'));  
+        $this->messenger()->addStatus(t('Thank you for accepting our invitation.'));  
          
         // Redirect the user to the home page. $status is typically SAVED_UPDATED.      
         $form_state->setRedirectUrl($this->getCancelUrl());           
     }
     else {
-        drupal_set_message(t('There was a technical issue accepting the invitation. Please contact us or try again later.'), 'error'); 
+        $this->messenger()->addError(t('There was a technical issue accepting the invitation. Please contact us or try again later.')); 
 
         // Form will be rebuilt.
         $form_state->setRebuild();        
diff --git a/src/Form/InvitationDeclineForm.php b/src/Form/InvitationDeclineForm.php
index 6f16b04..88b7a18 100644
--- a/src/Form/InvitationDeclineForm.php
+++ b/src/Form/InvitationDeclineForm.php
@@ -2,6 +2,7 @@
 
 namespace Drupal\multi_peer_review\Form;
 
+use Drupal\multi_peer_review\MPREmail;
 use Drupal\Component\Datetime\TimeInterface;
 use Drupal\Core\Entity\ContentEntityConfirmFormBase;
 use Drupal\Core\Entity\EntityConstraintViolationListInterface;
@@ -26,13 +27,13 @@ class InvitationDeclineForm extends CommonConfirmForm implements InvitationFormI
   /**
    * Constructs a new InvitationDeclineForm.
    *
-   * @param \Drupal\Core\Entity\EntityRepositoryInterface $entity_repository
+   * @param EntityRepositoryInterface $entity_repository
    *   The entity repository service.
-   * @param \Drupal\Core\Messenger\MessengerInterface $messenger
+   * @param MessengerInterface $messenger
    *   The messenger service.
-   * @param \Drupal\Core\Entity\EntityTypeBundleInfoInterface $entity_type_bundle_info
+   * @param EntityTypeBundleInfoInterface $entity_type_bundle_info
    *   The entity type bundle service.
-   * @param \Drupal\Component\Datetime\TimeInterface $time
+   * @param TimeInterface $time
    *   The time service.
    */
   public function __construct(EntityRepositoryInterface $entity_repository, MessengerInterface $messenger, EntityTypeBundleInfoInterface $entity_type_bundle_info = NULL, TimeInterface $time = NULL) {
@@ -140,7 +141,7 @@ class InvitationDeclineForm extends CommonConfirmForm implements InvitationFormI
         $invitation = Invitation::load($invitation->id());
         
 
-        $email = \Drupal\multi_peer_review\MPREmail::createFromEmailTemplate(
+        $email = MPREmail::createFromEmailTemplate(
             'invitation_declined', 
             [$invitation, $paper, $reviewer], 
             $invitation->getOwner()->getEmail(), 
@@ -153,13 +154,13 @@ class InvitationDeclineForm extends CommonConfirmForm implements InvitationFormI
         $email->send();  
         
         $invitation->logAction($this->logger('multi_peer_review'), 'declined');
-        drupal_set_message(t('Thank you for responding to our invitation.'));  
+        $this->messenger()->addStatus(t('Thank you for responding to our invitation.'));  
          
         // Redirect the user to the home page. $status is typically SAVED_UPDATED.      
         $form_state->setRedirectUrl($this->getCancelUrl());           
     }
     else {
-        drupal_set_message(t('There was a technical problem declining the invitation. Please contact us or try again later.'), 'error'); 
+        $this->messenger()->addError(t('There was a technical problem declining the invitation. Please contact us or try again later.')); 
 
         // Form will be rebuilt.
         $form_state->setRebuild();        
diff --git a/src/Form/InvitationForm.php b/src/Form/InvitationForm.php
index d8117f6..a2f0b78 100644
--- a/src/Form/InvitationForm.php
+++ b/src/Form/InvitationForm.php
@@ -2,6 +2,7 @@
 
 namespace Drupal\multi_peer_review\Form;
 
+use Drupal\multi_peer_review\Entity\EmailTemplate;
 use Drupal\Component\Datetime\TimeInterface;
 use Drupal\Core\Entity\ContentEntityForm;
 use Drupal\Core\Entity\EntityConstraintViolationListInterface;
@@ -71,7 +72,7 @@ class InvitationForm extends CommonContentEntityForm implements InvitationFormIn
     ];    
     
     if ($invitation->isNew() == TRUE) {
-        $email_template = \Drupal\multi_peer_review\Entity\EmailTemplate::load('invitation_new');
+        $email_template = EmailTemplate::load('invitation_new');
         $default_values = [
             'email_subject' => $email_template->getSubject(),
             'email_body' => $email_template->getBody(),            
diff --git a/src/Form/InvitationRetractForm.php b/src/Form/InvitationRetractForm.php
index 2510862..e35691f 100644
--- a/src/Form/InvitationRetractForm.php
+++ b/src/Form/InvitationRetractForm.php
@@ -2,6 +2,7 @@
 
 namespace Drupal\multi_peer_review\Form;
 
+use Drupal\multi_peer_review\MPREmail;
 use Drupal\Component\Datetime\TimeInterface;
 use Drupal\Core\Entity\ContentEntityConfirmFormBase;
 use Drupal\Core\Entity\EntityConstraintViolationListInterface;
@@ -34,20 +35,20 @@ class InvitationRetractForm extends ContentEntityConfirmFormBase implements Invi
   /**
    * The messenger service.
    *
-   * @var \Drupal\Core\Messenger\MessengerInterface
+   * @var MessengerInterface
    */
   protected $messenger;
 
   /**
    * Constructs a new InvitationRetractForm.
    *
-   * @param \Drupal\Core\Entity\EntityRepositoryInterface $entity_repository
+   * @param EntityRepositoryInterface $entity_repository
    *   The entity repository service.
-   * @param \Drupal\Core\Messenger\MessengerInterface $messenger
+   * @param MessengerInterface $messenger
    *   The messenger service.
-   * @param \Drupal\Core\Entity\EntityTypeBundleInfoInterface $entity_type_bundle_info
+   * @param EntityTypeBundleInfoInterface $entity_type_bundle_info
    *   The entity type bundle service.
-   * @param \Drupal\Component\Datetime\TimeInterface $time
+   * @param TimeInterface $time
    *   The time service.
    */
   public function __construct(EntityRepositoryInterface $entity_repository, MessengerInterface $messenger, EntityTypeBundleInfoInterface $entity_type_bundle_info = NULL, TimeInterface $time = NULL) {
@@ -284,7 +285,7 @@ class InvitationRetractForm extends ContentEntityConfirmFormBase implements Invi
     $paper = $invitation->fabricateAndLoadPaper();
     $reviewer = $invitation->fabricateAndLoadReviewer();
       
-    $email = \Drupal\multi_peer_review\MPREmail::createFromEmailTemplate(
+    $email = MPREmail::createFromEmailTemplate(
                 'invitation_retract',
                 [$invitation, $paper, $reviewer],
                 $reviewer->getEmail(),
@@ -302,7 +303,7 @@ class InvitationRetractForm extends ContentEntityConfirmFormBase implements Invi
     
 
     if ($email->send() == TRUE) {
-        drupal_set_message(t('The retraction has been sent.'));        
+        $this->messenger()->addStatus(t('The retraction has been sent.'));        
         $invitation->logAction($this->logger('multi_peer_review'), 'retracted');
         
         // Update Invitation status.
@@ -329,7 +330,7 @@ class InvitationRetractForm extends ContentEntityConfirmFormBase implements Invi
         
     } 
     else {
-        drupal_set_message(t('There was a problem sending the retraction. It has not been sent.'), 'error'); 
+        $this->messenger()->addError(t('There was a problem sending the retraction. It has not been sent.')); 
     }    
     
     
diff --git a/src/Form/InvitationSendForm.php b/src/Form/InvitationSendForm.php
index e25d7b7..e39d1d8 100644
--- a/src/Form/InvitationSendForm.php
+++ b/src/Form/InvitationSendForm.php
@@ -2,6 +2,7 @@
 
 namespace Drupal\multi_peer_review\Form;
 
+use Drupal\multi_peer_review\MPREmail;
 use Drupal\Component\Datetime\TimeInterface;
 use Drupal\Core\Entity\ContentEntityConfirmFormBase;
 use Drupal\Core\Entity\EntityConstraintViolationListInterface;
@@ -33,20 +34,20 @@ class InvitationSendForm extends ContentEntityConfirmFormBase implements Invitat
   /**
    * The messenger service.
    *
-   * @var \Drupal\Core\Messenger\MessengerInterface
+   * @var MessengerInterface
    */
   protected $messenger;
 
   /**
    * Constructs a new InvitationForm.
    *
-   * @param \Drupal\Core\Entity\EntityRepositoryInterface $entity_repository
+   * @param EntityRepositoryInterface $entity_repository
    *   The entity repository service.
-   * @param \Drupal\Core\Messenger\MessengerInterface $messenger
+   * @param MessengerInterface $messenger
    *   The messenger service.
-   * @param \Drupal\Core\Entity\EntityTypeBundleInfoInterface $entity_type_bundle_info
+   * @param EntityTypeBundleInfoInterface $entity_type_bundle_info
    *   The entity type bundle service.
-   * @param \Drupal\Component\Datetime\TimeInterface $time
+   * @param TimeInterface $time
    *   The time service.
    */
   public function __construct(EntityRepositoryInterface $entity_repository, MessengerInterface $messenger, EntityTypeBundleInfoInterface $entity_type_bundle_info = NULL, TimeInterface $time = NULL) {
@@ -124,7 +125,7 @@ class InvitationSendForm extends ContentEntityConfirmFormBase implements Invitat
       
     
     // Does not include the actual Paper.
-    $email = \Drupal\multi_peer_review\MPREmail::createFromEmailCapableEntity(
+    $email = MPREmail::createFromEmailCapableEntity(
         'invitation_new', 
         [$invitation, $paper, $reviewer], 
         $reviewer->getEmail(), 
@@ -135,7 +136,7 @@ class InvitationSendForm extends ContentEntityConfirmFormBase implements Invitat
 
     
     if ($email->send() == TRUE) {
-        drupal_set_message(t('The Invitation has been sent.'));             
+        $this->messenger()->addStatus(t('The Invitation has been sent.'));             
         $invitation->logAction($this->logger('multi_peer_review'), 'sent');
         
         // Update Invitation status.
@@ -163,7 +164,7 @@ class InvitationSendForm extends ContentEntityConfirmFormBase implements Invitat
         
     } 
     else {
-        drupal_set_message(t('There was a problem sending the Invitation. It has not been sent.'), 'error'); 
+        $this->messenger()->addError(t('There was a problem sending the Invitation. It has not been sent.')); 
     }    
     
     
diff --git a/src/Form/ReviewCancelForm.php b/src/Form/ReviewCancelForm.php
index b065f77..9191e01 100644
--- a/src/Form/ReviewCancelForm.php
+++ b/src/Form/ReviewCancelForm.php
@@ -2,6 +2,7 @@
 
 namespace Drupal\multi_peer_review\Form;
 
+use Drupal\multi_peer_review\MPREmail;
 use Drupal\Component\Datetime\TimeInterface;
 use Drupal\Core\Entity\ContentEntityConfirmFormBase;
 use Drupal\Core\Entity\EntityConstraintViolationListInterface;
@@ -28,13 +29,13 @@ class ReviewCancelForm extends CommonConfirmForm implements ReviewFormInterface
   /**
    * Constructs a new ReviewCancelForm.
    *
-   * @param \Drupal\Core\Entity\EntityRepositoryInterface $entity_repository
+   * @param EntityRepositoryInterface $entity_repository
    *   The entity repository service.
-   * @param \Drupal\Core\Messenger\MessengerInterface $messenger
+   * @param MessengerInterface $messenger
    *   The messenger service.
-   * @param \Drupal\Core\Entity\EntityTypeBundleInfoInterface $entity_type_bundle_info
+   * @param EntityTypeBundleInfoInterface $entity_type_bundle_info
    *   The entity type bundle service.
-   * @param \Drupal\Component\Datetime\TimeInterface $time
+   * @param TimeInterface $time
    *   The time service.
    */
   public function __construct(EntityRepositoryInterface $entity_repository, MessengerInterface $messenger, EntityTypeBundleInfoInterface $entity_type_bundle_info = NULL, TimeInterface $time = NULL) {
@@ -209,7 +210,7 @@ class ReviewCancelForm extends CommonConfirmForm implements ReviewFormInterface
         
         if ($this->isAdmin() == FALSE) {
             // Send an email to staff regarding the cancellation.
-            $email = \Drupal\multi_peer_review\MPREmail::createFromEmailTemplate(
+            $email = MPREmail::createFromEmailTemplate(
                 'review_cancelled_notify', 
                 [$invitation, $paper, $reviewer, $review],
                 $invitation->getOwner()->getEmail(), 
@@ -221,7 +222,7 @@ class ReviewCancelForm extends CommonConfirmForm implements ReviewFormInterface
         }        
         else {
             // Send an email to the Reviewer about the cancellation.
-            $email = \Drupal\multi_peer_review\MPREmail::createFromEmailTemplate(
+            $email = MPREmail::createFromEmailTemplate(
                 'review_cancelled',
                 [$invitation, $paper, $reviewer, $review],
                 $reviewer->getEmail(),
@@ -242,17 +243,17 @@ class ReviewCancelForm extends CommonConfirmForm implements ReviewFormInterface
         $review->logAction($this->logger('multi_peer_review'), 'cancelled');
 
         if ($this->isAdmin() == FALSE) {
-            drupal_set_message(t('Your review of %paper-title has been cancelled.', $placeholder_replacements));  
+            $this->messenger()->addStatus(t('Your review of %paper-title has been cancelled.', $placeholder_replacements));  
         }
         else {
-            drupal_set_message(t('The review of %paper-title has been cancelled.', $placeholder_replacements));  
+            $this->messenger()->addStatus(t('The review of %paper-title has been cancelled.', $placeholder_replacements));  
         }
          
         // Redirect the user.    
         $form_state->setRedirectUrl($this->getCancelUrl());           
     }
     else {
-        drupal_set_message(t('There was a technical problem cancelling the review. Please contact us or try again later.'), 'error'); 
+        $this->messenger()->addError(t('There was a technical problem cancelling the review. Please contact us or try again later.')); 
 
         // Form will be rebuilt.
         $form_state->setRebuild();        
diff --git a/src/Form/ReviewSubmitForm.php b/src/Form/ReviewSubmitForm.php
index cefe131..3ee4557 100644
--- a/src/Form/ReviewSubmitForm.php
+++ b/src/Form/ReviewSubmitForm.php
@@ -29,13 +29,13 @@ class ReviewSubmitForm extends CommonConfirmForm implements ReviewFormInterface
   /**
    * Constructs a new ReviewSubmitForm.
    *
-   * @param \Drupal\Core\Entity\EntityRepositoryInterface $entity_repository
+   * @param EntityRepositoryInterface $entity_repository
    *   The entity repository service.
-   * @param \Drupal\Core\Messenger\MessengerInterface $messenger
+   * @param MessengerInterface $messenger
    *   The messenger service.
-   * @param \Drupal\Core\Entity\EntityTypeBundleInfoInterface $entity_type_bundle_info
+   * @param EntityTypeBundleInfoInterface $entity_type_bundle_info
    *   The entity type bundle service.
-   * @param \Drupal\Component\Datetime\TimeInterface $time
+   * @param TimeInterface $time
    *   The time service.
    */
   public function __construct(EntityRepositoryInterface $entity_repository, MessengerInterface $messenger, EntityTypeBundleInfoInterface $entity_type_bundle_info = NULL, TimeInterface $time = NULL) {
@@ -174,10 +174,10 @@ class ReviewSubmitForm extends CommonConfirmForm implements ReviewFormInterface
     
     if ($status == SAVED_UPDATED) {
         if ($this->isAdmin() == TRUE) {
-            drupal_set_message(t('The Review has been submitted.')); 
+            $this->messenger()->addStatus(t('The Review has been submitted.')); 
         }
         else {
-            drupal_set_message(t('Thank you for submitting your review.')); 
+            $this->messenger()->addStatus(t('Thank you for submitting your review.')); 
         }
         
         $review->logAction($this->logger('multi_peer_review'), 'submitted');
@@ -226,10 +226,10 @@ class ReviewSubmitForm extends CommonConfirmForm implements ReviewFormInterface
     }
     else {
         if ($this->isAdmin() == TRUE) {
-            drupal_set_message(t('There was a problem submitting the Review. It has not been submitted.'), 'error'); 
+            $this->messenger()->addError(t('There was a problem submitting the Review. It has not been submitted.')); 
         }
         else {
-            drupal_set_message(t('There was a technical issue submitting your review. Please contact us or try again later.'), 'error'); 
+            $this->messenger()->addError(t('There was a technical issue submitting your review. Please contact us or try again later.')); 
         }
     }
                 
diff --git a/src/Form/ReviewerForm.php b/src/Form/ReviewerForm.php
index 99e187b..9bf1587 100644
--- a/src/Form/ReviewerForm.php
+++ b/src/Form/ReviewerForm.php
@@ -2,6 +2,7 @@
 
 namespace Drupal\multi_peer_review\Form;
 
+use Drupal\multi_peer_review\Entity\SearchResultItem;
 use Drupal\Component\Datetime\TimeInterface;
 use Drupal\Core\Entity\ContentEntityForm;
 use Drupal\Core\Entity\EntityConstraintViolationListInterface;
@@ -42,7 +43,7 @@ class ReviewerForm extends CommonContentEntityForm implements ReviewerFormInterf
         
         $copy_item = \Drupal::request()->query->get('copy_item');
         if (empty($copy_item) == FALSE) {
-            $copy =  \Drupal\multi_peer_review\Entity\SearchResultItem::load($copy_item);
+            $copy =  SearchResultItem::load($copy_item);
 
             $flexible_defaults['first_name'] = $copy->getFirstName();
             $flexible_defaults['last_name']  = $copy->getLastName();
@@ -189,7 +190,7 @@ class ReviewerForm extends CommonContentEntityForm implements ReviewerFormInterf
     if ($reviewer->isNew() == TRUE) {
         $copy_item = \Drupal::request()->query->get('copy_item');
         if (empty($copy_item) == FALSE) {
-            $copied_entity = \Drupal\multi_peer_review\Entity\SearchResultItem::load($copy_item);
+            $copied_entity = SearchResultItem::load($copy_item);
             
             $reviewer->set('source_search_result_item', $copy_item);
         } 
diff --git a/src/Form/ReviewerNewAccountForm.php b/src/Form/ReviewerNewAccountForm.php
index e9801f1..307bb49 100644
--- a/src/Form/ReviewerNewAccountForm.php
+++ b/src/Form/ReviewerNewAccountForm.php
@@ -2,6 +2,9 @@
 
 namespace Drupal\multi_peer_review\Form;
 
+use Drupal\Core\Access\AccessResult;
+use Drupal\user\Entity\User;
+use Drupal\multi_peer_review\MPREmail;
 use Drupal\Component\Datetime\TimeInterface;
 use Drupal\Core\Entity\ContentEntityConfirmFormBase;
 use Drupal\Core\Entity\EntityConstraintViolationListInterface;
@@ -34,20 +37,20 @@ class ReviewerNewAccountForm extends ContentEntityConfirmFormBase implements Rev
   /**
    * The messenger service.
    *
-   * @var \Drupal\Core\Messenger\MessengerInterface
+   * @var MessengerInterface
    */
   protected $messenger;
 
   /**
    * Constructs a new ReviewerNewAccountForm.
    *
-   * @param \Drupal\Core\Entity\EntityRepositoryInterface $entity_repository
+   * @param EntityRepositoryInterface $entity_repository
    *   The entity repository service.
-   * @param \Drupal\Core\Messenger\MessengerInterface $messenger
+   * @param MessengerInterface $messenger
    *   The messenger service.
-   * @param \Drupal\Core\Entity\EntityTypeBundleInfoInterface $entity_type_bundle_info
+   * @param EntityTypeBundleInfoInterface $entity_type_bundle_info
    *   The entity type bundle service.
-   * @param \Drupal\Component\Datetime\TimeInterface $time
+   * @param TimeInterface $time
    *   The time service.
    */
   public function __construct(EntityRepositoryInterface $entity_repository, MessengerInterface $messenger, EntityTypeBundleInfoInterface $entity_type_bundle_info = NULL, TimeInterface $time = NULL) {
@@ -182,7 +185,7 @@ class ReviewerNewAccountForm extends ContentEntityConfirmFormBase implements Rev
   public function buildForm(array $form, FormStateInterface $form_state) {
         
     // Access check.
-    if (\Drupal\Core\Access\AccessResult::allowedIfHasPermission($this->currentUser(), 'administer multi_peer_review_users_add')->isAllowed() == TRUE) {
+    if (AccessResult::allowedIfHasPermission($this->currentUser(), 'administer multi_peer_review_users_add')->isAllowed() == TRUE) {
         $res = parent::buildForm($form, $form_state);
     }
     else {
@@ -213,7 +216,7 @@ class ReviewerNewAccountForm extends ContentEntityConfirmFormBase implements Rev
         }
         else {
             // Check uniqueness.
-            foreach (\Drupal\user\Entity\User::loadMultiple() as $user_id => $user) {
+            foreach (User::loadMultiple() as $user_id => $user) {
                 if ($user->getUsername() == $username) {
                     $placeholder_replacements = ['%value' => $username];
                     $form_state->setErrorByName('username', $this->t('The username %value is already taken.', $placeholder_replacements));                    
@@ -248,7 +251,7 @@ class ReviewerNewAccountForm extends ContentEntityConfirmFormBase implements Rev
 
     
     $language = \Drupal::languageManager()->getCurrentLanguage()->getId();
-    $user = \Drupal\user\Entity\User::create();
+    $user = User::create();
     
     $user->setUsername($form['username']['#value']);
     $user->setPassword($form['password']['#value']);
@@ -286,7 +289,7 @@ class ReviewerNewAccountForm extends ContentEntityConfirmFormBase implements Rev
             }            
                     
                         
-            $email = \Drupal\multi_peer_review\MPREmail::createFromEmailTemplate(
+            $email = MPREmail::createFromEmailTemplate(
                     'reviewer_registered', 
                     [$reviewer], 
                     $reviewer->getEmail(), 
@@ -300,16 +303,16 @@ class ReviewerNewAccountForm extends ContentEntityConfirmFormBase implements Rev
             $reviewer->logAction($this->logger('multi_peer_review'), 'user account generated');
             
             if ($this->isAdmin() == TRUE) {
-                drupal_set_message(t('An account has been created successfully for %reviewer-full-name. An email has been sent to %reviewer-email.', $placeholder_replacements)); 
+                $this->messenger()->addStatus(t('An account has been created successfully for %reviewer-full-name. An email has been sent to %reviewer-email.', $placeholder_replacements)); 
             }
             else {
-                drupal_set_message(t('Thank you for registering an account with us. An email has been sent to %reviewer-email.', $placeholder_replacements)); 
+                $this->messenger()->addStatus(t('Thank you for registering an account with us. An email has been sent to %reviewer-email.', $placeholder_replacements)); 
             }
         }
         else {
             // A technical error has occurred.
             
-            drupal_set_message(t('There was a problem creating the account. It has not been created.'), 'error'); 
+            $this->messenger()->addError(t('There was a problem creating the account. It has not been created.')); 
             
             MPRCommon::sendEmailToTechnicalContact($this->t('A user account was generated but could not be attached to %reviewer-full-name. Please check for orphaned user accounts.', $placeholder_replacements));
         }
diff --git a/src/Form/SearchResultItemForm.php b/src/Form/SearchResultItemForm.php
index 584b93c..d19533f 100644
--- a/src/Form/SearchResultItemForm.php
+++ b/src/Form/SearchResultItemForm.php
@@ -28,20 +28,20 @@ class SearchResultItemForm extends ContentEntityForm implements SearchResultItem
   /**
    * The messenger service.
    *
-   * @var \Drupal\Core\Messenger\MessengerInterface
+   * @var MessengerInterface
    */
   protected $messenger;
 
   /**
    * Constructs a new SearchResultItemForm.
    *
-   * @param \Drupal\Core\Entity\EntityRepositoryInterface $entity_repository
+   * @param EntityRepositoryInterface $entity_repository
    *   The entity repository service.
-   * @param \Drupal\Core\Messenger\MessengerInterface $messenger
+   * @param MessengerInterface $messenger
    *   The messenger service.
-   * @param \Drupal\Core\Entity\EntityTypeBundleInfoInterface $entity_type_bundle_info
+   * @param EntityTypeBundleInfoInterface $entity_type_bundle_info
    *   The entity type bundle service.
-   * @param \Drupal\Component\Datetime\TimeInterface $time
+   * @param TimeInterface $time
    *   The time service.
    */
   public function __construct(EntityRepositoryInterface $entity_repository, MessengerInterface $messenger, EntityTypeBundleInfoInterface $entity_type_bundle_info = NULL, TimeInterface $time = NULL) {
diff --git a/src/Form/UserPaperForm.php b/src/Form/UserPaperForm.php
index 73f4bc5..4844d5c 100644
--- a/src/Form/UserPaperForm.php
+++ b/src/Form/UserPaperForm.php
@@ -2,6 +2,7 @@
 
 namespace Drupal\multi_peer_review\Form;
 
+use Drupal\user\Entity\User;
 use Drupal\Core\Form\FormBase;
 use Drupal\Core\Form\FormStateInterface;
 use Drupal\Component\Render\FormattableMarkup;
@@ -578,7 +579,7 @@ class UserPaperForm extends CommonFormBase {
             ->condition('status', 1)
             ->condition('roles', MPRCommon::ROLE_STAFF)
             ->execute();
-            $staff_users = \Drupal\user\Entity\User::loadMultiple($staff_user_ids);
+            $staff_users = User::loadMultiple($staff_user_ids);
             
             // Compile a list of email addresses.
             $recipients = [];
diff --git a/src/Form/WebRequestCacheItemForm.php b/src/Form/WebRequestCacheItemForm.php
index 07b303f..f3ee9a6 100644
--- a/src/Form/WebRequestCacheItemForm.php
+++ b/src/Form/WebRequestCacheItemForm.php
@@ -28,20 +28,20 @@ class WebRequestCacheItemForm extends ContentEntityForm implements WebRequestCac
   /**
    * The messenger service.
    *
-   * @var \Drupal\Core\Messenger\MessengerInterface
+   * @var MessengerInterface
    */
   protected $messenger;
 
   /**
    * Constructs a new WebRequestCacheItemForm.
    *
-   * @param \Drupal\Core\Entity\EntityRepositoryInterface $entity_repository
+   * @param EntityRepositoryInterface $entity_repository
    *   The entity repository service.
-   * @param \Drupal\Core\Messenger\MessengerInterface $messenger
+   * @param MessengerInterface $messenger
    *   The messenger service.
-   * @param \Drupal\Core\Entity\EntityTypeBundleInfoInterface $entity_type_bundle_info
+   * @param EntityTypeBundleInfoInterface $entity_type_bundle_info
    *   The entity type bundle service.
-   * @param \Drupal\Component\Datetime\TimeInterface $time
+   * @param TimeInterface $time
    *   The time service.
    */
   public function __construct(EntityRepositoryInterface $entity_repository, MessengerInterface $messenger, EntityTypeBundleInfoInterface $entity_type_bundle_info = NULL, TimeInterface $time = NULL) {
diff --git a/src/InvitationHtmlRouteProvider.php b/src/InvitationHtmlRouteProvider.php
index f39f78a..10156b1 100644
--- a/src/InvitationHtmlRouteProvider.php
+++ b/src/InvitationHtmlRouteProvider.php
@@ -29,15 +29,15 @@ class InvitationHtmlRouteProvider extends AdminHtmlRouteProvider {
     if ($send_form_route = $this->getSendFormRoute($entity_type)) {
       $collection->add("entity.{$entity_type_id}.send_form", $send_form_route);
     }
-    
+
     if ($retract_form_route = $this->getRetractFormRoute($entity_type)) {
       $collection->add("entity.{$entity_type_id}.retract_form", $retract_form_route);
     }    
-    
+
 //    if ($accept_form_route = $this->getAcceptFormRoute($entity_type)) {
 //      $collection->add("entity.{$entity_type_id}.accept_form", $accept_form_route);
 //    }    
-    
+
 //    if ($accept_form_route = $this->getSendFormRoute($entity_type)) {
 //      $collection->add("entity.{$entity_type_id}.cancel_form", $accept_form_route);
 //    }       
@@ -45,17 +45,17 @@ class InvitationHtmlRouteProvider extends AdminHtmlRouteProvider {
     return $collection;
   }
 
-  
-  
-  
+
+
+
 
   /**
    * Gets the send form route.
    *
-   * @param \Drupal\Core\Entity\EntityTypeInterface $entity_type
+   * @param EntityTypeInterface $entity_type
    *   The entity type.
    *
-   * @return \Symfony\Component\Routing\Route|null
+   * @return Route|null
    *   The generated route, if available.
    */
   protected function getSendFormRoute(EntityTypeInterface $entity_type) {
@@ -86,18 +86,18 @@ class InvitationHtmlRouteProvider extends AdminHtmlRouteProvider {
     }
   }
 
-  
-  
-  
-  
+
+
+
+
 
   /**
    * Gets the retract form route.
    *
-   * @param \Drupal\Core\Entity\EntityTypeInterface $entity_type
+   * @param EntityTypeInterface $entity_type
    *   The entity type.
    *
-   * @return \Symfony\Component\Routing\Route|null
+   * @return Route|null
    *   The generated route, if available.
    */
   protected function getRetractFormRoute(EntityTypeInterface $entity_type) {
@@ -127,9 +127,9 @@ class InvitationHtmlRouteProvider extends AdminHtmlRouteProvider {
       return $route;
     }
   }
-  
-  
-  
+
+
+
 //  
 //  /**
 //   * Gets the acceptance form route.
diff --git a/src/InvitationListBuilder.php b/src/InvitationListBuilder.php
index 9d7e7e7..5464db1 100644
--- a/src/InvitationListBuilder.php
+++ b/src/InvitationListBuilder.php
@@ -27,18 +27,18 @@ class InvitationListBuilder extends FilteredEntityListBuilder {
   /**
    * The renderer service.
    *
-   * @var \Drupal\Core\Render\RendererInterface
+   * @var RendererInterface
    */
   protected $renderer;
 
   /**
    * Constructs a new EntityListBuilder object.
    *
-   * @param \Drupal\Core\Entity\EntityTypeInterface $entity_type
+   * @param EntityTypeInterface $entity_type
    *   The entity type definition.
-   * @param \Drupal\Core\Entity\EntityStorageInterface $storage
+   * @param EntityStorageInterface $storage
    *   The entity storage class.
-   * @param \Drupal\Core\Render\RendererInterface $renderer
+   * @param RendererInterface $renderer
    *   The renderer service.
    */
   public function __construct(EntityTypeInterface $entity_type, EntityStorageInterface $storage, RendererInterface $renderer) {
diff --git a/src/MPRBlank.php b/src/MPRBlank.php
index 201141d..38b01d6 100644
--- a/src/MPRBlank.php
+++ b/src/MPRBlank.php
@@ -2,7 +2,7 @@
 
 namespace Drupal\multi_peer_review;
 
-
+use Drupal\multi_peer_review\Entity\SearchResultItem;
 /**
  * Defines a blank class used during development of the Multi Peer Review module.
  * Should be empty when used in production.
@@ -231,7 +231,7 @@ class MPRBlank extends MPRSearchEngineScript {
                 case "new":
                     $person["state"] = "cached";
 
-                    $search_result_item = \Drupal\multi_peer_review\Entity\SearchResultItem::create([
+                    $search_result_item = SearchResultItem::create([
                         "id" => $search_result_item_id,
                         "label" => $search_result_item_id . " " . $person["full_name"],
                         "full_name" => $person["full_name"],
@@ -252,7 +252,7 @@ class MPRBlank extends MPRSearchEngineScript {
                 case "updated":                                                
                     $person["state"] = "cached";
 
-                    $search_result_item = \Drupal\multi_peer_review\Entity\SearchResultItem::load($search_result_item_id);
+                    $search_result_item = SearchResultItem::load($search_result_item_id);
                     if (empty($search_result_item) == TRUE) {
                         /* Update the search result. */
 
diff --git a/src/MPRCommon.php b/src/MPRCommon.php
index b2e9857..834ebda 100644
--- a/src/MPRCommon.php
+++ b/src/MPRCommon.php
@@ -2,6 +2,10 @@
 
 namespace Drupal\multi_peer_review;
 
+use Drupal\Core\Url;
+use Drupal\multi_peer_review\Entity\WebRequestCacheItem;
+use Drupal\Component\Utility\Xss;
+use Drupal\file\Entity\File;
 use Drupal\Core\Field\BaseFieldDefinition;
 use Drupal\Core\StringTranslation\TranslatableMarkup;
 
@@ -969,7 +973,7 @@ class MPRCommon
         $config = \Drupal::config('system.site');        
         $placeholder_replacements[$placeholder_prefix . 'site-name'] = $config->get('name');        
         
-        $home = \Drupal\Core\Url::fromRoute('<front>');
+        $home = Url::fromRoute('<front>');
         $home->setAbsolute();
         $placeholder_replacements[$placeholder_prefix . 'site-url'] = $home->toString();
         
@@ -1011,7 +1015,7 @@ class MPRCommon
         
         // Get page source from the cache if possible and requested.
         if ($bypass_cache == FALSE) {
-            $cache_item = Entity\WebRequestCacheItem::load($url_hash);
+            $cache_item = WebRequestCacheItem::load($url_hash);
         }
         else {
             $cache_item = NULL;
@@ -1029,7 +1033,7 @@ class MPRCommon
                 
                 if ($bypass_cache == FALSE) {
                     // Store response in cache.
-                    $cache_item = Entity\WebRequestCacheItem::create([
+                    $cache_item = WebRequestCacheItem::create([
                         'id' => $url_hash,
                         'label' => $url_hash,
                         'external_url' => $external_url,
@@ -1134,7 +1138,7 @@ class MPRCommon
         $allowed_html_tags = MPRCommon::getDistinctValues($allowed_html_tags);
     }
     else {
-        $allowed_html_tags = \Drupal\Component\Utility\Xss::getAdminTagList();
+        $allowed_html_tags = Xss::getAdminTagList();
     }
     
     $res = $html;
@@ -1146,7 +1150,7 @@ class MPRCommon
     }
     
     // Filter HTML to remove cross-site scripts.
-    $res = \Drupal\Component\Utility\Xss::filter($res, $allowed_html_tags);
+    $res = Xss::filter($res, $allowed_html_tags);
     
     // Revert tokens in links to original values.
     // Example: href="[paper|title] to href="[paper:title]
@@ -1162,7 +1166,7 @@ class MPRCommon
     // Delete files used by the module.    
     $file_usage = \Drupal::service('file.usage');
     $file_system = \Drupal::service('file_system');
-    foreach (\Drupal\file\Entity\File::loadMultiple() as $file_entity) {      
+    foreach (File::loadMultiple() as $file_entity) {      
         
         // Only process files stored in the multi_peer_review directory.
         $file_uri = $file_entity->getFileUri();
diff --git a/src/MPREmail.php b/src/MPREmail.php
index 691badb..3c53cec 100644
--- a/src/MPREmail.php
+++ b/src/MPREmail.php
@@ -1,6 +1,7 @@
 <?php
 namespace Drupal\multi_peer_review;
 
+use Drupal\file\Entity\File;
 use Drupal\Core\Field\BaseFieldDefinition;
 use Drupal\Core\StringTranslation\TranslatableMarkup;
 use Drupal\multi_peer_review\Entity\EmailTemplate;
@@ -74,7 +75,7 @@ class MPREmail {
     if (empty($files) == FALSE) {
         foreach ($files as $fid => $file_item) {
           
-            $file = \Drupal\file\Entity\File::load($fid);
+            $file = File::load($fid);
 
             $file_info = [
                 'filecontent' => file_get_contents($file->getFileUri()),
diff --git a/src/MPRSearchEngineScript.php b/src/MPRSearchEngineScript.php
index 8de9fe0..1fa3319 100644
--- a/src/MPRSearchEngineScript.php
+++ b/src/MPRSearchEngineScript.php
@@ -2,6 +2,7 @@
 
 namespace Drupal\multi_peer_review;
 
+use Drupal\multi_peer_review\Entity\SearchResultItem;
 /**
  * @file
  * This file contains an abstract class that provides a template and helper methods for developing Search Engine Scripts that can be executed by the Multi Peer Review module.
@@ -9,7 +10,6 @@ namespace Drupal\multi_peer_review;
  * Example:
  * class YourSearchEngineScript extends \Drupal\multi_peer_review\MPRSearchEngineScript
  */
-
 /**
  * Class MPRSearchEngineScript
  * @package Drupal\multi_peer_review
@@ -170,11 +170,11 @@ abstract class MPRSearchEngineScript
     }
     
     public function getSearchResultItems() {
-        return Entity\SearchResultItem::getSearchResultItems($this->storage_handler->id());
+        return SearchResultItem::getSearchResultItems($this->storage_handler->id());
     }
     
     public function getSearchResultItemByRemoteId($remote_id) {
-        return Entity\SearchResultItem::loadByRemoteId($remote_id, $this->storage_handler->id());
+        return SearchResultItem::loadByRemoteId($remote_id, $this->storage_handler->id());
     }
     
     public function clearSearchCache() {
diff --git a/src/PaperListBuilder.php b/src/PaperListBuilder.php
index 9af1bd0..9763051 100644
--- a/src/PaperListBuilder.php
+++ b/src/PaperListBuilder.php
@@ -25,18 +25,18 @@ class PaperListBuilder extends FilteredEntityListBuilder {
   /**
    * The renderer service.
    *
-   * @var \Drupal\Core\Render\RendererInterface
+   * @var RendererInterface
    */
   protected $renderer;
 
   /**
    * Constructs a new EntityListBuilder object.
    *
-   * @param \Drupal\Core\Entity\EntityTypeInterface $entity_type
+   * @param EntityTypeInterface $entity_type
    *   The entity type definition.
-   * @param \Drupal\Core\Entity\EntityStorageInterface $storage
+   * @param EntityStorageInterface $storage
    *   The entity storage class.
-   * @param \Drupal\Core\Render\RendererInterface $renderer
+   * @param RendererInterface $renderer
    *   The renderer service.
    */
   public function __construct(EntityTypeInterface $entity_type, EntityStorageInterface $storage, RendererInterface $renderer) {
diff --git a/src/Plugin/Mail/HtmlEnabledPhpMail.php b/src/Plugin/Mail/HtmlEnabledPhpMail.php
index e6b054f..f295443 100644
--- a/src/Plugin/Mail/HtmlEnabledPhpMail.php
+++ b/src/Plugin/Mail/HtmlEnabledPhpMail.php
@@ -2,7 +2,7 @@
 
 namespace Drupal\multi_peer_review\Plugin\Mail;
 
-
+use Drupal\Core\Mail\Plugin\Mail\PhpMail;
 /**
  * Defines the default Multi Peer Review mail back-end, using PHP's native mail() function.
  *
@@ -12,7 +12,7 @@ namespace Drupal\multi_peer_review\Plugin\Mail;
  *   description = @Translation("Sends the message as HTML, using PHP's native mail() function, but without converting it to plain-text.")
  * )
  */
-class HtmlEnabledPhpMail extends \Drupal\Core\Mail\Plugin\Mail\PhpMail {
+class HtmlEnabledPhpMail extends PhpMail {
 
   
   /**
diff --git a/src/ReviewHtmlRouteProvider.php b/src/ReviewHtmlRouteProvider.php
index fbf4edd..c47c14e 100644
--- a/src/ReviewHtmlRouteProvider.php
+++ b/src/ReviewHtmlRouteProvider.php
@@ -42,10 +42,10 @@ class ReviewHtmlRouteProvider extends AdminHtmlRouteProvider {
   /**
    * Gets the submit form route.
    *
-   * @param \Drupal\Core\Entity\EntityTypeInterface $entity_type
+   * @param EntityTypeInterface $entity_type
    *   The entity type.
    *
-   * @return \Symfony\Component\Routing\Route|null
+   * @return Route|null
    *   The generated route, if available.
    */
   protected function getSubmitFormRoute(EntityTypeInterface $entity_type) {
@@ -81,10 +81,10 @@ class ReviewHtmlRouteProvider extends AdminHtmlRouteProvider {
   /**
    * Gets the cancel form route.
    *
-   * @param \Drupal\Core\Entity\EntityTypeInterface $entity_type
+   * @param EntityTypeInterface $entity_type
    *   The entity type.
    *
-   * @return \Symfony\Component\Routing\Route|null
+   * @return Route|null
    *   The generated route, if available.
    */
   protected function getCancelFormRoute(EntityTypeInterface $entity_type) {
diff --git a/src/ReviewListBuilder.php b/src/ReviewListBuilder.php
index 991ddfd..6fd74cf 100644
--- a/src/ReviewListBuilder.php
+++ b/src/ReviewListBuilder.php
@@ -26,18 +26,18 @@ class ReviewListBuilder extends FilteredEntityListBuilder {
   /**
    * The renderer service.
    *
-   * @var \Drupal\Core\Render\RendererInterface
+   * @var RendererInterface
    */
   protected $renderer;
 
   /**
    * Constructs a new EntityListBuilder object.
    *
-   * @param \Drupal\Core\Entity\EntityTypeInterface $entity_type
+   * @param EntityTypeInterface $entity_type
    *   The entity type definition.
-   * @param \Drupal\Core\Entity\EntityStorageInterface $storage
+   * @param EntityStorageInterface $storage
    *   The entity storage class.
-   * @param \Drupal\Core\Render\RendererInterface $renderer
+   * @param RendererInterface $renderer
    *   The renderer service.
    */
   public function __construct(EntityTypeInterface $entity_type, EntityStorageInterface $storage, RendererInterface $renderer) {
diff --git a/src/ReviewerHtmlRouteProvider.php b/src/ReviewerHtmlRouteProvider.php
index fcc35ac..1cbb380 100644
--- a/src/ReviewerHtmlRouteProvider.php
+++ b/src/ReviewerHtmlRouteProvider.php
@@ -38,10 +38,10 @@ class ReviewerHtmlRouteProvider extends AdminHtmlRouteProvider {
   /**
    * Gets the new account form route.
    *
-   * @param \Drupal\Core\Entity\EntityTypeInterface $entity_type
+   * @param EntityTypeInterface $entity_type
    *   The entity type.
    *
-   * @return \Symfony\Component\Routing\Route|null
+   * @return Route|null
    *   The generated route, if available.
    */
   protected function getNewAccountFormRoute(EntityTypeInterface $entity_type) {
diff --git a/src/ReviewerListBuilder.php b/src/ReviewerListBuilder.php
index c240c45..26e231e 100644
--- a/src/ReviewerListBuilder.php
+++ b/src/ReviewerListBuilder.php
@@ -29,18 +29,18 @@ class ReviewerListBuilder extends FilteredEntityListBuilder {
   /**
    * The renderer service.
    *
-   * @var \Drupal\Core\Render\RendererInterface
+   * @var RendererInterface
    */
   protected $renderer;
 
   /**
    * Constructs a new EntityListBuilder object.
    *
-   * @param \Drupal\Core\Entity\EntityTypeInterface $entity_type
+   * @param EntityTypeInterface $entity_type
    *   The entity type definition.
-   * @param \Drupal\Core\Entity\EntityStorageInterface $storage
+   * @param EntityStorageInterface $storage
    *   The entity storage class.
-   * @param \Drupal\Core\Render\RendererInterface $renderer
+   * @param RendererInterface $renderer
    *   The renderer service.
    */
   public function __construct(EntityTypeInterface $entity_type, EntityStorageInterface $storage, RendererInterface $renderer) {
diff --git a/src/SESListBuilder.php b/src/SESListBuilder.php
index 6e0628f..13c9748 100644
--- a/src/SESListBuilder.php
+++ b/src/SESListBuilder.php
@@ -22,18 +22,18 @@ class SESListBuilder extends EntityListBuilder {
   /**
    * The renderer service.
    *
-   * @var \Drupal\Core\Render\RendererInterface
+   * @var RendererInterface
    */
   protected $renderer;
 
   /**
    * Constructs a new EntityListBuilder object.
    *
-   * @param \Drupal\Core\Entity\EntityTypeInterface $entity_type
+   * @param EntityTypeInterface $entity_type
    *   The entity type definition.
-   * @param \Drupal\Core\Entity\EntityStorageInterface $storage
+   * @param EntityStorageInterface $storage
    *   The entity storage class.
-   * @param \Drupal\Core\Render\RendererInterface $renderer
+   * @param RendererInterface $renderer
    *   The renderer service.
    */
   public function __construct(EntityTypeInterface $entity_type, EntityStorageInterface $storage, RendererInterface $renderer) {
diff --git a/src/SearchResultItemListBuilder.php b/src/SearchResultItemListBuilder.php
index 54b5fe7..579fff1 100644
--- a/src/SearchResultItemListBuilder.php
+++ b/src/SearchResultItemListBuilder.php
@@ -29,18 +29,18 @@ class SearchResultItemListBuilder extends FilteredEntityListBuilder {
   /**
    * The renderer service.
    *
-   * @var \Drupal\Core\Render\RendererInterface
+   * @var RendererInterface
    */
   protected $renderer;
 
   /**
    * Constructs a new EntityListBuilder object.
    *
-   * @param \Drupal\Core\Entity\EntityTypeInterface $entity_type
+   * @param EntityTypeInterface $entity_type
    *   The entity type definition.
-   * @param \Drupal\Core\Entity\EntityStorageInterface $storage
+   * @param EntityStorageInterface $storage
    *   The entity storage class.
-   * @param \Drupal\Core\Render\RendererInterface $renderer
+   * @param RendererInterface $renderer
    *   The renderer service.
    */
   public function __construct(EntityTypeInterface $entity_type, EntityStorageInterface $storage, RendererInterface $renderer) {
diff --git a/tests/src/Functional/LoadTest.php b/tests/src/Functional/LoadTest.php
index 90c4970..40da758 100644
--- a/tests/src/Functional/LoadTest.php
+++ b/tests/src/Functional/LoadTest.php
@@ -2,6 +2,7 @@
 
 namespace Drupal\Tests\multi_peer_review\Functional;
 
+use Drupal\user\UserInterface;
 use Drupal\Core\Url;
 use Drupal\Tests\BrowserTestBase;
 
@@ -30,14 +31,14 @@ class LoadTest extends BrowserTestBase {
   /**
    * A user with permission to administer site configuration.
    *
-   * @var \Drupal\user\UserInterface
+   * @var UserInterface
    */
   protected $user;
 
   /**
    * {@inheritdoc}
    */
-  protected function setUp() {
+  protected function setUp(): void {
     parent::setUp();
     $this->user = $this->drupalCreateUser(['administer site configuration']);
     $this->drupalLogin($this->user);

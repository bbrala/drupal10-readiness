diff --git a/external_link_change.info.yml b/external_link_change.info.yml
index d0e1644..334d8a9 100644
--- a/external_link_change.info.yml
+++ b/external_link_change.info.yml
@@ -1,6 +1,5 @@
 name: External Link Change
 type: module
 description: Provides a filter to add prefix and suffix to external links.
-core: 8.x
-core_version_requirement: ^8 || ^9
+core_version_requirement: ^9.3 || ^10
 package: External link
diff --git a/external_link_change.module b/external_link_change.module
index c86f395..38abe1c 100644
--- a/external_link_change.module
+++ b/external_link_change.module
@@ -14,7 +14,7 @@ function external_link_change_help($route_name, RouteMatchInterface $route_match
   switch ($route_name) {
     // Main module help for the external_link_change module.
     case 'help.page.external_link_change':
-      $output = '<pre>' . file_get_contents(drupal_get_path('module', 'external_link_change') . '/README.md') . '</pre>';
+      $output = '<pre>' . file_get_contents(\Drupal::service('extension.list.module')->getPath('external_link_change') . '/README.md') . '</pre>';
       return $output;
 
     default:
diff --git a/tests/src/Functional/LoadTest.php b/tests/src/Functional/LoadTest.php
index 1f336d5..2173965 100644
--- a/tests/src/Functional/LoadTest.php
+++ b/tests/src/Functional/LoadTest.php
@@ -2,6 +2,7 @@
 
 namespace Drupal\Tests\external_link_change\Functional;
 
+use Drupal\user\UserInterface;
 use Drupal\Core\Url;
 use Drupal\Tests\BrowserTestBase;
 
@@ -22,14 +23,14 @@ class LoadTest extends BrowserTestBase {
   /**
    * A user with permission to administer site configuration.
    *
-   * @var \Drupal\user\UserInterface
+   * @var UserInterface
    */
   protected $user;
 
   /**
    * {@inheritdoc}
    */
-  protected function setUp() {
+  protected function setUp(): void {
     parent::setUp();
     $this->user = $this->drupalCreateUser(['administer site configuration']);
     $this->drupalLogin($this->user);
@@ -40,7 +41,7 @@ class LoadTest extends BrowserTestBase {
    */
   public function testLoad() {
     $this->drupalGet(Url::fromRoute('<front>'));
-    $this->assertResponse(200);
+    $this->assertSession()->statusCodeEquals(200);
   }
 
 }

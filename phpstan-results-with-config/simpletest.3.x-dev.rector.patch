diff --git a/simpletest.module b/simpletest.module
index 11d2eb5..025390e 100644
--- a/simpletest.module
+++ b/simpletest.module
@@ -62,7 +62,7 @@ function simpletest_theme() {
 function simpletest_js_alter(&$javascript, AttachedAssetsInterface $assets) {
   // Since SimpleTest is a special use case for the table select, stick the
   // SimpleTest JavaScript above the table select.
-  $simpletest = drupal_get_path('module', 'simpletest') . '/simpletest.js';
+  $simpletest = \Drupal::service('extension.list.module')->getPath('simpletest') . '/simpletest.js';
   if (array_key_exists($simpletest, $javascript) && array_key_exists('core/misc/tableselect.js', $javascript)) {
     $javascript[$simpletest]['weight'] = $javascript['core/misc/tableselect.js']['weight'] - 1;
   }
diff --git a/src/Cache/Context/TestDiscoveryCacheContext.php b/src/Cache/Context/TestDiscoveryCacheContext.php
index e3d5cf3..d0e669e 100644
--- a/src/Cache/Context/TestDiscoveryCacheContext.php
+++ b/src/Cache/Context/TestDiscoveryCacheContext.php
@@ -18,14 +18,14 @@ class TestDiscoveryCacheContext implements CacheContextInterface {
   /**
    * The test discovery service.
    *
-   * @var \Drupal\simpletest\TestDiscovery
+   * @var TestDiscovery
    */
   protected $testDiscovery;
 
   /**
    * The private key service.
    *
-   * @var \Drupal\Core\PrivateKey
+   * @var PrivateKey
    */
   protected $privateKey;
 
@@ -43,9 +43,9 @@ class TestDiscoveryCacheContext implements CacheContextInterface {
   /**
    * Construct a test discovery cache context.
    *
-   * @param \Drupal\simpletest\TestDiscovery $test_discovery
+   * @param TestDiscovery $test_discovery
    *   The test discovery service.
-   * @param \Drupal\Core\PrivateKey $private_key
+   * @param PrivateKey $private_key
    *   The private key service.
    */
   public function __construct(TestDiscovery $test_discovery, PrivateKey $private_key) {
diff --git a/src/EnvironmentCleanerFactory.php b/src/EnvironmentCleanerFactory.php
index dc48b50..3aeba54 100644
--- a/src/EnvironmentCleanerFactory.php
+++ b/src/EnvironmentCleanerFactory.php
@@ -17,14 +17,14 @@ class EnvironmentCleanerFactory {
   /**
    * The container.
    *
-   * @var \Drupal\Core\DependencyInjection\Container
+   * @var Container
    */
   protected $container;
 
   /**
    * Construct an environment cleaner factory.
    *
-   * @param \Drupal\Core\DependencyInjection\Container $container
+   * @param Container $container
    *   The container.
    */
   public function __construct(Container $container) {
@@ -34,7 +34,7 @@ class EnvironmentCleanerFactory {
   /**
    * Factory method to create the environment cleaner service.
    *
-   * @return \Drupal\simpletest\EnvironmentCleanerService
+   * @return EnvironmentCleanerService
    *   The environment cleaner service.
    */
   public function createCleaner() {
diff --git a/src/EnvironmentCleanerService.php b/src/EnvironmentCleanerService.php
index b4de578..85fe0c3 100644
--- a/src/EnvironmentCleanerService.php
+++ b/src/EnvironmentCleanerService.php
@@ -18,28 +18,28 @@ class EnvironmentCleanerService extends EnvironmentCleaner {
   /**
    * Messenger service.
    *
-   * @var \Drupal\Core\Messenger\MessengerInterface
+   * @var MessengerInterface
    */
   protected $messenger;
 
   /**
    * The translation service.
    *
-   * @var \Drupal\Core\StringTranslation\TranslationInterface
+   * @var TranslationInterface
    */
   protected $translation;
 
   /**
    * The config factory.
    *
-   * @var \Drupal\Core\Config\ConfigFactory
+   * @var ConfigFactory
    */
   protected $configFactory;
 
   /**
    * Default cache.
    *
-   * @var \Drupal\Core\Cache\CacheBackendInterface
+   * @var CacheBackendInterface
    */
   protected $cacheDefault;
 
@@ -48,12 +48,12 @@ class EnvironmentCleanerService extends EnvironmentCleaner {
    *
    * @param string $root
    *   The path to the root of the Drupal installation.
-   * @param \Drupal\Core\Database\Connection $test_database
+   * @param Connection $test_database
    *   Connection to the database against which tests were run.
-   * @param \Drupal\Core\Database\Connection $results_database
+   * @param Connection $results_database
    *   Connection to the database where test results were stored. This could be
    *   the same as $test_database, or it could be different.
-   * @param \Drupal\Core\StringTranslation\TranslationInterface|null $translation
+   * @param TranslationInterface|null $translation
    *   (optional) The translation service. If none is supplied, this class will
    *   attempt to discover one using \Drupal.
    */
diff --git a/src/Form/SimpletestResultsForm.php b/src/Form/SimpletestResultsForm.php
index adc1d4a..52040f6 100644
--- a/src/Form/SimpletestResultsForm.php
+++ b/src/Form/SimpletestResultsForm.php
@@ -34,14 +34,14 @@ class SimpletestResultsForm extends FormBase {
   /**
    * The database connection service.
    *
-   * @var \Drupal\Core\Database\Connection
+   * @var Connection
    */
   protected $database;
 
   /**
    * The environment cleaner service.
    *
-   * @var \Drupal\Core\Test\EnvironmentCleanerInterface
+   * @var EnvironmentCleanerInterface
    */
   protected $cleaner;
 
@@ -58,7 +58,7 @@ class SimpletestResultsForm extends FormBase {
   /**
    * Constructs a \Drupal\simpletest\Form\SimpletestResultsForm object.
    *
-   * @param \Drupal\Core\Database\Connection $database
+   * @param Connection $database
    *   The database connection service.
    */
   public function __construct(Connection $database, EnvironmentCleanerInterface $cleaner) {
diff --git a/src/Form/SimpletestTestForm.php b/src/Form/SimpletestTestForm.php
index 69f31e4..a7cb554 100644
--- a/src/Form/SimpletestTestForm.php
+++ b/src/Form/SimpletestTestForm.php
@@ -18,14 +18,14 @@ class SimpletestTestForm extends FormBase {
   /**
    * The renderer.
    *
-   * @var \Drupal\Core\Render\RendererInterface
+   * @var RendererInterface
    */
   protected $renderer;
 
   /**
    * The test discovery service.
    *
-   * @var \Drupal\simpletest\TestDiscovery
+   * @var TestDiscovery
    */
   protected $testDiscovery;
 
@@ -42,9 +42,9 @@ class SimpletestTestForm extends FormBase {
   /**
    * Constructs a new SimpletestTestForm.
    *
-   * @param \Drupal\Core\Render\RendererInterface $renderer
+   * @param RendererInterface $renderer
    *   The renderer.
-   * @param \Drupal\simpletest\TestDiscovery $test_discovery
+   * @param TestDiscovery $test_discovery
    *   The test discovery service.
    */
   public function __construct(RendererInterface $renderer, TestDiscovery $test_discovery) {

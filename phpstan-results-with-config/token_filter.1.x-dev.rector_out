modules/contrib/token_filter/src/Plugin/CKEditorPlugin/TokenBrowser.php
modules/contrib/token_filter/src/Plugin/Filter/TokenFilter.php
modules/contrib/token_filter/token_filter.module

2 files with changes
====================

1) modules/contrib/token_filter/src/Plugin/Filter/TokenFilter.php:31

    ---------- begin diff ----------
@@ @@
   /**
    * The token service.
    *
-   * @var \Drupal\Core\Utility\Token
+   * @var Token
    */
   protected $token;

@@ @@
   /**
    * The token entity mapper service.
    *
-   * @var \Drupal\token\TokenEntityMapperInterface
+   * @var TokenEntityMapperInterface
    */
   protected $tokenEntityMapper;

@@ @@
   /**
    * The renderer service.
    *
-   * @var \Drupal\Core\Render\RendererInterface
+   * @var RendererInterface
    */
   protected $renderer;

@@ @@
   /**
    * The route match service.
    *
-   * @var \Drupal\Core\Routing\RouteMatchInterface
+   * @var RouteMatchInterface
    */
   protected $routeMatch;

@@ @@
    *   The plugin_id for the plugin instance.
    * @param mixed $plugin_definition
    *   The plugin implementation definition.
-   * @param \Drupal\Core\Utility\Token $token
+   * @param Token $token
    *   The token service.
-   * @param \Drupal\token\TokenEntityMapperInterface $token_entity_mapper
+   * @param TokenEntityMapperInterface $token_entity_mapper
    *   The token entity mapper service.
-   * @param \Drupal\Core\Render\RendererInterface $renderer
+   * @param RendererInterface $renderer
    *   The renderer service.
-   * @param \Drupal\Core\Routing\RouteMatchInterface $current_route_match
+   * @param RouteMatchInterface $current_route_match
    *   The route match service.
    */
   public function __construct(array $configuration, $plugin_id, $plugin_definition, Token $token, TokenEntityMapperInterface $token_entity_mapper, RendererInterface $renderer, RouteMatchInterface $current_route_match) {
    ----------- end diff -----------

Applied rules:
 * ArgumentRemoverRector
 * PseudoNamespaceToNamespaceRector
 * WrapReturnRector
 * AddParamTypeDeclarationRector
 * ChangeMethodVisibilityRector


2) modules/contrib/token_filter/src/Plugin/CKEditorPlugin/TokenBrowser.php:29

    ---------- begin diff ----------
@@ @@
   /**
    * The CSRF token manager service.
    *
-   * @var Drupal\Core\Access\CsrfTokenGenerator
+   * @var CsrfTokenGenerator
    */
   protected $csrfTokenService;

@@ @@
   /**
    * {@inheritdoc}
    *
-   * @param Drupal\Core\Access\CsrfTokenGenerator $csrf_token_service
+   * @param CsrfTokenGenerator $csrf_token_service
    */
   public function __construct(
     array $configuration,
@@ @@
       'tokenbrowser' => [
         'id' => 'tokenbrowser',
         'label' => t('Token browser'),
-        'image' => file_create_url($this->getImage()),
+        'image' => \Drupal::service('file_url_generator')->generateAbsoluteString($this->getImage()),
         'link' => $this->getUrl($token_types)->toString(),
       ],
     ];
@@ @@
   /**
    * Fetches the URL.
    *
-   * @return Drupal\Core\Url
+   * @return Url
    *   The URL.
    *
    * @see TokenTreeController::outputTree().
@@ @@
    *   The string representation of the module's path.
    */
   protected function getModulePath(string $module_name): string {
-    return drupal_get_path('module', $module_name);
+    return \Drupal::service('extension.list.module')->getPath($module_name);
   }

   /**
    ----------- end diff -----------

Applied rules:
 * DrupalGetPathRector
 * FileCreateUrlRector
 * ArgumentRemoverRector
 * PseudoNamespaceToNamespaceRector
 * WrapReturnRector
 * AddParamTypeDeclarationRector
 * ChangeMethodVisibilityRector


 [OK] 2 files have been changed by Rector                                       


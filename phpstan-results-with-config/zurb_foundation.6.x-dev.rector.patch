diff --git a/inc/menu.inc b/inc/menu.inc
index 529901d..3c08436 100644
--- a/inc/menu.inc
+++ b/inc/menu.inc
@@ -1,5 +1,7 @@
 <?php
 
+use Drupal\Core\Block\BlockManager;
+use Drupal\language\Plugin\Block\LanguageBlock;
 /**
  * @file
  * Theme preprocess hooks related to menu elements.
@@ -129,9 +131,9 @@ function zurb_foundation_preprocess_menu__main(&$variables) {
     // Embed the Language switcher inside the Top Bar.
     if (\Drupal::moduleHandler()->moduleExists('language')) {
       if (theme_get_setting('zurb_foundation_top_bar_languageswitcher')) {
-        /** @var \Drupal\Core\Block\BlockManager $block_plugin_manager */
+        /** @var BlockManager $block_plugin_manager */
         $block_plugin_manager = \Drupal::service('plugin.manager.block');
-        /** @var \Drupal\language\Plugin\Block\LanguageBlock $language_block */
+        /** @var LanguageBlock $language_block */
         $language_block = $block_plugin_manager->createInstance('language_block:language_interface', []);
         $variables['top_bar_languageswitcher'] = $language_block->build();
       }
diff --git a/inc/zurb_foundation.drush.inc b/inc/zurb_foundation.drush.inc
index d76bd76..d24e54b 100644
--- a/inc/zurb_foundation.drush.inc
+++ b/inc/zurb_foundation.drush.inc
@@ -44,7 +44,7 @@ function drush_zurb_foundation_foundation_sub_theme($name = NULL, $machine_name
   // Eliminate hyphens.
   $machine_name = str_replace('-', '_', $machine_name);
 
-  $zurb_path = drush_get_context('DRUSH_DRUPAL_ROOT') . '/' . drupal_get_path('theme', 'zurb_foundation');
+  $zurb_path = drush_get_context('DRUSH_DRUPAL_ROOT') . '/' . \Drupal::service('extension.list.theme')->getPath('zurb_foundation');
   $subtheme_path = explode('/', $zurb_path);
   array_pop($subtheme_path);
   $subtheme_path = implode('/', $subtheme_path) . '/' . $machine_name;
diff --git a/zurb_foundation.theme b/zurb_foundation.theme
index 5309711..f01f888 100644
--- a/zurb_foundation.theme
+++ b/zurb_foundation.theme
@@ -306,7 +306,7 @@ function zurb_foundation_preprocess_page(&$variables) {
 function zurb_foundation_css_alter(&$css) {
   // Remove base theme CSS.
   if (theme_get_setting('zurb_foundation_disable_base_css') == TRUE) {
-    $theme_path = drupal_get_path('theme', 'zurb_foundation');
+    $theme_path = \Drupal::service('extension.list.theme')->getPath('zurb_foundation');
 
     foreach ($css as $path => $values) {
       if (strpos((string) $values['data'], $theme_path) === 0) {
@@ -321,7 +321,7 @@ function zurb_foundation_css_alter(&$css) {
  */
 function zurb_foundation_js_alter(&$js) {
   // Remove base theme JS.
-  $theme_path = drupal_get_path('theme', 'zurb_foundation');
+  $theme_path = \Drupal::service('extension.list.theme')->getPath('zurb_foundation');
   if (theme_get_setting('zurb_foundation_disable_base_js') == TRUE) {
 
     foreach ($js as $path => $values) {

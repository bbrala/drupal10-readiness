modules/contrib/baguettebox/baguettebox.install
modules/contrib/baguettebox/baguettebox.module
modules/contrib/baguettebox/src/Plugin/Field/FieldFormatter/BaguetteboxFormatter.php

1 file with changes
===================

1) modules/contrib/baguettebox/src/Plugin/Field/FieldFormatter/BaguetteboxFormatter.php:1

    ---------- begin diff ----------
@@ @@

 namespace Drupal\baguettebox\Plugin\Field\FieldFormatter;

+use Drupal\image\ImageStyleInterface;
+use Drupal\file\FileInterface;
 use Drupal\Core\Cache\Cache;
 use Drupal\Core\Field\FieldItemListInterface;
 use Drupal\Core\Form\FormStateInterface;
@@ @@

     // Prepare image styles.
     if ($settings['baguette_image_style']) {
-      /** @var \Drupal\image\ImageStyleInterface $baguette_image_style */
+      /** @var ImageStyleInterface $baguette_image_style */
       $baguette_image_style = $this->imageStyleStorage->load($settings['baguette_image_style']);
     }

@@ @@
       }
     }

-    /** @var \Drupal\file\FileInterface[] $files */
+    /** @var FileInterface[] $files */
     foreach ($files as $delta => $file) {
       $cache_contexts = [];
       $cache_contexts[] = 'url.site';
@@ @@
       unset($item->_attributes);

       $image_uri = $file->getFileUri();
-      $default_url = file_create_url($image_uri);
+      $default_url = \Drupal::service('file_url_generator')->generateAbsoluteString($image_uri);
       $link_attributes = [];
       foreach ($baguette_image_style_responsive as $width => $image_style) {
         $link_attributes['data-at-' . $width] = $image_style ? $image_style->buildUrl($image_uri) : $default_url;
    ----------- end diff -----------

Applied rules:
 * FileCreateUrlRector
 * ArgumentRemoverRector
 * PseudoNamespaceToNamespaceRector
 * WrapReturnRector
 * AddParamTypeDeclarationRector
 * ChangeMethodVisibilityRector


 [OK] 1 file has been changed by Rector                                         


diff --git a/src/Controller/ListenerPageController.php b/src/Controller/ListenerPageController.php
index f0ae0a0..e93c6f7 100644
--- a/src/Controller/ListenerPageController.php
+++ b/src/Controller/ListenerPageController.php
@@ -14,10 +14,10 @@ class ListenerPageController {
   /**
    * Check for page access. Only if listening is set to true.
    *
-   * @param \Drupal\Core\Session\AccountInterface $account
+   * @param AccountInterface $account
    *   Current User account.
    *
-   * @return \Drupal\Core\Access\AccessResult
+   * @return AccessResult
    *   Access result fo rthis controller.
    */
   public function access(AccountInterface $account) {
@@ -29,7 +29,7 @@ class ListenerPageController {
   /**
    * Renders the content for a dashboard that is listening.
    *
-   * @return Symfony\Component\HttpFoundation\JsonResponse
+   * @return JsonResponse
    *   A json response representing the content.
    */
   public function content() {
@@ -61,7 +61,7 @@ class ListenerPageController {
    * @param string $type
    *   One of "error" or "status".
    *
-   * @return Symfony\Component\HttpFoundation\JsonResponse
+   * @return JsonResponse
    *   Message and Type wrapped in a json structure.
    */
   public function jsonResponse($message, $type) {
diff --git a/src/UpdateHelper.php b/src/UpdateHelper.php
index aed4c9e..0d47078 100644
--- a/src/UpdateHelper.php
+++ b/src/UpdateHelper.php
@@ -22,28 +22,28 @@ class UpdateHelper implements UpdateHelperInterface {
   /**
    * Configuration Factory service.
    *
-   * @var \Drupal\Core\Config\ConfigFactory
+   * @var ConfigFactory
    */
   protected $configFactory;
 
   /**
    * Module Handler service.
    *
-   * @var Drupal\Core\Extension\ModuleHandlerInterface
+   * @var ModuleHandlerInterface
    */
   protected $moduleHandler;
 
   /**
    * Theme Handler service.
    *
-   * @var \Drupal\Core\Extension\ThemeHandlerInterface
+   * @var ThemeHandlerInterface
    */
   protected $themeHandler;
 
   /**
    * Messenger Service.
    *
-   * @var \Drupal\Core\Messenger\MessengerInterface
+   * @var MessengerInterface
    */
   protected $messenger;
 
@@ -111,7 +111,7 @@ class UpdateHelper implements UpdateHelperInterface {
         $sender_data['updates'][$module] = $data[$module];
         // In some cases (like multisite installations),
         // modules on certain paths are considered unimportant.
-        $sender_data['updates'][$module]['module_path'] = str_replace('/' . $module, '', drupal_get_path('module', $module));
+        $sender_data['updates'][$module]['module_path'] = str_replace('/' . $module, '', \Drupal::service('extension.list.module')->getPath($module));
       }
     }
 

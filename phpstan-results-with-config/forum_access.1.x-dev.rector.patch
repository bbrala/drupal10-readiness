diff --git a/forum_access.install b/forum_access.install
index a62afad..71542e4 100644
--- a/forum_access.install
+++ b/forum_access.install
@@ -1,5 +1,6 @@
 <?php
 
+use Drupal\Core\Entity\EntityInterface;
 /**
  * @file
  * Forum Access install file.
@@ -58,7 +59,7 @@ function forum_access_install() {
   // which understands only integer as gid.
   $config = \Drupal::configFactory()->getEditable('forum_access.settings');
   $roles_gids = [];
-  /** @var Drupal\Core\Entity\EntityInterface[] $roles */
+  /** @var EntityInterface[] $roles */
   $roles = Role::loadMultiple();
   $i = 1;
   foreach ($roles as $role) {
diff --git a/forum_access.module b/forum_access.module
index cf8e699..aeed460 100644
--- a/forum_access.module
+++ b/forum_access.module
@@ -1,5 +1,6 @@
 <?php
 
+use Drupal\node\NodeAccessControlHandlerInterface;
 /**
  * @file
  * Contains forum_access.module.
@@ -226,7 +227,7 @@ function forum_access_node_insert(EntityInterface $entity) {
   // To preserve database integrity, only write grants if the node
   // loads successfully.
   if (!empty($node)) {
-    /** @var \Drupal\node\NodeAccessControlHandlerInterface $access_control_handler */
+    /** @var NodeAccessControlHandlerInterface $access_control_handler */
     $access_control_handler = \Drupal::entityTypeManager()->getAccessControlHandler('node');
     $grants = $access_control_handler->acquireGrants($node);
     \Drupal::service('node.grant_storage')->write($node, $grants);
@@ -777,9 +778,9 @@ function forum_access_set_settings($tid, array $settings) {
 /**
  * Get list of available tids for specific account.
  *
- * @param \Drupal\Core\Session\AccountInterface $account
+ * @param AccountInterface $account
  *   The user object for the user whose access is being checked.
- * @param \Drupal\node\NodeInterface $node
+ * @param NodeInterface $node
  *   The node to check.
  *
  * @return array
diff --git a/includes/forum_access.admin.inc b/includes/forum_access.admin.inc
index 71c1336..3eb81d4 100644
--- a/includes/forum_access.admin.inc
+++ b/includes/forum_access.admin.inc
@@ -1,5 +1,6 @@
 <?php
 
+use Drupal\node\NodeAccessControlHandlerInterface;
 /**
  * @file
  * Contains integrations with forms.
@@ -78,7 +79,7 @@ function _forum_access_update_access_records($form_state) {
           ->fetchField();
         $batch = [
           'title' => t('Updating content access permissions'),
-          'file' => drupal_get_path('module', 'forum_access') . '/includes/forum_access.admin.inc',
+          'file' => \Drupal::service('extension.list.module')->getPath('forum_access') . '/includes/forum_access.admin.inc',
           'operations' => [
             ['_forum_access_update_batch_operation', [$tid, $limit, $count]],
           ],
@@ -121,7 +122,7 @@ function _forum_access_update_batch_operation($tid, $limit, $count, &$context) {
     // To preserve database integrity, only write grants if the node
     // loads successfully.
     if (!empty($node)) {
-      /** @var \Drupal\node\NodeAccessControlHandlerInterface $access_control_handler */
+      /** @var NodeAccessControlHandlerInterface $access_control_handler */
       $access_control_handler = \Drupal::entityTypeManager()
         ->getAccessControlHandler('node');
       $grants = $access_control_handler->acquireGrants($node);

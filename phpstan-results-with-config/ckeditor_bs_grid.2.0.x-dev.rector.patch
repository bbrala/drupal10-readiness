diff --git a/ckeditor_bs_grid.info.yml b/ckeditor_bs_grid.info.yml
index a8d255f..e80e8d8 100644
--- a/ckeditor_bs_grid.info.yml
+++ b/ckeditor_bs_grid.info.yml
@@ -1,7 +1,7 @@
 name: CKEditor Bootstrap Grid
 type: module
 description: Ckeditor plugin for adding Bootstrap grids inline
-core_version_requirement: ^8 || ^9
+core_version_requirement: ^9.3 || ^10
 package: CKEditor
 dependencies:
   - drupal:editor
diff --git a/ckeditor_bs_grid.install b/ckeditor_bs_grid.install
index b92ce3b..297b5d7 100644
--- a/ckeditor_bs_grid.install
+++ b/ckeditor_bs_grid.install
@@ -1,5 +1,6 @@
 <?php
 
+use Drupal\editor\EditorInterface;
 /**
  * @file
  * Install, update and uninstall functions for the ckeditor_bs_grid module.
@@ -12,14 +13,14 @@ use Drupal\Core\Config\FileStorage;
  */
 function ckeditor_bs_grid_update_8201() {
   // Load the entire install file.
-  $config_path = drupal_get_path('module', 'ckeditor_bs_grid') . '/config/install';
+  $config_path = \Drupal::service('extension.list.module')->getPath('ckeditor_bs_grid') . '/config/install';
   $source = new FileStorage($config_path);
   $config_storage = \Drupal::service('config.storage');
   $config_storage->write('ckeditor_bs_grid.settings', $source->read('ckeditor_bs_grid.settings'));
 
   // Load editors with BS Grid enabled and add default options.
   $editor_config = \Drupal::entityTypeManager()->getStorage('editor')->loadMultiple();
-  /** @var \Drupal\editor\EditorInterface $editor */
+  /** @var EditorInterface $editor */
   foreach ($editor_config as $editor) {
     $settings = $editor->getSettings();
     if (isset($settings['plugins']['bs_grid'])) {
diff --git a/src/Form/GridDialog.php b/src/Form/GridDialog.php
index 5bb43b5..c48a644 100644
--- a/src/Form/GridDialog.php
+++ b/src/Form/GridDialog.php
@@ -25,14 +25,14 @@ class GridDialog extends FormBase {
   /**
    * The form builder.
    *
-   * @var \Drupal\Core\Form\FormBuilderInterface
+   * @var FormBuilderInterface
    */
   protected $formBuilder;
 
   /**
    * GridDialog class initialize..
    *
-   * @param \Drupal\Core\Form\FormBuilderInterface $form_builder
+   * @param FormBuilderInterface $form_builder
    *   The form builder.
    */
   public function __construct(FormBuilderInterface $form_builder) {
@@ -112,7 +112,7 @@ class GridDialog extends FormBase {
    *
    * @param array $form
    *   The form.
-   * @param \Drupal\Core\Form\FormStateInterface $form_state
+   * @param FormStateInterface $form_state
    *   The form state.
    *
    * @return array
@@ -126,7 +126,7 @@ class GridDialog extends FormBase {
     $available_cols = array_filter($settings['editor_settings']['available_columns']);
     foreach ($available_cols as $column) {
       $title = $this->t('Column @num', ['@num' => $column]);
-      $img = drupal_get_path('module', 'ckeditor_bs_grid') . '/images/ui/col_' . $column . '.png';
+      $img = \Drupal::service('extension.list.module')->getPath('ckeditor_bs_grid') . '/images/ui/col_' . $column . '.png';
       $img_src = '<img src="/' . $img . '" title="' . $title . '" />';
       $columns[$column] = $img_src . '<p>' . $this->t('Column @num', ['@num' => $column]) . '</p>';
     }
@@ -180,7 +180,7 @@ class GridDialog extends FormBase {
    *
    * @param array $form
    *   The form.
-   * @param \Drupal\Core\Form\FormStateInterface $form_state
+   * @param FormStateInterface $form_state
    *   The form state.
    *
    * @return array
@@ -321,7 +321,7 @@ class GridDialog extends FormBase {
    *
    * @param array $form
    *   The form.
-   * @param \Drupal\Core\Form\FormStateInterface $form_state
+   * @param FormStateInterface $form_state
    *   The form state.
    *
    * @return array
@@ -447,10 +447,10 @@ class GridDialog extends FormBase {
    *
    * @param array $form
    *   The form.
-   * @param \Drupal\Core\Form\FormStateInterface $form_state
+   * @param FormStateInterface $form_state
    *   The form state.
    *
-   * @return \Drupal\Core\Ajax\AjaxResponse
+   * @return AjaxResponse
    *   An ajax response.
    */
   public function submitStep(array &$form, FormStateInterface $form_state) {
@@ -480,10 +480,10 @@ class GridDialog extends FormBase {
    *
    * @param array $form
    *   The form.
-   * @param \Drupal\Core\Form\FormStateInterface $form_state
+   * @param FormStateInterface $form_state
    *   The form state.
    *
-   * @return \Drupal\Core\Ajax\AjaxResponse
+   * @return AjaxResponse
    *   An ajax response.
    */
   public function submitBackStep(array &$form, FormStateInterface $form_state) {
@@ -503,10 +503,10 @@ class GridDialog extends FormBase {
    *
    * @param array $form
    *   The form.
-   * @param \Drupal\Core\Form\FormStateInterface $form_state
+   * @param FormStateInterface $form_state
    *   The form state.
    *
-   * @return \Drupal\Core\Ajax\AjaxResponse
+   * @return AjaxResponse
    *   An ajax response.
    */
   public function submitDialog(array &$form, FormStateInterface $form_state) {
diff --git a/src/Form/Settings.php b/src/Form/Settings.php
index 44a1cdb..a82921c 100644
--- a/src/Form/Settings.php
+++ b/src/Form/Settings.php
@@ -305,7 +305,7 @@ class Settings extends ConfigFormBase {
   /**
    * Helper to find the name of the trigger item.
    *
-   * @param \Drupal\Core\Form\FormStateInterface $form_state
+   * @param FormStateInterface $form_state
    *   The form state.
    *
    * @return array
diff --git a/src/Plugin/CKEditorPlugin/BsGrid.php b/src/Plugin/CKEditorPlugin/BsGrid.php
index ecd0952..169fcb5 100644
--- a/src/Plugin/CKEditorPlugin/BsGrid.php
+++ b/src/Plugin/CKEditorPlugin/BsGrid.php
@@ -24,7 +24,7 @@ class BsGrid extends CKEditorPluginBase implements CKEditorPluginConfigurableInt
   /**
    * The config factory.
    *
-   * @var \Drupal\Core\Config\ConfigFactoryInterface
+   * @var ConfigFactoryInterface
    */
   protected $configFactory;
 
@@ -52,7 +52,7 @@ class BsGrid extends CKEditorPluginBase implements CKEditorPluginConfigurableInt
    * {@inheritdoc}
    */
   public function getButtons() {
-    $path = drupal_get_path('module', 'ckeditor_bs_grid') . '/js/plugins/bs_grid';
+    $path = \Drupal::service('extension.list.module')->getPath('ckeditor_bs_grid') . '/js/plugins/bs_grid';
     return [
       'bs_grid' => [
         'label' => 'Bootstrap Grid',
@@ -65,7 +65,7 @@ class BsGrid extends CKEditorPluginBase implements CKEditorPluginConfigurableInt
    * {@inheritdoc}
    */
   public function getFile() {
-    return drupal_get_path('module', 'ckeditor_bs_grid') . '/js/plugins/bs_grid/plugin.js';
+    return \Drupal::service('extension.list.module')->getPath('ckeditor_bs_grid') . '/js/plugins/bs_grid/plugin.js';
   }
 
   /**

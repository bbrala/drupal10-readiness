diff --git a/freeagent.module b/freeagent.module
index b1aaaef..166d53f 100644
--- a/freeagent.module
+++ b/freeagent.module
@@ -27,7 +27,7 @@ function freeagent_cron() {
 function freeagent_form_user_form_alter(&$form, FormStateInterface $form_state, $form_id) {
   $user = \Drupal::currentUser();
   if ($user->hasPermission('administer freeagent')) {
-    /** @var \Drupal\user\UserInterface $account */
+    /** @var UserInterface $account */
     $entity = $form_state->getFormObject()->getEntity();
 
     $options = [0 => t('- None -')];
diff --git a/freeagent_reports/freeagent_reports.module b/freeagent_reports/freeagent_reports.module
index 2ac69dd..5eaad94 100644
--- a/freeagent_reports/freeagent_reports.module
+++ b/freeagent_reports/freeagent_reports.module
@@ -1,5 +1,6 @@
 <?php
 
+use Drupal\views\Entity\View;
 /**
  * @file
  * FreeAgent Reports module.
@@ -48,7 +49,7 @@ function freeagent_reports_views_pre_render(ViewExecutable $view) {
  * Implements hook_form_FORM_ID_alter().
  */
 function freeagent_reports_form_views_exposed_form_alter(&$form, FormStateInterface $form_state, $form_id) {
-  /** @var \Drupal\views\Entity\View $view */
+  /** @var View $view */
   $view = $form_state->getStorage()['view'];
   switch ($view->id()) {
     case 'freeagent_timeslips':
diff --git a/freeagent_reports/src/Controller/FreeAgentReportsController.php b/freeagent_reports/src/Controller/FreeAgentReportsController.php
index 40b22bd..0aa0894 100644
--- a/freeagent_reports/src/Controller/FreeAgentReportsController.php
+++ b/freeagent_reports/src/Controller/FreeAgentReportsController.php
@@ -17,23 +17,23 @@ class FreeAgentReportsController extends ControllerBase {
   /**
    * The FreeAgent service.
    *
-   * @var \Drupal\freeagent\FreeAgent
+   * @var FreeAgent
    */
   protected $freeAgent;
 
   /**
    * The current user.
    *
-   * @var \Drupal\Core\Session\AccountInterface
+   * @var AccountInterface
    */
   protected $currentUser;
 
   /**
    * Constructs a new FreeAgentReportsController instance.
    *
-   * @param \Drupal\freeagent\FreeAgent $freeagent
+   * @param FreeAgent $freeagent
    *   The FreeAgent service.
-   * @param \Drupal\Core\Session\AccountInterface $current_user
+   * @param AccountInterface $current_user
    *   The current user.
    */
   public function __construct(FreeAgent $freeagent, AccountInterface $current_user) {
@@ -71,7 +71,7 @@ class FreeAgentReportsController extends ControllerBase {
    * @param int $contact_id
    *   The contact Id.
    *
-   * @return \Drupal\Core\Ajax\AjaxResponse
+   * @return AjaxResponse
    *   The Ajax response.
    */
   public function ajaxProjectsByContact($contact_id) {
@@ -96,7 +96,7 @@ class FreeAgentReportsController extends ControllerBase {
    * @param int $contact_id
    *   The contact Id.
    *
-   * @return \Drupal\Core\Ajax\AjaxResponse
+   * @return AjaxResponse
    *   The Ajax response.
    */
   public function ajaxTasksByContact($contact_id) {
@@ -130,7 +130,7 @@ class FreeAgentReportsController extends ControllerBase {
    * @param int $project_id
    *   The project Id.
    *
-   * @return \Drupal\Core\Ajax\AjaxResponse
+   * @return AjaxResponse
    *   The Ajax response.
    */
   public function ajaxTasksByProject($project_id) {
diff --git a/freeagent_reports/src/Plugin/views/access/MyTimeslips.php b/freeagent_reports/src/Plugin/views/access/MyTimeslips.php
index 64f056b..516ae4d 100644
--- a/freeagent_reports/src/Plugin/views/access/MyTimeslips.php
+++ b/freeagent_reports/src/Plugin/views/access/MyTimeslips.php
@@ -23,7 +23,7 @@ class MyTimeslips extends AccessPluginBase {
   /**
    * The current user.
    *
-   * @var \Drupal\Core\Session\AccountInterface
+   * @var AccountInterface
    */
   protected $currentUser;
 
@@ -36,7 +36,7 @@ class MyTimeslips extends AccessPluginBase {
    *   The plugin_id for the plugin instance.
    * @param mixed $plugin_definition
    *   The plugin implementation definition.
-   * @param \Drupal\Core\Session\AccountInterface $current_user
+   * @param AccountInterface $current_user
    *   The current user.
    */
   public function __construct(array $configuration, $plugin_id, $plugin_definition, AccountInterface $current_user) {
diff --git a/src/Controller/FreeAgentController.php b/src/Controller/FreeAgentController.php
index 5fe30f7..cbbe58f 100644
--- a/src/Controller/FreeAgentController.php
+++ b/src/Controller/FreeAgentController.php
@@ -2,6 +2,7 @@
 
 namespace Drupal\freeagent\Controller;
 
+use Symfony\Component\HttpFoundation\Response;
 use Drupal\Core\Controller\ControllerBase;
 use Drupal\freeagent\FreeAgent;
 use Symfony\Component\DependencyInjection\ContainerInterface;
@@ -14,14 +15,14 @@ class FreeAgentController extends ControllerBase {
   /**
    * The FreeAgent service.
    *
-   * @var \Drupal\freeagent\FreeAgent
+   * @var FreeAgent
    */
   protected $freeAgent;
 
   /**
    * Constructs a new FreeAgentController instance.
    *
-   * @param \Drupal\freeagent\FreeAgent $freeagent
+   * @param FreeAgent $freeagent
    *   The FreeAgent service.
    */
   public function __construct(FreeAgent $freeagent) {
@@ -43,7 +44,7 @@ class FreeAgentController extends ControllerBase {
    * @param string $request_type
    *   (optional) The request type.
    *
-   * @return \Symfony\Component\HttpFoundation\Response
+   * @return Response
    *   The controller response.
    */
   public function authorise($request_type = NULL) {
diff --git a/src/Form/SettingsForm.php b/src/Form/SettingsForm.php
index 1d72bc1..6bcfa10 100644
--- a/src/Form/SettingsForm.php
+++ b/src/Form/SettingsForm.php
@@ -17,25 +17,25 @@ class SettingsForm extends ConfigFormBase {
   /**
    * The state key/value store.
    *
-   * @var \Drupal\Core\State\StateInterface
+   * @var StateInterface
    */
   protected $state;
 
   /**
    * The FreeAgent service.
    *
-   * @var \Drupal\freeagent\FreeAgent
+   * @var FreeAgent
    */
   protected $freeAgent;
 
   /**
    * Constructs a new SettingsForm instance.
    *
-   * @param \Drupal\Core\Config\ConfigFactoryInterface $config_factory
+   * @param ConfigFactoryInterface $config_factory
    *   The factory for configuration objects.
-   * @param \Drupal\Core\State\StateInterface $state
+   * @param StateInterface $state
    *   The state key/value store.
-   * @param \Drupal\freeagent\FreeAgent $freeagent
+   * @param FreeAgent $freeagent
    *   The FreeAgent service.
    */
   public function __construct(ConfigFactoryInterface $config_factory, StateInterface $state, FreeAgent $freeagent) {
diff --git a/src/FreeAgent.php b/src/FreeAgent.php
index 36e4605..8e6413c 100644
--- a/src/FreeAgent.php
+++ b/src/FreeAgent.php
@@ -2,6 +2,8 @@
 
 namespace Drupal\freeagent;
 
+use Drupal\Core\Config\Config;
+use Symfony\Component\HttpFoundation\Response;
 use Drupal\Core\Cache\CacheBackendInterface;
 use Drupal\Core\Config\ConfigFactoryInterface;
 use Drupal\Core\Database\Connection;
@@ -21,68 +23,68 @@ class FreeAgent {
   /**
    * The database connection.
    *
-   * @var \Drupal\Core\Database\Connection
+   * @var Connection
    */
   protected $connection;
 
   /**
    * The freeagent.settings config object.
    *
-   * @var \Drupal\Core\Config\Config
+   * @var Config
    */
   protected $config;
 
   /**
    * The state key/value store.
    *
-   * @var \Drupal\Core\State\StateInterface
+   * @var StateInterface
    */
   protected $state;
 
   /**
    * The cache backend.
    *
-   * @var \Drupal\Core\Cache\CacheBackendInterface
+   * @var CacheBackendInterface
    */
   protected $cache;
 
   /**
    * The messenger.
    *
-   * @var \Drupal\Core\Messenger\MessengerInterface
+   * @var MessengerInterface
    */
   protected $messenger;
 
   /**
    * The logger.
    *
-   * @var \Psr\Log\LoggerInterface
+   * @var LoggerInterface
    */
   protected $logger;
 
   /**
    * The module handler.
    *
-   * @var \Drupal\Core\Extension\ModuleHandlerInterface
+   * @var ModuleHandlerInterface
    */
   protected $moduleHandler;
 
   /**
    * Constructs a new FreeAgent instance.
    *
-   * @param \Drupal\Core\Database\Connection $connection
+   * @param Connection $connection
    *   The database connection.
-   * @param \Drupal\Core\Config\ConfigFactoryInterface $config_factory
+   * @param ConfigFactoryInterface $config_factory
    *   The factory for configuration objects.
-   * @param \Drupal\Core\State\StateInterface $state
+   * @param StateInterface $state
    *   The state key/value store.
-   * @param \Drupal\Core\Cache\CacheBackendInterface $cache
+   * @param CacheBackendInterface $cache
    *   The cache backend service.
-   * @param \Drupal\Core\Messenger\MessengerInterface $messenger
+   * @param MessengerInterface $messenger
    *   The messenger.
-   * @param \Psr\Log\LoggerInterface $logger
+   * @param LoggerInterface $logger
    *   The logger.
-   * @param \Drupal\Core\Extension\ModuleHandlerInterface $module_handler
+   * @param ModuleHandlerInterface $module_handler
    *   The module handler.
    */
   public function __construct(Connection $connection, ConfigFactoryInterface $config_factory, StateInterface $state, CacheBackendInterface $cache, MessengerInterface $messenger, LoggerInterface $logger, ModuleHandlerInterface $module_handler) {
@@ -101,7 +103,7 @@ class FreeAgent {
    * @param string $request_type
    *   (optional) The request type.
    *
-   * @return \Symfony\Component\HttpFoundation\Response|null
+   * @return Response|null
    *   The authorisation response, if available.
    */
   public function authorise($request_type = NULL) {
diff --git a/src/Plugin/views/argument/ArgumentTrait.php b/src/Plugin/views/argument/ArgumentTrait.php
index cabc91a..f4db7a0 100644
--- a/src/Plugin/views/argument/ArgumentTrait.php
+++ b/src/Plugin/views/argument/ArgumentTrait.php
@@ -13,7 +13,7 @@ trait ArgumentTrait {
   /**
    * The database connection.
    *
-   * @var \Drupal\Core\Database\Connection
+   * @var Connection
    */
   protected $connection;
 
@@ -26,7 +26,7 @@ trait ArgumentTrait {
    *   The plugin_id for the plugin instance.
    * @param mixed $plugin_definition
    *   The plugin implementation definition.
-   * @param \Drupal\Core\Database\Connection $connection
+   * @param Connection $connection
    *   The database connection.
    */
   public function __construct(array $configuration, $plugin_id, $plugin_definition, Connection $connection) {
diff --git a/src/Plugin/views/filter/FilterTrait.php b/src/Plugin/views/filter/FilterTrait.php
index f554edb..c3fca1c 100644
--- a/src/Plugin/views/filter/FilterTrait.php
+++ b/src/Plugin/views/filter/FilterTrait.php
@@ -13,7 +13,7 @@ trait FilterTrait {
   /**
    * The FreeAgent service.
    *
-   * @var \Drupal\freeagent\FreeAgent
+   * @var FreeAgent
    */
   protected $freeAgent;
 
@@ -26,7 +26,7 @@ trait FilterTrait {
    *   The plugin_id for the plugin instance.
    * @param mixed $plugin_definition
    *   The plugin implementation definition.
-   * @param \Drupal\freeagent\FreeAgent $freeagent
+   * @param FreeAgent $freeagent
    *   The FreeAgent service.
    */
   public function __construct(array $configuration, $plugin_id, $plugin_definition, FreeAgent $freeagent) {

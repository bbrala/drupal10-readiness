diff --git a/src/Plugin/EntityPrint/PrintEngine/BrowserlessApi.php b/src/Plugin/EntityPrint/PrintEngine/BrowserlessApi.php
index 3e37c71..b4b36f1 100644
--- a/src/Plugin/EntityPrint/PrintEngine/BrowserlessApi.php
+++ b/src/Plugin/EntityPrint/PrintEngine/BrowserlessApi.php
@@ -2,6 +2,10 @@
 
 namespace Drupal\entity_print_browserless_pdf\Plugin\EntityPrint\PrintEngine;
 
+use Symfony\Component\HttpFoundation\Request;
+use Drupal\Core\Logger\LoggerChannelInterface;
+use GuzzleHttp\Client;
+use Symfony\Component\Validator\ConstraintViolationInterface;
 use Drupal\Core\Form\FormStateInterface;
 use Drupal\Core\Http\ClientFactory;
 use Drupal\Core\Http\RequestStack;
@@ -42,14 +46,14 @@ class BrowserlessApi extends PdfEngineBase implements ContainerFactoryPluginInte
   /**
    * Current Request.
    *
-   * @var \Symfony\Component\HttpFoundation\Request
+   * @var Request
    */
   protected $request;
 
   /**
    * Module logger.
    *
-   * @var \Drupal\Core\Logger\LoggerChannelInterface
+   * @var LoggerChannelInterface
    */
   protected $logger;
 
@@ -63,7 +67,7 @@ class BrowserlessApi extends PdfEngineBase implements ContainerFactoryPluginInte
   /**
    * Http client to connect to Browserless API.
    *
-   * @var \GuzzleHttp\Client
+   * @var Client
    */
   protected $httpClient;
 
@@ -335,7 +339,7 @@ class BrowserlessApi extends PdfEngineBase implements ContainerFactoryPluginInte
       'allowExtraFields' => TRUE,
     ]);
 
-    /** @var \Symfony\Component\Validator\ConstraintViolationInterface[] $violations */
+    /** @var ConstraintViolationInterface[] $violations */
     $violations = $validator->validate($values, $data_constraint);
 
     // Map violation messages back to the field.
@@ -354,9 +358,9 @@ class BrowserlessApi extends PdfEngineBase implements ContainerFactoryPluginInte
    *
    * @param array $values
    *   The form values.
-   * @param \Symfony\Component\Validator\ConstraintViolation $violation
+   * @param ConstraintViolation $violation
    *   The violation.
-   * @param \Drupal\Core\Form\FormStateInterface $form_state
+   * @param FormStateInterface $form_state
    *   The form state object.
    */
   public function validateValues(array $values, ConstraintViolation $violation, FormStateInterface $form_state) {

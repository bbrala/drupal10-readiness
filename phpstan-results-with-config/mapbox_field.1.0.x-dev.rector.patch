diff --git a/mapbox_field.info.yml b/mapbox_field.info.yml
index dcd4569..c4ff3c5 100644
--- a/mapbox_field.info.yml
+++ b/mapbox_field.info.yml
@@ -1,7 +1,7 @@
 name: 'Mapbox Field'
 type: module
 description: 'Provides a field type to create map via Mapbox.'
-core_version_requirement: ^8 || ^9
+core_version_requirement: ^9.3 || ^10
 package: 'Mapbox'
 configure: mapbox_field.config_form
 dependencies:
diff --git a/src/Plugin/Field/FieldFormatter/MapboxFormatter.php b/src/Plugin/Field/FieldFormatter/MapboxFormatter.php
index 19bfed3..63fe65f 100644
--- a/src/Plugin/Field/FieldFormatter/MapboxFormatter.php
+++ b/src/Plugin/Field/FieldFormatter/MapboxFormatter.php
@@ -29,7 +29,7 @@ class MapboxFormatter extends FormatterBase implements ContainerFactoryPluginInt
   /**
    * Drupal\mapbox\Mapbox definition.
    *
-   * @var \Drupal\mapbox\Mapbox
+   * @var Mapbox
    */
   protected $mapbox;
 
@@ -111,7 +111,7 @@ class MapboxFormatter extends FormatterBase implements ContainerFactoryPluginInt
         'zoom'        => (int) $items[$delta]->zoom,
       ];
       $mapbox['position'] = [
-        'marker'          => $items[$delta]->marker ? $items[$delta]->marker : '/'.drupal_get_path('module', 'mapbox_field').'/marker.png',
+        'marker'          => $items[$delta]->marker ? $items[$delta]->marker : '/'.\Drupal::service('extension.list.module')->getPath('mapbox_field').'/marker.png',
         'markerText'      => $items[$delta]->markerText ?? $items[$delta]->markerText,
         'lat'             => (float) $items[$delta]->lat,
         'lng'             => (float) $items[$delta]->lng,
@@ -134,7 +134,7 @@ class MapboxFormatter extends FormatterBase implements ContainerFactoryPluginInt
   /**
    * Generate the output appropriate for one field item.
    *
-   * @param \Drupal\Core\Field\FieldItemInterface $item
+   * @param FieldItemInterface $item
    *   One field item.
    *
    * @return string
diff --git a/src/Plugin/Field/FieldFormatter/MapboxSingle.php b/src/Plugin/Field/FieldFormatter/MapboxSingle.php
index 209c052..ccb206b 100644
--- a/src/Plugin/Field/FieldFormatter/MapboxSingle.php
+++ b/src/Plugin/Field/FieldFormatter/MapboxSingle.php
@@ -27,7 +27,7 @@ class MapboxSingle extends FormatterBase {
   /**
    * Drupal\mapbox\Mapbox definition.
    *
-   * @var \Drupal\mapbox\Mapbox
+   * @var Mapbox
    */
   protected $mapbox;
 
@@ -164,7 +164,7 @@ class MapboxSingle extends FormatterBase {
       $positions[]  = [
         'lat'            => (float) $items[$delta]->lat,
         'lng'            => (float) $items[$delta]->lng,
-        'marker'         => $items[$delta]->marker ? $items[$delta]->marker : '/'.drupal_get_path('module', 'mapbox').'/marker.png',
+        'marker'         => $items[$delta]->marker ? $items[$delta]->marker : '/'.\Drupal::service('extension.list.module')->getPath('mapbox').'/marker.png',
         'markerText'     => $items[$delta]->markerText ?? $items[$delta]->markerText,
         'markerInstance' => $items[$delta]->getEntity()->id().$delta.$instance_num
       ];
@@ -189,7 +189,7 @@ class MapboxSingle extends FormatterBase {
   /**
    * Generate the output appropriate for one field item.
    *
-   * @param \Drupal\Core\Field\FieldItemInterface $item
+   * @param FieldItemInterface $item
    *   One field item.
    *
    * @return string
diff --git a/src/Plugin/Field/FieldWidget/MapboxWidget.php b/src/Plugin/Field/FieldWidget/MapboxWidget.php
index b798ed1..89152d1 100644
--- a/src/Plugin/Field/FieldWidget/MapboxWidget.php
+++ b/src/Plugin/Field/FieldWidget/MapboxWidget.php
@@ -30,7 +30,7 @@ class MapboxWidget extends WidgetBase implements ContainerFactoryPluginInterface
   /**
    * Drupal\mapbox\Mapbox definition.
    *
-   * @var \Drupal\mapbox\Mapbox
+   * @var Mapbox
    */
   protected $mapbox;
   /**
@@ -80,7 +80,7 @@ class MapboxWidget extends WidgetBase implements ContainerFactoryPluginInterface
       'zoom'        => (int) $items[$delta]->zoom,
     ];
     $mapbox['position'] = [
-      'marker'          => $items[$delta]->marker ? $items[$delta]->marker : '/'.drupal_get_path('module', 'mapbox_field').'/marker.png',
+      'marker'          => $items[$delta]->marker ? $items[$delta]->marker : '/'.\Drupal::service('extension.list.module')->getPath('mapbox_field').'/marker.png',
       'markerText'      => $items[$delta]->markerText ?? $items[$delta]->markerText,
       'lat'             => (float) $items[$delta]->lat,
       'lng'             => (float) $items[$delta]->lng,

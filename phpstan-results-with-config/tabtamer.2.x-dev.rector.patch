diff --git a/src/Form/TabTamerForm.php b/src/Form/TabTamerForm.php
index 7048ee6..43b804a 100644
--- a/src/Form/TabTamerForm.php
+++ b/src/Form/TabTamerForm.php
@@ -2,6 +2,8 @@
 
 namespace Drupal\tabtamer\Form;
 
+use Drupal\tabtamer\TabTamerInterface;
+use Drupal\Core\Menu\LocalTaskDefault;
 use Drupal\Core\Entity\EntityForm;
 use Drupal\Core\Form\FormStateInterface;
 use Drupal\Core\Menu\LocalTaskManagerInterface;
@@ -11,30 +13,30 @@ use Symfony\Component\HttpFoundation\RequestStack;
 /**
  * Tab tamer form.
  *
- * @property \Drupal\tabtamer\TabTamerInterface $entity
+ * @property TabTamerInterface $entity
  */
 class TabTamerForm extends EntityForm {
 
   /**
    * Local task manager.
    *
-   * @var \Drupal\Core\Menu\LocalTaskManagerInterface
+   * @var LocalTaskManagerInterface
    */
   protected $localTaskManager;
 
   /**
    * RequestStack.
    *
-   * @var \Symfony\Component\HttpFoundation\RequestStack
+   * @var RequestStack
    */
   protected $requestStack;
 
   /**
    * TabTamerForm constructor.
    *
-   * @param \Drupal\Core\Menu\LocalTaskManagerInterface $localTaskManager
+   * @param LocalTaskManagerInterface $localTaskManager
    *   Local task manager.
-   * @param \Symfony\Component\HttpFoundation\RequestStack $requestStack
+   * @param RequestStack $requestStack
    *   RequestStack.
    */
   public function __construct(LocalTaskManagerInterface $localTaskManager, RequestStack $requestStack) {
@@ -106,7 +108,7 @@ class TabTamerForm extends EntityForm {
     $getTabs = reset($getTabs);
 
     if ($getTabs) {
-      /** @var \Drupal\Core\Menu\LocalTaskDefault $val */
+      /** @var LocalTaskDefault $val */
       foreach ($getTabs as $val) {
         $options = $val->getPluginDefinition();
         // Load a new tabs for current route.
@@ -191,7 +193,7 @@ class TabTamerForm extends EntityForm {
    *
    * @param array $form
    *   Form.
-   * @param \Drupal\Core\Form\FormStateInterface $formState
+   * @param FormStateInterface $formState
    *   Form state.
    *
    * @return mixed
diff --git a/src/TabTamerListBuilder.php b/src/TabTamerListBuilder.php
index 56e7e00..7715a3f 100644
--- a/src/TabTamerListBuilder.php
+++ b/src/TabTamerListBuilder.php
@@ -24,7 +24,7 @@ class TabTamerListBuilder extends ConfigEntityListBuilder {
    * {@inheritdoc}
    */
   public function buildRow(EntityInterface $entity) {
-    /** @var \Drupal\tabtamer\TabTamerInterface $entity */
+    /** @var TabTamerInterface $entity */
     $row['label'] = $entity->label();
     $row['id'] = $entity->id();
     $row['status'] = $entity->status() ? $this->t('Enabled') : $this->t('Disabled');

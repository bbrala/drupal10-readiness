diff --git a/src/Plugin/migrate/FieldableEntityDeriverBase.php b/src/Plugin/migrate/FieldableEntityDeriverBase.php
index 4216fdb..797e777 100644
--- a/src/Plugin/migrate/FieldableEntityDeriverBase.php
+++ b/src/Plugin/migrate/FieldableEntityDeriverBase.php
@@ -30,14 +30,14 @@ abstract class FieldableEntityDeriverBase extends DeriverBase implements Contain
   /**
    * The migration field discovery service.
    *
-   * @var \Drupal\migrate_drupal\FieldDiscoveryInterface
+   * @var FieldDiscoveryInterface
    */
   protected $fieldDiscovery;
 
   /**
    * The migration plugin manager.
    *
-   * @var \Drupal\migrate\Plugin\MigrationPluginManagerInterface
+   * @var MigrationPluginManagerInterface
    */
   protected $migrationManager;
 
@@ -46,9 +46,9 @@ abstract class FieldableEntityDeriverBase extends DeriverBase implements Contain
    *
    * @param string $base_plugin_id
    *   The base plugin ID for the plugin ID.
-   * @param \Drupal\migrate_drupal\FieldDiscoveryInterface $field_discovery
+   * @param FieldDiscoveryInterface $field_discovery
    *   The migration field discovery service.
-   * @param \Drupal\migrate\Plugin\MigrationPluginManagerInterface $migration_plugin_manager
+   * @param MigrationPluginManagerInterface $migration_plugin_manager
    *   The migration plugin manager.
    */
   public function __construct($base_plugin_id, FieldDiscoveryInterface $field_discovery, MigrationPluginManagerInterface $migration_plugin_manager) {
diff --git a/src/Plugin/migrate/MultifieldConfigDeriver.php b/src/Plugin/migrate/MultifieldConfigDeriver.php
index 1fde185..a996c09 100644
--- a/src/Plugin/migrate/MultifieldConfigDeriver.php
+++ b/src/Plugin/migrate/MultifieldConfigDeriver.php
@@ -49,7 +49,7 @@ class MultifieldConfigDeriver extends DeriverBase implements ContainerDeriverInt
   /**
    * The migration field discovery service.
    *
-   * @var \Drupal\migrate_drupal\FieldDiscoveryInterface
+   * @var FieldDiscoveryInterface
    */
   protected $fieldDiscovery;
 
@@ -58,7 +58,7 @@ class MultifieldConfigDeriver extends DeriverBase implements ContainerDeriverInt
    *
    * @param string $base_plugin_id
    *   The base plugin ID for the plugin ID.
-   * @param \Drupal\migrate_drupal\FieldDiscoveryInterface $field_discovery
+   * @param FieldDiscoveryInterface $field_discovery
    *   The migration field discovery service.
    */
   public function __construct($base_plugin_id, FieldDiscoveryInterface $field_discovery) {
diff --git a/src/Plugin/migrate/field/FieldCollection.php b/src/Plugin/migrate/field/FieldCollection.php
index 1c50917..23e9ed3 100644
--- a/src/Plugin/migrate/field/FieldCollection.php
+++ b/src/Plugin/migrate/field/FieldCollection.php
@@ -155,7 +155,7 @@ class FieldCollection extends ParagraphsFieldPluginBase {
   /**
    * Adds process for view mode settings.
    *
-   * @param \Drupal\migrate\Plugin\MigrationInterface $migration
+   * @param MigrationInterface $migration
    *   The migration.
    */
   protected function addViewModeProcess(MigrationInterface $migration) {
diff --git a/src/Plugin/migrate/field/Paragraphs.php b/src/Plugin/migrate/field/Paragraphs.php
index bdd3b5f..807d1c1 100644
--- a/src/Plugin/migrate/field/Paragraphs.php
+++ b/src/Plugin/migrate/field/Paragraphs.php
@@ -164,7 +164,7 @@ class Paragraphs extends ParagraphsFieldPluginBase {
   /**
    * Adds process for view mode settings.
    *
-   * @param \Drupal\migrate\Plugin\MigrationInterface $migration
+   * @param MigrationInterface $migration
    *   The migration.
    */
   protected function addViewModeProcess(MigrationInterface $migration) {
@@ -185,7 +185,7 @@ class Paragraphs extends ParagraphsFieldPluginBase {
   /**
    * Adds processes for paragraphs field widgets.
    *
-   * @param \Drupal\migrate\Plugin\MigrationInterface $migration
+   * @param MigrationInterface $migration
    *   The migration.
    */
   protected function paragraphAlterFieldWidgetMigration(MigrationInterface $migration) {
diff --git a/src/Plugin/migrate/field/ParagraphsFieldPluginBase.php b/src/Plugin/migrate/field/ParagraphsFieldPluginBase.php
index 5832eeb..3df4a7c 100644
--- a/src/Plugin/migrate/field/ParagraphsFieldPluginBase.php
+++ b/src/Plugin/migrate/field/ParagraphsFieldPluginBase.php
@@ -64,7 +64,7 @@ abstract class ParagraphsFieldPluginBase extends FieldPluginBase {
   /**
    * Returns the migration dependencies for an entity migration.
    *
-   * @param \Drupal\migrate\Plugin\MigrationInterface $migration
+   * @param MigrationInterface $migration
    *   The migration to parse.
    * @param string $field_name
    *   The name of the paragraphs or the field_collection field.
@@ -145,7 +145,7 @@ abstract class ParagraphsFieldPluginBase extends FieldPluginBase {
   /**
    * Checks whether a migration's destination is an entity revision.
    *
-   * @param \Drupal\migrate\Plugin\MigrateDestinationInterface $destination
+   * @param MigrateDestinationInterface $destination
    *   The migration destination to check.
    *
    * @return bool
@@ -167,7 +167,7 @@ abstract class ParagraphsFieldPluginBase extends FieldPluginBase {
   /**
    * Returns the dep type of component migrations in the current migration.
    *
-   * @param \Drupal\migrate\Plugin\MigrationInterface|string $migration_or_migration_id
+   * @param MigrationInterface|string $migration_or_migration_id
    *   The migration or the full ID of the migration which depends on component
    *   migrations. "Component migration" means paragraphs item or field
    *   collection item migration.
diff --git a/src/Plugin/migrate/process/FieldCollectionFieldInstanceSettings.php b/src/Plugin/migrate/process/FieldCollectionFieldInstanceSettings.php
index 5d05e9b..bb0b8ad 100644
--- a/src/Plugin/migrate/process/FieldCollectionFieldInstanceSettings.php
+++ b/src/Plugin/migrate/process/FieldCollectionFieldInstanceSettings.php
@@ -23,7 +23,7 @@ class FieldCollectionFieldInstanceSettings extends ProcessPluginBase implements
   /**
    * The migrate lookup service.
    *
-   * @var \Drupal\migrate\MigrateLookupInterface
+   * @var MigrateLookupInterface
    */
   protected $migrateLookup;
 
@@ -36,9 +36,9 @@ class FieldCollectionFieldInstanceSettings extends ProcessPluginBase implements
    *   The plugin_id for the plugin instance.
    * @param mixed $plugin_definition
    *   The plugin implementation definition.
-   * @param \Drupal\Core\Entity\EntityTypeBundleInfoInterface $entity_type_bundle_info
+   * @param EntityTypeBundleInfoInterface $entity_type_bundle_info
    *   The entity bundle info service.
-   * @param \Drupal\migrate\MigrateLookupInterface $migrate_lookup
+   * @param MigrateLookupInterface $migrate_lookup
    *   The migrate lookup service.
    */
   public function __construct(array $configuration, $plugin_id, $plugin_definition, EntityTypeBundleInfoInterface $entity_type_bundle_info, MigrateLookupInterface $migrate_lookup) {
diff --git a/src/Plugin/migrate/process/MultifieldFieldInstanceSettings.php b/src/Plugin/migrate/process/MultifieldFieldInstanceSettings.php
index a0326e9..c6ac917 100644
--- a/src/Plugin/migrate/process/MultifieldFieldInstanceSettings.php
+++ b/src/Plugin/migrate/process/MultifieldFieldInstanceSettings.php
@@ -21,7 +21,7 @@ class MultifieldFieldInstanceSettings extends CoreProcessPluginBase implements C
   /**
    * The migrate lookup service.
    *
-   * @var \Drupal\migrate\MigrateLookupInterface
+   * @var MigrateLookupInterface
    */
   protected $migrateLookup;
 
@@ -34,7 +34,7 @@ class MultifieldFieldInstanceSettings extends CoreProcessPluginBase implements C
    *   The plugin_id for the plugin instance.
    * @param mixed $plugin_definition
    *   The plugin implementation definition.
-   * @param \Drupal\migrate\MigrateLookupInterface $migrate_lookup
+   * @param MigrateLookupInterface $migrate_lookup
    *   The migrate lookup service.
    */
   public function __construct(array $configuration, $plugin_id, $plugin_definition, MigrateLookupInterface $migrate_lookup) {
diff --git a/src/Plugin/migrate/process/MultifieldFieldTranslatable.php b/src/Plugin/migrate/process/MultifieldFieldTranslatable.php
index 2dd5f30..fa7c297 100644
--- a/src/Plugin/migrate/process/MultifieldFieldTranslatable.php
+++ b/src/Plugin/migrate/process/MultifieldFieldTranslatable.php
@@ -26,7 +26,7 @@ class MultifieldFieldTranslatable extends CoreProcessPluginBase implements Conta
   /**
    * The current migration plugin instance.
    *
-   * @var \Drupal\migrate\Plugin\MigrationInterface
+   * @var MigrationInterface
    */
   protected $migration;
 
@@ -39,7 +39,7 @@ class MultifieldFieldTranslatable extends CoreProcessPluginBase implements Conta
    *   The plugin ID for the plugin instance.
    * @param mixed $plugin_definition
    *   The plugin implementation definition.
-   * @param \Drupal\migrate\Plugin\MigrationInterface $migration
+   * @param MigrationInterface $migration
    *   The current migration plugin instance.
    */
   public function __construct(array $configuration, $plugin_id, $plugin_definition, MigrationInterface $migration) {
diff --git a/src/Plugin/migrate/process/ParagraphsFieldInstanceSettings.php b/src/Plugin/migrate/process/ParagraphsFieldInstanceSettings.php
index 3fd396b..d9dfc6f 100644
--- a/src/Plugin/migrate/process/ParagraphsFieldInstanceSettings.php
+++ b/src/Plugin/migrate/process/ParagraphsFieldInstanceSettings.php
@@ -22,7 +22,7 @@ class ParagraphsFieldInstanceSettings extends ProcessPluginBase implements Conta
   /**
    * The migrate lookup service.
    *
-   * @var \Drupal\migrate\MigrateLookupInterface
+   * @var MigrateLookupInterface
    */
   protected $migrateLookup;
 
@@ -35,9 +35,9 @@ class ParagraphsFieldInstanceSettings extends ProcessPluginBase implements Conta
    *   The plugin_id for the plugin instance.
    * @param mixed $plugin_definition
    *   The plugin implementation definition.
-   * @param \Drupal\Core\Entity\EntityTypeBundleInfoInterface $entity_type_bundle_info
+   * @param EntityTypeBundleInfoInterface $entity_type_bundle_info
    *   The entity bundle info service.
-   * @param \Drupal\migrate\MigrateLookupInterface $migrate_lookup
+   * @param MigrateLookupInterface $migrate_lookup
    *   The migrate lookup service.
    */
   public function __construct(array $configuration, $plugin_id, $plugin_definition, EntityTypeBundleInfoInterface $entity_type_bundle_info, MigrateLookupInterface $migrate_lookup) {
diff --git a/src/Plugin/migrate/process/ParagraphsLookup.php b/src/Plugin/migrate/process/ParagraphsLookup.php
index a1adb76..160e544 100644
--- a/src/Plugin/migrate/process/ParagraphsLookup.php
+++ b/src/Plugin/migrate/process/ParagraphsLookup.php
@@ -2,6 +2,8 @@
 
 namespace Drupal\paragraphs_migration\Plugin\migrate\process;
 
+use Drupal\migrate\Plugin\MigratePluginManager;
+use Drupal\migrate\MigrateSkipProcessException;
 use Drupal\Component\Plugin\Exception\PluginException;
 use Drupal\migrate\MigrateException;
 use Drupal\migrate\MigrateExecutableInterface;
@@ -36,14 +38,14 @@ class ParagraphsLookup extends MigrationLookup {
   /**
    * The migration plugin manager.
    *
-   * @var \Drupal\migrate\Plugin\MigrationPluginManagerInterface
+   * @var MigrationPluginManagerInterface
    */
   protected $migrationPluginManager;
 
   /**
    * The process plugin manager.
    *
-   * @var \Drupal\migrate\Plugin\MigratePluginManager
+   * @var MigratePluginManager
    */
   protected $processPluginManager;
 
@@ -56,15 +58,15 @@ class ParagraphsLookup extends MigrationLookup {
    *   The plugin_id for the plugin instance.
    * @param mixed $plugin_definition
    *   The plugin implementation definition.
-   * @param \Drupal\migrate\Plugin\MigrationInterface $migration
+   * @param MigrationInterface $migration
    *   The Migration the plugin is being used in.
-   * @param \Drupal\migrate\MigrateLookupInterface $migrate_lookup
+   * @param MigrateLookupInterface $migrate_lookup
    *   The migrate lookup service.
-   * @param \Drupal\migrate\MigrateStubInterface $migrate_stub
+   * @param MigrateStubInterface $migrate_stub
    *   The migrate stub service.
-   * @param \Drupal\migrate\Plugin\MigrationPluginManagerInterface $migration_plugin_manager
+   * @param MigrationPluginManagerInterface $migration_plugin_manager
    *   The Migration Plugin Manager Interface.
-   * @param \Drupal\migrate\Plugin\MigratePluginManagerInterface $process_plugin_manager
+   * @param MigratePluginManagerInterface $process_plugin_manager
    *   The process migration plugin manager.
    */
   public function __construct(array $configuration, $plugin_id, $plugin_definition, MigrationInterface $migration, MigrateLookupInterface $migrate_lookup, MigrateStubInterface $migrate_stub, MigrationPluginManagerInterface $migration_plugin_manager, MigratePluginManagerInterface $process_plugin_manager) {
@@ -100,7 +102,7 @@ class ParagraphsLookup extends MigrationLookup {
     if (isset($this->configuration['tags'])) {
       $tags = (array) $this->configuration['tags'];
       foreach ($tags as $tag) {
-        /** @var \Drupal\migrate\Plugin\MigrationInterface[] $tag_migrations */
+        /** @var MigrationInterface[] $tag_migrations */
         $tag_migrations = $this->migrationPluginManager->createInstancesByTag($tag);
         $migrations += $tag_migrations;
         if (isset($this->configuration['tag_ids'][$tag])) {
@@ -129,7 +131,7 @@ class ParagraphsLookup extends MigrationLookup {
       if (!is_array($migration_ids)) {
         $migration_ids = (array) $migration_ids;
       }
-      /** @var \Drupal\migrate\Plugin\MigrationInterface[] $migrations */
+      /** @var MigrationInterface[] $migrations */
       $migrations = $this->migrationPluginManager->createInstances($migration_ids);
       foreach ($migrations as $migration_id => $migration) {
         if (isset($this->configuration['source_ids'][$migration_id])) {
@@ -208,7 +210,7 @@ class ParagraphsLookup extends MigrationLookup {
   /**
    * Look for destination records.
    *
-   * @param \Drupal\migrate\Plugin\MigrationInterface $migration
+   * @param MigrationInterface $migration
    *   The migration that should be checked.
    * @param string|string[] $value
    *   The source ID.
@@ -217,8 +219,8 @@ class ParagraphsLookup extends MigrationLookup {
    *   The array of the destination identifiers, or FALSE if destination cannot
    *   be determined.
    *
-   * @throws \Drupal\migrate\MigrateException
-   * @throws \Drupal\migrate\MigrateSkipProcessException
+   * @throws MigrateException
+   * @throws MigrateSkipProcessException
    */
   protected function lookupDestination(MigrationInterface $migration, $value) {
     $value = (array) $value;
diff --git a/src/Plugin/migrate/process/ProcessPluginBase.php b/src/Plugin/migrate/process/ProcessPluginBase.php
index 3e85a1d..fa7186b 100644
--- a/src/Plugin/migrate/process/ProcessPluginBase.php
+++ b/src/Plugin/migrate/process/ProcessPluginBase.php
@@ -17,7 +17,7 @@ abstract class ProcessPluginBase extends MigrateProcessPluginBase implements Con
   /**
    * The entity bundle info service.
    *
-   * @var \Drupal\Core\Entity\EntityTypeBundleInfoInterface
+   * @var EntityTypeBundleInfoInterface
    */
   protected $entityTypeBundleInfo;
 
diff --git a/src/Plugin/migrate/source/d7/MultifieldTranslationSettings.php b/src/Plugin/migrate/source/d7/MultifieldTranslationSettings.php
index ca4cf76..c67e001 100644
--- a/src/Plugin/migrate/source/d7/MultifieldTranslationSettings.php
+++ b/src/Plugin/migrate/source/d7/MultifieldTranslationSettings.php
@@ -34,11 +34,11 @@ class MultifieldTranslationSettings extends MultifieldType {
    *   The plugin_id for the plugin instance.
    * @param mixed $plugin_definition
    *   The plugin implementation definition.
-   * @param \Drupal\migrate\Plugin\MigrationInterface $migration
+   * @param MigrationInterface $migration
    *   The current migration.
-   * @param \Drupal\Core\State\StateInterface $state
+   * @param StateInterface $state
    *   The state service.
-   * @param \Drupal\Core\Entity\EntityTypeManagerInterface $entity_type_manager
+   * @param EntityTypeManagerInterface $entity_type_manager
    *   The entity type manager.
    * @param bool $content_translation_is_installed
    *   Whether Content Translation is installed on the destination site.
diff --git a/src/Utility/MultifieldMigration.php b/src/Utility/MultifieldMigration.php
index 6c65abf..4e12c20 100644
--- a/src/Utility/MultifieldMigration.php
+++ b/src/Utility/MultifieldMigration.php
@@ -55,7 +55,7 @@ final class MultifieldMigration {
   }
 
   /**
-   * @param \Drupal\Core\Database\Query\SelectInterface $query
+   * @param SelectInterface $query
    */
   public static function addCruicalMultifieldFieldProperties(SelectInterface $query) {
     if (empty(self::getMultifieldFields())) {

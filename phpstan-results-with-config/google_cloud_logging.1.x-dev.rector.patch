diff --git a/src/Logger.php b/src/Logger.php
index 33fb264..76f7af4 100644
--- a/src/Logger.php
+++ b/src/Logger.php
@@ -2,6 +2,7 @@
 
 namespace Drupal\google_cloud_logging;
 
+use Drupal\Core\Queue\QueueInterface;
 use DateTimeImmutable as DateTime;
 use Drupal\Core\Logger\RfcLoggerTrait;
 use Drupal\Core\Queue\QueueFactory;
@@ -18,23 +19,23 @@ class Logger implements LoggerInterface {
   /**
    * Work queue.
    *
-   * @var \Drupal\Core\Queue\QueueInterface
+   * @var QueueInterface
    */
   protected $queue;
 
   /**
    * Request stack.
    *
-   * @var \Symfony\Component\HttpFoundation\RequestStack
+   * @var RequestStack
    */
   protected $requestStack;
 
   /**
    * Constructs a new object.
    *
-   * @param \Drupal\Core\Queue\QueueFactory $queueFactory
+   * @param QueueFactory $queueFactory
    *   Queue factory.
-   * @param \Symfony\Component\HttpFoundation\RequestStack $requestStack
+   * @param RequestStack $requestStack
    *   Request stack.
    */
   public function __construct(QueueFactory $queueFactory, RequestStack $requestStack) {
diff --git a/src/Plugin/QueueWorker/LogQueue.php b/src/Plugin/QueueWorker/LogQueue.php
index 98928c0..827d80b 100644
--- a/src/Plugin/QueueWorker/LogQueue.php
+++ b/src/Plugin/QueueWorker/LogQueue.php
@@ -2,6 +2,7 @@
 
 namespace Drupal\google_cloud_logging\Plugin\QueueWorker;
 
+use Drupal\Core\Queue\QueueInterface;
 use DomainException;
 use Drupal\Core\Logger\RfcLogLevel;
 use Drupal\Core\Plugin\ContainerFactoryPluginInterface;
@@ -50,14 +51,14 @@ class LogQueue extends QueueWorkerBase implements ContainerFactoryPluginInterfac
   /**
    * Logger that sends entries to Google Cloud.
    *
-   * @var \Google\Cloud\Logging\PsrLogger
+   * @var PsrLogger
    */
   protected $logger;
 
   /**
    * Work queue.
    *
-   * @var \Drupal\Core\Queue\QueueInterface
+   * @var QueueInterface
    */
   protected $queue;
 
@@ -70,7 +71,7 @@ class LogQueue extends QueueWorkerBase implements ContainerFactoryPluginInterfac
    *   Plugin ID.
    * @param mixed $plugin_definition
    *   Plugin definition
-   * @param \Drupal\Core\Queue\QueueFactory $queueFactory
+   * @param QueueFactory $queueFactory
    *   Queue factory.
    */
   public function __construct(array $configuration, $plugin_id, $plugin_definition, QueueFactory $queueFactory) {

modules/contrib/mapbox_field/mapbox_field.module
modules/contrib/mapbox_field/src/Plugin/Field/FieldFormatter/MapboxFormatter.php
modules/contrib/mapbox_field/src/Plugin/Field/FieldFormatter/MapboxRawFormatter.php
modules/contrib/mapbox_field/src/Plugin/Field/FieldFormatter/MapboxSingle.php
modules/contrib/mapbox_field/src/Plugin/Field/FieldType/MapboxField.php
modules/contrib/mapbox_field/src/Plugin/Field/FieldWidget/MapboxWidget.php

3 files with changes
====================

1) modules/contrib/mapbox_field/src/Plugin/Field/FieldWidget/MapboxWidget.php:29

    ---------- begin diff ----------
@@ @@
   /**
    * Drupal\mapbox\Mapbox definition.
    *
-   * @var \Drupal\mapbox\Mapbox
+   * @var Mapbox
    */
   protected $mapbox;
   /**
@@ @@
       'zoom'        => (int) $items[$delta]->zoom,
     ];
     $mapbox['position'] = [
-      'marker'          => $items[$delta]->marker ? $items[$delta]->marker : '/'.drupal_get_path('module', 'mapbox_field').'/marker.png',
+      'marker'          => $items[$delta]->marker ? $items[$delta]->marker : '/'.\Drupal::service('extension.list.module')->getPath('mapbox_field').'/marker.png',
       'markerText'      => $items[$delta]->markerText ?? $items[$delta]->markerText,
       'lat'             => (float) $items[$delta]->lat,
       'lng'             => (float) $items[$delta]->lng,
    ----------- end diff -----------

Applied rules:
 * DrupalGetPathRector
 * ArgumentRemoverRector
 * PseudoNamespaceToNamespaceRector
 * WrapReturnRector
 * AddParamTypeDeclarationRector
 * ChangeMethodVisibilityRector


2) modules/contrib/mapbox_field/src/Plugin/Field/FieldFormatter/MapboxSingle.php:26

    ---------- begin diff ----------
@@ @@
   /**
    * Drupal\mapbox\Mapbox definition.
    *
-   * @var \Drupal\mapbox\Mapbox
+   * @var Mapbox
    */
   protected $mapbox;

@@ @@
       $positions[]  = [
         'lat'            => (float) $items[$delta]->lat,
         'lng'            => (float) $items[$delta]->lng,
-        'marker'         => $items[$delta]->marker ? $items[$delta]->marker : '/'.drupal_get_path('module', 'mapbox').'/marker.png',
+        'marker'         => $items[$delta]->marker ? $items[$delta]->marker : '/'.\Drupal::service('extension.list.module')->getPath('mapbox').'/marker.png',
         'markerText'     => $items[$delta]->markerText ?? $items[$delta]->markerText,
         'markerInstance' => $items[$delta]->getEntity()->id().$delta.$instance_num
       ];
@@ @@
   /**
    * Generate the output appropriate for one field item.
    *
-   * @param \Drupal\Core\Field\FieldItemInterface $item
+   * @param FieldItemInterface $item
    *   One field item.
    *
    * @return string
    ----------- end diff -----------

Applied rules:
 * DrupalGetPathRector
 * ArgumentRemoverRector
 * PseudoNamespaceToNamespaceRector
 * WrapReturnRector
 * AddParamTypeDeclarationRector
 * ChangeMethodVisibilityRector


3) modules/contrib/mapbox_field/src/Plugin/Field/FieldFormatter/MapboxFormatter.php:28

    ---------- begin diff ----------
@@ @@
   /**
    * Drupal\mapbox\Mapbox definition.
    *
-   * @var \Drupal\mapbox\Mapbox
+   * @var Mapbox
    */
   protected $mapbox;

@@ @@
         'zoom'        => (int) $items[$delta]->zoom,
       ];
       $mapbox['position'] = [
-        'marker'          => $items[$delta]->marker ? $items[$delta]->marker : '/'.drupal_get_path('module', 'mapbox_field').'/marker.png',
+        'marker'          => $items[$delta]->marker ? $items[$delta]->marker : '/'.\Drupal::service('extension.list.module')->getPath('mapbox_field').'/marker.png',
         'markerText'      => $items[$delta]->markerText ?? $items[$delta]->markerText,
         'lat'             => (float) $items[$delta]->lat,
         'lng'             => (float) $items[$delta]->lng,
@@ @@
   /**
    * Generate the output appropriate for one field item.
    *
-   * @param \Drupal\Core\Field\FieldItemInterface $item
+   * @param FieldItemInterface $item
    *   One field item.
    *
    * @return string
    ----------- end diff -----------

Applied rules:
 * DrupalGetPathRector
 * ArgumentRemoverRector
 * PseudoNamespaceToNamespaceRector
 * WrapReturnRector
 * AddParamTypeDeclarationRector
 * ChangeMethodVisibilityRector


 [OK] 3 files have been changed by Rector                                       


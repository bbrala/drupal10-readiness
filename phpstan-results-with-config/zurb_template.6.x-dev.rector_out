themes/contrib/zurb_template/STARTER/STARTER.theme
themes/contrib/zurb_template/STARTER/inc/form.inc
themes/contrib/zurb_template/STARTER/theme-settings.php
themes/contrib/zurb_template/inc/form.inc
themes/contrib/zurb_template/inc/menu.inc
themes/contrib/zurb_template/inc/theme.inc
themes/contrib/zurb_template/inc/zurb_template.drush.inc
themes/contrib/zurb_template/theme-settings.php
themes/contrib/zurb_template/zurb_template.theme

5 files with changes
====================

1) themes/contrib/zurb_template/zurb_template.theme:0

    ---------- begin diff ----------
@@ @@
 <?php

+use Drupal\Core\Url;
 /**
  * @file
  * Functions to support theming in the zurb_template theme.
@@ @@
   }

   $title = strip_tags($site_name) . ' ' . t('Home');
-  $url = \Drupal\Core\Url::fromRoute('<front>');
+  $url = Url::fromRoute('<front>');
   $url->setOption('attributes', array('title' => $title));

   if (theme_get_setting('zurb_template_page_site_logo')) {
    ----------- end diff -----------

Applied rules:
 * ArgumentRemoverRector


2) themes/contrib/zurb_template/theme-settings.php:8

    ---------- begin diff ----------
@@ @@
 /**
  * Implements hook_form_FORM_ID_alter().
  * @param $form
- * @param \Drupal\Core\Form\FormStateInterface $form_state
+ * @param FormStateInterface $form_state
  */
 function zurb_template_form_system_theme_settings_alter(&$form, FormStateInterface $form_state) {
   // Foundation Top Bar.
    ----------- end diff -----------

3) themes/contrib/zurb_template/inc/zurb_template.drush.inc:42

    ---------- begin diff ----------
@@ @@
   // Eliminate hyphens
   $machine_name = str_replace('-', '_', $machine_name);

-  $zurb_path = drush_get_context('DRUSH_DRUPAL_ROOT') . '/' . drupal_get_path('theme', 'zurb_template');
+  $zurb_path = drush_get_context('DRUSH_DRUPAL_ROOT') . '/' . \Drupal::service('extension.list.theme')->getPath('zurb_template');
   $subtheme_path = explode('/', $zurb_path);
   array_pop($subtheme_path);
   $subtheme_path = implode('/', $subtheme_path) . '/' . $machine_name;
    ----------- end diff -----------

Applied rules:
 * DrupalGetPathRector
 * ArgumentRemoverRector


4) themes/contrib/zurb_template/inc/menu.inc:0

    ---------- begin diff ----------
@@ @@
 <?php

+use Drupal\Core\Template\Attribute;
+use Drupal\Core\Url;
 use Drupal\Core\Link;

 /**
@@ @@
  */
 function zurb_template_preprocess_menu__main(&$variables) {
   if ($variables['top_bar'] = theme_get_setting('zurb_template_top_bar_enable')) {
-    $top_bar_attributes = new \Drupal\Core\Template\Attribute();
+    $top_bar_attributes = new Attribute();

     if (theme_get_setting('zurb_template_top_bar_sticky')) {
       $top_bar_attributes->addClass('sticky', 'large-12');
@@ @@
     // Format the linked site name.
     $site_name = \Drupal::config('system.site')->get('name');
     $title = strip_tags($site_name) . ' ' . t('Home');
-    $url = \Drupal\Core\Url::fromRoute('<front>');
+    $url = Url::fromRoute('<front>');
     $url->setOption('attributes', array('title' => $title));

     $variables['linked_site_name'] = '';
    ----------- end diff -----------

Applied rules:
 * ArgumentRemoverRector


5) themes/contrib/zurb_template/STARTER/theme-settings.php:10

    ---------- begin diff ----------
@@ @@
 /**
  * Implements hook_form_FORM_ID_alter().
  * @param $form
- * @param \Drupal\Core\Form\FormStateInterface $form_state
+ * @param FormStateInterface $form_state
  */
 function STARTER_form_system_theme_settings_alter(&$form, FormStateInterface $form_state) {
   // Build link to style guide.
-  $style_guide_link = Url::fromUri(Drupal::request()->getSchemeAndHttpHost() . '/' . drupal_get_path('theme', 'STARTER') . '/dist/styleguide.html');
+  $style_guide_link = Url::fromUri(Drupal::request()->getSchemeAndHttpHost() . '/' . \Drupal::service('extension.list.theme')->getPath('STARTER') . '/dist/styleguide.html');
   $link_options = array(
     'attributes' => array(
       'class' => array(
    ----------- end diff -----------

Applied rules:
 * DrupalGetPathRector
 * ArgumentRemoverRector


 [OK] 5 files have been changed by Rector                                       


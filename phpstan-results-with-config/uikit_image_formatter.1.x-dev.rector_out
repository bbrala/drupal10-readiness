modules/contrib/uikit_image_formatter/src/Plugin/Field/FieldFormatter/UikitLightbox.php
modules/contrib/uikit_image_formatter/src/Plugin/Field/FieldFormatter/UikitSlider.php
modules/contrib/uikit_image_formatter/src/Plugin/Field/FieldFormatter/UikitSlideshow.php
modules/contrib/uikit_image_formatter/tests/src/Functional/LoadTest.php
modules/contrib/uikit_image_formatter/uikit_image_formatter.module
modules/contrib/uikit_image_formatter/uikit_image_formatter.theme.inc

2 files with changes
====================

1) modules/contrib/uikit_image_formatter/uikit_image_formatter.theme.inc:56

    ---------- begin diff ----------
@@ @@
     $style->transformDimensions($dimensions, $uri);
   }
   else {
-    $path = file_create_url($uri);
+    $path = \Drupal::service('file_url_generator')->generateAbsoluteString($uri);
   }

   // Render as a standard image if an image style is not given.
@@ @@
           $style->transformDimensions($dimensions, $uri);
         }
         else {
-          $path = file_create_url($uri);
+          $path = \Drupal::service('file_url_generator')->generateAbsoluteString($uri);
         }
         if (!empty($caption)) {
           $variables['attributes'] = [
@@ @@
     }
     if($mime_parts[0] == 'video') {
       // We have a video
-      $variables['video'] = file_create_url($uri);
+      $variables['video'] = \Drupal::service('file_url_generator')->generateAbsoluteString($uri);
     }
   }
   else {
@@ @@
       $style->transformDimensions($dimensions, $uri);
     }
     else {
-      $path = file_create_url($uri);
+      $path = \Drupal::service('file_url_generator')->generateAbsoluteString($uri);
     }
     if (!empty($caption)) {
       $variables['attributes'] = [
    ----------- end diff -----------

Applied rules:
 * FileCreateUrlRector
 * ArgumentRemoverRector


2) modules/contrib/uikit_image_formatter/tests/src/Functional/LoadTest.php:1

    ---------- begin diff ----------
@@ @@

 namespace Drupal\Tests\uikit_image_formatter\Functional;

+use Drupal\user\UserInterface;
 use Drupal\Core\Url;
 use Drupal\Tests\BrowserTestBase;

@@ @@
   /**
    * A user with permission to administer site configuration.
    *
-   * @var \Drupal\user\UserInterface
+   * @var UserInterface
    */
   protected $user;

@@ @@
   /**
    * {@inheritdoc}
    */
-  protected function setUp() {
+  protected function setUp(): void {
     parent::setUp();
     $this->user = $this->drupalCreateUser(['administer site configuration']);
     $this->drupalLogin($this->user);
    ----------- end diff -----------

Applied rules:
 * ExceptionAnnotationRector
 * RemoveDataProviderTestPrefixRector
 * ArgumentRemoverRector
 * RenameAnnotationRector
 * PseudoNamespaceToNamespaceRector
 * WrapReturnRector
 * AddReturnTypeDeclarationRector
 * ChangeMethodVisibilityRector


 [OK] 2 files have been changed by Rector                                       


diff --git a/src/Plugin/views/argument_default/MediaIds.php b/src/Plugin/views/argument_default/MediaIds.php
index f066abb..35aab38 100755
--- a/src/Plugin/views/argument_default/MediaIds.php
+++ b/src/Plugin/views/argument_default/MediaIds.php
@@ -22,7 +22,7 @@ class MediaIds extends ArgumentDefaultPluginBase implements CacheableDependencyI
   /**
    * The route match.
    *
-   * @var \Drupal\Core\Routing\RouteMatchInterface
+   * @var RouteMatchInterface
    */
   protected $routeMatch;
 
@@ -35,7 +35,7 @@ class MediaIds extends ArgumentDefaultPluginBase implements CacheableDependencyI
    *   The plugin_id for the plugin instance.
    * @param mixed $plugin_definition
    *   The plugin implementation definition.
-   * @param \Drupal\Core\Routing\RouteMatchInterface $route_match
+   * @param RouteMatchInterface $route_match
    *   The route match.
    */
   public function __construct(array $configuration, $plugin_id, $plugin_definition, RouteMatchInterface $route_match) {
diff --git a/src/Plugin/views/argument_default/MediaTaxonnomyId.php b/src/Plugin/views/argument_default/MediaTaxonnomyId.php
index c331657..04df57d 100755
--- a/src/Plugin/views/argument_default/MediaTaxonnomyId.php
+++ b/src/Plugin/views/argument_default/MediaTaxonnomyId.php
@@ -28,14 +28,14 @@ class MediaTaxonnomyId extends ArgumentDefaultPluginBase implements CacheableDep
   /**
    * The route match.
    *
-   * @var \Drupal\Core\Routing\RouteMatchInterface
+   * @var RouteMatchInterface
    */
   protected $routeMatch;
 
   /**
    * The vocabulary storage.
    *
-   * @var \Drupal\taxonomy\VocabularyStorageInterface
+   * @var VocabularyStorageInterface
    */
   protected $vocabularyStorage;
 
@@ -48,9 +48,9 @@ class MediaTaxonnomyId extends ArgumentDefaultPluginBase implements CacheableDep
    *   The plugin_id for the plugin instance.
    * @param mixed $plugin_definition
    *   The plugin implementation definition.   *.
-   * @param \Drupal\Core\Routing\RouteMatchInterface $route_match
+   * @param RouteMatchInterface $route_match
    *   The route match.
-   * @param \Drupal\taxonomy\VocabularyStorageInterface $vocabulary_storage
+   * @param VocabularyStorageInterface $vocabulary_storage
    *   The vocabulary storage.
    */
   public function __construct(array $configuration, $plugin_id, $plugin_definition, RouteMatchInterface $route_match, VocabularyStorageInterface $vocabulary_storage) {
@@ -193,7 +193,7 @@ class MediaTaxonnomyId extends ArgumentDefaultPluginBase implements CacheableDep
         foreach ($media->getFieldDefinitions() as $field) {
           if ($field->getType() == 'entity_reference' && $field->getSetting('target_type') == 'taxonomy_term') {
             $taxonomy_terms = $media->{$field->getName()}->referencedEntities();
-            /** @var \Drupal\taxonomy\TermInterface $taxonomy_term */
+            /** @var TermInterface $taxonomy_term */
             foreach ($taxonomy_terms as $taxonomy_term) {
               $taxonomy[$taxonomy_term->id()] = $taxonomy_term->bundle();
             }

diff --git a/src/Plugin/views/argument_default/Cookie.php b/src/Plugin/views/argument_default/Cookie.php
index 118da64..81d5a7a 100644
--- a/src/Plugin/views/argument_default/Cookie.php
+++ b/src/Plugin/views/argument_default/Cookie.php
@@ -26,21 +26,21 @@ class Cookie extends ArgumentDefaultPluginBase implements CacheableDependencyInt
   /**
    * The current path.
    *
-   * @var \Drupal\Core\Session\AccountProxyInterface
+   * @var AccountProxyInterface
    */
   protected $current_user;
 
   /**
    * Module Handler.
    *
-   * @var \Drupal\Core\Extension\ModuleHandlerInterface
+   * @var ModuleHandlerInterface
    */
   protected $moduleHandler;
 
   /**
    * Token Handler.
    *
-   * @var \Drupal\Core\Utility\Token
+   * @var Token
    */
   protected $token;
 
@@ -53,9 +53,9 @@ class Cookie extends ArgumentDefaultPluginBase implements CacheableDependencyInt
    *   The plugin_id for the plugin instance.
    * @param mixed $plugin_definition
    *   The plugin implementation definition.
-   * @param \Drupal\Core\Session\AccountProxyInterface $current_user
+   * @param AccountProxyInterface $current_user
    *   The current user.
-   * @param \Drupal\Core\Extension\ModuleHandlerInterface $moduleHandler
+   * @param ModuleHandlerInterface $moduleHandler
    *   The module handler.
    * @param \Drupal\Core\Utility\Token
    *   The token service.
diff --git a/src/Plugin/views/argument_default/Session.php b/src/Plugin/views/argument_default/Session.php
index 7a95633..c29d1f0 100644
--- a/src/Plugin/views/argument_default/Session.php
+++ b/src/Plugin/views/argument_default/Session.php
@@ -25,21 +25,21 @@ class Session extends ArgumentDefaultPluginBase implements CacheableDependencyIn
   /**
    * The current path.
    *
-   * @var \Drupal\Core\Session\AccountProxyInterface
+   * @var AccountProxyInterface
    */
   protected $current_user;
 
   /**
    * Module Handler.
    *
-   * @var \Drupal\Core\Extension\ModuleHandlerInterface
+   * @var ModuleHandlerInterface
    */
   protected $moduleHandler;
 
   /**
    * Token Handler.
    *
-   * @var \Drupal\Core\Utility\Token
+   * @var Token
    */
   protected $token;
 
@@ -52,9 +52,9 @@ class Session extends ArgumentDefaultPluginBase implements CacheableDependencyIn
    *   The plugin_id for the plugin instance.
    * @param mixed $plugin_definition
    *   The plugin implementation definition.
-   * @param \Drupal\Core\Session\AccountProxyInterface $current_user
+   * @param AccountProxyInterface $current_user
    *   The current user.
-   * @param \Drupal\Core\Extension\ModuleHandlerInterface $moduleHandler
+   * @param ModuleHandlerInterface $moduleHandler
    *   The module handler.
    * @param \Drupal\Core\Utility\Token
    *   The token service.
diff --git a/src/Plugin/views/argument_default/TempStore.php b/src/Plugin/views/argument_default/TempStore.php
index eec2543..90f9ca6 100644
--- a/src/Plugin/views/argument_default/TempStore.php
+++ b/src/Plugin/views/argument_default/TempStore.php
@@ -26,28 +26,28 @@ class TempStore extends ArgumentDefaultPluginBase implements CacheableDependency
   /**
    * The current user.
    *
-   * @var \Drupal\Core\Session\AccountProxyInterface
+   * @var AccountProxyInterface
    */
   protected $currentUser;
 
   /**
    * Module Handler.
    *
-   * @var \Drupal\Core\Extension\ModuleHandlerInterface
+   * @var ModuleHandlerInterface
    */
   protected $moduleHandler;
 
   /**
    * Token Handler.
    *
-   * @var \Drupal\Core\Utility\Token
+   * @var Token
    */
   protected $token;
 
   /**
    * Token Handler.
    *
-   * @var \Drupal\Core\TempStore\PrivateTempStoreFactory
+   * @var PrivateTempStoreFactory
    */
   protected $tempStoreFactory;
 
@@ -60,13 +60,13 @@ class TempStore extends ArgumentDefaultPluginBase implements CacheableDependency
    *   The plugin_id for the plugin instance.
    * @param mixed $plugin_definition
    *   The plugin implementation definition.
-   * @param \Drupal\Core\Session\AccountProxyInterface $current_user
+   * @param AccountProxyInterface $current_user
    *   The current user.
-   * @param \Drupal\Core\Extension\ModuleHandlerInterface $moduleHandler
+   * @param ModuleHandlerInterface $moduleHandler
    *   The module handler.
-   * @param \Drupal\Core\Utility\Token $token
+   * @param Token $token
    *   The token service.
-   * @param \Drupal\Core\TempStore\PrivateTempStoreFactory $temp_store_factory
+   * @param PrivateTempStoreFactory $temp_store_factory
    *   The tempStore Service.
    */
   public function __construct(array $configuration, $plugin_id, $plugin_definition, AccountProxyInterface $currentUser, ModuleHandlerInterface $moduleHandler, Token $token, PrivateTempStoreFactory $temp_store_factory) {
diff --git a/views_extras.info.yml b/views_extras.info.yml
index d8cd757..24088c3 100644
--- a/views_extras.info.yml
+++ b/views_extras.info.yml
@@ -1,6 +1,5 @@
 name: Views Extras (Session/Cookie/Token Support)
 type: module
 description: Views Arguments based on session, cookie or token.
-core: 8.x
-core_version_requirement: ^8 || ^9
+core_version_requirement: ^9.3 || ^10
 package: Views
diff --git a/views_extras.module b/views_extras.module
index f4c6cc9..c0f69aa 100644
--- a/views_extras.module
+++ b/views_extras.module
@@ -14,7 +14,7 @@ function views_extras_help($route_name, RouteMatchInterface $route_match) {
   switch ($route_name) {
     // Main module help for the apf_custom module.
     case 'help.page.apf_custom':
-      $output = file_get_contents(drupal_get_path('module', 'views_extras') . '/README.md');
+      $output = file_get_contents(\Drupal::service('extension.list.module')->getPath('views_extras') . '/README.md');
       return $output;
 
     default:

diff --git a/chart_suite.module b/chart_suite.module
index 913d825..cd0b048 100644
--- a/chart_suite.module
+++ b/chart_suite.module
@@ -48,7 +48,7 @@ function chart_suite_help(string $routeName, RouteMatchInterface $routeMatch)
 
     // Get module information.
     $moduleTitle = \Drupal::moduleHandler()->getName('chart_suite');
-    $imagePath = '/' . drupal_get_path('module', 'chart_suite') . '/images/';
+    $imagePath = '/' . \Drupal::service('extension.list.module')->getPath('chart_suite') . '/images/';
 
     //
     // About
diff --git a/src/Form/AdminSettings.php b/src/Form/AdminSettings.php
index c124109..7402b43 100644
--- a/src/Form/AdminSettings.php
+++ b/src/Form/AdminSettings.php
@@ -54,7 +54,6 @@ final class AdminSettings extends ConfigFormBase {
    * Build form.
    *
    *---------------------------------------------------------------------*/
-
   /**
    * Builds a form to adjust the module settings.
    *
@@ -62,7 +61,7 @@ final class AdminSettings extends ConfigFormBase {
    *
    * @param array $form
    *   An associative array containing the renderable structure of the form.
-   * @param \Drupal\Core\Form\FormStateInterface $formState
+   * @param FormStateInterface $formState
    *   (optional) The current state of the form.
    *
    * @return array
@@ -100,13 +99,12 @@ final class AdminSettings extends ConfigFormBase {
    * Validate.
    *
    *---------------------------------------------------------------------*/
-
   /**
    * Validates the form values.
    *
    * @param array $form
    *   The form configuration.
-   * @param \Drupal\Core\Form\FormStateInterface $formState
+   * @param FormStateInterface $formState
    *   The entered values for the form.
    */
   public function validateForm(array &$form, FormStateInterface $formState) {
@@ -120,13 +118,12 @@ final class AdminSettings extends ConfigFormBase {
    * Submit.
    *
    *---------------------------------------------------------------------*/
-
   /**
    * Stores the submitted form values.
    *
    * @param array $form
    *   The form configuration.
-   * @param \Drupal\Core\Form\FormStateInterface $formState
+   * @param FormStateInterface $formState
    *   The entered values for the form.
    */
   public function submitForm(array &$form, FormStateInterface $formState) {
@@ -142,7 +139,6 @@ final class AdminSettings extends ConfigFormBase {
    * About tab.
    *
    *---------------------------------------------------------------------*/
-
   /**
    * Builds the about tab for the form.
    *
@@ -150,7 +146,7 @@ final class AdminSettings extends ConfigFormBase {
    *
    * @param array $form
    *   An associative array containing the structure of the form.
-   * @param \Drupal\Core\Form\FormStateInterface $formState
+   * @param FormStateInterface $formState
    *   The current state of the form.
    * @param string $tabGroup
    *   The name of the tab group.
@@ -234,7 +230,7 @@ final class AdminSettings extends ConfigFormBase {
    *
    * @param array $form
    *   The form configuration.
-   * @param \Drupal\Core\Form\FormStateInterface $formState
+   * @param FormStateInterface $formState
    *   The entered values for the form.
    */
   private function validateAboutTab(
@@ -248,7 +244,7 @@ final class AdminSettings extends ConfigFormBase {
    *
    * @param array $form
    *   The form configuration.
-   * @param \Drupal\Core\Form\FormStateInterface $formState
+   * @param FormStateInterface $formState
    *   The entered values for the form.
    */
   private function submitAboutTab(
@@ -262,7 +258,6 @@ final class AdminSettings extends ConfigFormBase {
    * Google Charts tab.
    *
    *---------------------------------------------------------------------*/
-
   /**
    * Builds the Google Charts tab for the form.
    *
@@ -270,7 +265,7 @@ final class AdminSettings extends ConfigFormBase {
    *
    * @param array $form
    *   An associative array containing the structure of the form.
-   * @param \Drupal\Core\Form\FormStateInterface $formState
+   * @param FormStateInterface $formState
    *   The current state of the form.
    * @param string $tabGroup
    *   The name of the tab group.
@@ -344,7 +339,7 @@ final class AdminSettings extends ConfigFormBase {
    *
    * @param array $form
    *   The form configuration.
-   * @param \Drupal\Core\Form\FormStateInterface $formState
+   * @param FormStateInterface $formState
    *   The entered values for the form.
    */
   private function validateGoogleChartsTab(
@@ -358,7 +353,7 @@ final class AdminSettings extends ConfigFormBase {
    *
    * @param array $form
    *   The form configuration.
-   * @param \Drupal\Core\Form\FormStateInterface $formState
+   * @param FormStateInterface $formState
    *   The entered values for the form.
    */
   private function submitGoogleChartsTab(
diff --git a/src/Plugin/Field/FieldFormatter/ChartSuiteFileFormatter.php b/src/Plugin/Field/FieldFormatter/ChartSuiteFileFormatter.php
index 2c664bb..ffee936 100644
--- a/src/Plugin/Field/FieldFormatter/ChartSuiteFileFormatter.php
+++ b/src/Plugin/Field/FieldFormatter/ChartSuiteFileFormatter.php
@@ -327,7 +327,7 @@ class ChartSuiteFileFormatter extends FileFormatterBase {
         $elements[$delta] = [
           '#theme'    => 'chart_suite_file_formatter_as_html',
           '#filename' => $file->getFilename(),
-          '#uri'      => file_create_url($file->getFileUri()),
+          '#uri'      => \Drupal::service('file_url_generator')->generateAbsoluteString($file->getFileUri()),
           '#filesize' => $file->getSize(),
           '#dolink'   => $doLink,
           '#markup'   => $markup,
@@ -346,7 +346,7 @@ class ChartSuiteFileFormatter extends FileFormatterBase {
    * - HTML theme containing an HTML version of the table.
    * - JSON theme containing an HTML + JavaScript version of the table.
    *
-   * @param \Drupal\file\FileInterface $file
+   * @param FileInterface $file
    *   The File object whose local file is parsed and presented.
    * @param \SDSC\StructuredData\Table $table
    *   The table to render.
@@ -411,7 +411,7 @@ class ChartSuiteFileFormatter extends FileFormatterBase {
       return [
         '#theme'    => 'chart_suite_file_formatter_as_html',
         '#filename' => $file->getFilename(),
-        '#uri'      => file_create_url($file->getFileUri()),
+        '#uri'      => \Drupal::service('file_url_generator')->generateAbsoluteString($file->getFileUri()),
         '#filesize' => $file->getSize(),
         '#dolink'   => $doLink,
         '#markup'   => HTMLPresenter::encodeTable($table),
@@ -437,7 +437,7 @@ class ChartSuiteFileFormatter extends FileFormatterBase {
     return [
       '#theme'       => $theme,
       '#filename'    => $file->getFilename(),
-      '#uri'         => file_create_url($file->getFileUri()),
+      '#uri'         => \Drupal::service('file_url_generator')->generateAbsoluteString($file->getFileUri()),
       '#filesize'    => $file->getSize(),
       '#id'          => $file->id(),
       '#title'       => $table->getBestName(),
@@ -463,7 +463,7 @@ class ChartSuiteFileFormatter extends FileFormatterBase {
    * - HTML theme containing an HTML version of the tree.
    * - JSON theme containing an HTML + JavaScript version of the tree.
    *
-   * @param \Drupal\file\FileInterface $file
+   * @param FileInterface $file
    *   The File object whose local file is parsed and presented.
    * @param \SDSC\StructuredData\Tree $tree
    *   The tree to render.
@@ -514,7 +514,7 @@ class ChartSuiteFileFormatter extends FileFormatterBase {
     return [
       '#theme'       => 'chart_suite_file_formatter_as_tree',
       '#filename'    => $file->getFilename(),
-      '#uri'         => file_create_url($file->getFileUri()),
+      '#uri'         => \Drupal::service('file_url_generator')->generateAbsoluteString($file->getFileUri()),
       '#filesize'    => $file->getSize(),
       '#id'          => $file->id(),
       '#title'       => $tree->getBestName(),
@@ -537,7 +537,7 @@ class ChartSuiteFileFormatter extends FileFormatterBase {
    * - Search index & results theme containing raw keywords.
    * - HTML theme containing an HTML version of the graph.
    *
-   * @param \Drupal\file\FileInterface $file
+   * @param FileInterface $file
    *   The File object whose local file is parsed and presented.
    * @param \SDSC\StructuredData\Graph $graph
    *   The graph to render.
@@ -594,7 +594,7 @@ class ChartSuiteFileFormatter extends FileFormatterBase {
     return [
       '#theme'    => 'chart_suite_file_formatter_as_html',
       '#filename' => $file->getFilename(),
-      '#uri'      => file_create_url($file->getFileUri()),
+      '#uri'      => \Drupal::service('file_url_generator')->generateAbsoluteString($file->getFileUri()),
       '#filesize' => $file->getSize(),
       '#dolink'   => $doLink,
       '#markup'   => HTMLPresenter::encodeGraph($graph),
diff --git a/src/SDSC/StructuredData/Format/AbstractFormat.php b/src/SDSC/StructuredData/Format/AbstractFormat.php
index 32bb734..9595278 100644
--- a/src/SDSC/StructuredData/Format/AbstractFormat.php
+++ b/src/SDSC/StructuredData/Format/AbstractFormat.php
@@ -8,6 +8,9 @@
 
 namespace Drupal\chart_suite\SDSC\StructuredData\Format;
 
+use Drupal\chart_suite\SDSC\StructuredData\Format\Drupal\chart_suite\SDSC\StructuredData\Table;
+use Drupal\chart_suite\SDSC\StructuredData\Format\Drupal\chart_suite\SDSC\StructuredData\Tree;
+use Drupal\chart_suite\SDSC\StructuredData\Format\Drupal\chart_suite\SDSC\StructuredData\Graph;
 use Drupal\chart_suite\SDSC\StructuredData\AbstractData;
 /**
  * @class AbstractFormat
@@ -204,13 +207,13 @@ abstract class AbstractFormat
         if ( $object == NULL )
             return false;
 
-        if ( $object instanceof Drupal\chart_suite\SDSC\StructuredData\Table )
+        if ( $object instanceof Table )
             return $this->canEncodeTables( );
 
-        if ( $object instanceof Drupal\chart_suite\SDSC\StructuredData\Tree )
+        if ( $object instanceof Tree )
             return $this->canEncodeTrees( );
 
-        if ( $object instanceof Drupal\chart_suite\SDSC\StructuredData\Graph )
+        if ( $object instanceof Graph )
             return $this->canEncodeGraphs( );
 
         return false;

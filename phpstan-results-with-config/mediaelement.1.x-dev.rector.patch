diff --git a/src/Form/MediaElementConfigForm.php b/src/Form/MediaElementConfigForm.php
index af83773..30f767e 100644
--- a/src/Form/MediaElementConfigForm.php
+++ b/src/Form/MediaElementConfigForm.php
@@ -259,7 +259,7 @@ class MediaElementConfigForm extends ConfigFormBase {
    *
    * @param array $fields
    *   The field names we want to parse.
-   * @param \Drupal\Core\Form\FormStateInterface $form_state
+   * @param FormStateInterface $form_state
    *   The submitted form state.
    *
    * @return array
diff --git a/src/Plugin/Field/FieldFormatter/MediaElementVideoFieldFormatter.php b/src/Plugin/Field/FieldFormatter/MediaElementVideoFieldFormatter.php
index ec19876..c34264c 100755
--- a/src/Plugin/Field/FieldFormatter/MediaElementVideoFieldFormatter.php
+++ b/src/Plugin/Field/FieldFormatter/MediaElementVideoFieldFormatter.php
@@ -2,6 +2,7 @@
 
 namespace Drupal\mediaelement\Plugin\Field\FieldFormatter;
 
+use Drupal\image\ImageStyleInterface;
 use Symfony\Component\DependencyInjection\ContainerInterface;
 use Drupal\Core\Plugin\ContainerFactoryPluginInterface;
 use Drupal\Core\Field\FieldDefinitionInterface;
@@ -37,21 +38,21 @@ class MediaElementVideoFieldFormatter extends FileVideoFormatter implements Cont
   /**
    * Entity Type Manager service.
    *
-   * @var \Drupal\Core\Entity\EntityTypeManager
+   * @var EntityTypeManager
    */
   protected $entityTypeManager;
 
   /**
    * Entity Field Manager service.
    *
-   * @var \Drupal\Core\Entity\EntityFieldManager
+   * @var EntityFieldManager
    */
   protected $entityFieldManager;
 
   /**
    * Image Style entity storage.
    *
-   * @var \Drupal\image\ImageStyleInterface
+   * @var ImageStyleInterface
    */
   protected $imageStyleStorage;
 
@@ -62,7 +63,7 @@ class MediaElementVideoFieldFormatter extends FileVideoFormatter implements Cont
    *   The plugin_id for the formatter.
    * @param mixed $plugin_definition
    *   The plugin implementation definition.
-   * @param \Drupal\Core\Field\FieldDefinitionInterface $field_definition
+   * @param FieldDefinitionInterface $field_definition
    *   The definition of the field to which the formatter is associated.
    * @param array $settings
    *   The formatter settings.
@@ -72,9 +73,9 @@ class MediaElementVideoFieldFormatter extends FileVideoFormatter implements Cont
    *   The view mode.
    * @param array $third_party_settings
    *   Any third party settings settings.
-   * @param \Drupal\Core\Entity\EntityTypeManager $entity_type_manager
+   * @param EntityTypeManager $entity_type_manager
    *   The entity manager.
-   * @param \Drupal\Core\Entity\EntityFieldManager $entity_field_manager
+   * @param EntityFieldManager $entity_field_manager
    *   The entity field manager.
    */
   public function __construct(
@@ -169,7 +170,7 @@ class MediaElementVideoFieldFormatter extends FileVideoFormatter implements Cont
   /**
    * Returns the file path for the video's `poster` attribute, if set.
    *
-   * @param \Drupal\Core\Entity\EntityInterface $entity
+   * @param EntityInterface $entity
    *   The current entity being rendered.
    *
    * @return string
@@ -186,10 +187,10 @@ class MediaElementVideoFieldFormatter extends FileVideoFormatter implements Cont
     $image_uri = $entity->{$image_field}->entity->getFileUri();
 
     $image_url = $image_style == 'raw'
-      ? file_create_url($image_uri)
+      ? \Drupal::service('file_url_generator')->generateAbsoluteString($image_uri)
       : $this->imageStyleStorage->load($image_style)->buildUrl($image_uri);
 
-    return file_url_transform_relative($image_url);
+    return \Drupal::service('file_url_generator')->transformRelative($image_url);
   }
 
   /**

diff --git a/src/Controller/CoffeeController.php b/src/Controller/CoffeeController.php
index 18aa026..28267c0 100644
--- a/src/Controller/CoffeeController.php
+++ b/src/Controller/CoffeeController.php
@@ -2,6 +2,8 @@
 
 namespace Drupal\coffee\Controller;
 
+use Drupal\Core\Config\ImmutableConfig;
+use Drupal\Core\Menu\MenuLinkTreeElement;
 use Drupal\Core\Access\AccessManagerInterface;
 use Drupal\Core\Config\ConfigFactoryInterface;
 use Drupal\Core\Controller\ControllerBase;
@@ -22,55 +24,55 @@ class CoffeeController extends ControllerBase {
   /**
    * The coffee config.
    *
-   * @var \Drupal\Core\Config\ImmutableConfig
+   * @var ImmutableConfig
    */
   protected $config;
 
   /**
    * The menu link tree service.
    *
-   * @var \Drupal\Core\Menu\MenuLinkTreeInterface
+   * @var MenuLinkTreeInterface
    */
   protected $menuLinkTree;
 
   /**
    * The local task manager service.
    *
-   * @var \Drupal\Core\Menu\LocalTaskManagerInterface
+   * @var LocalTaskManagerInterface
    */
   protected $localTaskManager;
 
   /**
    * The access manager service.
    *
-   * @var \Drupal\Core\Access\AccessManagerInterface
+   * @var AccessManagerInterface
    */
   protected $accessManager;
 
   /**
    * The url generator service.
    *
-   * @var \Drupal\Core\Routing\UrlGeneratorInterface
+   * @var UrlGeneratorInterface
    */
   protected $urlGenerator;
 
   /**
    * The current route match.
    *
-   * @var \Drupal\Core\Routing\RouteMatchInterface
+   * @var RouteMatchInterface
    */
   protected $routeMatch;
 
   /**
    * Constructs a new CoffeeController object.
    *
-   * @param \Drupal\Core\Config\ConfigFactoryInterface $config_factory
+   * @param ConfigFactoryInterface $config_factory
    *   The config factory service.
-   * @param \Drupal\Core\Menu\MenuLinkTreeInterface $menu_link_tree
+   * @param MenuLinkTreeInterface $menu_link_tree
    *   The menu link tree service.
-   * @param \Drupal\Core\Menu\LocalTaskManagerInterface $local_task_manager
+   * @param LocalTaskManagerInterface $local_task_manager
    *   The local task manager service.
-   * @param \Drupal\Core\Access\AccessManagerInterface $access_manager
+   * @param AccessManagerInterface $access_manager
    *   The access manager service.
    */
   public function __construct(ConfigFactoryInterface $config_factory, MenuLinkTreeInterface $menu_link_tree, LocalTaskManagerInterface $local_task_manager, AccessManagerInterface $access_manager, UrlGeneratorInterface $url_generator, RouteMatchInterface $route_match) {
@@ -99,7 +101,7 @@ class CoffeeController extends ControllerBase {
   /**
    * Outputs the data that is used for the Coffee autocompletion in JSON.
    *
-   * @return \Symfony\Component\HttpFoundation\JsonResponse
+   * @return JsonResponse
    *   The json response.
    */
   public function coffeeData() {
@@ -152,7 +154,7 @@ class CoffeeController extends ControllerBase {
    * @param string $menu_name
    *   The menu name.
    *
-   * @return \Drupal\Core\Menu\MenuLinkTreeElement[]
+   * @return MenuLinkTreeElement[]
    *   A flatten array of menu link tree elements for the given menu.
    */
   protected function getMenuTreeElements($menu_name) {
diff --git a/tests/src/Functional/CoffeeTest.php b/tests/src/Functional/CoffeeTest.php
index 9a59bb2..295afcc 100644
--- a/tests/src/Functional/CoffeeTest.php
+++ b/tests/src/Functional/CoffeeTest.php
@@ -2,6 +2,7 @@
 
 namespace Drupal\Tests\coffee\Functional;
 
+use Drupal\user\UserInterface;
 use Drupal\menu_link_content\Entity\MenuLinkContent;
 use Drupal\system\Entity\Menu;
 use Drupal\Tests\BrowserTestBase;
@@ -24,28 +25,28 @@ class CoffeeTest extends BrowserTestBase {
   /**
    * The user for tests.
    *
-   * @var \Drupal\user\UserInterface
+   * @var UserInterface
    */
   protected $webUser;
 
   /**
    * The user for tests.
    *
-   * @var \Drupal\user\UserInterface
+   * @var UserInterface
    */
   protected $coffeeUser;
 
   /**
    * The user for tests.
    *
-   * @var \Drupal\user\UserInterface
+   * @var UserInterface
    */
   protected $coffeeAdmin;
 
   /**
    * {@inheritdoc}
    */
-  public function setUp() {
+  public function setUp(): void {
     parent::setUp();
 
     $this->webUser = $this->drupalCreateUser();
@@ -71,7 +72,8 @@ class CoffeeTest extends BrowserTestBase {
       'coffee_menus[account]' => 'account',
       'max_results' => 15,
     ];
-    $this->drupalPostForm('admin/config/user-interface/coffee', $edit, t('Save configuration'));
+    $this->drupalGet('admin/config/user-interface/coffee');
+    $this->submitForm($edit, t('Save configuration'));
     $this->assertSession()->pageTextContains(t('The configuration options have been saved.'));
 
     $expected = [
@@ -190,6 +192,6 @@ class CoffeeTest extends BrowserTestBase {
     $token = substr($result[0]->value, strpos($result[0]->value, 'token=') + 6);
 
     $this->drupalGet('/coffee-test-csrf', ['query' => ['token' => $token]]);
-    $this->assertResponse(200);
+    $this->assertSession()->statusCodeEquals(200);
   }
 }
diff --git a/tests/src/Kernel/CoffeeCommandsTest.php b/tests/src/Kernel/CoffeeCommandsTest.php
index 8253743..e1813a7 100644
--- a/tests/src/Kernel/CoffeeCommandsTest.php
+++ b/tests/src/Kernel/CoffeeCommandsTest.php
@@ -25,7 +25,7 @@ class CoffeeCommandsTest extends KernelTestBase {
   /**
    * {@inheritdoc}
    */
-  protected function setUp() {
+  protected function setUp(): void {
     parent::setUp();
 
     $this->installSchema('system', ['sequences', 'router']);

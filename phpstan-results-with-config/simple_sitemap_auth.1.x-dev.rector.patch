diff --git a/src/Form/SimplesitemapauthSettingsForm.php b/src/Form/SimplesitemapauthSettingsForm.php
index 4a8f7ec..4b7d33e 100644
--- a/src/Form/SimplesitemapauthSettingsForm.php
+++ b/src/Form/SimplesitemapauthSettingsForm.php
@@ -2,6 +2,8 @@
 
 namespace Drupal\simple_sitemap_auth\Form;
 
+use Drupal\user\UserStorage;
+use Drupal\Core\Session\AccountInterface;
 use Symfony\Component\DependencyInjection\ContainerInterface;
 use Drupal\Core\Entity\EntityTypeManager;
 use Drupal\Core\Form\FormStateInterface;
@@ -20,27 +22,27 @@ class SimplesitemapauthSettingsForm extends SimplesitemapFormBase {
   /**
    * Language manager.
    *
-   * @var \Drupal\Core\Language\LanguageManagerInterface
+   * @var LanguageManagerInterface
    */
   protected $languageManager;
 
   /**
    * The variable containing the user manager.
    *
-   * @var \Drupal\user\UserStorage
+   * @var UserStorage
    */
   protected $userManager;
 
   /**
    * SimplesitemapSettingsForm constructor.
    *
-   * @param \Drupal\simple_sitemap\Simplesitemap $generator
+   * @param Simplesitemap $generator
    *   Sitemap generator.
-   * @param \Drupal\simple_sitemap\Form\FormHelper $form_helper
+   * @param FormHelper $form_helper
    *   Form helper.
-   * @param \Drupal\Core\Language\LanguageManagerInterface $language_manager
+   * @param LanguageManagerInterface $language_manager
    *   Language manager.
-   * @param \Drupal\Core\Entity\EntityTypeManager $entityTypeManager
+   * @param EntityTypeManager $entityTypeManager
    *   Entity type manager.
    */
   public function __construct(
@@ -150,7 +152,7 @@ class SimplesitemapauthSettingsForm extends SimplesitemapFormBase {
       ->condition($user_group)
       ->execute();
     $all_users = $this->userManager->loadMultiple($ids);
-    /** @var \Drupal\Core\Session\AccountInterface $value */
+    /** @var AccountInterface $value */
     foreach ($all_users as $key => $value) {
       $filtered_users[$key] = $value->getDisplayName();
     }
diff --git a/src/Plugin/simple_sitemap/UrlGenerator/AuthEntityUrlGenerator.php b/src/Plugin/simple_sitemap/UrlGenerator/AuthEntityUrlGenerator.php
index cd17dcf..e26587c 100644
--- a/src/Plugin/simple_sitemap/UrlGenerator/AuthEntityUrlGenerator.php
+++ b/src/Plugin/simple_sitemap/UrlGenerator/AuthEntityUrlGenerator.php
@@ -2,6 +2,7 @@
 
 namespace Drupal\simple_sitemap_auth\Plugin\simple_sitemap\UrlGenerator;
 
+use Drupal\Core\Entity\EntityInterface;
 use Drupal\simple_sitemap\EntityHelper;
 use Drupal\simple_sitemap\Logger;
 use Drupal\simple_sitemap\Plugin\simple_sitemap\UrlGenerator\EntityUrlGenerator;
@@ -35,19 +36,19 @@ class AuthEntityUrlGenerator extends EntityUrlGenerator {
    *   PluginID.
    * @param mixed $plugin_definition
    *   Plugin definition.
-   * @param \Drupal\simple_sitemap\Simplesitemap $generator
+   * @param Simplesitemap $generator
    *   Sitemap generator.
-   * @param \Drupal\simple_sitemap\Logger $logger
+   * @param Logger $logger
    *   Logger.
-   * @param \Drupal\Core\Language\LanguageManagerInterface $language_manager
+   * @param LanguageManagerInterface $language_manager
    *   Language manager.
-   * @param \Drupal\Core\Entity\EntityTypeManagerInterface $entity_type_manager
+   * @param EntityTypeManagerInterface $entity_type_manager
    *   Entity type manager.
-   * @param \Drupal\simple_sitemap\EntityHelper $entityHelper
+   * @param EntityHelper $entityHelper
    *   Entity helper.
-   * @param \Drupal\simple_sitemap\Plugin\simple_sitemap\UrlGenerator\UrlGeneratorManager $url_generator_manager
+   * @param UrlGeneratorManager $url_generator_manager
    *   Url generator manager.
-   * @param \Drupal\Core\Cache\MemoryCache\MemoryCacheInterface $memory_cache
+   * @param MemoryCacheInterface $memory_cache
    *   Memory cache.
    */
   public function __construct(
@@ -102,7 +103,7 @@ class AuthEntityUrlGenerator extends EntityUrlGenerator {
   /**
    * Set user access for generated links.
    *
-   * @return \Drupal\user\Entity\User|\Drupal\Core\Entity\EntityInterface|null
+   * @return User|EntityInterface|null
    *   User object.
    */
   protected function getUser() {
diff --git a/tests/src/Functional/SimplesitemapAuthTest.php b/tests/src/Functional/SimplesitemapAuthTest.php
index 3043a45..43967fb 100644
--- a/tests/src/Functional/SimplesitemapAuthTest.php
+++ b/tests/src/Functional/SimplesitemapAuthTest.php
@@ -2,6 +2,8 @@
 
 namespace Drupal\Tests\simple_sitemap_auth\Functional;
 
+use Drupal\user\Entity\User;
+use Drupal\node\Entity\Node;
 use Drupal\simple_sitemap\Queue\QueueWorker;
 use Drupal\Tests\simple_sitemap\Functional\SimplesitemapTestBase;
 
@@ -28,21 +30,21 @@ class SimplesitemapAuthTest extends SimplesitemapTestBase {
   /**
    * Auth user.
    *
-   * @var \Drupal\user\Entity\User
+   * @var User
    */
   protected $user;
 
   /**
    * A node.
    *
-   * @var \Drupal\node\Entity\Node
+   * @var Node
    */
   protected $node3;
 
   /**
    * {@inheritdoc}
    */
-  protected function setUp() {
+  protected function setUp(): void {
     parent::setUp();
 
     $this->user = $this->drupalCreateUser([], NULL, FALSE);
@@ -79,7 +81,8 @@ class SimplesitemapAuthTest extends SimplesitemapTestBase {
       'anonymous[view any page content]' => TRUE,
       'authenticated[view any article content]' => TRUE,
     ], 'Save permissions');
-    $this->drupalPostForm('admin/reports/status/rebuild', [], 'Rebuild permissions');
+    $this->drupalGet('admin/reports/status/rebuild');
+    $this->submitForm([], 'Rebuild permissions');
 
     // Set permissions.
     $this->node3 = $this->createNode(['title' => 'Node3', 'type' => 'article']);

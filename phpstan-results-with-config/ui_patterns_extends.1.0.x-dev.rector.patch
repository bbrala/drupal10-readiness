diff --git a/src/UIPatternsExtends.php b/src/UIPatternsExtends.php
index 0334c36..550509e 100644
--- a/src/UIPatternsExtends.php
+++ b/src/UIPatternsExtends.php
@@ -12,14 +12,14 @@ class UIPatternsExtends {
   /**
    * The pattern definition.
    *
-   * @var \Drupal\ui_patterns\Definition\PatternDefinition
+   * @var PatternDefinition
    */
   private $definition;
 
   /**
    * List of all pattern definitions.
    *
-   * @var \Drupal\ui_patterns\Definition\PatternDefinition[]
+   * @var PatternDefinition[]
    */
   private $definitions;
 
diff --git a/tests/src/Kernel/UIPatternExtendsTest.php b/tests/src/Kernel/UIPatternExtendsTest.php
index 936e692..65dcc8b 100644
--- a/tests/src/Kernel/UIPatternExtendsTest.php
+++ b/tests/src/Kernel/UIPatternExtendsTest.php
@@ -51,12 +51,12 @@ class UIPatternExtendsTest extends AbstractUiPatternsTest {
   public function testExtendComplete($pattern_id, $fields_count, $settings_count) {
     UiPatterns::getManager()->clearCachedDefinitions();
     $definitions = UiPatterns::getManager()->getDefinitions();
-    $this->assertEqual(count($definitions[$pattern_id]->getFields()), $fields_count);
+    $this->assertEquals(count($definitions[$pattern_id]->getFields()), $fields_count);
     $additional = $definitions[$pattern_id]->getAdditional();
     if (!isset($additional['settings'])) {
       $additional['settings'] = [];
     }
-    $this->assertEqual(count($additional['settings']), $settings_count);
+    $this->assertEquals(count($additional['settings']), $settings_count);
   }
 
 }
diff --git a/ui_patterns_extends.module b/ui_patterns_extends.module
index ccb20c7..f6c92ca 100644
--- a/ui_patterns_extends.module
+++ b/ui_patterns_extends.module
@@ -1,5 +1,6 @@
 <?php
 
+use Drupal\ui_patterns\Definition\PatternDefinition;
 /**
  * @file
  * Contains ui_patterns_extends.module.
@@ -11,7 +12,7 @@ use Drupal\ui_patterns_extends\UIPatternsExtends;
  * Implements hook_ui_patterns_info_alter().
  */
 function ui_patterns_extends_ui_patterns_info_alter(array &$definitions) {
-  /** @var \Drupal\ui_patterns\Definition\PatternDefinition $definition */
+  /** @var PatternDefinition $definition */
   foreach ($definitions as $definition) {
     $additional = $definition->getAdditional();
     if (isset($additional['extends'])) {

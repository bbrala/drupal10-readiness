modules/contrib/social_timeline/src/Plugin/Block/SocialTimelineBlock.php
modules/contrib/social_timeline/src/SocialTimelineManager.php

2 files with changes
====================

1) modules/contrib/social_timeline/src/SocialTimelineManager.php:182

    ---------- begin diff ----------
@@ @@
   public static function formatDefaultFeeds($feeds = array()) {
     $default_feeds = static::getFeeds();
     isset($_SERVER['HTTPS']) ? $protocol = 'https://' : $protocol = 'http://';
-    $module_full_path = $protocol . $_SERVER['HTTP_HOST']. base_path() . drupal_get_path('module', 'social_timeline');
+    $module_full_path = $protocol . $_SERVER['HTTP_HOST']. base_path() . \Drupal::service('extension.list.module')->getPath('social_timeline');

     $feed_settings = array();
     foreach ($feeds as $k => $v) {
    ----------- end diff -----------

Applied rules:
 * DrupalGetPathRector
 * ArgumentRemoverRector
 * PseudoNamespaceToNamespaceRector
 * WrapReturnRector


2) modules/contrib/social_timeline/src/Plugin/Block/SocialTimelineBlock.php:370

    ---------- begin diff ----------
@@ @@
    *
    * @param array $form
    *   The form array.
-   * @param \Drupal\Core\Form\FormStateInterface $form_state
+   * @param FormStateInterface $form_state
    *   The form state object.
    *
    * @return array
@@ @@
    *
    * @param array $form
    *   The form array.
-   * @param \Drupal\Core\Form\FormStateInterface $form_state
+   * @param FormStateInterface $form_state
    *   The form state object.
    *
    * @return array
    ----------- end diff -----------

Applied rules:
 * ArgumentRemoverRector
 * PseudoNamespaceToNamespaceRector
 * WrapReturnRector
 * AddParamTypeDeclarationRector
 * ChangeMethodVisibilityRector


 [OK] 2 files have been changed by Rector                                       


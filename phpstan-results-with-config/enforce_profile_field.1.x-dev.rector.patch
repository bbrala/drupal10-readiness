diff --git a/enforce_profile_field.module b/enforce_profile_field.module
index 3d96ba1..5d4d767 100644
--- a/enforce_profile_field.module
+++ b/enforce_profile_field.module
@@ -1,5 +1,8 @@
 <?php
 
+use Drupal\Core\Entity\Display\EntityFormDisplayInterface;
+use Drupal\Core\Field\FieldDefinitionInterface;
+use Drupal\field\Entity\FieldConfig;
 /**
  * @file
  * Enforce Profile Field module.
@@ -32,9 +35,9 @@ use Drupal\enforce_profile_field\EnforceProfile;
  * For consistency, the set of values returned when an $entity is provided
  * should be a subset of the values returned when no $entity is provided.
  *
- * @param \Drupal\Core\Field\FieldStorageDefinitionInterface $definition
+ * @param FieldStorageDefinitionInterface $definition
  *   The field storage definition.
- * @param \Drupal\Core\Entity\FieldableEntityInterface|null $entity
+ * @param FieldableEntityInterface|null $entity
  *   (optional) The entity context if known, or NULL if the allowed values are
  *   being collected without the context of a specific entity.
  * @param bool &$cacheable
@@ -72,7 +75,7 @@ function enforce_profile_field_allowed_values(FieldStorageDefinitionInterface $d
     $form_mode_id = $entity_type_id . '.' . $bundle . '.' . $form_mode;
 
     // Get the selected entity form display.
-    /** @var \Drupal\Core\Entity\Display\EntityFormDisplayInterface $entity_form_display */
+    /** @var EntityFormDisplayInterface $entity_form_display */
     $entity_form_display = \Drupal::entityTypeManager()
       ->getStorage('entity_form_display')
       ->load($form_mode_id);
@@ -87,7 +90,7 @@ function enforce_profile_field_allowed_values(FieldStorageDefinitionInterface $d
       foreach ($fields as $machine_name => $field_settings) {
         // Process only standard fields.
         if (isset($fields_definition[$machine_name])) {
-          /** @var \Drupal\Core\Field\FieldDefinitionInterface $field */
+          /** @var FieldDefinitionInterface $field */
           $field = $fields_definition[$machine_name];
           $field_label = $field->getLabel();
 
@@ -103,7 +106,7 @@ function enforce_profile_field_allowed_values(FieldStorageDefinitionInterface $d
 /**
  * Get Enforce Profile field form mode.
  *
- * @param \Drupal\Core\Entity\EntityInterface $entity
+ * @param EntityInterface $entity
  *   An entity holding the field.
  * @param string $field_name
  *   A field name.
@@ -125,7 +128,7 @@ function enforce_profile_field_get_form_mode(EntityInterface $entity, $field_nam
 /**
  * Get Enforce Profile field active view modes.
  *
- * @param \Drupal\Core\Entity\EntityInterface $entity
+ * @param EntityInterface $entity
  *   An entity holding the field.
  * @param string $field_name
  *   A field name.
@@ -192,9 +195,9 @@ function enforce_profile_field_entity_view_alter(array &$build, EntityInterface
 /**
  * Get entity display view mode.
  *
- * @param \Drupal\Core\Entity\EntityInterface $entity
+ * @param EntityInterface $entity
  *   An entity.
- * @param \Drupal\Core\Entity\Display\EntityViewDisplayInterface $display
+ * @param EntityViewDisplayInterface $display
  *   A display.
  *
  * @return int|null|string
@@ -224,7 +227,7 @@ function enforce_profile_field_form_alter(&$form, FormStateInterface $form_state
   switch ($form_id) {
     // Disable cardinality settings and set it as unlimited by default.
     case 'field_storage_config_edit_form':
-      /** @var \Drupal\field\Entity\FieldConfig $field_config */
+      /** @var FieldConfig $field_config */
       $field_config = $form_state->getStorage()['field_config'];
       // Only for enforce profile field.
       if ($field_config->getType() == 'enforce_profile_field') {
diff --git a/src/EnforceProfile.php b/src/EnforceProfile.php
index 072b9ce..ecc7efd 100644
--- a/src/EnforceProfile.php
+++ b/src/EnforceProfile.php
@@ -2,6 +2,8 @@
 
 namespace Drupal\enforce_profile_field;
 
+use Drupal\user\UserInterface;
+use Drupal\Core\Entity\Display\EntityFormDisplayInterface;
 use Drupal\Core\Entity\EntityInterface;
 use Drupal\Core\Field\FieldItemListInterface;
 use Drupal\Core\Url;
@@ -41,7 +43,7 @@ class EnforceProfile {
   /**
    * Get from mode.
    *
-   * @param \Drupal\Core\Entity\EntityInterface $entity
+   * @param EntityInterface $entity
    *   An entity holding the form mode field.
    * @param string $field_name
    *   A field machine name.
@@ -60,7 +62,7 @@ class EnforceProfile {
   /**
    * Validate field items.
    *
-   * @param \Drupal\Core\Field\FieldItemListInterface $item
+   * @param FieldItemListInterface $item
    *   A field item list.
    */
   public function validate(FieldItemListInterface $item) {
@@ -71,12 +73,12 @@ class EnforceProfile {
   /**
    * Collect required fields.
    *
-   * @param \Drupal\Core\Field\FieldItemListInterface $item
+   * @param FieldItemListInterface $item
    *   A field item list.
    */
   private function collectRequiredFields(FieldItemListInterface $item) {
     $user = \Drupal::currentUser();
-    /** @var \Drupal\user\UserInterface $user_account */
+    /** @var UserInterface $user_account */
     $user_account = \Drupal::entityTypeManager()
       ->getStorage('user')
       ->load($user->id());
@@ -102,7 +104,7 @@ class EnforceProfile {
   /**
    * Set required fields.
    *
-   * @param \Drupal\Core\Field\FieldItemListInterface[] $fields
+   * @param FieldItemListInterface[] $fields
    *   An array of required field items.
    */
   private function setRequiredFields(array $fields) {
@@ -112,7 +114,7 @@ class EnforceProfile {
   /**
    * Validate required fields.
    *
-   * @param \Drupal\Core\Field\FieldItemListInterface $item
+   * @param FieldItemListInterface $item
    *   A field item list.
    */
   private function validateRequiredFields(FieldItemListInterface $item) {
@@ -131,7 +133,7 @@ class EnforceProfile {
   /**
    * Get required fields.
    *
-   * @return \Drupal\Core\Field\FieldItemListInterface[]
+   * @return FieldItemListInterface[]
    *   An array of required field items.
    */
   private function getRequiredFields() {
@@ -141,7 +143,7 @@ class EnforceProfile {
   /**
    * Redirect the user to fill in missing fields.
    *
-   * @param \Drupal\Core\Field\FieldItemListInterface $item
+   * @param FieldItemListInterface $item
    *   A field item list.
    */
   private function redirect(FieldItemListInterface $item) {
@@ -218,14 +220,14 @@ class EnforceProfile {
    * @param string $bundle
    *   A bundle machine name.
    *
-   * @return \Drupal\Core\Entity\EntityInterface|null
+   * @return EntityInterface|null
    *   A form display.
    */
   private function getFormDisplay($entity_type_id = 'user', $bundle = 'user') {
     $form_mode_id = $entity_type_id . '.' . $bundle . '.' . $this->formMode;
 
     // Get the selected entity form display.
-    /** @var \Drupal\Core\Entity\Display\EntityFormDisplayInterface $entity_form_display */
+    /** @var EntityFormDisplayInterface $entity_form_display */
     $form_display = \Drupal::entityTypeManager()
       ->getStorage('entity_form_display')
       ->load($form_mode_id);
diff --git a/src/Plugin/Field/FieldType/EnforceProfileItem.php b/src/Plugin/Field/FieldType/EnforceProfileItem.php
index f03e0d3..74a2a7f 100644
--- a/src/Plugin/Field/FieldType/EnforceProfileItem.php
+++ b/src/Plugin/Field/FieldType/EnforceProfileItem.php
@@ -2,6 +2,7 @@
 
 namespace Drupal\enforce_profile_field\Plugin\Field\FieldType;
 
+use Drupal\Core\Entity\EntityDisplayRepositoryInterface;
 use Drupal\Core\Field\FieldStorageDefinitionInterface;
 use Drupal\Core\Form\FormStateInterface;
 use Drupal\Core\TypedData\DataDefinition;
@@ -126,7 +127,7 @@ class EnforceProfileItem extends ListItemBase {
    *   An array of entity type's from modes by id.
    */
   private function getFormModes($entity_type_id = 'user', $bundle = 'user') {
-    /** @var \Drupal\Core\Entity\EntityDisplayRepositoryInterface $entity_display_repository */
+    /** @var EntityDisplayRepositoryInterface $entity_display_repository */
     $entity_display_repository = \Drupal::service('entity_display.repository');
     // Get form modes of the user entity type.
     $options = $entity_display_repository->getFormModeOptionsByBundle($entity_type_id, $bundle);
@@ -155,7 +156,7 @@ class EnforceProfileItem extends ListItemBase {
     $entity_type_id = $this->getEntity()->getEntityTypeId();
     $entity_bundle = $this->getEntity()->bundle();
 
-    /** @var \Drupal\Core\Entity\EntityDisplayRepositoryInterface $entity_display_repository */
+    /** @var EntityDisplayRepositoryInterface $entity_display_repository */
     $entity_display_repository = \Drupal::service('entity_display.repository');
     // Get active view modes.
     $options = $entity_display_repository->getViewModeOptionsByBundle($entity_type_id, $entity_bundle);

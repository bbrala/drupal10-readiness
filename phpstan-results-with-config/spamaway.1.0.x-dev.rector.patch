diff --git a/spamaway.install b/spamaway.install
index baeecb2..a02467a 100644
--- a/spamaway.install
+++ b/spamaway.install
@@ -1,5 +1,6 @@
 <?php
 
+use Drupal\Core\Database\Database;
 /**
  * Implements hook_schema().
  *
@@ -73,7 +74,7 @@ function spamaway_schema() {
 function spamaway_update_9001() {
   $table = 'spamaway_webform_submission';
   $schema_definition = spamaway_schema();
-  $schema = \Drupal\Core\Database\Database::getConnection()->schema();
+  $schema = Database::getConnection()->schema();
 
   if (!$schema->tableExists($table)) {
     $schema->createTable($table, $schema_definition['spamaway_webform_submission']);
diff --git a/src/Plugin/WebformHandler/AntiSpamHandler.php b/src/Plugin/WebformHandler/AntiSpamHandler.php
index 5877408..08c51e4 100644
--- a/src/Plugin/WebformHandler/AntiSpamHandler.php
+++ b/src/Plugin/WebformHandler/AntiSpamHandler.php
@@ -2,6 +2,7 @@
 
 namespace Drupal\spamaway\Plugin\WebformHandler;
 
+use Drupal\Core\Database\Connection;
 use Drupal\Core\Form\FormStateInterface;
 use Drupal\webform\Plugin\WebformHandlerBase;
 use Drupal\webform\WebformSubmissionInterface;
@@ -29,7 +30,7 @@ class AntiSpamHandler extends WebformHandlerBase {
   const SPAMAWAY_SUBMISSION_TABLE = 'spamaway_webform_submission';
 
   /**
-   * @var \Drupal\Core\Database\Connection $connection
+   * @var Connection $connection
    */
   protected $connection;
 
@@ -275,9 +276,9 @@ class AntiSpamHandler extends WebformHandlerBase {
    *
    * @param array $form
    *   An associative array containing the structure of the form.
-   * @param \Drupal\Core\Form\FormStateInterface $form_state
+   * @param FormStateInterface $form_state
    *   The current state of the form.
-   * @param \Drupal\webform\WebformSubmissionInterface $webform_submission
+   * @param WebformSubmissionInterface $webform_submission
    *   A webform submission.
    */
   public function validateForm(array &$form, FormStateInterface $form_state, WebformSubmissionInterface $webform_submission) {
@@ -332,9 +333,9 @@ class AntiSpamHandler extends WebformHandlerBase {
    *
    * @param array $form
    *   An associative array containing the structure of the form.
-   * @param \Drupal\Core\Form\FormStateInterface $form_state
+   * @param FormStateInterface $form_state
    *   The current state of the form.
-   * @param \Drupal\webform\WebformSubmissionInterface $webform_submission
+   * @param WebformSubmissionInterface $webform_submission
    *   A webform submission.
    */
   protected function validateFormWithSavedSubmissions(array &$form, FormStateInterface $form_state, WebformSubmissionInterface $webform_submission) {
@@ -413,9 +414,9 @@ class AntiSpamHandler extends WebformHandlerBase {
    *
    * @param array $form
    *   An associative array containing the structure of the form.
-   * @param \Drupal\Core\Form\FormStateInterface $form_state
+   * @param FormStateInterface $form_state
    *   The current state of the form.
-   * @param \Drupal\webform\WebformSubmissionInterface $webform_submission
+   * @param WebformSubmissionInterface $webform_submission
    *   A webform submission.
    */
   public function validateFormCustomSubmissions(array &$form, FormStateInterface $form_state, WebformSubmissionInterface $webform_submission) {

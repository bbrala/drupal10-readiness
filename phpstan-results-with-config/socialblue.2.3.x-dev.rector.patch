diff --git a/socialblue.theme b/socialblue.theme
index 7325b13..83fd5ff 100644
--- a/socialblue.theme
+++ b/socialblue.theme
@@ -30,7 +30,7 @@ function socialblue_library_info_alter(&$libraries, $extension) {
   // Add extra CSS files if specific style is chosen.
   $style = theme_get_setting('style');
   if (!empty($style) && $extension === 'socialblue') {
-    $theme_path = drupal_get_path('theme', $extension);
+    $theme_path = \Drupal::service('extension.list.theme')->getPath($extension);
     $base_path = 'assets/css/';
     foreach ($libraries as $key => $library) {
       $style_css = $base_path . $key . '--' . $style . '.css';
diff --git a/src/Plugin/Preprocess/Node.php b/src/Plugin/Preprocess/Node.php
index b14220c..0126dfd 100644
--- a/src/Plugin/Preprocess/Node.php
+++ b/src/Plugin/Preprocess/Node.php
@@ -2,6 +2,7 @@
 
 namespace Drupal\socialblue\Plugin\Preprocess;
 
+use Drupal\node\NodeInterface;
 use Drupal\Core\Url;
 use Drupal\socialbase\Plugin\Preprocess\Node as NodeBase;
 use Drupal\bootstrap\Utility\Element;
@@ -27,7 +28,7 @@ class Node extends NodeBase {
       return;
     }
 
-    /** @var \Drupal\node\NodeInterface $node */
+    /** @var NodeInterface $node */
     $node = $variables['node'];
 
     $view_modes = [
@@ -71,7 +72,7 @@ class Node extends NodeBase {
           break;
 
         case 'teaser':
-          $variables['label'] = render($variables['label']);
+          $variables['label'] = \Drupal::service('renderer')->render($variables['label']);
           $variables['card_link_label'] = $this->t('View album');
 
           if (views_get_view_result('albums', 'embed_album_cover', $node->id())) {
diff --git a/theme-settings.php b/theme-settings.php
index 2346aaa..d60190a 100644
--- a/theme-settings.php
+++ b/theme-settings.php
@@ -1,5 +1,6 @@
 <?php
 
+use Drupal\Core\Entity\EntityStorageException;
 /**
  * @file
  * Allows users to change the color scheme of themes.
@@ -207,10 +208,10 @@ function socialblue_form_system_theme_settings_alter(&$form, FormStateInterface
  *
  * @param array $form
  *   The submitted form structure.
- * @param \Drupal\Core\Form\FormStateInterface $form_state
+ * @param FormStateInterface $form_state
  *   The state of the submitted form.
  *
- * @throws \Drupal\Core\Entity\EntityStorageException
+ * @throws EntityStorageException
  */
 function socialblue_update_gin_color_settings(array $form, FormStateInterface $form_state) {
   // Grab the default socialblue colors, these are set if the color settings
@@ -252,10 +253,10 @@ function socialblue_update_gin_color_settings(array $form, FormStateInterface $f
  *
  * @param array $form
  *   The submitted form structure.
- * @param \Drupal\Core\Form\FormStateInterface $form_state
+ * @param FormStateInterface $form_state
  *   The state of the submitted form.
  *
- * @throws \Drupal\Core\Entity\EntityStorageException
+ * @throws EntityStorageException
  */
 function socialblue_save_email_logo(array $form, FormStateInterface $form_state) {
   $email_logo = $form_state->getValue('email_logo');

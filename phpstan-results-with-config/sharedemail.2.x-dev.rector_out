modules/contrib/sharedemail/sharedemail.module
modules/contrib/sharedemail/src/Form/SharedEmailSettingsForm.php
modules/contrib/sharedemail/src/Plugin/Validation/Constraint/SharedEmailUnique.php
modules/contrib/sharedemail/src/Plugin/Validation/Constraint/SharedEmailUniqueValidator.php
modules/contrib/sharedemail/tests/src/Functional/SharedEmailTest.php

2 files with changes
====================

1) modules/contrib/sharedemail/tests/src/Functional/SharedEmailTest.php:14

    ---------- begin diff ----------
@@ @@
   /**
    * A user with permission to administer site configuration.
    *
-   * @var \Drupal\user\UserInterface
+   * @var UserInterface
    */
   protected $user;

@@ @@
   /**
    * {@inheritdoc}
    */
-  protected function setUp() {
+  protected function setUp(): void {
     parent::setUp();
     $this->user = $this->drupalCreateUser([
       'administer users',
@@ @@
     $edit = [
       'sharedemail_msg' => 'Test message',
     ];
+    $this->drupalGet('admin/config/people/shared-email');

     // Post the form.
-    $this->drupalPostForm('admin/config/people/shared-email', $edit, t('Save configuration'));
+    $this->submitForm($edit, t('Save configuration'));

     // Test the new values are there.
     $this->drupalGet('admin/config/people/shared-email');
@@ @@
       'pass[pass1]' => 'Test1Password',
       'pass[pass2]' => 'Test1Password',
     ];
+    $this->drupalGet('admin/people/create');

     // Attempt to create a new account using a unique email address.
-    $this->drupalPostForm('admin/people/create', $edit, t('Create new account'));
+    $this->submitForm($edit, t('Create new account'));

     $this->assertSession()->pageTextContains(t('Created a new user account for @name. No email has been sent.', ['@name' => $edit['name']]));
     $this->assertSession()->pageTextNotContains($this->config('sharedemail.settings')->get('sharedemail_msg'));
@@ @@
       'pass[pass1]' => 'Test1Password',
       'pass[pass2]' => 'Test1Password',
     ];
+    $this->drupalGet('admin/people/create');

     // Attempt to create a new account using an existing email address.
-    $this->drupalPostForm('admin/people/create', $edit, t('Create new account'));
+    $this->submitForm($edit, t('Create new account'));

     $this->assertSession()->pageTextContains(t('The email address @email is already taken.', ['@email' => $duplicateUser->getEmail()]));
   }
@@ @@
       'pass[pass1]' => 'Test1Password',
       'pass[pass2]' => 'Test1Password',
     ];
+    $this->drupalGet('admin/people/create');

     // Attempt to create a new account using an existing email address.
-    $this->drupalPostForm('admin/people/create', $edit, t('Create new account'));
+    $this->submitForm($edit, t('Create new account'));

     $this->assertSession()->pageTextContains(t('Created a new user account for @name. No email has been sent.', ['@name' => $edit['name']]));
   }
@@ @@
       'pass[pass1]' => 'Test1Password',
       'pass[pass2]' => 'Test1Password',
     ];
+    $this->drupalGet('admin/people/create');

     // Attempt to create a new account using an existing email address.
-    $this->drupalPostForm('admin/people/create', $edit, t('Create new account'));
+    $this->submitForm($edit, t('Create new account'));

     $this->assertSession()->pageTextContains(t('The email address @email is already taken.', ['@email' => $duplicateUser->getEmail()]));
   }
@@ @@
       'pass[pass1]' => 'Test1Password',
       'pass[pass2]' => 'Test1Password',
     ];
+    $this->drupalGet('admin/people/create');

     // Attempt to create a new account using an existing email address.
-    $this->drupalPostForm('admin/people/create', $edit, t('Create new account'));
+    $this->submitForm($edit, t('Create new account'));

     $this->assertSession()->pageTextContains(t('Created a new user account for @name. No email has been sent.', ['@name' => $edit['name']]));
   }
@@ @@
       'pass[pass1]' => 'Test1Password',
       'pass[pass2]' => 'Test1Password',
     ];
+    $this->drupalGet('admin/people/create');

     // Attempt to create a new account using an existing email address.
-    $this->drupalPostForm('admin/people/create', $edit, t('Create new account'));
+    $this->submitForm($edit, t('Create new account'));

     $this->assertSession()->pageTextContains(t('Created a new user account for @name. No email has been sent.', ['@name' => $edit['name']]));
     $this->assertSession()->pageTextContains($this->config('sharedemail.settings')->get('sharedemail_msg'));
@@ @@
       'pass[pass1]' => 'Test1Password',
       'pass[pass2]' => 'Test1Password',
     ];
+    $this->drupalGet('admin/people/create');

     // Attempt to create a new account using an existing email address.
-    $this->drupalPostForm('admin/people/create', $edit, t('Create new account'));
+    $this->submitForm($edit, t('Create new account'));

     $this->assertSession()->pageTextContains(t('Created a new user account for @name. No email has been sent.', ['@name' => $edit['name']]));
     $this->assertSession()->pageTextNotContains($this->config('sharedemail.settings')->get('sharedemail_msg'));
    ----------- end diff -----------

Applied rules:
 * UiHelperTraitDrupalPostFormRector
 * ExceptionAnnotationRector
 * RemoveDataProviderTestPrefixRector
 * ArgumentRemoverRector
 * RenameAnnotationRector
 * PseudoNamespaceToNamespaceRector
 * WrapReturnRector
 * AddReturnTypeDeclarationRector
 * ChangeMethodVisibilityRector


2) modules/contrib/sharedemail/src/Plugin/Validation/Constraint/SharedEmailUniqueValidator.php:18

    ---------- begin diff ----------
@@ @@
   /**
    * Provides config.factory service.
    *
-   * @var \Drupal\Core\Config\ConfigFactory
+   * @var ConfigFactory
    */
   protected $config;

@@ @@
   /**
    * Returns the current_user service.
    *
-   * @var \Drupal\Core\Session\AccountInterface
+   * @var AccountInterface
    */
   protected $currentUser;

@@ @@
   /**
    * Constructs a new SharedEmailUniqueValidator.
    *
-   * @param \Drupal\Core\Config\ConfigFactory $config
+   * @param ConfigFactory $config
    *   Defines the configuration object factory.
-   * @param \Drupal\Core\Session\AccountInterface $current_user
+   * @param AccountInterface $current_user
    *   Defines an account interface which represents the current user.
    */
   public function __construct(ConfigFactory $config, AccountInterface $current_user) {
    ----------- end diff -----------

Applied rules:
 * ArgumentRemoverRector
 * PseudoNamespaceToNamespaceRector
 * WrapReturnRector
 * AddParamTypeDeclarationRector
 * ChangeMethodVisibilityRector


 [OK] 2 files have been changed by Rector                                       


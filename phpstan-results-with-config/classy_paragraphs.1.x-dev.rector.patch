diff --git a/classy_paragraphs.info.yml b/classy_paragraphs.info.yml
index bf1c84b..aadcfb7 100644
--- a/classy_paragraphs.info.yml
+++ b/classy_paragraphs.info.yml
@@ -1,7 +1,7 @@
 name: Classy Paragraphs
 type: module
 description: Allows an editor to apply a class to paragraphs.
-core_version_requirement: ^8 || ^9
+core_version_requirement: ^8 || ^9 || ^10
 package: Paragraphs
 dependencies:
   - paragraphs:paragraphs
diff --git a/classy_paragraphs.module b/classy_paragraphs.module
index 48b9436..fda9b61 100644
--- a/classy_paragraphs.module
+++ b/classy_paragraphs.module
@@ -1,5 +1,6 @@
 <?php
 
+use Drupal\field\Entity\FieldStorageConfig;
 /**
  * @file
  * Contains classy_paragraphs.module.
@@ -43,7 +44,7 @@ function classy_paragraphs_entity_view_alter(array &$build, EntityInterface $ent
   $fields = $entity->getFieldDefinitions();
   foreach($fields as $field) {
     if ($field instanceof FieldConfigInterface) {
-      /** @var Drupal\field\Entity\FieldStorageConfig $field_storage */
+      /** @var FieldStorageConfig $field_storage */
       $field_storage = $field->getFieldStorageDefinition();
 
       $target = $field_storage->getSetting('target_type');
diff --git a/composer.json b/composer.json
index a030000..af41ba6 100644
--- a/composer.json
+++ b/composer.json
@@ -8,7 +8,7 @@
   },
   "license": "GPL-2.0",
   "require": {
-    "drupal/core": "^8 || ^9",
-    "drupal/paragraphs": "~1.0"
+    "drupal/paragraphs": "~1.0",
+    "drupal/core": "^8 || ^9 || ^10"
   }
 }
diff --git a/src/Form/ClassyParagraphsStyleForm.php b/src/Form/ClassyParagraphsStyleForm.php
index caea28c..b6e4fbf 100644
--- a/src/Form/ClassyParagraphsStyleForm.php
+++ b/src/Form/ClassyParagraphsStyleForm.php
@@ -2,6 +2,7 @@
 
 namespace Drupal\classy_paragraphs\Form;
 
+use Drupal\classy_paragraphs\Entity\ClassyParagraphsStyle;
 use Drupal\Core\Entity\EntityInterface;
 use Drupal\Core\Entity\EntityForm;
 use Drupal\Core\Form\FormStateInterface;
@@ -18,7 +19,7 @@ class ClassyParagraphsStyleForm extends EntityForm {
   public function form(array $form, FormStateInterface $form_state) {
     $form = parent::form($form, $form_state);
 
-    /** @var \Drupal\classy_paragraphs\Entity\ClassyParagraphsStyle $style */
+    /** @var ClassyParagraphsStyle $style */
     $style = $this->entity;
     $form['label'] = [
       '#type' => 'textfield',
diff --git a/src/Tests/ClassyParagraphsTest.php b/src/Tests/ClassyParagraphsTest.php
index 7e57ca9..4f5d00e 100644
--- a/src/Tests/ClassyParagraphsTest.php
+++ b/src/Tests/ClassyParagraphsTest.php
@@ -23,7 +23,7 @@ class ClassyParagraphsTest extends BrowserTestBase {
   /**
    * {@inheritdoc}
    */
-  protected function setUp() {
+  protected function setUp(): void {
     parent::setUp();
 
     $admin_user = $this->drupalCreateUser(['access content',
@@ -70,7 +70,10 @@ class ClassyParagraphsTest extends BrowserTestBase {
    */
   public function testStyleConfigurationPage() {
     $this->drupalGet('admin/structure/classy_paragraphs_style');
-    $this->assertText('Classy paragraphs style');
+    // TODO: Drupal Rector Notice: Please delete the following comment after you've made any necessary changes.
+    // Verify the assertion: pageTextContains() for HTML responses, responseContains() for non-HTML responses.
+    // The passed text should be HTML decoded, exactly as a human sees it in the browser.
+    $this->assertSession()->pageTextContains('Classy paragraphs style');
   }
 
   /**
@@ -79,11 +82,17 @@ class ClassyParagraphsTest extends BrowserTestBase {
   public function testClassyParagraphsTestModule() {
     // Check the test text paragraph type.
     $this->drupalGet('admin/structure/paragraphs_type');
-    $this->assertText('Text (CP Test)');
+    // TODO: Drupal Rector Notice: Please delete the following comment after you've made any necessary changes.
+    // Verify the assertion: pageTextContains() for HTML responses, responseContains() for non-HTML responses.
+    // The passed text should be HTML decoded, exactly as a human sees it in the browser.
+    $this->assertSession()->pageTextContains('Text (CP Test)');
 
     // Check the test content type type.
     $this->drupalGet('admin/structure/types');
-    $this->assertText('CP Test');
+    // TODO: Drupal Rector Notice: Please delete the following comment after you've made any necessary changes.
+    // Verify the assertion: pageTextContains() for HTML responses, responseContains() for non-HTML responses.
+    // The passed text should be HTML decoded, exactly as a human sees it in the browser.
+    $this->assertSession()->pageTextContains('CP Test');
   }
 
   /**
@@ -92,7 +101,7 @@ class ClassyParagraphsTest extends BrowserTestBase {
   public function testClassyParagraphsCheckSingleStyle() {
     $style = ['cp_test_loud'];
     $style_css = 'loud-background';
-    /** @var \Drupal\node\Entity\Node $node */
+    /** @var Node $node */
     list ($node) = $this->createNodeWithParagraph($style);
 
     $this->drupalGet('node/' . $node->id());

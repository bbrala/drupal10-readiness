diff --git a/src/Plugin/Alter/ThemeSuggestions.php b/src/Plugin/Alter/ThemeSuggestions.php
index 50097a5b..bd6e2f3c 100644
--- a/src/Plugin/Alter/ThemeSuggestions.php
+++ b/src/Plugin/Alter/ThemeSuggestions.php
@@ -2,6 +2,11 @@
 
 namespace Drupal\socialbase\Plugin\Alter;
 
+use Drupal\node\NodeInterface;
+use Drupal\layout_builder\SectionStorageInterface;
+use Drupal\file\Plugin\Field\FieldType\FileItem;
+use Drupal\Core\Field\FieldDefinitionInterface;
+use Drupal\views\ViewExecutable;
 use Drupal\Component\Utility\Html;
 use Drupal\bootstrap\Plugin\Alter\ThemeSuggestions as BaseThemeSuggestions;
 use Drupal\file\Entity\File;
@@ -32,9 +37,9 @@ class ThemeSuggestions extends BaseThemeSuggestions {
           _social_dashboard_is_activity_stream_on_dashboard() &&
           $variables['elements']['#view_mode'] === 'activity_comment'
         ) {
-          /** @var \Drupal\node\NodeInterface $node */
+          /** @var NodeInterface $node */
           $node = \Drupal::routeMatch()->getParameter('node');
-          /** @var \Drupal\layout_builder\SectionStorageInterface $section_storage */
+          /** @var SectionStorageInterface $section_storage */
           $section_storage = \Drupal::routeMatch()->getParameter('section_storage');
           $bundle = is_null($node) ? $section_storage->getContextValues()['entity']->bundle() : $node->bundle();
 
@@ -181,7 +186,7 @@ class ThemeSuggestions extends BaseThemeSuggestions {
 
           // For comments in activities we show the amount of attachments.
           if (isset($file->_referringItem) && $file->_referringItem !== NULL) {
-            /** @var \Drupal\file\Plugin\Field\FieldType\FileItem $item  */
+            /** @var FileItem $item  */
             $item = $file->_referringItem;
             $name = $item->getFieldDefinition()->getName();
             // For field comment files we add a new suggestion.
@@ -202,7 +207,7 @@ class ThemeSuggestions extends BaseThemeSuggestions {
           return;
         }
 
-        /** @var \Drupal\file\Entity\File $c_file */
+        /** @var File $c_file */
         $c_file = $context1['file'];
 
         // If the file link is part of a node field, suggest another template.
@@ -214,7 +219,7 @@ class ThemeSuggestions extends BaseThemeSuggestions {
 
           // Loop over all fields and target only file fields.
           foreach ($field_definitions as $field_name => $field_definition) {
-            /** @var \Drupal\Core\Field\FieldDefinitionInterface $field_definition */
+            /** @var FieldDefinitionInterface $field_definition */
             if ($field_definition->getType() === 'file') {
               $files = $node->get($field_name)->getValue();
               foreach ($files as $file) {
@@ -352,7 +357,7 @@ class ThemeSuggestions extends BaseThemeSuggestions {
 
       case 'views_view_fields':
 
-        /** @var \Drupal\views\ViewExecutable $view */
+        /** @var ViewExecutable $view */
         $view = $variables['view'];
         if (($view) && $view->id() == 'who_liked_this_entity') {
           $suggestions[] = $variables['theme_hook_original'] . '__wholiked';
diff --git a/src/Plugin/Preprocess/Block.php b/src/Plugin/Preprocess/Block.php
index 921417c4..479fadb4 100644
--- a/src/Plugin/Preprocess/Block.php
+++ b/src/Plugin/Preprocess/Block.php
@@ -201,12 +201,12 @@ class Block extends PreprocessBase {
     if (isset($variables['elements']['content']['#block_content'])) {
       if ($variables['elements']['content']['#block_content']->bundle() == 'hero_call_to_action_block') {
         if (isset($variables['elements']['content']['field_hero_image']) && isset($variables['elements']['content']['field_hero_image'][0])) {
-          /** @var \Drupal\image\Plugin\Field\FieldType\ImageItem $image_item */
+          /** @var ImageItem $image_item */
           $image_item = $variables['elements']['content']['field_hero_image'][0]['#item'];
           $file_id = NULL;
 
           if ($image_item && $image_item instanceof ImageItem) {
-            /** @var \Drupal\block_content\Entity\BlockContent $block_content */
+            /** @var BlockContent $block_content */
             $block_content = $image_item->getEntity();
             if ($block_content && $block_content instanceof BlockContent) {
               $file_id = $block_content->get('field_hero_image')->target_id;
diff --git a/src/Plugin/Preprocess/Html.php b/src/Plugin/Preprocess/Html.php
index dbca7290..06cbb632 100644
--- a/src/Plugin/Preprocess/Html.php
+++ b/src/Plugin/Preprocess/Html.php
@@ -30,7 +30,7 @@ class Html extends PreprocessBase {
     }
 
     // Get all SVG Icons.
-    $variables['svg_icons'] = file_get_contents(drupal_get_path('theme', 'socialbase') . '/assets/icons/icons.svg');
+    $variables['svg_icons'] = file_get_contents(\Drupal::service('extension.list.theme')->getPath('socialbase') . '/assets/icons/icons.svg');
 
   }
 
diff --git a/src/Plugin/Preprocess/Links.php b/src/Plugin/Preprocess/Links.php
index 00ddaba9..3ca89bf9 100644
--- a/src/Plugin/Preprocess/Links.php
+++ b/src/Plugin/Preprocess/Links.php
@@ -2,6 +2,7 @@
 
 namespace Drupal\socialbase\Plugin\Preprocess;
 
+use Drupal\Core\Template\Attribute;
 use Drupal\bootstrap\Plugin\Preprocess\PreprocessBase;
 use Drupal\bootstrap\Utility\Variables;
 
@@ -24,11 +25,11 @@ class Links extends PreprocessBase {
     // Move CSS class to "li" tag for marking a no-link item as disabled.
     foreach ($variables['links'] as &$link) {
       if (isset($link['text_attributes'])) {
-        /** @var \Drupal\Core\Template\Attribute $text_attributes */
+        /** @var Attribute $text_attributes */
         $text_attributes = $link['text_attributes'];
 
         if ($link['disabled'] = $text_attributes->hasClass('disabled')) {
-          /** @var \Drupal\Core\Template\Attribute $attributes */
+          /** @var Attribute $attributes */
           $attributes = $link['attributes'];
 
           $attributes->addClass('disabled');

diff --git a/modules/freelinking_prepopulate/src/Plugin/freelinking/FreelinkingPrepopulate.php b/modules/freelinking_prepopulate/src/Plugin/freelinking/FreelinkingPrepopulate.php
index 44a5ccb..753854b 100644
--- a/modules/freelinking_prepopulate/src/Plugin/freelinking/FreelinkingPrepopulate.php
+++ b/modules/freelinking_prepopulate/src/Plugin/freelinking/FreelinkingPrepopulate.php
@@ -2,6 +2,7 @@
 
 namespace Drupal\freelinking_prepopulate\Plugin\freelinking;
 
+use Drupal\node\Entity\NodeType;
 use Drupal\Component\Utility\NestedArray;
 use Drupal\Component\Utility\Xss;
 use Drupal\Core\Entity\EntityTypeManagerInterface;
@@ -41,21 +42,21 @@ class FreelinkingPrepopulate extends FreelinkingPluginBase implements ContainerF
   /**
    * The entity field manager.
    *
-   * @var \Drupal\Core\Entity\EntityFieldManagerInterface
+   * @var EntityFieldManagerInterface
    */
   protected $entityFieldManager;
 
   /**
    * The entity type manager.
    *
-   * @var \Drupal\Core\Entity\EntityTypeManagerInterface
+   * @var EntityTypeManagerInterface
    */
   protected $entityTypeManager;
 
   /**
    * Module handler.
    *
-   * @var \Drupal\Core\Extension\ModuleHandlerInterface
+   * @var ModuleHandlerInterface
    */
   protected $moduleHandler;
 
@@ -68,11 +69,11 @@ class FreelinkingPrepopulate extends FreelinkingPluginBase implements ContainerF
    *   The plugin ID.
    * @param mixed $plugin_definition
    *   The plugin definition array.
-   * @param \Drupal\Core\Entity\EntityTypeManagerInterface $entityTypeManager
+   * @param EntityTypeManagerInterface $entityTypeManager
    *   The entity type manager service.
-   * @param \Drupal\Core\Entity\EntityFieldManagerInterface $entityFieldManager
+   * @param EntityFieldManagerInterface $entityFieldManager
    *   The entity field manager service.
-   * @param \Drupal\Core\Extension\ModuleHandlerInterface $moduleHandler
+   * @param ModuleHandlerInterface $moduleHandler
    *   The module handler service.
    */
   public function __construct(array $configuration, $plugin_id, $plugin_definition, EntityTypeManagerInterface $entityTypeManager, EntityFieldManagerInterface $entityFieldManager, ModuleHandlerInterface $moduleHandler) {
@@ -146,7 +147,7 @@ class FreelinkingPrepopulate extends FreelinkingPluginBase implements ContainerF
     $node_types = $this->entityTypeManager->getStorage('node_type')->loadMultiple();
 
     $options = array_reduce($node_types, function (&$result, $node_type) {
-      /** @var \Drupal\node\Entity\NodeType $node_type */
+      /** @var NodeType $node_type */
       $result[$node_type->id()] = $node_type->label();
       return $result;
     }, []);
diff --git a/modules/freelinking_prepopulate/tests/src/Functional/FreelinkingPrepopulateTest.php b/modules/freelinking_prepopulate/tests/src/Functional/FreelinkingPrepopulateTest.php
index 36780ab..7908cf8 100644
--- a/modules/freelinking_prepopulate/tests/src/Functional/FreelinkingPrepopulateTest.php
+++ b/modules/freelinking_prepopulate/tests/src/Functional/FreelinkingPrepopulateTest.php
@@ -2,6 +2,7 @@
 
 namespace Drupal\Tests\freelinking_prepopulate\Functional;
 
+use Behat\Mink\Exception\ExpectationException;
 use Drupal\Core\Entity\Entity\EntityFormDisplay;
 use Drupal\field\Entity\FieldConfig;
 use Drupal\field\Entity\FieldStorageConfig;
@@ -32,7 +33,7 @@ class FreelinkingPrepopulateTest extends FreelinkingBrowserTestBase {
   /**
    * {@inheritdoc}
    */
-  protected function setUp() {
+  protected function setUp(): void {
     parent::setUp();
 
     // Creates a content type to act as default prepopulate content type.
@@ -106,7 +107,7 @@ class FreelinkingPrepopulateTest extends FreelinkingBrowserTestBase {
   /**
    * Asserts that prepopulate links are displayed correctly.
    *
-   * @throws \Behat\Mink\Exception\ExpectationException
+   * @throws ExpectationException
    */
   public function testFreelinkingPrepopulate() {
     // Creates a node for the assertions.
@@ -119,7 +120,8 @@ class FreelinkingPrepopulateTest extends FreelinkingBrowserTestBase {
         <li>With fields: [[create:Sam|Sam|Add Sam|body="A page about Sam."|field_basic="test"|field_tags="tag1,tag2,tag3"]]</li>
       </ul>
 EOF;
-    $this->drupalPostForm('node/add/page', $edit, t('Save'));
+    $this->drupalGet('node/add/page');
+    $this->submitForm($edit, t('Save'));
 
     // This is the most ridiculous developer-unfriendly way to test hrefs ever.
     // DrupalWTF. MinkWTF.
diff --git a/modules/freelinking_prepopulate/tests/src/Unit/Plugin/freelinking/FreelinkingPrepopulateTest.php b/modules/freelinking_prepopulate/tests/src/Unit/Plugin/freelinking/FreelinkingPrepopulateTest.php
index 054f752..0f58b27 100644
--- a/modules/freelinking_prepopulate/tests/src/Unit/Plugin/freelinking/FreelinkingPrepopulateTest.php
+++ b/modules/freelinking_prepopulate/tests/src/Unit/Plugin/freelinking/FreelinkingPrepopulateTest.php
@@ -2,6 +2,7 @@
 
 namespace Drupal\Tests\freelinking_prepopulate\Unit\Plugin\freelinking;
 
+use Prophecy\PhpUnit\ProphecyTrait;
 use Drupal\Core\DependencyInjection\ContainerBuilder;
 use Drupal\freelinking_prepopulate\Plugin\freelinking\FreelinkingPrepopulate;
 use Drupal\Tests\UnitTestCase;
@@ -13,10 +14,11 @@ use Drupal\Tests\UnitTestCase;
  */
 class FreelinkingPrepopulateTest extends UnitTestCase {
 
+  use ProphecyTrait;
   /**
    * {@inheritdoc}
    */
-  protected function setUp() {
+  protected function setUp(): void {
     // Mocks returning content types.
     $nodeTypeProphet = $this->prophesize('\Drupal\Core\Entity\EntityInterface');
     $nodeTypeProphet->id()->willReturn('page');
diff --git a/src/Annotation/Freelinking.php b/src/Annotation/Freelinking.php
index 231d9dd..41712cb 100644
--- a/src/Annotation/Freelinking.php
+++ b/src/Annotation/Freelinking.php
@@ -2,6 +2,7 @@
 
 namespace Drupal\freelinking\Annotation;
 
+use Drupal\Core\Annotation\Translation;
 use Drupal\Component\Annotation\Plugin;
 
 /**
@@ -35,7 +36,7 @@ class Freelinking extends Plugin {
    *
    * @ingroup plugin_translatable
    *
-   * @var \Drupal\Core\Annotation\Translation
+   * @var Translation
    */
   public $title;
 
diff --git a/src/FreelinkingManager.php b/src/FreelinkingManager.php
index 2c2dd59..e98985b 100644
--- a/src/FreelinkingManager.php
+++ b/src/FreelinkingManager.php
@@ -21,7 +21,7 @@ class FreelinkingManager extends DefaultPluginManager implements FreelinkingMana
   /**
    * Language manager.
    *
-   * @var \Drupal\Core\Language\LanguageManagerInterface
+   * @var LanguageManagerInterface
    */
   protected $languageManager;
 
@@ -58,7 +58,7 @@ class FreelinkingManager extends DefaultPluginManager implements FreelinkingMana
       }
 
       foreach ($allowed_plugins as $plugin_name => $plugin_info) {
-        /** @var \Drupal\freelinking\Plugin\FreelinkingPluginInterface $plugin */
+        /** @var FreelinkingPluginInterface $plugin */
         $default_configuration['settings'] = isset($plugin_info['settings']) ? $plugin_info['settings'] : [];
         $plugin = $this->createInstance($plugin_name, $default_configuration);
         if (preg_match($plugin->getIndicator(), $indicator)) {
@@ -184,14 +184,14 @@ class FreelinkingManager extends DefaultPluginManager implements FreelinkingMana
       'settings' => isset($plugin_settings['settings']) ? $plugin_settings['settings'] : [],
     ];
 
-    /** @var \Drupal\freelinking\Plugin\FreelinkingPluginInterface $plugin */
+    /** @var FreelinkingPluginInterface $plugin */
     $plugin = $this->createInstance($plugin_id, $configuration);
     $config = $plugin->getConfiguration();
     $target_array = $this->parseTarget($target, $langcode);
     $target_array['indicator'] = $indicator;
     $link = $this->buildLink($plugin, $target_array);
     if (is_array($link) && isset($link['error']) && isset($config['settings']['failover'])) {
-      /** @var \Drupal\freelinking\Plugin\FreelinkingPluginInterface $failover_plugin */
+      /** @var FreelinkingPluginInterface $failover_plugin */
       $failover_settings = unserialize($failover_settings_string);
       $failover_plugin_id = $config['settings']['failover'];
       $default_configuration = [
diff --git a/src/FreelinkingManagerInterface.php b/src/FreelinkingManagerInterface.php
index b9c5e23..a17446f 100644
--- a/src/FreelinkingManagerInterface.php
+++ b/src/FreelinkingManagerInterface.php
@@ -18,11 +18,11 @@ interface FreelinkingManagerInterface extends PluginManagerInterface {
    *
    * @param \Traversable $namespaces
    *   An array of namespaces.
-   * @param \Drupal\Core\Cache\CacheBackendInterface $cacheBackend
+   * @param CacheBackendInterface $cacheBackend
    *   The cache backend.
-   * @param \Drupal\Core\Extension\ModuleHandlerInterface $module_handler
+   * @param ModuleHandlerInterface $module_handler
    *   The module handler for alter functionality.
-   * @param \Drupal\Core\Language\LanguageManagerInterface $language_manager
+   * @param LanguageManagerInterface $language_manager
    *   The language manager.
    */
   public function __construct(\Traversable $namespaces, CacheBackendInterface $cacheBackend, ModuleHandlerInterface $module_handler, LanguageManagerInterface $language_manager);
@@ -37,7 +37,7 @@ interface FreelinkingManagerInterface extends PluginManagerInterface {
    * @param array $options
    *   The settings from the filter. This is an associative array.
    *
-   * @return \Drupal\freelinking\Plugin\FreelinkingPluginInterface|false
+   * @return FreelinkingPluginInterface|false
    *   The plugin to use or FALSE if not found.
    *
    * @see \Drupal\freelinking\Plugin\Filter\Freelinking::process()
@@ -47,7 +47,7 @@ interface FreelinkingManagerInterface extends PluginManagerInterface {
   /**
    * Build link structure for a plugin with target parameters.
    *
-   * @param \Drupal\freelinking\Plugin\FreelinkingPluginInterface $plugin
+   * @param FreelinkingPluginInterface $plugin
    *   The freelinking plugin.
    * @param array $target
    *   The target array. @see ::parseTarget().
diff --git a/src/Plugin/Filter/Freelinking.php b/src/Plugin/Filter/Freelinking.php
index c53a9cf..5c601ad 100644
--- a/src/Plugin/Filter/Freelinking.php
+++ b/src/Plugin/Filter/Freelinking.php
@@ -35,14 +35,14 @@ class Freelinking extends FilterBase implements ContainerFactoryPluginInterface
   /**
    * Freelinking plugin manager.
    *
-   * @var \Drupal\freelinking\FreelinkingManagerInterface
+   * @var FreelinkingManagerInterface
    */
   protected $freelinkingManager;
 
   /**
    * Current user account.
    *
-   * @var \Drupal\Core\Session\AccountProxyInterface
+   * @var AccountProxyInterface
    */
   protected $currentUser;
 
@@ -55,9 +55,9 @@ class Freelinking extends FilterBase implements ContainerFactoryPluginInterface
    *   The plugin ID.
    * @param array $plugin_definition
    *   The plugin definition array.
-   * @param \Drupal\freelinking\FreelinkingManagerInterface $freelinkingManager
+   * @param FreelinkingManagerInterface $freelinkingManager
    *   The Freelinking plugin manager.
-   * @param \Drupal\Core\Session\AccountProxyInterface $current_user
+   * @param AccountProxyInterface $current_user
    *   The current user account.
    */
   public function __construct(array $configuration, $plugin_id, array $plugin_definition, FreelinkingManagerInterface $freelinkingManager, AccountProxyInterface $current_user) {
diff --git a/src/Plugin/FreelinkingPluginInterface.php b/src/Plugin/FreelinkingPluginInterface.php
index fbb450b..409296a 100644
--- a/src/Plugin/FreelinkingPluginInterface.php
+++ b/src/Plugin/FreelinkingPluginInterface.php
@@ -2,6 +2,7 @@
 
 namespace Drupal\freelinking\Plugin;
 
+use Drupal\Core\StringTranslation\TranslatableMarkup;
 use Drupal\Component\Plugin\ConfigurableInterface;
 use Drupal\Component\Plugin\DependentPluginInterface;
 use Drupal\Component\Plugin\PluginInspectionInterface;
@@ -17,7 +18,7 @@ interface FreelinkingPluginInterface extends ConfigurableInterface, DependentPlu
    *
    * Tips are displayed as part of the freelinking filter plugin.
    *
-   * @return \Drupal\Core\StringTranslation\TranslatableMarkup
+   * @return TranslatableMarkup
    *   Translatable markup.
    */
   public function getTip();
@@ -35,7 +36,7 @@ interface FreelinkingPluginInterface extends ConfigurableInterface, DependentPlu
    *
    * @param array $form
    *   The form element array for the filter plugin.
-   * @param \Drupal\Core\Form\FormStateInterface $form_state
+   * @param FormStateInterface $form_state
    *   The form state of the parent form.
    *
    * @return array
diff --git a/src/Plugin/freelinking/Builtin.php b/src/Plugin/freelinking/Builtin.php
index e163c97..ca22a46 100644
--- a/src/Plugin/freelinking/Builtin.php
+++ b/src/Plugin/freelinking/Builtin.php
@@ -23,7 +23,7 @@ class Builtin extends FreelinkingPluginBase implements ContainerFactoryPluginInt
   /**
    * Current user account.
    *
-   * @var \Drupal\Core\Session\AccountProxyInterface
+   * @var AccountProxyInterface
    */
   protected $account;
 
@@ -36,7 +36,7 @@ class Builtin extends FreelinkingPluginBase implements ContainerFactoryPluginInt
    *   The plugin ID.
    * @param array $plugin_definition
    *   The plugin definition array.
-   * @param \Drupal\Core\Session\AccountProxyInterface $account
+   * @param AccountProxyInterface $account
    *   The current user account.
    */
   public function __construct(array $configuration, $plugin_id, $plugin_definition, AccountProxyInterface $account) {
diff --git a/src/Plugin/freelinking/External.php b/src/Plugin/freelinking/External.php
index f288b63..af4d126 100644
--- a/src/Plugin/freelinking/External.php
+++ b/src/Plugin/freelinking/External.php
@@ -30,7 +30,7 @@ class External extends FreelinkingPluginBase implements ContainerFactoryPluginIn
   /**
    * Drupal Guzzle Client.
    *
-   * @var \GuzzleHttp\Client
+   * @var Client
    */
   protected $client;
 
@@ -43,7 +43,7 @@ class External extends FreelinkingPluginBase implements ContainerFactoryPluginIn
    *   The plugin ID.
    * @param mixed $plugin_definition
    *   The plugin definition array.
-   * @param \GuzzleHttp\Client $client
+   * @param Client $client
    *   A configured HTTP Request client.
    */
   public function __construct(array $configuration, $plugin_id, $plugin_definition, Client $client) {
@@ -145,7 +145,7 @@ class External extends FreelinkingPluginBase implements ContainerFactoryPluginIn
    * @return string
    *   The page title to use.
    *
-   * @throws \GuzzleHttp\Exception\RequestException
+   * @throws RequestException
    */
   protected function getPageTitle($url) {
     // Try to fetch the URL.
diff --git a/src/Plugin/freelinking/File.php b/src/Plugin/freelinking/File.php
index 65e5acb..e532292 100644
--- a/src/Plugin/freelinking/File.php
+++ b/src/Plugin/freelinking/File.php
@@ -2,6 +2,7 @@
 
 namespace Drupal\freelinking\Plugin\freelinking;
 
+use Drupal\Core\Config\ImmutableConfig;
 use Drupal\Core\Config\ConfigFactoryInterface;
 use Drupal\Core\Extension\ModuleHandlerInterface;
 use Drupal\Core\Form\FormStateInterface;
@@ -30,21 +31,21 @@ class File extends FreelinkingPluginBase implements ContainerFactoryPluginInterf
   /**
    * File system configuration.
    *
-   * @var \Drupal\Core\Config\ImmutableConfig
+   * @var ImmutableConfig
    */
   protected $fileSystemConfig;
 
   /**
    * Stream wrapper manager.
    *
-   * @var \Drupal\Core\StreamWrapper\StreamWrapperManagerInterface
+   * @var StreamWrapperManagerInterface
    */
   protected $streamWrapperManager;
 
   /**
    * Module handler.
    *
-   * @var \Drupal\Core\Extension\ModuleHandlerInterface
+   * @var ModuleHandlerInterface
    */
   protected $moduleHandler;
 
@@ -57,11 +58,11 @@ class File extends FreelinkingPluginBase implements ContainerFactoryPluginInterf
    *   The plugin ID.
    * @param array $plugin_definition
    *   The plugin definition array.
-   * @param \Drupal\Core\Config\ConfigFactoryInterface $configFactory
+   * @param ConfigFactoryInterface $configFactory
    *   The configuration factory.
-   * @param \Drupal\Core\StreamWrapper\StreamWrapperManagerInterface $streamWrapperManager
+   * @param StreamWrapperManagerInterface $streamWrapperManager
    *   The stream wrapper manager.
-   * @param \Drupal\Core\Extension\ModuleHandlerInterface $moduleHandler
+   * @param ModuleHandlerInterface $moduleHandler
    *   The module handler.
    */
   public function __construct(array $configuration, $plugin_id, $plugin_definition, ConfigFactoryInterface $configFactory, StreamWrapperManagerInterface $streamWrapperManager, ModuleHandlerInterface $moduleHandler) {
@@ -173,7 +174,7 @@ class File extends FreelinkingPluginBase implements ContainerFactoryPluginInterf
    * @param string $scheme
    *   The scheme to load.
    *
-   * @return \Drupal\Core\StreamWrapper\StreamWrapperInterface
+   * @return StreamWrapperInterface
    *   The stream wrapper to use.
    */
   protected function loadScheme($scheme) {
diff --git a/src/Plugin/freelinking/GoogleSearch.php b/src/Plugin/freelinking/GoogleSearch.php
index 8fbe0c3..362f2d9 100644
--- a/src/Plugin/freelinking/GoogleSearch.php
+++ b/src/Plugin/freelinking/GoogleSearch.php
@@ -51,9 +51,9 @@ class GoogleSearch extends FreelinkingPluginBase {
    *   plus signs.
    * @param string $text
    *   The text to display for the link.
-   * @param \Drupal\Core\Language\LanguageInterface|null $language
+   * @param LanguageInterface|null $language
    *   The language code.
-   * @param \Drupal\Core\StringTranslation\TranslatableMarkup $tip
+   * @param TranslatableMarkup $tip
    *   The tooltip to use.
    *
    * @return array
diff --git a/src/Plugin/freelinking/Node.php b/src/Plugin/freelinking/Node.php
index 63aa190..53320d6 100644
--- a/src/Plugin/freelinking/Node.php
+++ b/src/Plugin/freelinking/Node.php
@@ -28,7 +28,7 @@ class Node extends FreelinkingPluginBase implements ContainerFactoryPluginInterf
   /**
    * Entity Type Manager.
    *
-   * @var \Drupal\Core\Entity\EntityTypeManagerInterface
+   * @var EntityTypeManagerInterface
    */
   protected $entityTypeManager;
 
@@ -41,7 +41,7 @@ class Node extends FreelinkingPluginBase implements ContainerFactoryPluginInterf
    *   The plugin identifier.
    * @param array $plugin_definition
    *   The plugin definition array.
-   * @param \Drupal\Core\Entity\EntityTypeManagerInterface $entityTypeManager
+   * @param EntityTypeManagerInterface $entityTypeManager
    *   The entity type manager.
    */
   public function __construct(array $configuration, $plugin_id, $plugin_definition, EntityTypeManagerInterface $entityTypeManager) {
diff --git a/src/Plugin/freelinking/NodeTitle.php b/src/Plugin/freelinking/NodeTitle.php
index d59cac7..be70ce7 100644
--- a/src/Plugin/freelinking/NodeTitle.php
+++ b/src/Plugin/freelinking/NodeTitle.php
@@ -32,14 +32,14 @@ class NodeTitle extends FreelinkingPluginBase implements FreelinkingPluginInterf
   /**
    * Entity type manager.
    *
-   * @var \Drupal\Core\Entity\EntityTypeManagerInterface
+   * @var EntityTypeManagerInterface
    */
   protected $entityTypeManager;
 
   /**
    * Module handler.
    *
-   * @var \Drupal\Core\Extension\ModuleHandlerInterface
+   * @var ModuleHandlerInterface
    */
   protected $moduleHandler;
 
@@ -52,9 +52,9 @@ class NodeTitle extends FreelinkingPluginBase implements FreelinkingPluginInterf
    *   Plugin Id.
    * @param mixed $plugin_definition
    *   Plugin definition.
-   * @param \Drupal\Core\Entity\EntityTypeManagerInterface $entityTypeManager
+   * @param EntityTypeManagerInterface $entityTypeManager
    *   Entity type manager for getting entity type.
-   * @param \Drupal\Core\Extension\ModuleHandlerInterface $moduleHandler
+   * @param ModuleHandlerInterface $moduleHandler
    *   The module handler service.
    */
   public function __construct(array $configuration, $plugin_id, $plugin_definition, EntityTypeManagerInterface $entityTypeManager, ModuleHandlerInterface $moduleHandler) {
diff --git a/src/Plugin/freelinking/PathAlias.php b/src/Plugin/freelinking/PathAlias.php
index 0b66dc6..766c395 100644
--- a/src/Plugin/freelinking/PathAlias.php
+++ b/src/Plugin/freelinking/PathAlias.php
@@ -28,14 +28,14 @@ class PathAlias extends FreelinkingPluginBase implements ContainerFactoryPluginI
   /**
    * Module handler.
    *
-   * @var \Drupal\Core\Extension\ModuleHandlerInterface
+   * @var ModuleHandlerInterface
    */
   protected $moduleHandler;
 
   /**
    * Path Alias Manager.
    *
-   * @var \Drupal\path_alias\AliasManagerInterface
+   * @var AliasManagerInterface
    */
   protected $aliasManager;
 
@@ -48,9 +48,9 @@ class PathAlias extends FreelinkingPluginBase implements ContainerFactoryPluginI
    *   The plugin ID.
    * @param mixed $plugin_definition
    *   The plugin definition array.
-   * @param \Drupal\Core\Extension\ModuleHandlerInterface $moduleHandler
+   * @param ModuleHandlerInterface $moduleHandler
    *   The module handler.
-   * @param \Drupal\path_alias\AliasManagerInterface $aliasManager
+   * @param AliasManagerInterface $aliasManager
    *   The path alias manager.
    */
   public function __construct(array $configuration, $plugin_id, $plugin_definition, ModuleHandlerInterface $moduleHandler, AliasManagerInterface $aliasManager) {
diff --git a/src/Plugin/freelinking/Search.php b/src/Plugin/freelinking/Search.php
index 3c86c18..e9e229c 100644
--- a/src/Plugin/freelinking/Search.php
+++ b/src/Plugin/freelinking/Search.php
@@ -29,7 +29,7 @@ class Search extends FreelinkingPluginBase implements ContainerFactoryPluginInte
   /**
    * Module handler interface.
    *
-   * @var \Drupal\Core\Extension\ModuleHandlerInterface
+   * @var ModuleHandlerInterface
    */
   protected $moduleHandler;
 
@@ -42,7 +42,7 @@ class Search extends FreelinkingPluginBase implements ContainerFactoryPluginInte
    *   The plugin ID.
    * @param array $plugin_definition
    *   The plugin definition array.
-   * @param \Drupal\Core\Extension\ModuleHandlerInterface $moduleHandler
+   * @param ModuleHandlerInterface $moduleHandler
    *   The module handler interface.
    */
   public function __construct(array $configuration, $plugin_id, $plugin_definition, ModuleHandlerInterface $moduleHandler) {
diff --git a/src/Plugin/freelinking/User.php b/src/Plugin/freelinking/User.php
index d3802c4..2cb275d 100644
--- a/src/Plugin/freelinking/User.php
+++ b/src/Plugin/freelinking/User.php
@@ -2,6 +2,7 @@
 
 namespace Drupal\freelinking\Plugin\freelinking;
 
+use Drupal\user\UserInterface;
 use Drupal\Core\Entity\EntityTypeManagerInterface;
 use Drupal\Core\Plugin\ContainerFactoryPluginInterface;
 use Drupal\Core\Session\AccountProxyInterface;
@@ -25,14 +26,14 @@ class User extends FreelinkingPluginBase implements ContainerFactoryPluginInterf
   /**
    * Entity Type Manager.
    *
-   * @var \Drupal\Core\Entity\EntityTypeManagerInterface
+   * @var EntityTypeManagerInterface
    */
   protected $entityTypeManager;
 
   /**
    * Current user account interface.
    *
-   * @var \Drupal\Core\Session\AccountProxyInterface
+   * @var AccountProxyInterface
    */
   protected $currentUser;
 
@@ -45,9 +46,9 @@ class User extends FreelinkingPluginBase implements ContainerFactoryPluginInterf
    *   The plugin ID.
    * @param array $plugin_definition
    *   The plugin definition array.
-   * @param \Drupal\Core\Entity\EntityTypeManagerInterface $entityTypeManager
+   * @param EntityTypeManagerInterface $entityTypeManager
    *   The entity type manager.
-   * @param \Drupal\Core\Session\AccountProxyInterface $currentUser
+   * @param AccountProxyInterface $currentUser
    *   The current user.
    */
   public function __construct(array $configuration, $plugin_id, $plugin_definition, EntityTypeManagerInterface $entityTypeManager, AccountProxyInterface $currentUser) {
@@ -82,7 +83,7 @@ class User extends FreelinkingPluginBase implements ContainerFactoryPluginInterf
         $account = $this->entityTypeManager->getStorage('user')->load($target['dest']);
       }
       else {
-        /** @var \Drupal\user\UserInterface[] $accounts */
+        /** @var UserInterface[] $accounts */
         $accounts = $this->entityTypeManager->getStorage('user')->loadByProperties([
           'name' => $target['dest'],
         ]);

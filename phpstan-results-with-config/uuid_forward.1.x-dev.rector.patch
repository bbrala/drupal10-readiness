diff --git a/src/Controller/UuidForwardController.php b/src/Controller/UuidForwardController.php
index d05250a..9949f1d 100644
--- a/src/Controller/UuidForwardController.php
+++ b/src/Controller/UuidForwardController.php
@@ -20,21 +20,21 @@ class UuidForwardController extends ControllerBase {
   /**
    * The entity repository.
    *
-   * @var \Drupal\Core\Entity\EntityRepositoryInterface
+   * @var EntityRepositoryInterface
    */
   protected $entityRepository;
 
   /**
-   * @var \Psr\Log\LoggerInterface
+   * @var LoggerInterface
    */
   protected $logger;
 
   /**
    * UuidForwardController constructor.
    *
-   * @param \Drupal\Core\Entity\EntityTypeManagerInterface $entity_type_manager
-   * @param \Drupal\Core\Entity\EntityRepositoryInterface $entity_repository
-   * @param \Psr\Log\LoggerInterface $logger
+   * @param EntityTypeManagerInterface $entity_type_manager
+   * @param EntityRepositoryInterface $entity_repository
+   * @param LoggerInterface $logger
    */
   public function __construct(EntityTypeManagerInterface $entity_type_manager, EntityRepositoryInterface $entity_repository, LoggerInterface $logger) {
     $this->entityTypeManager = $entity_type_manager;
@@ -56,11 +56,11 @@ class UuidForwardController extends ControllerBase {
   /**
    * Forward a UUID path to its entity.
    *
-   * @param \Symfony\Component\HttpFoundation\Request $request
+   * @param Request $request
    * @param string $uuid
    *   The UUID being requested.
    *
-   * @return \Symfony\Component\HttpFoundation\RedirectResponse
+   * @return RedirectResponse
    */
   public function forward(Request $request, $uuid) {
     foreach ($this->entityTypeManager->getDefinitions() as $entity_type_id => $entity_type) {

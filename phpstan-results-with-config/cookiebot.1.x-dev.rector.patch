diff --git a/cookiebot.module b/cookiebot.module
index 19b12f1..df63c2e 100644
--- a/cookiebot.module
+++ b/cookiebot.module
@@ -17,7 +17,7 @@ function cookiebot_help($route_name) {
     return '';
   }
 
-  $text = file_get_contents(drupal_get_path('module', 'cookiebot') . '/README.md');
+  $text = file_get_contents(\Drupal::service('extension.list.module')->getPath('cookiebot') . '/README.md');
 
   if (!\Drupal::moduleHandler()->moduleExists('markdown')) {
     return [
@@ -194,7 +194,7 @@ function cookiebot_preprocess_menu(&$vars) {
 
   foreach ($vars['items'] as &$item) {
     if ($item['url']->toString() === '/cookiebot-renew') {
-      /** @var \Drupal\Core\Template\Attribute $attributes */
+      /** @var Attribute $attributes */
       $attributes = $item['attributes'];
       $attributes->addClass('cookiebot-renew');
     }
diff --git a/src/Form/CookiebotForm.php b/src/Form/CookiebotForm.php
index 14dc90e..25740a6 100644
--- a/src/Form/CookiebotForm.php
+++ b/src/Form/CookiebotForm.php
@@ -19,34 +19,34 @@ class CookiebotForm extends ConfigFormBase {
   /**
    * Entity type manager.
    *
-   * @var \Drupal\Core\Entity\EntityTypeManagerInterface
+   * @var EntityTypeManagerInterface
    */
   protected $entityTypeManager;
 
   /**
    * Alias manager.
    *
-   * @var \Drupal\path_alias\AliasManagerInterface
+   * @var AliasManagerInterface
    */
   protected $aliasManager;
 
   /**
    * The cache tag invalidator service.
    *
-   * @var \Drupal\Core\Cache\CacheTagsInvalidatorInterface
+   * @var CacheTagsInvalidatorInterface
    */
   private $cacheTagsInvalidator;
 
   /**
    * Constructs a object.
    *
-   * @param \Drupal\Core\Config\ConfigFactoryInterface $config_factory
+   * @param ConfigFactoryInterface $config_factory
    *   The factory for configuration objects.
-   * @param \Drupal\Core\Entity\EntityTypeManagerInterface $entity_manager
+   * @param EntityTypeManagerInterface $entity_manager
    *   The entity type manager.
-   * @param \Drupal\path_alias\AliasManagerInterface $alias_manager
+   * @param AliasManagerInterface $alias_manager
    *   The alias manager.
-   * @param \Drupal\Core\Cache\CacheTagsInvalidatorInterface $cache_tags_invalidator
+   * @param CacheTagsInvalidatorInterface $cache_tags_invalidator
    *   The cache tag invalidator service.
    */
   public function __construct(ConfigFactoryInterface $config_factory, EntityTypeManagerInterface $entity_manager, AliasManagerInterface $alias_manager, CacheTagsInvalidatorInterface $cache_tags_invalidator) {
diff --git a/src/Plugin/Block/CookieDeclarationBlock.php b/src/Plugin/Block/CookieDeclarationBlock.php
index 059e7b2..d9db68c 100644
--- a/src/Plugin/Block/CookieDeclarationBlock.php
+++ b/src/Plugin/Block/CookieDeclarationBlock.php
@@ -22,14 +22,14 @@ class CookieDeclarationBlock extends BlockBase implements ContainerFactoryPlugin
   /**
    * The config factory.
    *
-   * @var \Drupal\Core\Config\ConfigFactoryInterface
+   * @var ConfigFactoryInterface
    */
   protected $configFactory;
 
   /**
    * The language manager service.
    *
-   * @var \Drupal\Core\Language\LanguageManagerInterface
+   * @var LanguageManagerInterface
    */
   protected $languageManager;
 
@@ -42,9 +42,9 @@ class CookieDeclarationBlock extends BlockBase implements ContainerFactoryPlugin
    *   The plugin_id for the plugin instance.
    * @param string $plugin_definition
    *   The plugin implementation definition.
-   * @param \Drupal\Core\Config\ConfigFactoryInterface $config_factory
+   * @param ConfigFactoryInterface $config_factory
    *   The config factory.
-   * @param \Drupal\Core\Language\LanguageManagerInterface $language_manager
+   * @param LanguageManagerInterface $language_manager
    *   The language manager service.
    */
   public function __construct(array $configuration, $plugin_id, $plugin_definition, ConfigFactoryInterface $config_factory, LanguageManagerInterface $language_manager) {

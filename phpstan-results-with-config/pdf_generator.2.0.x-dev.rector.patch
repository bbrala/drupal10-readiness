diff --git a/pdf_generator.api.php b/pdf_generator.api.php
index eb63113..eee8aab 100644
--- a/pdf_generator.api.php
+++ b/pdf_generator.api.php
@@ -10,7 +10,7 @@ use Dompdf\Dompdf;
 /**
  * Alter Dompdf object before rendering.
  *
- * @param \Dompdf\Dompdf $dompdf
+ * @param Dompdf $dompdf
  *   Dompdf instance.
  */
 function hook_mymodule_pdf_generator_pre_render_alter(Dompdf &$dompdf) {
@@ -24,7 +24,7 @@ function hook_mymodule_pdf_generator_pre_render_alter(Dompdf &$dompdf) {
 /**
  * Alter Dompdf object after rendering.
  *
- * @param \Dompdf\Dompdf $dompdf
+ * @param Dompdf $dompdf
  *   Dompdf instance.
  */
 function hook_mymodule_pdf_generator_post_render_alter(Dompdf &$dompdf) {
diff --git a/src/DomPdfGenerator.php b/src/DomPdfGenerator.php
index f5847f3..df43017 100644
--- a/src/DomPdfGenerator.php
+++ b/src/DomPdfGenerator.php
@@ -23,55 +23,55 @@ class DomPdfGenerator {
   /**
    * Options for dompdf.
    *
-   * @var \Dompdf\Options
+   * @var Options
    */
   protected $options;
 
   /**
    * Dompdf library.
    *
-   * @var \Dompdf\Dompdf
+   * @var Dompdf
    */
   protected $dompdf;
 
   /**
    * Module handler.
    *
-   * @var \Drupal\Core\Extension\ModuleHandlerInterface
+   * @var ModuleHandlerInterface
    */
   protected $moduleHandler;
 
   /**
    * The object renderer.
    *
-   * @var \Drupal\Core\Render\Renderer
+   * @var Renderer
    */
   protected $renderer;
 
   /**
    * Protected requestStack.
    *
-   * @var \Symfony\Component\HttpFoundation\RequestStack
+   * @var RequestStack
    */
   protected $requestStack;
 
   /**
    * The module extension list.
    *
-   * @var \Drupal\Core\Extension\ModuleExtensionList
+   * @var ModuleExtensionList
    */
   protected $moduleExtensionList;
 
   /**
    * Constructs a new object.
    *
-   * @param \Drupal\Core\Render\Renderer $renderer
+   * @param Renderer $renderer
    *   The object renderer.
-   * @param \Symfony\Component\HttpFoundation\RequestStack $request_stack
+   * @param RequestStack $request_stack
    *   The current request stack.
-   * @param \Drupal\Core\Extension\ModuleHandlerInterface $module_handler
+   * @param ModuleHandlerInterface $module_handler
    *   The module handler.
-   * @param \Drupal\Core\Extension\ModuleExtensionList $extension_list_module
+   * @param ModuleExtensionList $extension_list_module
    *   The module extension list.
    */
   public function __construct(
diff --git a/src/Plugin/views/display/Pdf.php b/src/Plugin/views/display/Pdf.php
index ffa81e3..3be31e1 100644
--- a/src/Plugin/views/display/Pdf.php
+++ b/src/Plugin/views/display/Pdf.php
@@ -35,7 +35,7 @@ class Pdf extends PathPluginBase implements ResponseDisplayPluginInterface, Cont
   /**
    * The date formatter service.
    *
-   * @var \Drupal\pdf_generator\DomPdfGenerator
+   * @var DomPdfGenerator
    */
   protected $pdfGenerator;
 
@@ -48,11 +48,11 @@ class Pdf extends PathPluginBase implements ResponseDisplayPluginInterface, Cont
    *   The plugin_id for the plugin instance.
    * @param mixed $plugin_definition
    *   The plugin implementation definition.
-   * @param \Drupal\Core\Routing\RouteProviderInterface $route_provider
+   * @param RouteProviderInterface $route_provider
    *   The route provider.
-   * @param \Drupal\Core\State\StateInterface $state
+   * @param StateInterface $state
    *   The state key value store.
-   * @param \Drupal\pdf_generator\DomPdfGenerator $pdfGenerator
+   * @param DomPdfGenerator $pdfGenerator
    *   The pdf generator.
    */
   public function __construct(array $configuration, $plugin_id, $plugin_definition, RouteProviderInterface $route_provider, StateInterface $state, DomPdfGenerator $pdfGenerator) {

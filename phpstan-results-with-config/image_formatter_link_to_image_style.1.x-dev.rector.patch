diff --git a/image_formatter_link_to_image_style.info.yml b/image_formatter_link_to_image_style.info.yml
index d67b30b..c5bd31c 100644
--- a/image_formatter_link_to_image_style.info.yml
+++ b/image_formatter_link_to_image_style.info.yml
@@ -2,6 +2,6 @@ name: Image Formatter Link to Image Style
 type: module
 description: 'Provides an additional formatter for image core field, to link to an image style.'
 package: Field
-core_version_requirement: ^8 || ^9
+core_version_requirement: ^9.3 || ^10
 dependencies:
   - drupal:image
diff --git a/src/Plugin/Field/FieldFormatter/ImageFormatterLinkToImageStyleFormatter.php b/src/Plugin/Field/FieldFormatter/ImageFormatterLinkToImageStyleFormatter.php
index a5254df..118c067 100644
--- a/src/Plugin/Field/FieldFormatter/ImageFormatterLinkToImageStyleFormatter.php
+++ b/src/Plugin/Field/FieldFormatter/ImageFormatterLinkToImageStyleFormatter.php
@@ -2,6 +2,7 @@
 
 namespace Drupal\image_formatter_link_to_image_style\Plugin\Field\FieldFormatter;
 
+use Drupal\image\ImageStyleStorageInterface;
 use Drupal\image\Entity\ImageStyle;
 use Drupal\image\Plugin\Field\FieldFormatter\ImageFormatterBase;
 use Drupal\Core\Entity\EntityStorageInterface;
@@ -37,21 +38,21 @@ class ImageFormatterLinkToImageStyleFormatter extends ImageFormatterBase impleme
   /**
    * The current user.
    *
-   * @var \Drupal\Core\Session\AccountInterface
+   * @var AccountInterface
    */
   protected $currentUser;
 
   /**
    * The link generator.
    *
-   * @var \Drupal\Core\Utility\LinkGeneratorInterface
+   * @var LinkGeneratorInterface
    */
   protected $linkGenerator;
 
   /**
    * The image style entity storage.
    *
-   * @var \Drupal\image\ImageStyleStorageInterface
+   * @var ImageStyleStorageInterface
    */
   protected $imageStyleStorage;
 
@@ -62,7 +63,7 @@ class ImageFormatterLinkToImageStyleFormatter extends ImageFormatterBase impleme
    *   The plugin_id for the formatter.
    * @param mixed $plugin_definition
    *   The plugin implementation definition.
-   * @param \Drupal\Core\Field\FieldDefinitionInterface $field_definition
+   * @param FieldDefinitionInterface $field_definition
    *   The definition of the field to which the formatter is associated.
    * @param array $settings
    *   The formatter settings.
@@ -72,11 +73,11 @@ class ImageFormatterLinkToImageStyleFormatter extends ImageFormatterBase impleme
    *   The view mode.
    * @param array $third_party_settings
    *   Any third party settings settings.
-   * @param \Drupal\Core\Session\AccountInterface $current_user
+   * @param AccountInterface $current_user
    *   The current user.
-   * @param \Drupal\Core\Utility\LinkGeneratorInterface $link_generator
+   * @param LinkGeneratorInterface $link_generator
    *   The link generator service.
-   * @param \Drupal\Core\Entity\EntityStorageInterface $image_style_storage
+   * @param EntityStorageInterface $image_style_storage
    *   The entity storage for the image.
    */
   public function __construct($plugin_id, $plugin_definition, FieldDefinitionInterface $field_definition, array $settings, $label, $view_mode, array $third_party_settings, AccountInterface $current_user, LinkGeneratorInterface $link_generator, EntityStorageInterface $image_style_storage) {
@@ -252,7 +253,7 @@ class ImageFormatterLinkToImageStyleFormatter extends ImageFormatterBase impleme
       else {
         $image_uri = $file->getFileUri();
       }
-      $url = Url::fromUri(file_create_url($image_uri));
+      $url = \Drupal::service('file_url_generator')->generate($image_uri);
 
       $url_attributes = [];
       if (!empty($this->getSetting('image_link_class'))) {

diff --git a/src/Plugin/Field/FieldFormatter/ImageBase64Formatter.php b/src/Plugin/Field/FieldFormatter/ImageBase64Formatter.php
index 48f3dda..2f8280c 100644
--- a/src/Plugin/Field/FieldFormatter/ImageBase64Formatter.php
+++ b/src/Plugin/Field/FieldFormatter/ImageBase64Formatter.php
@@ -2,6 +2,8 @@
 
 namespace Drupal\image_base64_formatter\Plugin\Field\FieldFormatter;
 
+use Drupal\image\ImageStyleInterface;
+use Drupal\file\FileInterface;
 use Drupal\Core\Cache\CacheableMetadata;
 use Drupal\Core\Entity\EntityStorageInterface;
 use Drupal\Core\Field\FieldDefinitionInterface;
@@ -32,28 +34,28 @@ class ImageBase64Formatter extends ImageFormatter {
   /**
    * The current user.
    *
-   * @var \Drupal\Core\Session\AccountInterface
+   * @var AccountInterface
    */
   protected $currentUser;
 
   /**
    * The image style entity storage.
    *
-   * @var \Drupal\Core\Entity\EntityStorageInterface
+   * @var EntityStorageInterface
    */
   protected $imageStyleStorage;
 
   /**
    * The image factory service.
    *
-   * @var \Drupal\Core\Image\ImageFactory
+   * @var ImageFactory
    */
   protected $imageFactory;
 
   /**
    * The file_system service.
    *
-   * @var \Drupal\Core\File\FileSystem
+   * @var FileSystem
    */
   protected $fileSystem;
 
@@ -211,12 +213,12 @@ class ImageBase64Formatter extends ImageFormatter {
       $link_file = TRUE;
     }
 
-    /** @var \Drupal\image\ImageStyleInterface $image_style */
+    /** @var ImageStyleInterface $image_style */
     $image_style_setting = $this->getSetting('image_style');
     $image_style = $this->imageStyleStorage->load($image_style_setting);
     $image_display = $this->getSetting('image_display');
 
-    /** @var \Drupal\file\FileInterface[] $images */
+    /** @var FileInterface[] $images */
     foreach ($images as $delta => $image) {
       $image_uri = $image->getFileUri();
       $image_type = $image->getMimeType();
@@ -261,7 +263,7 @@ class ImageBase64Formatter extends ImageFormatter {
           elseif (isset($url)) {
             $markup = '<a href="' . $url . '">' . $markup . '</a>';
           }
-          $markup = Markup::create(render($markup));
+          $markup = Markup::create(\Drupal::service('renderer')->render($markup));
           break;
 
         case 'file':
diff --git a/tests/src/Functional/LoadTest.php b/tests/src/Functional/LoadTest.php
index 0342a71..4b9cbbe 100644
--- a/tests/src/Functional/LoadTest.php
+++ b/tests/src/Functional/LoadTest.php
@@ -2,6 +2,7 @@
 
 namespace Drupal\Tests\image_base64_formatter\Functional;
 
+use Drupal\user\UserInterface;
 use Drupal\Core\Url;
 use Drupal\Tests\BrowserTestBase;
 
@@ -27,7 +28,7 @@ class LoadTest extends BrowserTestBase {
   /**
    * A user with permission to administer site configuration.
    *
-   * @var \Drupal\user\UserInterface
+   * @var UserInterface
    */
   protected $user;
 

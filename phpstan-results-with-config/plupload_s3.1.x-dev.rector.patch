diff --git a/src/Plugin/Field/FieldWidget/S3UploadFileWidget.php b/src/Plugin/Field/FieldWidget/S3UploadFileWidget.php
index c4c34f4..8d4371c 100644
--- a/src/Plugin/Field/FieldWidget/S3UploadFileWidget.php
+++ b/src/Plugin/Field/FieldWidget/S3UploadFileWidget.php
@@ -38,14 +38,14 @@ class S3UploadFileWidget extends WidgetBase {
   /**
    * The key repository service.
    *
-   * @var \Drupal\key\KeyRepositoryInterface
+   * @var KeyRepositoryInterface
    */
   protected $keyRepository;
 
   /**
    * Module handler.
    *
-   * @var \Drupal\Core\Extension\ModuleHandlerInterface
+   * @var ModuleHandlerInterface
    */
   protected $moduleHandler;
 
@@ -59,14 +59,14 @@ class S3UploadFileWidget extends WidgetBase {
   /**
    * The current user.
    *
-   * @var \Drupal\Core\Session\AccountInterface
+   * @var AccountInterface
    */
   protected $currentUser;
 
   /**
    * Plupload Element processor.
    *
-   * @var \Drupal\plupload_s3\PluploadElementProcessor
+   * @var PluploadElementProcessor
    */
   protected $elementProcessor;
 
@@ -252,7 +252,7 @@ class S3UploadFileWidget extends WidgetBase {
    *
    * @see https://docs.aws.amazon.com/aws-sdk-php/v3/guide/guide/credentials.html#creating-a-custom-provider
    *
-   * @return \Closure|\GuzzleHttp\Promise\RejectedPromise
+   * @return \Closure|RejectedPromise
    */
   protected function getAwsCredentialsProvider() {
     $keyName = $this->getSetting('key_name');
@@ -275,7 +275,7 @@ class S3UploadFileWidget extends WidgetBase {
   /**
    * Get an S3 client.
    *
-   * @return \Aws\S3\S3ClientInterface
+   * @return S3ClientInterface
    */
   protected function getClient(): S3ClientInterface {
     return new S3Client([
diff --git a/src/PluploadElementProcessor.php b/src/PluploadElementProcessor.php
index 8111562..0ef119a 100644
--- a/src/PluploadElementProcessor.php
+++ b/src/PluploadElementProcessor.php
@@ -2,6 +2,7 @@
 
 namespace Drupal\plupload_s3;
 
+use function GuzzleHttp\json_decode;
 use Aws\S3\PostObjectV4;
 use Aws\S3\S3ClientInterface;
 use Drupal\Core\File\Exception\FileException;
@@ -18,7 +19,7 @@ class PluploadElementProcessor {
   /**
    * File system.
    *
-   * @var \Drupal\Core\File\FileSystemInterface
+   * @var FileSystemInterface
    */
   protected $fileSystem;
 
@@ -36,7 +37,7 @@ class PluploadElementProcessor {
    *   Render element to adjust.
    * @param string[] $allowedExtensions
    *   Array of allowed extensions.
-   * @param \Aws\S3\S3ClientInterface $client
+   * @param S3ClientInterface $client
    *   S3 client.
    * @param string $bucket
    *   S3 bucket.
@@ -106,7 +107,7 @@ class PluploadElementProcessor {
    *   File data object from plupload payload.
    * @param string $destinationPrefix
    *   Destination URI with optional path prefix, e.g. scheme://[prefix]
-   * @param \Aws\S3\S3ClientInterface $client
+   * @param S3ClientInterface $client
    *   S3 client.
    * @param string $bucket
    *   S3 bucket to utilize.
@@ -167,7 +168,7 @@ class PluploadElementProcessor {
    *   Posted JSON string from Plupload.
    * @param string $destinationPrefix
    *   Destination prefix, in form of scheme://[prefix]
-   * @param \Aws\S3\S3ClientInterface $client
+   * @param S3ClientInterface $client
    *   S3 client.
    * @param string $bucket
    *   Bucket.
@@ -177,7 +178,7 @@ class PluploadElementProcessor {
    */
   public function processPostedFiles(string $json, string $destinationPrefix, S3ClientInterface $client, string $bucket) {
     try {
-      $files = \GuzzleHttp\json_decode($json);
+      $files = json_decode($json);
     }
     catch (\InvalidArgumentException $e) {
       // @todo - handle and warn.

modules/contrib/config_overview/config_overview.module
modules/contrib/config_overview/src/Controller/ConfigOverviewController.php
modules/contrib/config_overview/tests/src/Functional/RoutingTest.php

2 files with changes
====================

1) modules/contrib/config_overview/tests/src/Functional/RoutingTest.php:1

    ---------- begin diff ----------
@@ @@

 namespace Drupal\Tests\config_overview\Functional;

+use Drupal\user\Entity\User;
+use Behat\Mink\Exception\ExpectationException;
 use Drupal\Core\Entity\EntityStorageException;
 use Drupal\Tests\BrowserTestBase;

@@ @@
   /**
    * The user.
    *
-   * @var \Drupal\user\Entity\User|false
+   * @var User|false
    */
   protected $user;

@@ @@
    *
    * @covers ::content
    * @covers ::__construct
-   * @throws \Behat\Mink\Exception\ExpectationException
+   * @throws ExpectationException
    */
   public function testConfigOverviewPageAccess() {
     try {
    ----------- end diff -----------

Applied rules:
 * ExceptionAnnotationRector
 * RemoveDataProviderTestPrefixRector
 * ArgumentRemoverRector
 * RenameAnnotationRector
 * PseudoNamespaceToNamespaceRector
 * WrapReturnRector
 * ChangeMethodVisibilityRector


2) modules/contrib/config_overview/src/Controller/ConfigOverviewController.php:19

    ---------- begin diff ----------
@@ @@
   /**
    * The config manager service.
    *
-   * @var \Drupal\Core\Config\ConfigManagerInterface
+   * @var ConfigManagerInterface
    */
   protected $configManager;

@@ @@
   /**
    * The module handler service.
    *
-   * @var \Drupal\Core\Extension\ModuleHandlerInterface
+   * @var ModuleHandlerInterface
    */
   protected $moduleHandler;

@@ @@
   /**
    * The form overrides service.
    *
-   * @var \Drupal\config_override_warn\FormOverrides
+   * @var FormOverrides
    */
   protected $formOverrides;

@@ @@
   /**
    * Constructs a ConfigOverviewController object.
    *
-   * @param \Drupal\Core\Config\ConfigManagerInterface $config_manager
+   * @param ConfigManagerInterface $config_manager
    *   The config manager service.
-   * @param \Drupal\Core\Extension\ModuleHandlerInterface $module_handler
+   * @param ModuleHandlerInterface $module_handler
    *   The module handler service.
-   * @param \Drupal\config_override_warn\FormOverrides $form_overrides
+   * @param FormOverrides $form_overrides
    *   The form overrides service.
    */
   public function __construct(ConfigManagerInterface $config_manager,
@@ @@
       ];
       $row = [
         $config->getName() . ' (' . $config->get('langcode') . ')',
-        render($overriddenImage),
-        render($overrideRows),
-        render($excludedImage),
+        \Drupal::service('renderer')->render($overriddenImage),
+        \Drupal::service('renderer')->render($overrideRows),
+        \Drupal::service('renderer')->render($excludedImage),
       ];
       if ($this->moduleHandler->moduleExists('config_split')) {
-        $row[] = render($protectedOnImportImage);
+        $row[] = \Drupal::service('renderer')->render($protectedOnImportImage);
       }
       $rows[] = $row;
     }
@@ @@
   /**
    * Determines if a config is excluded from configurations synchronization.
    *
-   * @param \Drupal\Core\Config\Config $config
+   * @param Config $config
    *   The config object.
    * @param array $excluded_modules
    *   An array of excluded modules.
    ----------- end diff -----------

Applied rules:
 * RenderRector
 * ArgumentRemoverRector
 * PseudoNamespaceToNamespaceRector
 * WrapReturnRector
 * AddParamTypeDeclarationRector
 * ChangeMethodVisibilityRector


 [OK] 2 files have been changed by Rector                                       


diff --git a/editor_md.module b/editor_md.module
index d696b10..5cb59fe 100644
--- a/editor_md.module
+++ b/editor_md.module
@@ -1,5 +1,6 @@
 <?php
 
+use Drupal\markdown\PluginManager\ParserManagerInterface;
 /**
  * @file
  * Contains editor_md.module.
@@ -17,7 +18,7 @@ function editor_md_help($route_name, RouteMatchInterface $route_match) {
       return '<pre>' . $text . '</pre>';
     }
     else {
-      /** @var \Drupal\markdown\PluginManager\ParserManagerInterface $parser_manager */
+      /** @var ParserManagerInterface $parser_manager */
       $parser_manager = \Drupal::service('plugin.manager.markdown.parser');
       $parser = $parser_manager->getDefaultParser([
         'render_strategy' => ['type' => 'none'],
diff --git a/src/Plugin/Editor/EditorMd.php b/src/Plugin/Editor/EditorMd.php
index 89d5cc9..4316189 100644
--- a/src/Plugin/Editor/EditorMd.php
+++ b/src/Plugin/Editor/EditorMd.php
@@ -100,14 +100,14 @@ class EditorMd extends EditorBase implements ContainerFactoryPluginInterface {
   /**
    * The discovery cache backend.
    *
-   * @var \Drupal\Core\Cache\CacheBackendInterface
+   * @var CacheBackendInterface
    */
   protected $discoveryCache;
 
   /**
    * The file system service.
    *
-   * @var \Drupal\Core\File\FileSystemInterface
+   * @var FileSystemInterface
    */
   protected $fileSystem;
 
@@ -120,9 +120,9 @@ class EditorMd extends EditorBase implements ContainerFactoryPluginInterface {
    *   The plugin_id for the plugin instance.
    * @param mixed $plugin_definition
    *   The plugin implementation definition.
-   * @param \Drupal\Core\Cache\CacheBackendInterface $discovery_cache
+   * @param CacheBackendInterface $discovery_cache
    *   The cache backend to use for caching the definitions.
-   * @param \Drupal\Core\File\FileSystemInterface $file_system
+   * @param FileSystemInterface $file_system
    *   The file handler.
    */
   public function __construct(array $configuration, $plugin_id, $plugin_definition, CacheBackendInterface $discovery_cache, FileSystemInterface $file_system) {

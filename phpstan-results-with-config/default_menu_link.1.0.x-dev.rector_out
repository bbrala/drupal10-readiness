modules/contrib/default_menu_link/default_menu_link.install
modules/contrib/default_menu_link/default_menu_link.module
modules/contrib/default_menu_link/hooks/default_menu_link.form_alter.inc
modules/contrib/default_menu_link/hooks/default_menu_link.form_node_type_form_alter.inc

2 files with changes
====================

1) modules/contrib/default_menu_link/hooks/default_menu_link.form_node_type_form_alter.inc:10

    ---------- begin diff ----------
@@ @@
  * Implements hook_form_FORM_ID_alter.
  *
  * @param array $form
- * @param \Drupal\Core\Form\FormStateInterface $form_state
+ * @param FormStateInterface $form_state
  */
 function default_menu_link_form_node_type_form_alter(array &$form, FormStateInterface $form_state) {
    ----------- end diff -----------

Applied rules:
 * ArgumentRemoverRector


2) modules/contrib/default_menu_link/default_menu_link.module:4

    ---------- begin diff ----------
@@ @@
  */

 $moduleName = basename(dirname(__FILE__));
-$pathsToInclude = glob(drupal_get_path('module', $moduleName) . '/hooks/*');
+$pathsToInclude = glob(\Drupal::service('extension.list.module')->getPath($moduleName) . '/hooks/*');

 if ($pathsToInclude) {
   foreach ($pathsToInclude as $path) {
     $pathInfo = pathinfo($path);
-    $directory = str_replace(drupal_get_path('module', $moduleName), '', $pathInfo['dirname']);
+    $directory = str_replace(\Drupal::service('extension.list.module')->getPath($moduleName), '', $pathInfo['dirname']);
     if (isset($pathInfo['extension'])) {
       module_load_include($pathInfo['extension'], $moduleName, $directory . '/' . $pathInfo['filename']);
     }
   }
 }
    ----------- end diff -----------

Applied rules:
 * DrupalGetPathRector
 * ArgumentRemoverRector


 [OK] 2 files have been changed by Rector                                       


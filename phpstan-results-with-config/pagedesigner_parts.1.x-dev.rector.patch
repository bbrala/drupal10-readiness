diff --git a/src/Plugin/Block/PagedesignerPart.php b/src/Plugin/Block/PagedesignerPart.php
index ea859dd..2beae3f 100644
--- a/src/Plugin/Block/PagedesignerPart.php
+++ b/src/Plugin/Block/PagedesignerPart.php
@@ -26,14 +26,14 @@ class PagedesignerPart extends BlockBase implements ContainerFactoryPluginInterf
   /**
    * The node to render.
    *
-   * @var \Drupal\node\Entity\Node
+   * @var Node
    */
   protected $node = NULL;
 
   /**
    * The pagedesigner renderer service.
    *
-   * @var \Drupal\pagedesigner\Service\Renderer
+   * @var Renderer
    */
   protected $renderer = NULL;
 
@@ -49,7 +49,7 @@ class PagedesignerPart extends BlockBase implements ContainerFactoryPluginInterf
    *   The plugin_id for the plugin instance.
    * @param mixed $plugin_definition
    *   The plugin implementation definition.
-   * @param \Drupal\pagedesigner\Service\Renderer $renderer
+   * @param Renderer $renderer
    *   The pagedesigner renderer service.
    */
   public function __construct(array $configuration, $plugin_id, $plugin_definition, Renderer $renderer) {
@@ -60,7 +60,7 @@ class PagedesignerPart extends BlockBase implements ContainerFactoryPluginInterf
   /**
    * {@inheritDoc}
    *
-   * @param \Symfony\Component\DependencyInjection\ContainerInterface $container
+   * @param ContainerInterface $container
    *   The container to pull out services used in the plugin.
    * @param array $configuration
    *   A configuration array containing information about the plugin instance.

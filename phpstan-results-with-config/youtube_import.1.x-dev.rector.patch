diff --git a/youtube_import.info.yml b/youtube_import.info.yml
index 16351a9..c331f45 100644
--- a/youtube_import.info.yml
+++ b/youtube_import.info.yml
@@ -2,6 +2,5 @@ name: YouTube Import
 description: Custom module created to import a YouTube channel.
 package: Other
 type: module
-core: 8.x
-core_version_requirement: ^8 || ^9
+core_version_requirement: ^9.3 || ^10
 configure: youtube_import.admin
diff --git a/youtube_import.module b/youtube_import.module
index 710dfc9..f88097b 100644
--- a/youtube_import.module
+++ b/youtube_import.module
@@ -1,5 +1,7 @@
 <?php
 
+use Drupal\Core\File\FileSystemInterface;
+use Drupal\node\Entity\Node;
 /**
  * @file
  * The primary file in the YouTube module.
@@ -269,7 +271,7 @@ function youtube_import_videos() {
             $filename = "public://" . $video->id . '.jpg';
           }
           // Make sure we haven't already done this.
-          if ($filepath = file_save_data(Request::getContent($value), $filename, FILE_EXISTS_REPLACE)) {
+          if ($filepath = \Drupal::service('file.repository')->writeData(Request::getContent($value), $filename, FileSystemInterface::EXISTS_REPLACE)) {
             $wrapper->$fieldname->file->set($filepath);
           }
         }
@@ -383,7 +385,7 @@ function youtube_import_find($youtube_id) {
     // If I found it, load it.
     if ($results->rowCount()) {
       // TODO.
-      return \Drupal\node\Entity\Node::load($results->fetchCol()[0]);
+      return Node::load($results->fetchCol()[0]);
 		//JAS
       // return node_load($results->fetchCol()[0]);
     }

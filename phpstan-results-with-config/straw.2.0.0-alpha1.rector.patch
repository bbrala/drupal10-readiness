diff --git a/src/Element/SuperTermReferenceAutocomplete.php b/src/Element/SuperTermReferenceAutocomplete.php
index da568b0..729d640 100644
--- a/src/Element/SuperTermReferenceAutocomplete.php
+++ b/src/Element/SuperTermReferenceAutocomplete.php
@@ -2,6 +2,8 @@
 
 namespace Drupal\straw\Element;
 
+use Drupal\taxonomy\Entity\Term;
+use Drupal\taxonomy\TermStorage;
 use Drupal\Core\Entity\Element\EntityAutocomplete;
 use Drupal\Component\Utility\Tags;
 
@@ -22,7 +24,7 @@ class SuperTermReferenceAutocomplete extends EntityAutocomplete {
    * the full tree path to the term. This method is also responsible for
    * checking the 'view label' access on the passed-in terms.
    *
-   * @param \Drupal\taxonomy\Entity\Term[] $terms
+   * @param Term[] $terms
    *   An array of term objects.
    *
    * @return string
@@ -31,7 +33,7 @@ class SuperTermReferenceAutocomplete extends EntityAutocomplete {
   public static function getEntityLabels(array $terms) {
     $term_labels = [];
 
-    /** @var \Drupal\taxonomy\TermStorage $term_storage */
+    /** @var TermStorage $term_storage */
     $term_storage = \Drupal::entityTypeManager()->getStorage('taxonomy_term');
 
     foreach ($terms as $term) {
diff --git a/src/NewTermStorage.php b/src/NewTermStorage.php
index 25f2484..88d5ff6 100644
--- a/src/NewTermStorage.php
+++ b/src/NewTermStorage.php
@@ -28,7 +28,7 @@ class NewTermStorage {
    * @param string $tree_path
    *   A string containing the hierarchy of what term to look up.
    *
-   * @return \Drupal\taxonomy\Entity\Term|null
+   * @return Term|null
    *   The stored term.
    */
   public function get($bundle, $tree_path) {
@@ -43,7 +43,7 @@ class NewTermStorage {
    * @param string $tree_path
    *   A string containing the hierarchy of where the term will exist once it is
    *   created.
-   * @param \Drupal\taxonomy\Entity\Term $term
+   * @param Term $term
    *   The newly created term.
    */
   public function set($bundle, $tree_path, Term $term) {
diff --git a/src/Plugin/EntityReferenceSelection/StrawSelection.php b/src/Plugin/EntityReferenceSelection/StrawSelection.php
index bae9415..a5d1770 100644
--- a/src/Plugin/EntityReferenceSelection/StrawSelection.php
+++ b/src/Plugin/EntityReferenceSelection/StrawSelection.php
@@ -2,6 +2,7 @@
 
 namespace Drupal\straw\Plugin\EntityReferenceSelection;
 
+use Drupal\straw\NewTermStorage;
 use Drupal\Component\Utility\Html;
 use Drupal\taxonomy\Entity\Vocabulary;
 use Drupal\taxonomy\Entity\Term;
@@ -123,7 +124,7 @@ class StrawSelection extends TermSelection {
     // EntityOwnerInterface).
     $term_names = explode('>>', $label);
 
-    /** @var \Drupal\straw\NewTermStorage $new_term_storage */
+    /** @var NewTermStorage $new_term_storage */
     $new_term_storage = \Drupal::service('straw.new_term_storage');
 
     // Tracks the deepest term we've already processed.

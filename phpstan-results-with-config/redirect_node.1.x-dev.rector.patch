diff --git a/redirect_node.info.yml b/redirect_node.info.yml
index 8649da6..2707f80 100644
--- a/redirect_node.info.yml
+++ b/redirect_node.info.yml
@@ -1,7 +1,6 @@
 name: Redirect Node
 description: 'Provides a Drupal Node type that redirects to an external URL with viewed directly.'
-core: 8.x
-core_version_requirement: ^8 || ^9
+core_version_requirement: ^8 || ^9 || ^10
 type: module
 dependencies:
   - drupal:path
diff --git a/src/EventSubscriber/RedirectRedirectNodes.php b/src/EventSubscriber/RedirectRedirectNodes.php
index 8e8aa55..f613fe8 100644
--- a/src/EventSubscriber/RedirectRedirectNodes.php
+++ b/src/EventSubscriber/RedirectRedirectNodes.php
@@ -2,13 +2,13 @@
 
 namespace Drupal\redirect_node\EventSubscriber;
 
+use Symfony\Component\HttpKernel\Event\RequestEvent;
 use Drupal\Core\Entity\EntityTypeManagerInterface;
 use Drupal\Core\Routing\TrustedRedirectResponse;
 use Drupal\Core\Session\AccountProxy;
 use Drupal\Core\Url;
 use Drupal\node\NodeInterface;
 use Symfony\Component\EventDispatcher\EventSubscriberInterface;
-use Symfony\Component\HttpKernel\Event\GetResponseEvent;
 use Symfony\Component\HttpKernel\KernelEvents;
 
 /**
@@ -22,23 +22,23 @@ class RedirectRedirectNodes implements EventSubscriberInterface {
   /**
    * Currently signed in user.
    *
-   * @var Drupal\Core\Session\AccountProxy
+   * @var AccountProxy
    */
   protected $currentUser;
 
   /**
    * The Entity type manager.
    *
-   * @var Drupal\Core\Entity\EntityTypeManagerInterface
+   * @var EntityTypeManagerInterface
    */
   protected $entityTypeManger;
 
   /**
    * Constructs a RedirectRedirectNodes object.
    *
-   * @param \Drupal\Core\Session\AccountProxy $currentUser
+   * @param AccountProxy $currentUser
    *   The current user object.
-   * @param \Drupal\Core\Entity\EntityTypeManagerInterface $entityTypeManger
+   * @param EntityTypeManagerInterface $entityTypeManger
    *   The Entity type manager.
    */
   public function __construct(AccountProxy $currentUser, EntityTypeManagerInterface $entityTypeManger) {
@@ -63,10 +63,10 @@ class RedirectRedirectNodes implements EventSubscriberInterface {
   /**
    * Redirect requests for `redirect` nodes to their destination url.
    *
-   * @param \Symfony\Component\HttpKernel\Event\GetResponseEvent $event
+   * @param RequestEvent $event
    *   Event object of current event.
    */
-  public function redirect(GetResponseEvent $event) {
+  public function redirect(RequestEvent $event) {
     $request = $event->getRequest();
 
     // This is necessary because this also gets called on

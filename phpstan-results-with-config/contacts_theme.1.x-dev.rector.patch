diff --git a/includes/blocks.theme.inc b/includes/blocks.theme.inc
index 2277762..59e7c54 100644
--- a/includes/blocks.theme.inc
+++ b/includes/blocks.theme.inc
@@ -1,5 +1,6 @@
 <?php
 
+use Drupal\Core\Link;
 /**
  * @file
  * Contacts module related theme functions and hooks for Contacts Theme.
@@ -179,7 +180,7 @@ function contacts_theme_preprocess_block(&$variables) {
 
       /** @var \Drupal\contacts\Plugin\Block\ContactsEntity $block */
       if ($link = $variables['content']['#title_edit'] ?? FALSE) {
-        /** @var \Drupal\Core\Link $link */
+        /** @var Link $link */
         $block->setConfigurationValue('edit_link', FALSE);
         $attributes = $link->getUrl()->getOption('attributes');
         $attributes['class'][] = 'btn';
diff --git a/includes/page.theme.inc b/includes/page.theme.inc
index b72350d..8642d78 100644
--- a/includes/page.theme.inc
+++ b/includes/page.theme.inc
@@ -13,13 +13,13 @@ use Drupal\Core\Template\Attribute;
  * @todo: Replace with an SVG logo.
  */
 function contacts_theme_preprocess_page(array &$variables) {
-  $path = drupal_get_path('theme', 'contacts_theme');
+  $path = \Drupal::service('extension.list.theme')->getPath('contacts_theme');
   $logo = theme_get_setting('logo.url');
-  if ($logo == file_url_transform_relative(file_create_url($path . '/logo.svg'))) {
-    $variables['logo'] = file_url_transform_relative(file_create_url($path . '/logo.png'));
+  if ($logo == \Drupal::service('file_url_generator')->generateString($path . '/logo.svg')) {
+    $variables['logo'] = \Drupal::service('file_url_generator')->generateString($path . '/logo.png');
   }
   else {
-    $variables['logo'] = file_url_transform_relative($logo);
+    $variables['logo'] = \Drupal::service('file_url_generator')->transformRelative($logo);
   }
 
   // See if we have a header image.
